---
phase: 01.5
slug: ui-redesign-inserted
plan: 02
id: 01.5-02
status: complete
self_check: PASSED
key_files:
  modified:
    - src/index.css
---

# 01.5-02 — Global CSS Token System + Keyframes

## What was built

Extended `src/index.css` from ~30 lines (Tailwind + minimal `@theme`) to 1293 lines containing the complete visual contract for Phase 1.5. Wave-2 component executors can now write JSX with `className="counter"` / `className="fl-pad"` / `className="faq-item"` etc. and get prototype-faithful rendering with no per-component styling.

## Tasks completed

### Task 1 — Extend `src/index.css` with the full design system

Single-file plan, executed as one commit.

**Design tokens in `@theme`:**
- Ink opacity ladder: `--color-ink` (#000), `--color-ink-70/50/35/20/10/06` (rgba black at matching alphas)
- Type scale: `--text-mark` / `--text-h1` / `--text-h2` / `--text-h3` / `--text-eyebrow` / `--text-meta` / `--text-body` / `--text-body-sm`
- Reserved accents: `--color-live-green-400/500/600`, `--color-wax-blue` (#2b35b0)
- Font stacks: `--font-sans` (Plus Jakarta Sans), `--font-serif` (Instrument Serif), `--font-mono` (Space Mono)
- Surfaces: `--color-paper` (#fefcf8), `--color-off-white` (#FDFCF9)

**Layout constants at `:root`:**
- `--shell: 1180px` (page max-width)
- `--narrow: 760px` (text-column max-width)

**Mobile overrides at `@media (max-width: 540px)`:**
- `--text-mark` and `--text-h1` shrunk to the prototype's mobile values

**Ten `@keyframes`:**
1. `wls-stamp` — success-card stamp settle
2. `wls-bob` — success-card arrow bob
3. `ping` — live-row pulse dot
4. `countBump` — counter digit attention
5. `blink` — typewriter caret (`.fl-caret`)
6. `digitOut` / `digitIn` — flip-card counter mechanics
7. `stepIn` / `stepOut` — first-letter step transitions
8. `waxDrop` — wax-seal drop animation

**Global CSS rule families:**
- Navigation: `nav.top`, `.mark`, `.join`
- Hero: `header.hero`, `.hero-inner`, `.wordmark`, `.headline`, `.hero-tagline`, `.hero-separator`
- Waitlist form: `form.waitlist`, `.btn-primary`
- Live row: `.live-row`
- Counter: `.counter`, `.counter-label`, `.digit-row`, `.digit`
- Success card: `.waitlist-success`, `.wls-card`, `.wls-mark`, `.wls-next`, `.wls-bob`
- Section divider: `.section-divider`
- How-it-works: `.step-card`, `.step-screenshot`, `.step-meta`
- Research: `.rs-stats`, `.rs-stat-num`, `.rs-headline`, `.rs-deck`, `.rs-divider`, `.rs-stamp`, `.rs-kicker`, `.rs-refs`
- First letter: `.fl-pad`, `.fl-pad-bg`, `.fl-pad-content`, `.fl-pad-body`, `.fl-typewriter`, `.fl-caret`, `.fl-textarea`, `.fl-cta`, `.fl-stage`, `.fl-step`, `.fl-step-hidden/entering/leaving`, `.fl-pad-email`, `.fl-stamps`, `.fl-arrow-btn`, `.fl-back`, `.fl-pad-success`, `.fl-env-back/flap`, `.fl-wax-seal`, `.fl-env-caption`, `.fl-promise`
- FAQ: `.faq-list`, `.faq-item`, `.faq-q`, `.faq-icon`, `.faq-a`, `.faq-a-inner`
- Footer: `.foot-inner`, `.foot-brand`, `.foot-col`, `.social-list`, `.foot-bottom`
- Reveal utilities: `.reveal`, `.reveal.in`, `.reveal.d1/d2/d3`

**Variable renames applied throughout the file:**
- `--ink-*` → `--color-ink-*`
- `--t-*` → `--text-*`

**Asset reference (D-02 closure):**
- `.fl-pad-bg` uses `background-image: url('/assets/paper-light.jpg')` — self-hosted from `public/assets/paper-light.jpg` (delivered by Plan 01.5-01), not the `i.postimg.cc` URL.

## Must-haves verified

| ID | Truth | Status |
|----|-------|--------|
| 1 | `@theme` contains ink-opacity ladder + type-scale + reserved accents | ✓ Lines 4–50 |
| 2 | Ten `@keyframes` rules declared once | ✓ All 10 present, each appearing exactly once as `@keyframes <name>` and once as a `var(--…)` reference |
| 3 | `--shell: 1180px` and `--narrow: 760px` at `:root` | ✓ Lines 52–53 |
| 4 | Mobile `:root` overrides at `@media (max-width: 540px)` | ✓ Present |
| 5 | Global CSS rules for all 14 prototype section families | ✓ All families present (see lists above) |
| 6 | `.fl-pad-bg` uses `/assets/paper-light.jpg`, not `i.postimg.cc` | ✓ Verified |

## Key-link contract for Wave 2

Wave-2 components can rely on these class names being globally styled:
`counter`, `digit`, `fl-*`, `wls-*`, `rs-*`, `faq-*`, `step-*`, `foot-*`, `join`, `mark`, `headline`, `wordmark`, `hero`, `waitlist`, `reveal`.

## Self-Check: PASSED

- [x] File is 1293 lines (well above 400-line minimum from plan)
- [x] All 10 `@keyframes` present and named correctly
- [x] All ink-opacity tokens (`--color-ink-70/50/35/20/10/06`) present
- [x] `--shell` and `--narrow` present at `:root` (lines 52–53)
- [x] `--color-live-green-500` and `--color-wax-blue` accents present
- [x] `.fl-caret` class present (for Typewriter consumption by Plan 01.5-07)
- [x] `/assets/paper-light.jpg` self-hosted reference (no `i.postimg.cc`)
- [x] Mobile `@media (max-width: 540px)` override block present

## Notes / Recovery

The executor agent wrote the complete file but hit a Bash permission denial when attempting `git add` / `git commit`. The orchestrator recovered the commit + this SUMMARY.md from outside the agent, with no semantic changes to the file the agent produced.
