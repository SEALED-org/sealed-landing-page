---
phase: 01.5
slug: ui-redesign-inserted
plan: 07
id: 01.5-07
type: execute
wave: 2
depends_on: [01.5-01, 01.5-02]
files_modified:
  - src/components/Typewriter.tsx
  - src/components/FirstLetter.tsx
autonomous: true
requirements: [UI-06, UI-10]
requirements_addressed: [UI-06, UI-10]
must_haves:
  truths:
    - "Typewriter exports the 13-phrase array (down from 14) matching the prototype byte-for-byte; the dropped phrase is the 10th: `To realize one day you were exactly where you needed to be`"
    - "Typewriter renders a `.fl-caret` span (not Tailwind's `animate-pulse`) so the global `@keyframes blink 1.1s steps(1) infinite` CSS rule fires the prototype's blink rhythm"
    - "FirstLetter is a 3-step state machine — Step 1 write (typewriter + auto-growing textarea + word-count + Seal & send CTA), Step 2 email (3 inline-SVG postage stamps + To + email input + arrow button + Back button), Step 3 success (envelope flap SVG + wax-seal PNG with waxDrop keyframe + countdown caption)"
    - "FirstLetter preserves the FirstLetterProps interface verbatim: `{ initialEmail?: string; onEmailSubmit: (email: string) => Promise<void>; waitlistCount: number }`"
    - "FirstLetter step transitions use PURE CSS classes (.fl-step-entering / .fl-step-leaving + stepIn / stepOut keyframes) per D-01 — no AnimatePresence, no motion/react wrappers on the step containers"
    - "FirstLetter computes daysToDelivery as `Math.max(0, Math.ceil((new Date(2027, 0, 1).getTime() - Date.now()) / 86_400_000))` — Math.ceil exactly per the prototype script lines 1751-1764"
    - "The promise strip (`Secure · Delivered Jan 1, 2027 · Only you can read it`) is rendered below the pad on all steps as a static element"
  artifacts:
    - path: "src/components/Typewriter.tsx"
      provides: "Typewriter helper trimmed to 13 phrases + new fl-caret class for prototype-faithful blink"
      min_lines: 50
      contains: "export const typewriterPhrases, 13 phrases, fl-caret"
    - path: "src/components/FirstLetter.tsx"
      provides: "3-step write→email→success state machine with paper pad, postage stamps, wax seal"
      min_lines: 200
      contains: "interface FirstLetterProps, initialEmail, onEmailSubmit, waitlistCount, type Step = 'write' | 'email' | 'success', useState, transitionTo, fl-step, fl-step-hidden, fl-step-entering, fl-step-leaving, fl-pad, fl-pad-bg, fl-pad-content, fl-pad-body, fl-pad-foot, fl-typewriter, fl-textarea, fl-cta, fl-pad-email, fl-stamps, fl-stamps-right, fl-address-block, fl-address-to-row, fl-address-to-label, fl-email-field, fl-arrow-btn, fl-back, fl-env-actions, fl-pad-success, fl-env-back, fl-env-flap, fl-wax-seal, fl-env-caption, fl-promise, Try it now, Your first letter, starts now., January 1, 2027., Seal &amp; send, Where should we send this?, ← Back, your@email.com, your future self, Math.ceil, 86_400_000, We&apos;ll deliver it on, in, days, Secure, Delivered Jan 1, 2027, Only you can read it, /assets/wax-seal.png, Typewriter, typewriterPhrases"
  key_links:
    - from: "src/components/FirstLetter.tsx Typewriter usage"
      to: "src/components/Typewriter.tsx export"
      via: "import Typewriter, { typewriterPhrases } from './Typewriter'"
      pattern: "from './Typewriter'"
    - from: "src/components/FirstLetter.tsx onEmailSubmit callback"
      to: "App.tsx parent (Wave 3)"
      via: "props.onEmailSubmit(email) — D-06 preserves the call shape"
      pattern: "onEmailSubmit\\(email\\)"
    - from: "src/components/FirstLetter.tsx success-step image"
      to: "/assets/wax-seal.png"
      via: "<img src=\"/assets/wax-seal.png\">"
      pattern: "/assets/wax-seal\\.png"
user_setup: []
---

<objective>
Edit `src/components/Typewriter.tsx` to (a) trim its `typewriterPhrases` export from 14 phrases to 13 matching the prototype, and (b) swap the Tailwind `animate-pulse` caret for a `.fl-caret` class consumed by the global `@keyframes blink` rule already declared in `src/index.css` (Plan 01.5-02). Then full-rewrite `src/components/FirstLetter.tsx` as a 3-step state machine (write → email → success) matching the prototype's `Landing.html` lines 1532–1692 verbatim. The component's outward contract (`FirstLetterProps` with `initialEmail`/`onEmailSubmit`/`waitlistCount`) is preserved verbatim per D-06 / UI-10 so App.tsx's Wave 3 wiring works with no changes.

Purpose: FirstLetter is the most complex section in the prototype — three distinct visual states sharing a container, with paper-pad texture, auto-growing textarea, three inline-SVG postage stamps, wax seal PNG with bouncy entrance keyframe, and a live client-side countdown to 2027-01-01. The prototype tuned the timings (300ms out + 400ms in CSS ease) and the showStep transition algorithm carefully; per D-01 we use the prototype's pure-CSS classes byte-for-byte rather than rederiving via motion/react. Bundling the Typewriter edits in the same plan as FirstLetter is appropriate because FirstLetter directly imports `Typewriter` and `typewriterPhrases`, so the Typewriter edits must land first (within this plan's execution order) or the FirstLetter typecheck breaks.

Output: One file edited (Typewriter.tsx), one file fully rewritten (FirstLetter.tsx). No edits to App.tsx in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.5-ui-redesign-inserted/01.5-CONTEXT.md
@.planning/phases/01.5-ui-redesign-inserted/01.5-RESEARCH.md
@.planning/phases/01.5-ui-redesign-inserted/01.5-UI-SPEC.md
@.planning/phases/01.5-ui-redesign-inserted/01.5-PATTERNS.md
@src/components/Typewriter.tsx
@src/components/FirstLetter.tsx
@src/App.tsx
@src/index.css

# Prototype source:
# .planning/reference/Landing.html lines 1532-1692 (FirstLetter 3 steps full markup)
#   - lines 1532-1545 + 1684-1692 (shell + head + .fl-stage + .fl-promise)
#   - lines 1547-1564 (write step .fl-pad with typewriter + textarea + word count + Seal & send CTA)
#   - lines 1566-1658 (email step .fl-pad.fl-pad-email + 3 inline-SVG stamps + address block + email input + arrow btn + Back)
#   - lines 1660-1681 (success step .fl-pad.fl-pad-success + envelope flap SVG + wax seal img + caption)
# .planning/reference/Landing.html lines 1574-1591 (Stamp 1 — wavy lines "FIRST CLASS · SEALED")
# .planning/reference/Landing.html lines 1599-1616 (Stamp 2 — circular MMXXVI)
# .planning/reference/Landing.html lines 1619-1637 (Stamp 3 — hexagonal 14 MAY 2026)
# .planning/reference/Landing.html lines 1751-1764 (countdown days computation)
# .planning/reference/Landing.html lines 1845-1856 (showStep function — the state-machine spec to translate to React)
# .planning/reference/Landing.html lines 715-1029 (all .fl-* CSS — already in src/index.css)
# .planning/reference/Landing.html lines 1880-1894 (Typewriter 13-phrase array)
# .planning/reference/Landing.html lines 1832-1839 (word count + CTA disable + typewriter hide logic)
# .planning/reference/Landing.html lines 1836-1837 (auto-grow textarea pattern)

<interfaces>
<!-- Preserved verbatim from current src/components/FirstLetter.tsx -->
interface FirstLetterProps {
  initialEmail?: string;
  onEmailSubmit: (email: string) => Promise<void>;
  waitlistCount: number;  // unused in new design but kept for API stability
}

<!-- Local state machine -->
type Step = 'write' | 'email' | 'success';
const [step, setStep] = useState<Step>('write');
const [letter, setLetter] = useState('');
const [email, setEmail] = useState(initialEmail ?? '');
const [leavingStep, setLeavingStep] = useState<Step | null>(null);
const [enteringStep, setEnteringStep] = useState<Step | null>(null);

<!-- Existing Typewriter API (preserved) -->
export const typewriterPhrases: string[];
interface TypewriterProps { phrases?: string[]; /* etc — current 4-line interface preserved */ }
export default function Typewriter(props: TypewriterProps): JSX.Element;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit src/components/Typewriter.tsx — trim phrases 14→13 and replace animate-pulse with fl-caret class</name>
  <files>src/components/Typewriter.tsx</files>
  <read_first>
    - src/components/Typewriter.tsx (current 62-line file — confirm the `typewriterPhrases` export shape, the current `<span className=\"animate-pulse ml-1\">|</span>` line, and the surrounding useEffect/setTimeout loop which is preserved verbatim)
    - .planning/reference/Landing.html lines 1880-1894 (the 13-phrase array — the source of truth; copy verbatim)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-UI-SPEC.md §Typewriter phrases (lines 262–278 — the 13-item list + the explicit note "Existing src/components/Typewriter.tsx already exports `typewriterPhrases` — this list has 14 phrases (one extra: *To realize one day you were exactly where you needed to be*). Update the export to match the prototype's 13-item list.")
    - .planning/phases/01.5-ui-redesign-inserted/01.5-PATTERNS.md `src/components/Typewriter.tsx (MINOR EDIT)` (lines 874–916 — the two edits documented with before/after examples)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-CONTEXT.md `<specifics>` 5th bullet (Typewriter trim — drop the existing 14th phrase entirely)
    - src/index.css (confirm the `.fl-caret` class + `@keyframes blink` exist — they were added by Plan 01.5-02)
  </read_first>
  <action>
    Edit `src/components/Typewriter.tsx`. Make exactly TWO changes; preserve everything else.

    **Change 1 — Trim `typewriterPhrases` array.** Locate the `export const typewriterPhrases = [ ... ]` block. The current array has 14 phrases. Replace it with the 13-phrase array verbatim from UI-SPEC §Typewriter phrases (lines 264–278) which matches `Landing.html` lines 1880–1894 byte-for-byte. The 13 phrases in order:
    1. `To the person you're becoming`
    2. `Because you're proud of yourself and you don't say that enough`
    3. `If you're about to take a risk`
    4. `Because you want to make a promise to yourself and this makes it real`
    5. `To reflect on the messy middle that no one talks about`
    6. `So you don't forget why you started`
    7. `To celebrate a recent win`
    8. `So future you has something to open when things get heavy`
    9. `Because a chapter is ending and you want to close it with intention`
    10. `From the version of you who is doing it scared`
    11. `To capture what you're feeling before life moves too fast`
    12. `For the adventure you're about to take`
    13. `So future you remembers how strong you were right now`

    The phrase that must be DROPPED is the 10th in the current 14-item list: `To realize one day you were exactly where you needed to be`. After this edit the array length is 13.

    **Change 2 — Replace `animate-pulse` caret with `.fl-caret`.** Locate the line `<span className=\"animate-pulse ml-1\">|</span>` (currently src/components/Typewriter.tsx line 58 per RESEARCH.md / PATTERNS.md). Replace it with `<span className=\"fl-caret ml-1\">|</span>`. The `.fl-caret` class (added by Plan 01.5-02 to `src/index.css`) declares `animation: blink 1.1s steps(1) infinite` plus `margin-left: 2px` and `color: rgba(0,0,0,0.45)` per Landing.html lines 799–803. The `ml-1` Tailwind utility provides a slight left margin — keep it for legibility.

    **DO NOT modify** the file's imports, the `TypewriterProps` interface, the useState/useEffect typewriter loop body, or any other line. The `setTimeout(30)` per-char, `setTimeout(3000)` between phrases, `setTimeout(20)` clear delay, and the cleanup pattern remain verbatim per PATTERNS.md line 916.
  </action>
  <verify>
    <automated>
      grep -c "To realize one day you were exactly where you needed to be" src/components/Typewriter.tsx | grep -qE '^0$' &amp;&amp; grep -F "To the person you're becoming" src/components/Typewriter.tsx &amp;&amp; grep -F "So future you remembers how strong you were right now" src/components/Typewriter.tsx &amp;&amp; grep -c '&gt;,$' src/components/Typewriter.tsx | head -1 &amp;&amp; grep -F 'fl-caret ml-1' src/components/Typewriter.tsx &amp;&amp; ! grep -F 'animate-pulse' src/components/Typewriter.tsx
    </automated>
  </verify>
  <acceptance_criteria>
    - Drop assertion: `grep -c "To realize one day you were exactly where you needed to be" src/components/Typewriter.tsx` returns 0 (the 10th phrase from the current list is gone)
    - Phrase count assertion: count the comma-terminated string lines inside the typewriterPhrases array. Approach: `node -e "import('./src/components/Typewriter.tsx').then(m => console.log(m.typewriterPhrases.length))"` — but that won't work because Vite hasn't built. Instead, use a tighter grep: `awk '/typewriterPhrases = \\[/,/\\]/' src/components/Typewriter.tsx | grep -cE "^\\s+['\"]"` should return 13 (counting lines that start with whitespace + a quote character — these are the phrase entries). If the array uses backticks instead of quotes, adjust to `^\\s+\`` in the grep.
    - First-phrase assertion: `grep -F "To the person you're becoming" src/components/Typewriter.tsx` returns 1 (the first phrase per UI-SPEC line 265)
    - Last-phrase assertion: `grep -F "So future you remembers how strong you were right now" src/components/Typewriter.tsx` returns 1 (the 13th phrase per UI-SPEC line 277)
    - Caret-class assertion: `grep -F 'fl-caret ml-1' src/components/Typewriter.tsx` returns 1 (the new caret class)
    - No-animate-pulse assertion: `grep -F 'animate-pulse' src/components/Typewriter.tsx` returns 0 (the old Tailwind class is removed)
    - Preserve-shape assertion: `grep -F 'export const typewriterPhrases' src/components/Typewriter.tsx` returns 1 (export is unchanged)
    - Preserve-shape assertion: `grep -F 'export default function Typewriter' src/components/Typewriter.tsx` returns 1 (default export is unchanged)
    - Preserve-shape assertion: the file still contains `useState` and `useEffect` imports (the typewriter loop is preserved)
  </acceptance_criteria>
  <done>`Typewriter.tsx` now exports 13 phrases matching the prototype byte-for-byte, and the caret span uses `.fl-caret` instead of `animate-pulse` so the global `@keyframes blink` (1.1s steps(1) infinite) fires the prototype's blink rhythm. All other Typewriter internals are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Fully rewrite src/components/FirstLetter.tsx as the 3-step write→email→success state machine</name>
  <files>src/components/FirstLetter.tsx</files>
  <read_first>
    - src/components/FirstLetter.tsx (current 193-line file using a different state shape — replace entirely; only the FirstLetterProps interface name + shape are preserved per D-06)
    - .planning/reference/Landing.html lines 1532-1692 (the entire FirstLetter section — head + 3 steps + promise strip)
    - .planning/reference/Landing.html lines 1845-1856 (the prototype's showStep function — the state-machine algorithm to translate into React state)
    - .planning/reference/Landing.html lines 1751-1764 (countdown days computation: `Math.max(0, Math.ceil((new Date(2027, 0, 1) - new Date()) / (1000 * 60 * 60 * 24)))`)
    - .planning/reference/Landing.html lines 1832-1839 (word-count + CTA-disable + typewriter-hide logic)
    - .planning/reference/Landing.html lines 1836-1837 (auto-grow textarea pattern: e.target.style.height = 'auto'; e.target.style.height = e.target.scrollHeight + 'px')
    - .planning/reference/Landing.html lines 1574-1591 + 1599-1616 + 1619-1637 (3 inline-SVG postage stamps to lift verbatim with JSX-name conversions)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-UI-SPEC.md §Component Contracts `FirstLetter.tsx — FULL REWRITE` (lines 545–571 — full prop signature + 3 internal states + behavior contract per step + the transition timing)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-UI-SPEC.md §First Letter (interactive black section) (lines 244–260 — copy contract for every text element) + §6 First Letter (lines 384–415 — visual contract for every step)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-PATTERNS.md `src/components/FirstLetter.tsx (FULL REWRITE)` (lines 589–792 — full file template, all 3 step JSX trees, state machine, transitionTo function, stepClass helper, word-count + auto-grow + typewriter-hide logic, countdown computation, promise strip JSX, inline-style overrides per landmine #10)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-RESEARCH.md Pattern 4 (lines 783–822 — D-01 pure-CSS step transitions with stepClass helper) + Pattern 5 (wax seal drop is CSS keyframe on mount) + Risk #6 (StrictMode-safe timer cleanup) + Risk #7 (auto-grow textarea cost) + Landmine #10 (inline-style overrides)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-CONTEXT.md D-01, D-01a (pure CSS for FirstLetter step transitions; library split otherwise) + D-06 (preserve `console.error('Waitlist join failed:', error)` in App.tsx — NOT in this file; the FirstLetter just calls `onEmailSubmit(email)` and the parent owns the catch)
    - src/App.tsx lines 197-211 (the current FirstLetter call site — confirms `onEmailSubmit` is `async (newEmail) => { ... try { await joinWaitlistLocal(newEmail); ... } catch (error) { console.error('Waitlist join failed:', error); } }` lives at the parent)
  </read_first>
  <action>
    Replace the entire contents of `src/components/FirstLetter.tsx` with a new implementation following the structure below. Preserve only the `FirstLetterProps` interface name + shape.

    **Imports:** `import { useEffect, useRef, useState } from 'react';` and `import Typewriter, { typewriterPhrases } from './Typewriter';`. Do NOT import `motion` or `AnimatePresence` from `motion/react` (per D-01 + D-01a — the FirstLetter head reveal could use motion/react but the simpler choice here is to skip the reveal on the head since it is a small section; PATTERNS.md lines 1172 leaves this as planner discretion, and the simpler choice is no motion in this file). Do NOT import `Loader2`, `Mail`, `CheckCircle2`, `ShareButtons`, or any Lucide icons (the new design uses inline SVGs).

    **Interface:** Declare `interface FirstLetterProps { initialEmail?: string; onEmailSubmit: (email: string) => Promise<void>; waitlistCount: number; }` verbatim from UI-SPEC line 549.

    **Default export:** `export default function FirstLetter({ initialEmail, onEmailSubmit, waitlistCount }: FirstLetterProps)`. Note: `waitlistCount` is currently unused in the new design (the count lives in the hero counter, not inside FirstLetter) but the prop is preserved per UI-SPEC's API-stability note. To avoid a TS-unused-variable warning, either reference it inside a comment or prefix with underscore: `({ initialEmail, onEmailSubmit, waitlistCount: _waitlistCount }: FirstLetterProps)` is acceptable. Alternatively, leave the rename out and accept the lint warning (no ESLint config exists yet per CLAUDE.md).

    **State:** Declare these state slots inside the function body:
    - `const [step, setStep] = useState<Step>('write');` where `type Step = 'write' | 'email' | 'success';` is declared at module top (above the interface).
    - `const [leavingStep, setLeavingStep] = useState<Step | null>(null);`
    - `const [enteringStep, setEnteringStep] = useState<Step | null>(null);`
    - `const [letter, setLetter] = useState('');`
    - `const [email, setEmail] = useState(initialEmail ?? '');`
    - `const timersRef = useRef<number[]>([]);` for StrictMode-safe transitionTo cleanup
    - On `initialEmail` prop change, sync the email state: a `useEffect(() => { if (initialEmail) setEmail(initialEmail); }, [initialEmail]);` so if the parent's hero-form submit pre-fills the email after FirstLetter is already mounted, the email step input reflects it.

    **Cleanup effect:** Add a `useEffect(() => () => { timersRef.current.forEach(window.clearTimeout); }, []);` so any pending step-transition timeouts are cleared on unmount.

    **`transitionTo(next: Step)` function:** Implement per RESEARCH.md Pattern 4 lines 795–803 and PATTERNS.md FirstLetter section lines 619–628. Algorithm: set `leavingStep` to current `step`; schedule `window.setTimeout(() => { setStep(next); setLeavingStep(null); setEnteringStep(next); window.setTimeout(() => setEnteringStep(null), 400); }, 300);`. Push both timeout ids onto `timersRef.current` so the cleanup effect can clear them. The 300ms / 400ms timings match the CSS `stepOut` (300ms) and `stepIn` (400ms) keyframes exactly.

    **`stepClass(s: Step): string` helper:** Per RESEARCH.md Pattern 4 lines 806–811. Implementation: `if (s === leavingStep) return 'fl-step fl-step-leaving'; if (s !== step) return 'fl-step fl-step-hidden'; if (s === enteringStep) return 'fl-step fl-step-entering'; return 'fl-step';`. This is a pure function of state; call it once per step container.

    **Derived word-count + CTA-disabled:** `const wordCount = letter.trim() ? letter.trim().split(/\\s+/).length : 0;` then `const wordLabel = wordCount + ' word' + (wordCount === 1 ? '' : 's');` and `const ctaDisabled = wordCount === 0;`. Match Landing.html lines 1832–1838 byte-for-byte (per PATTERNS.md lines 645–650).

    **Auto-grow textarea handler:** Inside the `onChange` of the write-step textarea, also set `e.target.style.height = 'auto'; e.target.style.height = e.target.scrollHeight + 'px';` after `setLetter(e.target.value)`. Per PATTERNS.md lines 653–663 + RESEARCH.md Risk #7.

    **Countdown days:** Compute at module body level inside the function (re-computed on every render is fine — the cost is trivial): `const daysToDelivery = Math.max(0, Math.ceil((new Date(2027, 0, 1).getTime() - Date.now()) / 86_400_000));`. Per Landing.html lines 1751–1764 and PATTERNS.md lines 774–777. Use `Math.ceil`, NOT `Math.floor`.

    **Email-submit handler:** `const handleEmailSubmit = (e: React.FormEvent) => { e.preventDefault(); if (!email.trim()) return; onEmailSubmit(email); transitionTo('success'); };`. Per PATTERNS.md lines 743–751: the parent owns the try/catch around `joinWaitlistLocal`; this component fires and forgets (the optimistic transition matches the prototype's perceived-instant submit). The `onEmailSubmit` returns a Promise but the handler does NOT await it — the parent's try/catch operates independently. Do NOT add a `try/catch` here; per D-06 the catch lives in App.tsx.

    **Render — the whole section:**

    `<section id=\"first-letter\" className=\"first-letter\">` containing `<div className=\"fl-inner\">` which contains four children:

    (A) **`<div className=\"fl-eyebrow\">`** with three spans: `<span className=\"rule\" />`, `<span>Try it now</span>`, `<span className=\"rule\" />`. Copy `Try it now` from UI-SPEC line 248.

    (B) **`<div className=\"fl-head\">`** containing:
    - `<h2>Your first letter <em>starts now.</em></h2>` (UI-SPEC line 249; italic em on `starts now.`)
    - `<p className=\"fl-head-sub\">Send a letter to your future self, and we'll deliver it on <strong style={{ background: 'rgba(255,255,255,0.18)', borderRadius: '3px', padding: '1px 5px' }}>January 1, 2027.</strong></p>` (UI-SPEC line 250 — the `<strong>` has the inline highlighted background per the prototype). The apostrophe in `we'll` is a curly right-quote U+2019 — preserve verbatim. (You can also use `we&apos;ll` in JSX text.)

    (C) **`<div className=\"fl-stage\">`** containing three step containers:

       **Write step:** `<div className={stepClass('write')} id=\"fl-step-write\">` containing:
       - `<div className=\"fl-pad\">` containing:
         - `<div className=\"fl-pad-bg\" />` (CSS paints the paper texture from `/assets/paper-light.jpg`)
         - `<div className=\"fl-pad-content\">` containing:
           - `<div className=\"fl-pad-body\" style={{ paddingTop: '8px', fontSize: '22px', fontWeight: 600 }}>` — these inline overrides per Landmine #10 / PATTERNS.md lines 678–688. Inside this body:
             - When `letter.length === 0`: render `<div className=\"fl-typewriter\"><Typewriter phrases={typewriterPhrases} /><span className=\"fl-caret\">|</span></div>`. The conditional uses `letter.length === 0` (strict length zero) — not `letter.trim().length === 0` — per RESEARCH.md §Section 8b "Typewriter visibility pattern" (PATTERNS.md line 666 calls out the exact-match-with-prototype reason).
             - Always render `<textarea className=\"fl-textarea\" spellCheck={false} value={letter} onChange={(e) => { setLetter(e.target.value); e.target.style.height = 'auto'; e.target.style.height = e.target.scrollHeight + 'px'; }} />`. Note `spellCheck` (camelCase, JSX) NOT `spellcheck`.
           - `<div className=\"fl-pad-foot\" style={{ justifyContent: 'flex-start' }}>` containing `<span>{wordLabel}</span>`. The inline `justifyContent: 'flex-start'` per Landmine #10 / PATTERNS.md line 685.
       - `<button type=\"button\" className=\"fl-cta\" disabled={ctaDisabled} onClick={() => transitionTo('email')}>Seal &amp; send</button>`. Note `Seal &amp; send` not `Seal & send` in JSX text (the `&` must be entity-escaped). The CTA label is exactly `Seal & send` per UI-SPEC line 253 — the JSX rendering of `Seal &amp; send` produces the visible string `Seal & send`.

       **Email step:** `<div className={stepClass('email')} id=\"fl-step-email\">` containing:
       - `<div className=\"fl-pad fl-pad-email\">` containing:
         - `<div className=\"fl-stamps\" aria-hidden=\"true\">` containing the inline SVG for Stamp 1 (the wavy "FIRST CLASS · SEALED" stamp from Landing.html lines 1574–1591). Apply JSX-name conversions: stroke-width→strokeWidth, stroke-linecap→strokeLinecap, stroke-linejoin→strokeLinejoin, text-anchor→textAnchor, font-family→fontFamily, font-size→fontSize, font-weight→fontWeight, letter-spacing→letterSpacing. The stamp `<svg>` has `width=\"119\"`, `height=\"93\"`, `viewBox=\"0 0 160 120\"`.
         - `<div className=\"fl-stamps-right\" aria-hidden=\"true\">` containing the inline SVG for Stamp 2 (circular MMXXVI, Landing.html lines 1599–1616, `<svg width=\"93\" height=\"93\" viewBox=\"0 0 130 130\">`) and Stamp 3 (hexagonal 14 MAY 2026, lines 1619–1637, `<svg width=\"101\" height=\"87\" viewBox=\"0 0 200 160\">`). Stamp 3's overlap is handled by the global CSS rule `.fl-stamps-right > svg:nth-child(2) { margin-left: -18px; }` already in `src/index.css`.
         - `<div className=\"fl-pad-content\" style={{ padding: '106px 0 0', justifyContent: 'center' }}>` per Landmine #10. Containing `<div className=\"fl-address-block\">` which contains:
           - `<div className=\"fl-address-label\" style={{ fontSize: '15px', fontWeight: 500, width: '444px', fontFamily: 'Space Mono' }}>Where should we send this?</div>` — all four inline style properties per Landmine #10. Copy `Where should we send this?` per UI-SPEC line 254.
           - `<form onSubmit={handleEmailSubmit} style={{ width: '100%' }}>` containing:
             - `<div className=\"fl-address-to-row\">` containing:
               - `<span className=\"fl-address-to-label\">To</span>` (UI-SPEC line 255)
               - `<input type=\"email\" id=\"fl-email-input\" placeholder=\"your@email.com\" required autoComplete=\"email\" className=\"fl-email-field\" value={email} onChange={(e) => setEmail(e.target.value)} />` (placeholder per UI-SPEC line 256)
               - `<button type=\"submit\" className=\"fl-arrow-btn\" aria-label=\"Seal it\">→</button>` (the `→` is U+2192; aria-label per UI-SPEC line 257)
             - `<div className=\"fl-env-actions\">` containing `<button type=\"button\" className=\"fl-back\" onClick={() => transitionTo('write')}>← Back</button>` (the `←` is U+2190; copy `← Back` per UI-SPEC line 258).

       **Success step:** `<div className={stepClass('success')} id=\"fl-step-success\">` containing:
       - `<div className=\"fl-pad fl-pad-success\">` containing:
         - `<div className=\"fl-env-back\">` containing:
           - `<svg className=\"fl-env-flap\" viewBox=\"0 0 800 440\" preserveAspectRatio=\"none\" aria-hidden=\"true\"><path d=\"M0 0 L400 270 L800 0 Z\" fill=\"rgba(0,0,0,0.022)\" /></svg>` (envelope flap triangle — lifted from Landing.html lines 1666–1669; `preserveAspectRatio=\"none\"` is correct, this is intentional to stretch the triangle to the full pad width)
           - `<div className=\"fl-wax-seal\" aria-hidden=\"true\"><img src=\"/assets/wax-seal.png\" alt=\"\" width=\"140\" height=\"143\" /></div>` (the wax-seal PNG; the CSS rule on `.fl-wax-seal` fires the `waxDrop` keyframe automatically on DOM mount per RESEARCH.md Pattern 5)
           - `<div className=\"fl-env-caption\">We&apos;ll deliver it on <strong>January 1, 2027</strong> · in <strong>{daysToDelivery} days</strong></div>` — caption per UI-SPEC line 259; the literal `· ` between the two strong tags is a middle dot U+00B7 surrounded by spaces.

    (D) **`<div className=\"fl-promise\" style={{ color: '#fff' }}>`** containing five children (from PATTERNS.md lines 781–789):
    - `<span>Secure</span>`
    - `<span className=\"sep\" style={{ background: 'rgba(255,255,255,0.8)' }} />`
    - `<span>Delivered Jan 1, 2027</span>`
    - `<span className=\"sep\" style={{ background: 'rgba(255,255,255,0.8)' }} />`
    - `<span>Only you can read it</span>`
    UI-SPEC line 260 names this the "promise strip"; it sits below the pad on all steps as a static element.

    Do NOT render `.fl-pad-head .salut` (`Dear future self,`) or `.fl-pad-foot .sign` (`Past self,`) — UI-SPEC §Out of Scope item 8. The CSS for them exists but the body markup must not render them.

    Do NOT add a `motion.div` wrapper around `.fl-head` for scroll-reveal — PATTERNS.md leaves this as planner discretion (line 1172); for context-budget reasons we skip it here and rely on the pure-CSS `.fl-head` styling that exists in `src/index.css`. If a future polish phase wants the reveal, it can be added.

    No `dangerouslySetInnerHTML` anywhere — every content string is a plain JSX child or attribute.
  </action>
  <verify>
    <automated>
      test -f src/components/FirstLetter.tsx &amp;&amp; grep -F 'interface FirstLetterProps' src/components/FirstLetter.tsx &amp;&amp; grep -F 'initialEmail?: string' src/components/FirstLetter.tsx &amp;&amp; grep -F 'onEmailSubmit: (email: string) =&gt; Promise&lt;void&gt;' src/components/FirstLetter.tsx &amp;&amp; grep -F 'waitlistCount' src/components/FirstLetter.tsx &amp;&amp; grep -F 'export default function FirstLetter' src/components/FirstLetter.tsx &amp;&amp; grep -E "type Step = 'write' \\| 'email' \\| 'success'" src/components/FirstLetter.tsx &amp;&amp; grep -F 'transitionTo' src/components/FirstLetter.tsx &amp;&amp; grep -F 'fl-step-leaving' src/components/FirstLetter.tsx &amp;&amp; grep -F 'fl-step-entering' src/components/FirstLetter.tsx &amp;&amp; grep -F 'fl-step-hidden' src/components/FirstLetter.tsx &amp;&amp; grep -F 'fl-pad-email' src/components/FirstLetter.tsx &amp;&amp; grep -F 'fl-pad-success' src/components/FirstLetter.tsx &amp;&amp; grep -F 'fl-stamps-right' src/components/FirstLetter.tsx &amp;&amp; grep -F 'fl-env-flap' src/components/FirstLetter.tsx &amp;&amp; grep -F 'fl-wax-seal' src/components/FirstLetter.tsx &amp;&amp; grep -F '/assets/wax-seal.png' src/components/FirstLetter.tsx &amp;&amp; grep -F 'fl-promise' src/components/FirstLetter.tsx &amp;&amp; grep -F 'Try it now' src/components/FirstLetter.tsx &amp;&amp; grep -F 'Your first letter' src/components/FirstLetter.tsx &amp;&amp; grep -F 'starts now.' src/components/FirstLetter.tsx &amp;&amp; grep -F 'January 1, 2027' src/components/FirstLetter.tsx &amp;&amp; grep -F 'Seal &amp; send' src/components/FirstLetter.tsx &amp;&amp; grep -F 'Where should we send this?' src/components/FirstLetter.tsx &amp;&amp; grep -F 'your@email.com' src/components/FirstLetter.tsx &amp;&amp; grep -F 'Back' src/components/FirstLetter.tsx &amp;&amp; grep -F 'Math.ceil' src/components/FirstLetter.tsx &amp;&amp; grep -F '86_400_000' src/components/FirstLetter.tsx &amp;&amp; grep -F 'Secure' src/components/FirstLetter.tsx &amp;&amp; grep -F 'Delivered Jan 1, 2027' src/components/FirstLetter.tsx &amp;&amp; grep -F 'Only you can read it' src/components/FirstLetter.tsx &amp;&amp; grep -F "import Typewriter, { typewriterPhrases } from './Typewriter'" src/components/FirstLetter.tsx &amp;&amp; ! grep -E "from 'motion/react'" src/components/FirstLetter.tsx &amp;&amp; ! grep -F 'AnimatePresence' src/components/FirstLetter.tsx &amp;&amp; ! grep -F "from 'lucide-react'" src/components/FirstLetter.tsx &amp;&amp; ! grep -F 'ShareButtons' src/components/FirstLetter.tsx &amp;&amp; ! grep -F 'joinWaitlistLocal' src/components/FirstLetter.tsx
    </automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'interface FirstLetterProps' src/components/FirstLetter.tsx` returns 1
    - Source assertion: all three preserved props appear with their exact types: `initialEmail?: string`, `onEmailSubmit: (email: string) =&gt; Promise&lt;void&gt;`, `waitlistCount: number` (or `waitlistCount` referenced with optional rename per the action's underscore-prefix note)
    - Source assertion: `grep -c 'export default function FirstLetter' src/components/FirstLetter.tsx` returns 1
    - State-machine assertion: `grep -E "type Step = 'write' \\| 'email' \\| 'success'" src/components/FirstLetter.tsx` returns 1
    - State-machine assertion: `grep -F 'transitionTo' src/components/FirstLetter.tsx` returns at least 3 (1 definition + at least 2 call sites: `transitionTo('email')` from write CTA, `transitionTo('success')` from email submit, `transitionTo('write')` from email Back button)
    - State-machine assertion: each of `fl-step-leaving`, `fl-step-entering`, `fl-step-hidden`, `fl-step` appears
    - Step IDs: each of `fl-step-write`, `fl-step-email`, `fl-step-success` appears as an `id=` attribute
    - Class assertion: every required className appears: `first-letter`, `fl-inner`, `fl-eyebrow`, `fl-head`, `fl-pad`, `fl-pad-bg`, `fl-pad-content`, `fl-pad-body`, `fl-pad-foot`, `fl-typewriter`, `fl-caret`, `fl-textarea`, `fl-cta`, `fl-stage`, `fl-step`, `fl-pad-email`, `fl-stamps`, `fl-stamps-right`, `fl-address-block`, `fl-address-label`, `fl-address-to-row`, `fl-address-to-label`, `fl-email-field`, `fl-arrow-btn`, `fl-back`, `fl-env-actions`, `fl-pad-success`, `fl-env-back`, `fl-env-flap`, `fl-wax-seal`, `fl-env-caption`, `fl-promise`
    - SVG assertion: `grep -c '&lt;svg ' src/components/FirstLetter.tsx` returns at least 4 (3 postage stamps in email step + 1 envelope flap in success step)
    - Asset assertion: `grep -F '/assets/wax-seal.png' src/components/FirstLetter.tsx` returns 1
    - Copy assertion: each of `Try it now`, `Your first letter`, `starts now.`, `Send a letter to your future self`, `January 1, 2027`, `Seal &amp; send`, `Where should we send this?`, `To`, `your@email.com`, `← Back`, `Secure`, `Delivered Jan 1, 2027`, `Only you can read it` appears
    - Countdown assertion: `grep -F 'Math.ceil' src/components/FirstLetter.tsx` returns at least 1 (NOT Math.floor; per Landing.html line 1763)
    - Countdown assertion: `grep -E '(86_400_000|86400000|1000 \\* 60 \\* 60 \\* 24)' src/components/FirstLetter.tsx` returns at least 1 (day-millisecond constant)
    - Word-count assertion: `grep -E "split\\(/\\\\\\\\s\\+/\\)" src/components/FirstLetter.tsx` returns at least 1 (the prototype's `split(/\\s+/)` pattern)
    - Auto-grow assertion: `grep -F 'scrollHeight' src/components/FirstLetter.tsx` returns at least 1 (textarea auto-grow)
    - Inline-style assertion: `grep -F "paddingTop: '8px'" src/components/FirstLetter.tsx` returns 1 (fl-pad-body override) + `grep -F "padding: '106px 0 0'" src/components/FirstLetter.tsx` returns 1 (email step fl-pad-content) + `grep -F "width: '444px'" src/components/FirstLetter.tsx` returns 1 (address label override)
    - Import assertion: `grep -F "import Typewriter, { typewriterPhrases } from './Typewriter'" src/components/FirstLetter.tsx` returns 1
    - No-Motion assertion: `grep -E "from 'motion/react'" src/components/FirstLetter.tsx` returns 0 (per D-01 — pure CSS step transitions)
    - No-AnimatePresence assertion: `grep -F 'AnimatePresence' src/components/FirstLetter.tsx` returns 0
    - No-Lucide assertion: `grep -E "from 'lucide-react'" src/components/FirstLetter.tsx` returns 0 (all icons are inline SVG; per D-07 + UI-SPEC)
    - No-ShareButtons assertion: `grep -F 'ShareButtons' src/components/FirstLetter.tsx` returns 0
    - No-direct-API assertion: `grep -F 'joinWaitlistLocal' src/components/FirstLetter.tsx` returns 0 (per D-06; parent owns the call)
    - Out-of-scope assertion: `grep -F 'Dear future self' src/components/FirstLetter.tsx` returns 0 (the .salut element is in CSS but not in body — UI-SPEC §Out of Scope item 8)
    - Out-of-scope assertion: `grep -F 'Past self' src/components/FirstLetter.tsx` returns 0 (the .sign element is in CSS but not in body)
    - StrictMode cleanup: file contains a `useEffect(() => () =&gt; { ...clearTimeout... }, []);` or equivalent cleanup pattern
    - File size: `wc -l src/components/FirstLetter.tsx` returns a value &gt;= 200 and &lt;= 450
  </acceptance_criteria>
  <done>`FirstLetter.tsx` has been fully rewritten as the 3-step write→email→success state machine matching the prototype byte-for-byte. The `FirstLetterProps` interface is preserved verbatim. Step transitions use pure CSS classes per D-01 (no AnimatePresence). All three inline-SVG postage stamps are in place. The wax-seal PNG references `/assets/wax-seal.png`. The countdown uses `Math.ceil` and `86_400_000`. The promise strip is rendered below the pad. The component imports Typewriter and the 13-phrase array from `./Typewriter`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → letter textarea | The user types arbitrary text into `.fl-textarea`. The text is stored in local `letter` state and never leaves the component in Phase 1.5 (the email step's `onEmailSubmit` callback receives only the email, NOT the letter body — that wire-up is Phase 4). |
| User → email input | User-supplied email crosses into the component as a controlled-input value. On email-step submit, the value is passed to the parent's `onEmailSubmit` callback unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.5-07-01 | Tampering | textarea letter content | accept | The letter body is local state only in Phase 1.5; it is not sent anywhere. Phase 4 will wire it to the Edge Function with proper input handling. |
| T-01.5-07-02 | Tampering | client-side email input | mitigate | HTML5 `type="email"` + `required` + `autoComplete="email"` provide first-line validation. The actual sanitization happens in Phase 2 (`SIGNUP-05` + Turnstile + IP rate limit in the Edge Function). |
| T-01.5-07-03 | Cross-Site Scripting | rendered output | accept | All rendered text comes from either static literals or controlled inputs (`{letter}`, `{email}`, `{wordLabel}`, `{daysToDelivery}`). React 19 escapes all of these. No `dangerouslySetInnerHTML` is used. |
| T-01.5-07-04 | Denial of Service | rapid step transitions | mitigate | The `transitionTo` function uses `setTimeout` ids tracked in `timersRef`, with cleanup on unmount. Rapid re-clicks of the CTA cannot accumulate timers because each press goes through `transitionTo` which re-uses the ref. Additional debouncing is not needed at Phase 1.5 because the surface is local-only. |

`[ASVS-L1: visual port preserves Phase 1 data path — Phase 2 owns the real signup security surface. Phase 1.5 sanity check: the email callback fires `joinWaitlistLocal` which is a no-op stub today; no DB writes occur.]`
</threat_model>

<verification>
After both tasks:

1. Both files exist and pass their grep-based acceptance criteria.
2. `npm run build` is NOT yet expected to pass — App.tsx still imports the old chrome. The build green check is owned by 01.5-09-PLAN.md.
3. The Typewriter array length is exactly 13 (one phrase dropped).
4. FirstLetter's `FirstLetterProps` interface is preserved verbatim, so App.tsx's existing call site (currently `<FirstLetter initialEmail={...} onEmailSubmit={...} waitlistCount={...} />`) works without change.
5. The pure-CSS step transition pattern per D-01 means the keyframes `stepIn` (400ms) and `stepOut` (300ms) declared in `src/index.css` (Plan 01.5-02) fire when the appropriate `.fl-step-entering` / `.fl-step-leaving` classes are toggled — no AnimatePresence orchestrates this.
</verification>

<success_criteria>
- `Typewriter.tsx` exports 13 phrases (was 14; the 10th dropped) and uses `.fl-caret` (not `animate-pulse`).
- `FirstLetter.tsx` is a 3-step state machine using pure-CSS classes for transitions (`fl-step-entering` / `fl-step-leaving` / `fl-step-hidden`).
- `FirstLetterProps` interface unchanged from current; App.tsx call site in Wave 3 works without modification.
- All 3 inline-SVG postage stamps are present in the email step; the wax-seal PNG references `/assets/wax-seal.png`; the countdown uses `Math.ceil` and `86_400_000`.
- Promise strip rendered below the pad on all steps.
- No Motion library, no AnimatePresence, no Lucide imports in either file.
- StrictMode-safe timer cleanup is in place in FirstLetter.
</success_criteria>

<output>
After completion, create `.planning/phases/01.5-ui-redesign-inserted/01.5-07-SUMMARY.md` listing: the final Typewriter phrase count (13), the FirstLetter file line count, confirmation that the FirstLetterProps interface is unchanged, the three inline-SVG stamp viewBox values, the wax-seal asset path, and the countdown formula used.
</output>
</content>
</invoke>