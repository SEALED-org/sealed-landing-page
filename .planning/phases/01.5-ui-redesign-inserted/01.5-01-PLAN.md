---
phase: 01.5
slug: ui-redesign-inserted
plan: 01
id: 01.5-01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/separator-ink.png
  - public/assets/step-write.png
  - public/assets/step-seal.png
  - public/assets/step-open.png
  - public/assets/wax-seal.png
  - public/assets/paper-light.jpg
  - .planning/reference/Landing.html
  - .planning/reference/assets/separator-ink.png
  - .planning/reference/assets/step-write.png
  - .planning/reference/assets/step-seal.png
  - .planning/reference/assets/step-open.png
  - .planning/reference/assets/wax-seal.png
  - index.html
autonomous: true
requirements: [UI-09]
requirements_addressed: [UI-09]
must_haves:
  truths:
    - "Vite serves /assets/separator-ink.png and the four other prototype PNGs at the site root"
    - "/assets/paper-light.jpg is locally hosted (no i.postimg.cc fetch at runtime)"
    - ".planning/reference/Landing.html exists with the same byte content as the source prototype so verification is reproducible in any future session"
    - "Document title in the browser tab reads `SEALED — Some letters are worth waiting for.`"
    - "Per D-03: all five required PNG assets (separator-ink.png, step-write.png, step-seal.png, step-open.png, wax-seal.png) are copied from the prototype bundle into public/assets/ during this foundational task — closes the asset-sourcing decision for the phase"
  artifacts:
    - path: "public/assets/separator-ink.png"
      provides: "Hero ink-stroke separator PNG (2572×190, ~127 KB)"
    - path: "public/assets/step-write.png"
      provides: "How-it-Works step 01 screenshot (~46 KB)"
    - path: "public/assets/step-seal.png"
      provides: "How-it-Works step 02 screenshot (~1.0 MB)"
    - path: "public/assets/step-open.png"
      provides: "How-it-Works step 03 screenshot (~97 KB)"
    - path: "public/assets/wax-seal.png"
      provides: "FirstLetter success-step wax seal PNG (290×290, ~128 KB)"
    - path: "public/assets/paper-light.jpg"
      provides: "Self-hosted paper texture used by .fl-pad-bg (closes CONTENT-03 per D-02)"
    - path: ".planning/reference/Landing.html"
      provides: "Reproducible prototype snapshot for side-by-side parity verification (per D-04)"
    - path: ".planning/reference/assets/"
      provides: "5 PNG snapshots so the reference Landing.html resolves its <img> tags"
    - path: "index.html"
      provides: "<title> matches the locked copy `SEALED — Some letters are worth waiting for.`"
  key_links:
    - from: "src/components (Wave 2)"
      to: "/assets/*.png"
      via: "absolute paths in JSX <img src> attributes served by Vite from public/"
      pattern: "/assets/(separator-ink|step-write|step-seal|step-open|wax-seal)\\.png"
    - from: "src/index.css .fl-pad-bg rule (Wave 1, Plan 02)"
      to: "/assets/paper-light.jpg"
      via: "background-image: url('/assets/paper-light.jpg')"
      pattern: "/assets/paper-light\\.jpg"
user_setup: []
---

<objective>
Stand up the static asset pipeline and finalize the document title so every downstream Wave-2 component plan has stable, locally-served inputs. Five PNG assets must be copied from the prototype bundle into `public/assets/`, the `paper-light.jpg` paper texture must be self-hosted (closes CONTENT-03 per D-02), the prototype `Landing.html` plus its five PNG assets must be snapshotted into `.planning/reference/` so verification is reproducible after `/tmp/` is cleared (D-04), and `index.html` `<title>` must match the locked copy in UI-SPEC.

Purpose: Wave-2 component executors reference `/assets/*.png` paths in JSX and CSS — if the files do not exist, every visual checkpoint fails and the asset paths show as broken in the dev server. The `.planning/reference/` snapshot exists so verification (`/gsd-verify-phase`) can open the reference HTML in a browser even if `/tmp/sealed-design/` is wiped. The title fix is a one-line change but it is part of the locked UI-SPEC copy contract.

Output: 6 PNG/JPG files under `public/assets/`, 6 reference files under `.planning/reference/`, and a one-line edit to `index.html`. No source-code (`src/`) changes in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01.5-ui-redesign-inserted/01.5-CONTEXT.md
@.planning/phases/01.5-ui-redesign-inserted/01.5-RESEARCH.md
@.planning/phases/01.5-ui-redesign-inserted/01.5-UI-SPEC.md
@.planning/phases/01.5-ui-redesign-inserted/01.5-PATTERNS.md
@index.html

# Source-of-truth prototype + assets (lives outside the repo at /tmp; copy operations target these paths)
# /tmp/sealed-design/sealed-prototype-landing-page-remix-remix/project/Landing.html
# /tmp/sealed-design/sealed-prototype-landing-page-remix-remix/project/assets/{separator-ink,step-write,step-seal,step-open,wax-seal}.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy 5 PNG assets to public/assets/ and snapshot prototype + assets into .planning/reference/</name>
  <files>
    public/assets/separator-ink.png,
    public/assets/step-write.png,
    public/assets/step-seal.png,
    public/assets/step-open.png,
    public/assets/wax-seal.png,
    .planning/reference/Landing.html,
    .planning/reference/assets/separator-ink.png,
    .planning/reference/assets/step-write.png,
    .planning/reference/assets/step-seal.png,
    .planning/reference/assets/step-open.png,
    .planning/reference/assets/wax-seal.png
  </files>
  <read_first>
    - .planning/phases/01.5-ui-redesign-inserted/01.5-CONTEXT.md (D-03 PNG asset copy; D-04 reference snapshot)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-RESEARCH.md §Asset Placeholder Strategy (table of 5 source/target paths, exact file sizes)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-UI-SPEC.md §Asset Contract (lines 642–662 — file table with target paths under public/assets/ and prohibition against ES-module imports)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-PATTERNS.md File Classification row for `public/assets/*.png` (data flow: file-I/O copy only)
  </read_first>
  <action>
    Create the directories `public/assets/` and `.planning/reference/assets/` if they do not exist (note: `public/` does not currently exist in the repo — verified by `ls`). Copy the five source PNG files from `/tmp/sealed-design/sealed-prototype-landing-page-remix-remix/project/assets/` to BOTH `public/assets/` (so Vite serves them at `/assets/*.png` at runtime) AND `.planning/reference/assets/` (so the reference Landing.html resolves its `<img>` tags during verification). Also copy `/tmp/sealed-design/sealed-prototype-landing-page-remix-remix/project/Landing.html` to `.planning/reference/Landing.html` (single file at the reference root, NOT inside the assets/ subdir). Use `cp` (binary copy) — do not transform or rename. Source filenames map 1:1 to target filenames: `separator-ink.png`, `step-write.png`, `step-seal.png`, `step-open.png`, `wax-seal.png`. The order of copies within the task is the planner's discretion (per CONTEXT.md last bullet under "Claude's Discretion") — perform them however is fastest; serial is fine since each is small. Do NOT add the files to `src/`, do NOT ES-import them (UI-SPEC explicitly says reference via absolute path `/assets/*.png` in JSX, not `import imgUrl from './assets/...'`). Per D-04, `.planning/reference/` is NOT served by Vite (it lives outside `public/`) — that is intentional; it is purely for verifier reproducibility.
  </action>
  <verify>
    <automated>
      ls -la public/assets/separator-ink.png public/assets/step-write.png public/assets/step-seal.png public/assets/step-open.png public/assets/wax-seal.png &amp;&amp; ls -la .planning/reference/Landing.html .planning/reference/assets/separator-ink.png .planning/reference/assets/step-write.png .planning/reference/assets/step-seal.png .planning/reference/assets/step-open.png .planning/reference/assets/wax-seal.png
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists: `public/assets/separator-ink.png` with size 126633 bytes (matches RESEARCH.md Asset Placeholder Strategy table)
    - File exists: `public/assets/step-write.png` with size 46494 bytes
    - File exists: `public/assets/step-seal.png` with size 1011873 bytes
    - File exists: `public/assets/step-open.png` with size 97405 bytes
    - File exists: `public/assets/wax-seal.png` with size 127807 bytes
    - File exists: `.planning/reference/Landing.html` and its byte size matches `/tmp/sealed-design/sealed-prototype-landing-page-remix-remix/project/Landing.html`
    - All five `.planning/reference/assets/*.png` files exist and are byte-identical to the source files in `/tmp/sealed-design/.../project/assets/`
    - Command `diff -rq /tmp/sealed-design/sealed-prototype-landing-page-remix-remix/project/assets/ public/assets/ | grep -v paper-light.jpg` produces zero lines of output (zero diffs ignoring paper-light.jpg which is handled in Task 2)
    - No new files inside `src/assets/` (the convention is `public/assets/`, not `src/assets/`)
  </acceptance_criteria>
  <done>5 prototype PNGs are present under `public/assets/` AND `.planning/reference/assets/`; `Landing.html` is mirrored at `.planning/reference/Landing.html`; the directory `public/` did not previously exist and was created by this task.</done>
</task>

<task type="auto">
  <name>Task 2: Download and self-host paper-light.jpg into public/assets/</name>
  <files>public/assets/paper-light.jpg</files>
  <read_first>
    - .planning/phases/01.5-ui-redesign-inserted/01.5-CONTEXT.md D-02 (full text: paper-light.jpg is self-hosted in this phase; download from https://i.postimg.cc/yWZ0XSgh/lightpaper.jpg; commit to public/assets/paper-light.jpg; CONTENT-03 closes here)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-UI-SPEC.md §Asset Contract (line 660 — "One asset NOT in the bundle: paper-light.jpg")
    - .planning/phases/01.5-ui-redesign-inserted/01.5-PATTERNS.md Critical fixes vs. prototype line about replacing `url('https://i.postimg.cc/yWZ0XSgh/lightpaper.jpg')` with `url('/assets/paper-light.jpg')` (the URL replacement happens in 01.5-02-PLAN.md when extending src/index.css — this task only places the file)
  </read_first>
  <action>
    Download the JPEG image at `https://i.postimg.cc/yWZ0XSgh/lightpaper.jpg` to `public/assets/paper-light.jpg`. Use `curl -fsSL` (fail on error, silent progress, follow redirects) — do not use the WebFetch tool because WebFetch transforms HTML to markdown and would corrupt the binary. The expected MIME type is `image/jpeg`. The resulting file size will be in the ~50–500 KB range (the postimg CDN serves the same image regardless of request time). Do NOT add a TODO comment to anything in this task — per D-02, no `// TODO(CONTENT-03)` marker is needed because CONTENT-03 closes here in Phase 1.5. The CSS URL replacement (`url('https://i.postimg.cc/...')` → `url('/assets/paper-light.jpg')`) happens inside 01.5-02-PLAN.md when the planner appends the global `.fl-pad-bg` rule — this current task only places the file on disk.
  </action>
  <verify>
    <automated>
      test -s public/assets/paper-light.jpg &amp;&amp; file public/assets/paper-light.jpg | grep -i 'JPEG image'
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists: `public/assets/paper-light.jpg`
    - File size &gt; 0 bytes (the download succeeded and was not a zero-byte error response)
    - `file public/assets/paper-light.jpg` output contains `JPEG image data` (correct MIME inferred from magic bytes, NOT an HTML error page)
    - File is NOT under `src/` — must be at `public/assets/paper-light.jpg` so Vite serves it at the absolute path `/assets/paper-light.jpg`
    - No TODO comment was added to any file (per D-02, no `// TODO(CONTENT-03)` marker)
  </acceptance_criteria>
  <done>`paper-light.jpg` is locally hosted under `public/assets/` and CSS rules can now reference it as `/assets/paper-light.jpg` (the actual rule edit lands in 01.5-02-PLAN.md). CONTENT-03 is satisfied early.</done>
</task>

<task type="auto">
  <name>Task 3: Update index.html document title to the locked UI-SPEC copy</name>
  <files>index.html</files>
  <read_first>
    - index.html (the entire file — only 30 lines; need to see the current `<title>` and surrounding meta)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-UI-SPEC.md line 185 (target title: `SEALED — Some letters are worth waiting for.`)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-PATTERNS.md `index.html` row (current line 6: `<title>SEALED — Letters to Your Future Self</title>`; target: `<title>SEALED — Some letters are worth waiting for.</title>`)
  </read_first>
  <action>
    Locate the `<title>` element in `index.html` (currently reads `SEALED — Letters to Your Future Self` per PATTERNS.md). Replace it so the inner text is exactly `SEALED — Some letters are worth waiting for.` — preserve the em-dash character (U+2014, not a hyphen-minus and not an en-dash). Do not edit any other element of `index.html` (no meta-tag changes, no OG tag edits, no script/link tag edits — those are out of scope for this phase). The `<title>` element should remain on its current line; only the text content between `<title>` and `</title>` changes.
  </action>
  <verify>
    <automated>
      grep -F '&lt;title&gt;SEALED — Some letters are worth waiting for.&lt;/title&gt;' index.html
    </automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c '&lt;title&gt;SEALED — Some letters are worth waiting for.&lt;/title&gt;' index.html` returns 1
    - Source assertion: `grep -c 'Letters to Your Future Self' index.html` returns 0 (old title fully replaced)
    - Em-dash check: `python3 -c "import sys; t = open('index.html').read(); assert '—' in t, 'em dash missing'"` exits 0 (the em-dash character U+2014 is present)
    - No other `index.html` content is changed (`git diff --shortstat index.html` shows a single-line edit only)
  </acceptance_criteria>
  <done>`index.html` `<title>` matches the UI-SPEC copy contract verbatim, including the em-dash (U+2014).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External CDN → repo | `paper-light.jpg` is fetched once from `i.postimg.cc` and committed to the repo. After this phase no runtime fetch crosses this boundary. |
| Filesystem → bundle | PNG assets are copied from `/tmp/sealed-design/` (outside the repo) into `public/assets/`. Source files are trusted (they are the locked Claude Design prototype). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.5-01-01 | Tampering | `paper-light.jpg` CDN download | accept | One-time fetch from a public CDN; the file becomes a committed binary in git, so any future tampering would require modifying the committed file and would be visible in `git log`. No PII, no executable content, low value target. |
| T-01.5-01-02 | Information Disclosure | static PNG/JPEG assets | accept | Asset files are public marketing imagery (hero separator, step screenshots, wax seal, paper texture). No user data, no secrets. ASVS-L1 N/A — assets are intended to be world-readable. |
| T-01.5-01-03 | Denial of Service | `paper-light.jpg` runtime fetch | mitigate | This task self-hosts the asset under `public/assets/paper-light.jpg`, eliminating the runtime dependency on `i.postimg.cc`. The site no longer fails if the third-party CDN is unreachable. |

`[ASVS-L1: N/A — static asset pipeline only; no user input, no auth surface, no data processing]`
</threat_model>

<verification>
After all three tasks complete:

1. `ls public/assets/` lists exactly 6 files: `separator-ink.png`, `step-write.png`, `step-seal.png`, `step-open.png`, `wax-seal.png`, `paper-light.jpg`.
2. `ls .planning/reference/` shows `Landing.html` and an `assets/` subdirectory.
3. `ls .planning/reference/assets/` lists the same 5 PNG filenames as `public/assets/` (no `paper-light.jpg` — the reference Landing.html still points at `i.postimg.cc` in its own HTML, so the snapshot is byte-faithful).
4. `grep -F 'SEALED — Some letters are worth waiting for.' index.html` finds exactly one match in the `<title>` element.
5. `npm run build` is NOT required to pass at the end of this plan because no source code (`src/`) changes have been made — the build is still broken in the same way it was before. The next plan (01.5-02) edits `src/index.css`; the build green check lands in the final compose plan (01.5-09).
</verification>

<success_criteria>
- `public/assets/` directory contains all 5 prototype PNGs at their target file sizes and a locally-hosted `paper-light.jpg`.
- `.planning/reference/` contains a byte-faithful snapshot of `Landing.html` and the 5 PNGs so future verification works without `/tmp/` access.
- `index.html` `<title>` is exactly `SEALED — Some letters are worth waiting for.` with the em-dash preserved.
- Wave-2 component executors can now reference `/assets/*.png` paths in JSX with confidence that Vite will serve them at runtime.
</success_criteria>

<output>
After completion, create `.planning/phases/01.5-ui-redesign-inserted/01.5-01-SUMMARY.md` recording the byte sizes of the copied PNGs, the byte size of the downloaded `paper-light.jpg`, the previous and new `<title>` strings, and confirmation that `public/` was a newly created directory.
</output>
</content>
</invoke>