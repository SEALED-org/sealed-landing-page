---
phase: 01.5
slug: ui-redesign-inserted
plan: 03
id: 01.5-03
type: execute
wave: 2
depends_on: [01.5-02]
files_modified:
  - src/components/Nav.tsx
  - src/components/Footer.tsx
autonomous: true
requirements: [UI-01, UI-08]
requirements_addressed: [UI-01, UI-08]
must_haves:
  truths:
    - "A new Nav component renders the fixed top bar with SEALED wordmark left and Join Waitlist ↑ button right, smooth-scrolling to #waitlist"
    - "A new Footer component renders the 4-column footer (Brand+tag / Discover / Connect / Legal) with three inline-SVG social icons (Instagram, X, Mail), all on a black background, exactly matching Landing.html lines 1708-1746"
    - "Both components are default-export PascalCase React 19 functional components with relative imports, matching the codebase's existing convention"
    - "Inline-style overrides on Nav (.mark and .join font weight/size) are preserved exactly per RESEARCH.md Landmine #10"
  artifacts:
    - path: "src/components/Nav.tsx"
      provides: "Fixed top nav with wordmark + Join Waitlist button"
      min_lines: 15
      contains: "export default function Nav, nav className=\"top\", className=\"mark\", className=\"join\", className=\"arrow\", scrollIntoView, behavior: 'smooth', fontWeight: 500"
    - path: "src/components/Footer.tsx"
      provides: "4-column black footer with brand, Discover, Connect (3 social inline SVGs), Legal columns, plus bottom copyright row"
      min_lines: 50
      contains: "export default function Footer, className=\"foot-inner\", className=\"foot-brand\", className=\"foot-col\", className=\"social-list\", className=\"foot-bottom\", privacy.html, terms.html, hello@sealed.io, #waitlist, #first-letter, Some letters are worth waiting for, © 2026 · All rights reserved, viewBox=\"0 0 24 24\", strokeWidth, strokeLinecap, strokeLinejoin"
  key_links:
    - from: "src/components/Nav.tsx button onClick"
      to: "element with id=\"waitlist\""
      via: "document.getElementById('waitlist')?.scrollIntoView({ behavior: 'smooth' })"
      pattern: "getElementById\\('waitlist'\\)\\?\\.scrollIntoView"
    - from: "src/components/Footer.tsx links"
      to: "privacy.html, terms.html (static), #waitlist, #first-letter (in-page anchors)"
      via: "<a href=\"...\">"
      pattern: "href=\"(privacy|terms)\\.html\"|href=\"#(waitlist|first-letter)\""
user_setup: []
---

<objective>
Extract the fixed top nav (currently inline in `src/App.tsx` lines 53–61) and the footer (currently inline in `src/App.tsx` lines 218–237) into two independent, self-contained components: `src/components/Nav.tsx` and `src/components/Footer.tsx`. Restyle both to match the Claude Design `Landing.html` prototype byte-faithfully — using the global CSS class names already defined in `src/index.css` by Plan 01.5-02. Neither component takes props; both are static and zero-state.

Purpose: These two components own the page chrome — they bracket every other section. Extracting them lets `App.tsx` (Wave 3) become a thin composition root that only handles state and lifts callbacks, without the cognitive load of inlined nav/footer markup. The components are small, independent, and have no shared dependencies (different files, different class namespaces) so they parallelize trivially with the other Wave-2 plans.

Output: Two new files. No edits to `src/App.tsx` in this plan — that happens in 01.5-09. After this plan, the new components exist but are not yet mounted; the build still compiles `App.tsx` against its old chrome.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.5-ui-redesign-inserted/01.5-CONTEXT.md
@.planning/phases/01.5-ui-redesign-inserted/01.5-RESEARCH.md
@.planning/phases/01.5-ui-redesign-inserted/01.5-UI-SPEC.md
@.planning/phases/01.5-ui-redesign-inserted/01.5-PATTERNS.md
@src/App.tsx
@src/index.css

# Prototype source for Nav (Landing.html):
# - Nav CSS: lines 105-134
# - Nav markup: lines 1335-1340

# Prototype source for Footer (Landing.html):
# - Footer CSS: lines 1134-1216
# - Footer markup: lines 1708-1746
# - Instagram inline SVG: line 1725
# - X (Twitter) inline SVG: line 1728
# - Email inline SVG: line 1731

<interfaces>
<!-- These components take NO props. Convention from existing files: -->
<!-- - default export per file -->
<!-- - PascalCase function name matching the file name -->
<!-- - relative imports (no @ alias) -->

Existing pattern from src/components/Typewriter.tsx:
```
export default function Typewriter({ text, ... }: TypewriterProps) { ... }
```

Existing pattern from src/components/Counter.tsx:
```
interface CounterProps { target: number }
export default function Counter({ target }: CounterProps) { ... }
```

For Nav and Footer (no props):
```
export default function Nav() { ... }
export default function Footer() { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/components/Nav.tsx — fixed top nav with wordmark + Join Waitlist button</name>
  <files>src/components/Nav.tsx</files>
  <read_first>
    - .planning/reference/Landing.html lines 1335-1340 (Nav markup — inline style attributes on `.mark` and `.join` are real overrides per RESEARCH.md Landmine #10)
    - .planning/reference/Landing.html lines 105-134 (Nav CSS — confirms the styling that 01.5-02-PLAN.md already ported into `src/index.css`)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-PATTERNS.md `src/components/Nav.tsx (NEW)` section (lines 222–267 — full file template with the smooth-scroll handler and inline-style override pattern)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-UI-SPEC.md §Section-by-Section Visual Contract `1. Nav` (lines 332–340 — confirms the "Smooth-scroll to #waitlist" override of the prototype's `window.scrollTo({top:0,...})`)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-RESEARCH.md §Inline-style preservation rationale (in §10 Landmine on `.fl-pad-body`, but the same principle applies here — preserve inline overrides)
    - src/App.tsx (lines 53–61 — the current inline nav being extracted; confirms the current `getElementById('waitlist')` pattern)
  </read_first>
  <action>
    Create the file `src/components/Nav.tsx`. The file exports a default function `Nav` taking no props. The component returns a single `<nav>` element with `className=\"top\"` containing two children: a `<div>` with `className=\"mark\"` and an inline style of `{ fontWeight: 500, fontSize: '20px' }` containing the text `SEALED`, and a `<button>` with `className=\"join\"`, `type=\"button\"`, an inline style of `{ fontWeight: 500, fontSize: '10px' }`, and an `onClick` handler that calls `document.getElementById('waitlist')?.scrollIntoView({ behavior: 'smooth' })`. The button's children are the text `Join Waitlist ` followed by a `<span>` with `className=\"arrow\"` containing the character `↑` (U+2191). Per PATTERNS.md `Nav.tsx` section "Critical detail" (line 265): use the UI-SPEC's `#waitlist` target — do NOT port the prototype's `window.scrollTo({top:0})` because UI-SPEC line 337 overrides it. Per RESEARCH.md Landmine #10 and PATTERNS.md "Inline-style preservation rationale" (line 267): the inline style attributes on `.mark` and `.join` are real overrides that take precedence over the CSS rules (the CSS says `.mark` is 22px / 400; inline says 20px / 500). Both inline styles must be present in JSX. Import only what is needed (`import React from 'react'` is NOT strictly required in React 19 if the file has no JSX-types that need it, but per conventions match existing components — `src/components/Counter.tsx` starts with `import { ... } from 'react'` not `import React`; this file has no hooks and no event-type annotations so the imports can be empty if no React types are referenced). Do NOT add ARIA attributes beyond what the prototype has (the prototype has none on nav itself). Do NOT add `aria-label` to the button; the label text `Join Waitlist ↑` is self-describing. The file must follow the codebase convention: 2-space indent, single quotes in imports, double quotes in JSX attributes, semicolons consistent, default export.
  </action>
  <verify>
    <automated>
      test -f src/components/Nav.tsx &amp;&amp; grep -F 'export default function Nav' src/components/Nav.tsx &amp;&amp; grep -F "className=\"top\"" src/components/Nav.tsx &amp;&amp; grep -F "className=\"mark\"" src/components/Nav.tsx &amp;&amp; grep -F "className=\"join\"" src/components/Nav.tsx &amp;&amp; grep -F "className=\"arrow\"" src/components/Nav.tsx &amp;&amp; grep -F "getElementById('waitlist')" src/components/Nav.tsx &amp;&amp; grep -F "behavior: 'smooth'" src/components/Nav.tsx &amp;&amp; grep -F 'fontWeight: 500' src/components/Nav.tsx &amp;&amp; grep -F "fontSize: '20px'" src/components/Nav.tsx &amp;&amp; grep -F "fontSize: '10px'" src/components/Nav.tsx
    </automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'export default function Nav' src/components/Nav.tsx` returns 1
    - Source assertion: `src/components/Nav.tsx` does NOT contain the strings `useState`, `useEffect`, `useRef`, `interface`, or `Props` (no state, no props, no interface)
    - Source assertion: `grep -F "type=\"button\"" src/components/Nav.tsx` returns 1 match on the join button (prevents implicit form-submit behavior in case Nav is ever nested inside a form)
    - Source assertion: `grep -F 'SEALED' src/components/Nav.tsx` returns 1 match (the wordmark text)
    - Source assertion: `grep -F 'Join Waitlist' src/components/Nav.tsx` returns 1 match (button label)
    - Source assertion: `grep -c '↑' src/components/Nav.tsx` returns 1 (the up-arrow Unicode character inside the `.arrow` span; not the literal string `&uarr;`)
    - Source assertion: `grep -F "getElementById('waitlist')?.scrollIntoView" src/components/Nav.tsx` returns 1 match (smooth-scroll to #waitlist, NOT to top of page)
    - Inline-style assertion: `grep -c 'fontWeight: 500' src/components/Nav.tsx` returns 2 (one on `.mark`, one on `.join`)
    - Inline-style assertion: `grep -F "fontSize: '20px'" src/components/Nav.tsx` returns 1 (the `.mark` inline override)
    - Inline-style assertion: `grep -F "fontSize: '10px'" src/components/Nav.tsx` returns 1 (the `.join` inline override)
    - TypeScript shape: the file declares no interface (no `interface NavProps`) since the component takes no props
    - File size: `wc -l src/components/Nav.tsx` returns a value &gt;= 12 and &lt;= 35 (a tight, single-purpose component)
  </acceptance_criteria>
  <done>`src/components/Nav.tsx` is a default-exported function component that renders the fixed top nav exactly per the prototype, with the smooth-scroll-to-#waitlist override and the two inline-style font-weight/font-size overrides preserved. No props, no state, no React effects.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/components/Footer.tsx — 4-column black footer with 3 inline-SVG social icons</name>
  <files>src/components/Footer.tsx</files>
  <read_first>
    - .planning/reference/Landing.html lines 1708-1746 (Footer markup including the three inline SVG icons; copy the SVG markup verbatim with the JSX-name conversions documented below)
    - .planning/reference/Landing.html lines 1134-1216 (Footer CSS — confirms the styling already ported into `src/index.css` by 01.5-02-PLAN.md)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-PATTERNS.md `src/components/Footer.tsx (NEW)` section (lines 271–326 — full file template + the three inline SVG source strings)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-UI-SPEC.md §Footer (lines 312–326 — copy contract for column headings, links, social handles, and bottom row) + §Section-by-Section Visual Contract `8. Footer` (lines 430–441 — CSS rules confirming the 4-column grid + circular social buttons)
    - src/App.tsx (lines 218–237 — the current inline footer being entirely replaced; do not port its content; the new structure is different)
  </read_first>
  <action>
    Create the file `src/components/Footer.tsx`. The file exports a default function `Footer` taking no props. Render a top-level `<footer className=\"foot\">` containing a `<div className=\"foot-inner\">` with four children plus a `<div className=\"foot-bottom\">` sibling.

    Inside `.foot-inner`, render these four columns in this order:

    Column 1 — Brand: `<div className=\"foot-brand\">` containing `<div className=\"mark\">SEALED</div>` and `<div className=\"tag\">Some letters are worth waiting for.</div>`. Do NOT render a `.seal` element (UI-SPEC §Out of Scope: CSS exists but body markup does not render it).

    Column 2 — Discover: `<div className=\"foot-col\">` containing `<h4>Discover</h4>` and a `<ul>` with two `<li>` items: `<a href=\"#waitlist\">Join the waitlist</a>` and `<a href=\"#first-letter\">Write a letter</a>`.

    Column 3 — Connect: `<div className=\"foot-col foot-social\">` containing `<h4>Connect</h4>` and a `<ul className=\"social-list\">` with three `<li>` items, each containing an `<a>` with the three inline SVG social icons. The three icons are lifted verbatim from `Landing.html` lines 1725, 1728, 1731 (per PATTERNS.md Footer section "Inline SVGs to lift verbatim" at lines 313–316). Apply these JSX-name conversions to every SVG attribute: `stroke-width` → `strokeWidth`, `stroke-linecap` → `strokeLinecap`, `stroke-linejoin` → `strokeLinejoin`, `fill-rule` → `fillRule` (if present), `clip-rule` → `clipRule` (if present), `text-anchor` → `textAnchor` (if present). Render `viewBox` as a string in double quotes (e.g., `viewBox=\"0 0 24 24\"`). Each social link uses an empty `href=\"#\"` for Instagram and X (per Landing.html — real handles are SOCIAL-01 in Phase 5; this phase only puts the icons in place), and `href=\"mailto:hello@sealed.io\"` for the Email link (per Landing.html line 1729). Set `aria-label=\"Instagram\"`, `aria-label=\"X\"`, `aria-label=\"Email\"` and `title=\"Instagram\"`, `title=\"X\"`, `title=\"hello@sealed.io\"` to match the prototype's accessibility hints. The three SVG element specifications: (a) Instagram = `<svg width=\"22\" height=\"22\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" strokeWidth=\"1.6\" strokeLinecap=\"round\" strokeLinejoin=\"round\">` containing `<rect x=\"3\" y=\"3\" width=\"18\" height=\"18\" rx=\"5\" />`, `<circle cx=\"12\" cy=\"12\" r=\"4\" />`, `<circle cx=\"17.5\" cy=\"6.5\" r=\"1\" fill=\"currentColor\" stroke=\"none\" />`. (b) X / Twitter = `<svg width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"currentColor\">` containing one `<path d=\"M18.244 2H21l-6.52 7.452L22 22h-6.828l-4.77-6.231L4.8 22H2l7.02-8.024L2 2h6.914l4.32 5.713L18.244 2zm-1.197 18h1.84L7.06 4H5.117l11.93 16z\" />`. (c) Email = `<svg width=\"22\" height=\"22\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" strokeWidth=\"1.6\" strokeLinecap=\"round\" strokeLinejoin=\"round\">` containing `<rect x=\"3\" y=\"5\" width=\"18\" height=\"14\" rx=\"2\" />` and `<polyline points=\"3,7 12,13 21,7\" />`.

    Column 4 — Legal: `<div className=\"foot-col\">` containing `<h4>Legal</h4>` and a `<ul>` with two `<li>` items: `<a href=\"privacy.html\">Privacy Policy</a>` and `<a href=\"terms.html\">Terms of Service</a>`. Do NOT prefix `privacy.html`/`terms.html` with a slash — they are relative paths to the standalone HTML pages at the site root, exactly as the prototype renders them.

    Below `.foot-inner` (still inside `<footer>`): `<div className=\"foot-bottom\"><span>© 2026 · All rights reserved</span></div>`. The `©` is the copyright character U+00A9; the `·` between year and text is the middle dot U+00B7. Both must be preserved as Unicode characters in the JSX, NOT as HTML entities (`&copy;` would render literally in JSX text).

    Do NOT import any icons from `lucide-react` (per UI-SPEC §Design System "icon library: inline SVG to match prototype" — only non-decorative icons elsewhere may use Lucide; footer social icons must be inline SVG). Do NOT use `motion/react` here — the footer has no animations.
  </action>
  <verify>
    <automated>
      test -f src/components/Footer.tsx &amp;&amp; grep -F 'export default function Footer' src/components/Footer.tsx &amp;&amp; grep -F 'className="foot-inner"' src/components/Footer.tsx &amp;&amp; grep -F 'className="foot-brand"' src/components/Footer.tsx &amp;&amp; grep -F 'className="foot-col"' src/components/Footer.tsx &amp;&amp; grep -F 'className="social-list"' src/components/Footer.tsx &amp;&amp; grep -F 'className="foot-bottom"' src/components/Footer.tsx &amp;&amp; grep -F 'href="#waitlist"' src/components/Footer.tsx &amp;&amp; grep -F 'href="#first-letter"' src/components/Footer.tsx &amp;&amp; grep -F 'href="privacy.html"' src/components/Footer.tsx &amp;&amp; grep -F 'href="terms.html"' src/components/Footer.tsx &amp;&amp; grep -F 'mailto:hello@sealed.io' src/components/Footer.tsx &amp;&amp; grep -F 'aria-label="Instagram"' src/components/Footer.tsx &amp;&amp; grep -F 'aria-label="X"' src/components/Footer.tsx &amp;&amp; grep -F 'aria-label="Email"' src/components/Footer.tsx &amp;&amp; grep -c '&lt;svg ' src/components/Footer.tsx | grep -qE '^3$' &amp;&amp; ! grep -E "from 'lucide-react'" src/components/Footer.tsx &amp;&amp; ! grep -E "from 'motion/react'" src/components/Footer.tsx
    </automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'export default function Footer' src/components/Footer.tsx` returns 1
    - Source assertion: `grep -c '&lt;svg ' src/components/Footer.tsx` returns exactly 3 (Instagram, X, Email — no others)
    - Source assertion: `grep -c '&lt;h4&gt;' src/components/Footer.tsx` returns 3 (Discover, Connect, Legal — Brand column has no h4)
    - Source assertion: `grep -c '&lt;li&gt;' src/components/Footer.tsx` returns at least 7 (2 Discover + 3 Connect + 2 Legal)
    - Copy assertion: `grep -F 'Some letters are worth waiting for.' src/components/Footer.tsx` returns 1 match (the .tag text)
    - Copy assertion: `grep -F '© 2026 · All rights reserved' src/components/Footer.tsx` returns 1 match (the foot-bottom text; the `©` and `·` Unicode chars must be present)
    - Copy assertion: `grep -F 'Join the waitlist' src/components/Footer.tsx` returns 1 match (Discover link 1)
    - Copy assertion: `grep -F 'Write a letter' src/components/Footer.tsx` returns 1 match (Discover link 2)
    - Copy assertion: `grep -F 'Privacy Policy' src/components/Footer.tsx` returns 1 match (Legal link 1)
    - Copy assertion: `grep -F 'Terms of Service' src/components/Footer.tsx` returns 1 match (Legal link 2)
    - Anchor assertion: `grep -E 'href="(privacy|terms)\.html"' src/components/Footer.tsx | wc -l` returns 2 (no leading slash)
    - Anchor assertion: `grep -E 'href="#(waitlist|first-letter)"' src/components/Footer.tsx | wc -l` returns 2 (in-page anchors)
    - Inline-SVG assertion: `grep -F 'strokeWidth="1.6"' src/components/Footer.tsx` returns 2 matches (Instagram + Email; X uses `fill=\"currentColor\"` and no stroke)
    - JSX-conversion assertion: `grep -E 'stroke-width|stroke-linecap|stroke-linejoin' src/components/Footer.tsx` returns 0 matches (all kebab-case attributes were converted to camelCase per JSX rules)
    - No-Lucide assertion: `grep -E "from 'lucide-react'" src/components/Footer.tsx` returns 0 matches
    - No-Motion assertion: `grep -E "from 'motion/react'" src/components/Footer.tsx` returns 0 matches (no animation in footer)
    - File size: `wc -l src/components/Footer.tsx` returns a value &gt;= 50 and &lt;= 120
  </acceptance_criteria>
  <done>`src/components/Footer.tsx` is a default-exported function component that renders the 4-column black footer with all copy verbatim from the prototype (Discover/Connect/Legal columns, the brand tag, the copyright row) and three inline-SVG social icons in the Connect column. No Lucide icons, no Motion library, no props, no state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Nav button | The button triggers `scrollIntoView` on a DOM node looked up by `id=\"waitlist\"`. No user input crosses the boundary; the lookup is fixed-string. |
| User → Footer link | The footer renders static `<a href>` links. No user input; no dynamic href construction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.5-03-01 | Information Disclosure | `mailto:hello@sealed.io` link | accept | The address is the public contact already published in the prototype and intended to be world-readable. |
| T-01.5-03-02 | Tampering | inline SVG markup | accept | All three social SVGs are static JSX literals with no `dangerouslySetInnerHTML`. React 19's automatic escaping prevents script injection. |
| T-01.5-03-03 | Information Disclosure | footer external links (`#`) | accept | Instagram and X links currently `href=\"#\"` — they target the current page and don't leak the user to external sites until Phase 5 (SOCIAL-01) sets real handles. |

`[ASVS-L1: N/A — static chrome components; no user input, no auth surface, no data processing]`
</threat_model>

<verification>
After both tasks:

1. Both files exist and pass their grep-based acceptance criteria.
2. `npm run build` is NOT yet expected to pass because `App.tsx` still imports the legacy chrome. The build green check is owned by 01.5-09-PLAN.md.
3. Visually verifying the new components requires Plan 01.5-09 to wire them into App.tsx.
</verification>

<success_criteria>
- `Nav.tsx` exists as a no-props default-exported component rendering the prototype's fixed top bar with the two inline-style overrides preserved.
- `Footer.tsx` exists as a no-props default-exported component rendering the 4-column black footer with all copy and three inline SVGs verbatim.
- Neither component imports from `lucide-react` (footer/nav social/decorative icons are inline SVG per UI-SPEC).
- Neither component is mounted in App.tsx yet — that happens in 01.5-09.
</success_criteria>

<output>
After completion, create `.planning/phases/01.5-ui-redesign-inserted/01.5-03-SUMMARY.md` listing the two new files, their line counts, the inline-style preservation points on Nav, and the three social-link href values on Footer (`#`, `#`, `mailto:hello@sealed.io`).
</output>
</content>
</invoke>