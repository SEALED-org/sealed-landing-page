---
phase: 01.5
slug: ui-redesign-inserted
plan: 09
id: 01.5-09
type: execute
wave: 3
depends_on: [01.5-01, 01.5-02, 01.5-03, 01.5-04, 01.5-05, 01.5-06, 01.5-07, 01.5-08]
files_modified:
  - src/App.tsx
  - src/components/ShareButtons.tsx
autonomous: false
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06, UI-07, UI-08, UI-09, UI-10]
requirements_addressed: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06, UI-07, UI-08, UI-09, UI-10]
must_haves:
  truths:
    - "src/App.tsx mounts all 8 Wave-2 components (Nav, WaitlistForm, WaitlistSuccessCard, Counter, HowItWorks, ResearchSection, FirstLetter, FAQ, Footer) plus inline hero shell + section divider, in the order specified by the prototype body markup"
    - "Phase 1 data layer is preserved VERBATIM per D-06: the getSignupCount/.catch(115) useEffect, the handleSubscribe try/catch/finally with console.error('Subscription failed:', error), the FirstLetter.onEmailSubmit callback with console.error('Waitlist join failed:', error), the (c) => (c ?? 115) + 1 increment, and the import { getSignupCount, joinWaitlistLocal } from './lib/supabase' all survive byte-for-byte"
    - "src/App.tsx no longer imports PenLine, Lock, Inbox, Users, ChevronRight, Twitter, Instagram, or any other Lucide icons that were used by the OLD inline JSX (per D-07)"
    - "showSticky state + scroll-listener useEffect + setShowSticky calls are DELETED per D-05 (CLAUDE.md anti-pattern); the new App.tsx does not declare or read this state"
    - "src/components/ShareButtons.tsx is deleted; no source file imports it (`grep -r ShareButtons src/` returns zero matches)"
    - "`npm run build` exits 0 with no errors and no new warnings; the dev server (`npm run dev`) renders the page without runtime errors at http://localhost:3000"
  artifacts:
    - path: "src/App.tsx"
      provides: "Composition root mounting all new Wave-2 components, preserving Phase 1 data layer verbatim"
      min_lines: 100
      contains: "import Nav, import Footer, import WaitlistForm, import WaitlistSuccessCard, import HowItWorks, import ResearchSection, import FirstLetter, import FAQ, import Counter, getSignupCount, joinWaitlistLocal, getSignupCount().then(setWaitlistCount).catch, handleSubscribe, console.error('Subscription failed:', error), console.error('Waitlist join failed:', error), (c) => (c ?? 115) + 1, className=\"min-h-screen, className=\"hero\", className=\"hero-inner\", className=\"wordmark\", className=\"headline\", className=\"hero-tagline\", className=\"hero-separator\", className=\"section-divider\", className=\"live-row\", id=\"first-letter\", id=\"waitlist\", /assets/separator-ink.png, marginBottom: '32px', Some letters are, worth waiting for., Write a letter to your future self., Sealed today, opened on the date you choose."
    - path: "src/components/ShareButtons.tsx"
      provides: "DELETED — no share UI in prototype (UI-SPEC §Out of Scope)"
  key_links:
    - from: "src/App.tsx WaitlistForm.onSubmit"
      to: "handleSubscribe (preserved verbatim)"
      via: "the form-submit callback path"
      pattern: "handleSubscribe|onSubmit"
    - from: "src/App.tsx WaitlistSuccessCard.onWriteLetter"
      to: "document.getElementById('first-letter')?.scrollIntoView({ behavior: 'smooth' })"
      via: "inline arrow function passed as prop"
      pattern: "getElementById\\('first-letter'\\)"
    - from: "src/App.tsx FirstLetter.onEmailSubmit"
      to: "joinWaitlistLocal + (c) => (c ?? 115) + 1 increment"
      via: "async callback preserved verbatim per D-06"
      pattern: "console.error\\('Waitlist join failed:"
user_setup: []
---

<objective>
Perform the App.tsx JSX-only surgery — replacing the inlined hero/sections/footer markup with the new component composition — while preserving the Phase 1 data layer byte-for-byte per D-06. Delete `src/components/ShareButtons.tsx` per UI-SPEC (no share UI in the prototype). Drop the unused Lucide icon imports per D-07 and the `showSticky` dead-state per D-05. Verify `npm run build` exits 0, then run a dev-server side-by-side parity check against `.planning/reference/Landing.html`.

Purpose: This is the integration plan that ties together everything Waves 1–2 produced. After this plan, the rebuilt landing page renders pixel-faithfully against the prototype, the Phase 1 Supabase data layer still works end-to-end (counter fetch on mount, fallback to 115 on error, +1 increment on submit), and the build is green again. The autonomous flag is `false` because this plan ends with a `checkpoint:human-verify` task asking the user to confirm side-by-side parity in a browser — the visual diff is the canonical acceptance gate for Phase 1.5.

Output: Two file changes (App.tsx rewritten / restructured, ShareButtons.tsx deleted). The build passes. The dev server renders correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.5-ui-redesign-inserted/01.5-CONTEXT.md
@.planning/phases/01.5-ui-redesign-inserted/01.5-RESEARCH.md
@.planning/phases/01.5-ui-redesign-inserted/01.5-UI-SPEC.md
@.planning/phases/01.5-ui-redesign-inserted/01.5-PATTERNS.md
@src/App.tsx
@src/components/ShareButtons.tsx
@src/lib/supabase.ts

# Prototype source (for the hero shell inline JSX that App.tsx still owns):
# .planning/reference/Landing.html lines 1335-1396 (hero shell — wordmark, headline, tagline, separator, form mount, success card mount, live-row + counter)
# .planning/reference/Landing.html lines 1398-1403 (section divider markup — note the stray </div> on line 1403 per RESEARCH.md Landmine #9; DROP it in the JSX port)

<interfaces>
<!-- Wave-2 component prop contracts that App.tsx must wire: -->

<Nav />  {/* no props */}
<WaitlistForm
  onSubmit={async (formEmail: string) => Promise<void>}
  isSubmitting={boolean}
  isSubmitted={boolean}
/>
<WaitlistSuccessCard
  onWriteLetter={() => void}
  isVisible={boolean}
/>
<Counter target={number} />
<HowItWorks />  {/* no props */}
<ResearchSection />  {/* no props */}
<FirstLetter
  initialEmail={string | undefined}
  onEmailSubmit={async (newEmail: string) => Promise<void>}
  waitlistCount={number}
/>
<FAQ />  {/* no props */}
<Footer />  {/* no props */}

<!-- D-06 verbatim — these blocks must survive byte-for-byte from current src/App.tsx: -->

// State (lines 11-14):
const [email, setEmail] = useState('');
const [isSubscribed, setIsSubscribed] = useState(false);
const [waitlistCount, setWaitlistCount] = useState<number | null>(null);

// Submit state (line 33):
const [isSubmitting, setIsSubmitting] = useState(false);

// Counter fetch (lines 24-31):
useEffect(() => {
  getSignupCount()
    .then(setWaitlistCount)
    .catch((error) => {
      console.error('Counter fetch failed:', error);
      setWaitlistCount(115);
    });
}, []);

// Submit handler (lines 35-49):
const handleSubscribe = async (e: React.FormEvent) => {
  e.preventDefault();
  if (email && !isSubmitting) {
    setIsSubmitting(true);
    try {
      await joinWaitlistLocal(email);
      setIsSubscribed(true);
      setWaitlistCount((c) => (c ?? 115) + 1);
    } catch (error) {
      console.error('Subscription failed:', error);
    } finally {
      setIsSubmitting(false);
    }
  }
};

// FirstLetter wiring (lines 197-211 — preserved verbatim in the new JSX position):
<FirstLetter
  initialEmail={isSubscribed ? email : undefined}
  onEmailSubmit={async (newEmail) => {
    setEmail(newEmail);
    try {
      await joinWaitlistLocal(newEmail);
      setIsSubscribed(true);
      setWaitlistCount((c) => (c ?? 115) + 1);
    } catch (error) {
      console.error('Waitlist join failed:', error);
    }
  }}
  waitlistCount={waitlistCount ?? 115}
/>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite src/App.tsx — preserve Phase 1 data layer, replace JSX, drop dead state, drop unused Lucide imports</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (current 241-line file; the lines that MUST survive verbatim are documented in the `<interfaces>` block above and in PATTERNS.md lines 920-1010)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-PATTERNS.md `src/App.tsx (TARGETED JSX REWRITE)` (lines 920-1089 — the full plan including the import list to KEEP / DROP / ADD, the composition pattern, the inline hero shell JSX, and the explicit note about WaitlistForm's onSubmit wiring)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-UI-SPEC.md §Migration Map row `src/App.tsx` (lines 670-674 — "Targeted rewrite of JSX only" + the data-layer preservation checklist) + §Data-layer preservation checklist (lines 686-693 — the 7 invariants)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-CONTEXT.md D-05, D-06, D-07 (the three explicit App.tsx surgery decisions)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-RESEARCH.md Landmine #9 (drop the stray `</div>` from Landing.html line 1403 when porting the section-divider markup)
    - src/lib/supabase.ts (confirm the exact named exports `getSignupCount`, `joinWaitlistLocal` — these are the only imports App.tsx needs from `./lib/supabase`)
  </read_first>
  <action>
    Edit `src/App.tsx`. Preserve the data-layer code blocks documented in `<interfaces>` byte-for-byte (these are the D-06 invariants). Replace the JSX between the outer wrapper `<div className=\"min-h-screen ...\">` and the closing `</div>` with the new composition. Drop the `showSticky` state + scroll-listener useEffect per D-05. Drop the unused Lucide icon imports per D-07. The resulting file structure:

    **Imports (top of file):**
    - `import React, { useState, useEffect } from 'react';` (keep — both hooks are still used)
    - `import { motion } from 'motion/react';` — KEEP only if the new App.tsx uses motion.div anywhere; per PATTERNS.md the recommended composition does not wrap any element in motion (each component owns its own scroll-reveal animations). The cleaner approach: DROP this import entirely. Verify by checking the new JSX for any `<motion.*>` usage; if zero, remove the import. If you do keep one wrapper (e.g., on the hero-inner for the prototype's `.reveal.in`), keep the import.
    - `import { getSignupCount, joinWaitlistLocal } from './lib/supabase';` (keep — Phase 1 data layer)
    - **DROP** `import { ArrowRight, Instagram, Twitter, ChevronRight, PenLine, Lock, Inbox, Users, Loader2, Mail } from 'lucide-react';` (and any subset thereof) — per D-07, none of these icons are used by the new JSX (each new component owns its own icons). Per PATTERNS.md line 994: `lucide-react` stays as a project dependency but App.tsx imports zero icons.
    - **DROP** `import ShareButtons from './components/ShareButtons';` — the file is being deleted in Task 2 and no JSX uses it anymore.
    - **DROP** `import { AnimatePresence } from 'motion/react'` if present — no AnimatePresence is used in the new composition.
    - **KEEP** `import Counter from './components/Counter';`
    - **ADD** these new imports:
      - `import Nav from './components/Nav';`
      - `import Footer from './components/Footer';`
      - `import WaitlistForm from './components/WaitlistForm';`
      - `import WaitlistSuccessCard from './components/WaitlistSuccessCard';`
      - `import HowItWorks from './components/HowItWorks';`
      - `import ResearchSection from './components/ResearchSection';`
      - `import FirstLetter from './components/FirstLetter';`
      - `import FAQ from './components/FAQ';`

    **State (inside `function App()`):** Preserve verbatim per D-06:
    - `const [email, setEmail] = useState('');`
    - `const [isSubscribed, setIsSubscribed] = useState(false);`
    - `const [waitlistCount, setWaitlistCount] = useState<number | null>(null);`
    - `const [isSubmitting, setIsSubmitting] = useState(false);`
    Per D-05: DELETE `const [showSticky, setShowSticky] = useState(false);` and DELETE the scroll-listener `useEffect` and any `setShowSticky(...)` calls.

    **Effects:** Preserve the counter-fetch useEffect verbatim per D-06 — the full block from lines 24–31 of the current file (including the `D-11 fallback to seeded floor` comment if present). Do NOT add any new useEffects (no scroll listener, no analytics, no IntersectionObserver in App.tsx).

    **Handlers:** Preserve the `handleSubscribe` function verbatim per D-06 — the full block from lines 35–49 of the current file, including the `console.error('Subscription failed:', error)` line.

    **JSX (replace the inlined hero/how/FAQ/footer markup with the new composition):**

    Outer wrapper: `<div className=\"min-h-screen selection:bg-black selection:text-white\">` (preserve the Tailwind selection styling if present in current; can be the simpler `<div className=\"min-h-screen\">` — either works).

    Children in this exact order:

    (1) `<Nav />`

    (2) **Hero section (inlined per PATTERNS.md decision — not extracted into Hero.tsx):**
    ```
    <header className=\"hero\">
      <div className=\"hero-inner\">
        <div className=\"wordmark\" style={{ marginBottom: '32px' }}>SEALED</div>
        <h1 className=\"headline\">Some letters are<br /><em>worth waiting for.</em></h1>
        <p className=\"hero-tagline\">
          Write a letter to your future self.<br />
          Sealed today, opened on the date you choose.
        </p>
        <img
          src=\"/assets/separator-ink.png\"
          alt=\"\"
          aria-hidden=\"true\"
          width=\"2572\"
          height=\"190\"
          className=\"hero-separator\"
          decoding=\"async\"
          fetchPriority=\"high\"
        />
        <WaitlistForm
          onSubmit={async (formEmail) => {
            setEmail(formEmail);
            // Reuse the verbatim D-06 handleSubscribe contract by manually
            // invoking the same body inline OR refactor handleSubscribe to
            // accept the email parameter. Recommended: refactor minimally to
            // accept an explicit email parameter, OR keep handleSubscribe
            // as-is and call setEmail(formEmail) here, then await handleSubscribeForEmail.
            // The simplest preservation is to inline the try/catch/finally body here:
            if (!formEmail || isSubmitting) return;
            setIsSubmitting(true);
            try {
              await joinWaitlistLocal(formEmail);
              setIsSubscribed(true);
              setWaitlistCount((c) => (c ?? 115) + 1);
            } catch (error) {
              console.error('Subscription failed:', error);
            } finally {
              setIsSubmitting(false);
            }
          }}
          isSubmitting={isSubmitting}
          isSubmitted={isSubscribed}
        />
        <WaitlistSuccessCard
          isVisible={isSubscribed}
          onWriteLetter={() => document.getElementById('first-letter')?.scrollIntoView({ behavior: 'smooth' })}
        />
        <div className=\"live-row\">
          <Counter target={waitlistCount ?? 115} />
        </div>
      </div>
    </header>
    ```

    Per the data-layer preservation requirement: the body of WaitlistForm's `onSubmit` callback above is the verbatim body of `handleSubscribe` with `email` substituted for the parameter `formEmail`. This preserves D-06's contract — the `console.error('Subscription failed:', error)` line and the `(c) => (c ?? 115) + 1` increment both survive. The `handleSubscribe` function can be kept as a stand-alone declaration above the return statement for clarity (per PATTERNS.md lines 952-967), but the WaitlistForm callback inlines the same body — this redundancy is acceptable for the data-layer preservation invariant. **Alternative**: refactor `handleSubscribe` to take an email parameter: `const handleSubscribe = async (e: React.FormEvent | null, submittedEmail: string) => { ... }`, then pass `(formEmail) => handleSubscribe(null, formEmail)` to `WaitlistForm.onSubmit`. Either approach is acceptable as long as the three D-06 invariants (try/catch around joinWaitlistLocal, console.error placeholder text, increment pattern) survive byte-for-byte.

    (3) **Section divider (inlined per UI-SPEC §Migration Map — kept in App.tsx):**
    ```
    <div className=\"section-divider\" aria-hidden=\"true\">
      <div className=\"inner\">
        <span className=\"rule\" />
      </div>
    </div>
    ```
    Per RESEARCH.md Landmine #9: drop the stray `</div>` from Landing.html line 1403. The valid markup is the 3-element nested structure above.

    (4) `<HowItWorks />`

    (5) `<ResearchSection />`

    (6) **FirstLetter wrapped in scrolltarget div:**
    ```
    <div id=\"first-letter\">
      <FirstLetter
        initialEmail={isSubscribed ? email : undefined}
        onEmailSubmit={async (newEmail) => {
          setEmail(newEmail);
          try {
            await joinWaitlistLocal(newEmail);
            setIsSubscribed(true);
            setWaitlistCount((c) => (c ?? 115) + 1);
          } catch (error) {
            console.error('Waitlist join failed:', error);
          }
        }}
        waitlistCount={waitlistCount ?? 115}
      />
    </div>
    ```
    Per D-06 the FirstLetter wiring is verbatim from current src/App.tsx lines 197-211. The inner FirstLetter component (rewritten in Plan 01.5-07) also renders `<section id=\"first-letter\" ...>` as its outermost element — that internal `id` would be the more idiomatic anchor, but to keep both APIs stable and avoid a double-id collision: EITHER (a) drop the outer `<div id=\"first-letter\">` wrapper in App.tsx and rely on the FirstLetter section's internal id (cleaner, recommended), OR (b) keep the outer wrapper. PATTERNS.md lines 1063-1067 wrap the FirstLetter in an outer `<div id=\"first-letter\">` — that is the locked pattern in PATTERNS.md, but having the same id on two adjacent elements is technically an HTML duplicate-id violation that browsers do not flag. **Choice:** use option (a) — drop the outer wrapper. The new FirstLetter component already renders `<section id=\"first-letter\" className=\"first-letter\">` as its root (per Plan 01.5-07 Action body), so the inner id is sufficient for the scroll target. Update WaitlistSuccessCard's `onWriteLetter` callback to use `document.getElementById('first-letter')?.scrollIntoView({ behavior: 'smooth' })` — this finds the inner element either way. This deviates from PATTERNS.md but produces cleaner HTML; PATTERNS.md acknowledges planner discretion in the surgery details.

    (7) `<FAQ />`

    (8) `<Footer />`

    **No `<AnimatePresence>` wrapper anywhere.** No `motion.div` wrappers around any component (the components handle their own animations).

    Do NOT delete the existing JSX comments that label sections (`{/* Hero Section */}`, `{/* How it Works */}`, etc.) — actually, DO delete them because the old comments referenced the old structure; if you want section comments in the new composition, add them fresh next to each component mount.
  </action>
  <verify>
    <automated>
      grep -F "import Nav from './components/Nav'" src/App.tsx &amp;&amp; grep -F "import Footer from './components/Footer'" src/App.tsx &amp;&amp; grep -F "import WaitlistForm from './components/WaitlistForm'" src/App.tsx &amp;&amp; grep -F "import WaitlistSuccessCard from './components/WaitlistSuccessCard'" src/App.tsx &amp;&amp; grep -F "import HowItWorks from './components/HowItWorks'" src/App.tsx &amp;&amp; grep -F "import ResearchSection from './components/ResearchSection'" src/App.tsx &amp;&amp; grep -F "import FirstLetter from './components/FirstLetter'" src/App.tsx &amp;&amp; grep -F "import FAQ from './components/FAQ'" src/App.tsx &amp;&amp; grep -F "import Counter from './components/Counter'" src/App.tsx &amp;&amp; grep -F "import { getSignupCount, joinWaitlistLocal } from './lib/supabase'" src/App.tsx &amp;&amp; grep -F 'getSignupCount()' src/App.tsx &amp;&amp; grep -F "console.error('Subscription failed:'" src/App.tsx &amp;&amp; grep -F "console.error('Waitlist join failed:'" src/App.tsx &amp;&amp; grep -F '(c) =&gt; (c ?? 115) + 1' src/App.tsx &amp;&amp; grep -F 'setWaitlistCount(115)' src/App.tsx &amp;&amp; grep -F '/assets/separator-ink.png' src/App.tsx &amp;&amp; grep -F 'Some letters are' src/App.tsx &amp;&amp; grep -F 'worth waiting for.' src/App.tsx &amp;&amp; grep -F 'Write a letter to your future self.' src/App.tsx &amp;&amp; grep -F 'Sealed today, opened on the date you choose.' src/App.tsx &amp;&amp; grep -F "marginBottom: '32px'" src/App.tsx &amp;&amp; grep -F 'className="section-divider"' src/App.tsx &amp;&amp; grep -F 'className="live-row"' src/App.tsx &amp;&amp; ! grep -F 'showSticky' src/App.tsx &amp;&amp; ! grep -F 'setShowSticky' src/App.tsx &amp;&amp; ! grep -F "from 'lucide-react'" src/App.tsx &amp;&amp; ! grep -F 'ShareButtons' src/App.tsx &amp;&amp; ! grep -F 'AnimatePresence' src/App.tsx
    </automated>
  </verify>
  <acceptance_criteria>
    - Import assertion: every required `import ... from './components/X'` line is present (Nav, Footer, WaitlistForm, WaitlistSuccessCard, HowItWorks, ResearchSection, FirstLetter, FAQ, Counter — 9 component imports total)
    - Import assertion: `grep -F "import { getSignupCount, joinWaitlistLocal } from './lib/supabase'" src/App.tsx` returns 1 (Phase 1 data layer preserved)
    - D-06 verbatim assertion: `grep -F "console.error('Subscription failed:'" src/App.tsx` returns at least 1 (the handleSubscribe / inline WaitlistForm.onSubmit error log)
    - D-06 verbatim assertion: `grep -F "console.error('Waitlist join failed:'" src/App.tsx` returns 1 (the FirstLetter.onEmailSubmit error log)
    - D-06 verbatim assertion: `grep -F '(c) =&gt; (c ?? 115) + 1' src/App.tsx` returns at least 2 (two call sites: WaitlistForm.onSubmit and FirstLetter.onEmailSubmit — both per D-06)
    - D-06 verbatim assertion: `grep -F 'setWaitlistCount(115)' src/App.tsx` returns 1 (the counter-fetch catch fallback)
    - D-06 verbatim assertion: `grep -F 'await joinWaitlistLocal' src/App.tsx` returns 2 (two call sites)
    - D-06 verbatim assertion: `grep -F 'getSignupCount()' src/App.tsx` returns 1 (counter fetch on mount)
    - D-07 assertion: `grep -E "from 'lucide-react'" src/App.tsx` returns 0 (all Lucide icon imports removed)
    - D-05 assertion: `grep -F 'showSticky' src/App.tsx` returns 0 (state and its references all removed)
    - D-05 assertion: `grep -F 'setShowSticky' src/App.tsx` returns 0
    - No-ShareButtons assertion: `grep -F 'ShareButtons' src/App.tsx` returns 0
    - No-AnimatePresence assertion: `grep -F 'AnimatePresence' src/App.tsx` returns 0
    - Hero copy assertion: each of `Some letters are`, `worth waiting for.`, `Write a letter to your future self.`, `Sealed today, opened on the date you choose.` appears in the file
    - Hero inline-style assertion: `grep -F "marginBottom: '32px'" src/App.tsx` returns 1 (the wordmark margin override per UI-SPEC line 345 / PATTERNS.md line 1017)
    - Asset assertion: `grep -F '/assets/separator-ink.png' src/App.tsx` returns 1 (hero separator image)
    - Section divider assertion: `grep -F 'className="section-divider"' src/App.tsx` returns 1
    - Section divider assertion: ZERO stray `</div>` after the section-divider closing — `awk` or visual inspection confirms the section-divider JSX is balanced (no orphan div per RESEARCH.md Landmine #9)
    - Live-row assertion: `grep -F 'className="live-row"' src/App.tsx` returns 1 (wraps Counter)
    - Counter mount assertion: `grep -F '&lt;Counter target={waitlistCount ?? 115}' src/App.tsx` returns 1 (Counter mount with the fallback floor)
    - FirstLetter mount assertion: `grep -F 'initialEmail={isSubscribed ? email : undefined}' src/App.tsx` returns 1 (per UI-SPEC line 569 + D-06)
    - FirstLetter mount assertion: `grep -F 'waitlistCount={waitlistCount ?? 115}' src/App.tsx` returns 1
    - Smooth-scroll assertion: `grep -F "getElementById('first-letter')?.scrollIntoView" src/App.tsx` returns at least 1 (the WaitlistSuccessCard onWriteLetter callback)
    - File size: `wc -l src/App.tsx` returns a value &gt;= 100 and &lt;= 200 (was 241; the rewrite is leaner because most JSX moved into components)
  </acceptance_criteria>
  <done>`src/App.tsx` is a lean composition root. All 9 Wave-2 components are imported and mounted in the correct order. The Phase 1 data layer is preserved verbatim per D-06. `showSticky` (D-05) and unused Lucide icons (D-07) are deleted. ShareButtons import is removed. The hero shell, the section divider, and the FirstLetter scroll-target id are all in place. No AnimatePresence wrapper.</done>
</task>

<task type="auto">
  <name>Task 2: Delete src/components/ShareButtons.tsx</name>
  <files>src/components/ShareButtons.tsx</files>
  <read_first>
    - src/components/ShareButtons.tsx (current 49-line file — confirm it exists; the file is being removed entirely)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-UI-SPEC.md §Migration Map row `src/components/ShareButtons.tsx` (line 675 — "DELETE — No share UI in prototype. Phase 5 may reintroduce.")
    - .planning/phases/01.5-ui-redesign-inserted/01.5-PATTERNS.md `src/components/ShareButtons.tsx (DELETE)` (lines 1092–1096 — verification step: after deletion `grep -r ShareButtons src/` must return zero results)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-CONTEXT.md Deferred Ideas — Share UI Surface (Phase 5 may scope; explicitly OUT for Phase 1.5)
  </read_first>
  <action>
    Delete the file `src/components/ShareButtons.tsx` from the filesystem. Use `rm src/components/ShareButtons.tsx` (the file is git-tracked; the deletion will appear in `git status`). The file has 0 callers after Task 1 of this plan (App.tsx no longer imports it, and FirstLetter.tsx — rewritten in Plan 01.5-07 — also no longer imports it). Per UI-SPEC §Out of Scope rule, the prototype has no share UI surface; SOCIAL-02 and SOCIAL-03 from REQUIREMENTS.md will be revisited in Phase 5 if a share affordance is added back to the design.
  </action>
  <verify>
    <automated>
      ! test -f src/components/ShareButtons.tsx &amp;&amp; ! grep -r 'ShareButtons' src/
    </automated>
  </verify>
  <acceptance_criteria>
    - File deletion assertion: `test -f src/components/ShareButtons.tsx` exits non-zero (file does not exist)
    - No-callers assertion: `grep -r 'ShareButtons' src/` returns 0 lines (no imports or references remain anywhere in `src/`)
    - Git assertion: `git status --short src/components/ShareButtons.tsx` shows `D ` (deleted) — verifiable manually; not required for automated check
  </acceptance_criteria>
  <done>`src/components/ShareButtons.tsx` is deleted. No source file imports or references it. The Phase 5 reintroduction of a share UI (if any) will be a new design surface, not a carry-forward of the deleted file.</done>
</task>

<task type="auto">
  <name>Task 3: Run npm run build and confirm zero errors</name>
  <files></files>
  <read_first>
    - package.json (confirm the `build` script is `vite build` and the dependencies are unchanged; this plan does not modify package.json)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-RESEARCH.md Risk #13 (Vite 6 + React 19 + Tailwind v4 builds are stable; no `"use client"` directives needed)
  </read_first>
  <action>
    Run `npm run build` from the repo root. The build must exit 0 with no TypeScript errors, no Tailwind v4 compilation errors, and no missing-module errors. Capture the output for the summary. Common things that would break the build: (a) a forgotten import after the App.tsx rewrite — the executor must double-check every component import path is correct; (b) a Tailwind v4 utility name that doesn't exist (e.g., if `@theme` token names don't match the expected `--color-*` / `--text-*` prefixes — verify by running `npm run build` and checking the output CSS for the expected utility classes); (c) a JSX attribute that wasn't camelCased (e.g., `stroke-width` instead of `strokeWidth` in one of the inline SVGs). If the build fails, the executor must read the error message, identify the offending file + line, and fix it before this task can complete. The build is the canonical acceptance gate for "the rewrite did not break anything." If `npm run build` does not exist as a script in package.json, the executor must check what build command exists (typically `vite build` or `npm run build:tsc`) — the project's package.json already includes a `build` script per the `scripts` field.
  </action>
  <verify>
    <automated>
      cd /Users/nourismaiel/Downloads/SEALED\ Landing\ Page\ Claude\ Design &amp;&amp; npm run build
    </automated>
  </verify>
  <acceptance_criteria>
    - Build exit code: `npm run build` returns exit code 0
    - Build output assertion: `dist/index.html` exists after the build
    - Build output assertion: `dist/assets/index-*.css` file exists and contains the major Tailwind utility classes that the appended `@theme` tokens auto-generate (verify by `grep -F 'text-mark' dist/assets/index-*.css || grep -F '.text-ink-70' dist/assets/index-*.css` — at least one of these returns 1 match; the exact utility name depends on how Tailwind v4 transforms `--text-mark` and `--color-ink-70`)
    - Build output assertion: the bundled JS does NOT contain the string `i.postimg.cc` (paper-light is now self-hosted)
    - No-error assertion: the build log contains no lines starting with `error TS` (TypeScript errors), no lines starting with `Error:` (Vite errors), and no `Failed to compile` lines
    - No-warning-on-missing-asset assertion: no warning lines containing `public/assets/` (which would indicate a missing asset — all 5 PNGs + paper-light.jpg should resolve)
    - Manual confirmation: open the build output and verify (a) the bundled JS does not error out at runtime due to a missing component import; (b) the bundled CSS contains the 10 keyframe rules; (c) the dist/assets/ subdir contains the hashed asset files referenced in `dist/index.html`.
  </acceptance_criteria>
  <done>`npm run build` exits 0. The bundled `dist/` is ready for deployment. No new errors or warnings introduced compared to the post-Phase-1 baseline (which was a green build for the OLD App.tsx).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verified side-by-side parity check between dev server and .planning/reference/Landing.html</name>
  <what-built>
    The full Phase 1.5 redesign:
    - public/assets/ (6 files: 5 prototype PNGs + paper-light.jpg) and .planning/reference/Landing.html + assets (Wave 1, Plan 01.5-01)
    - src/index.css extended with @theme tokens + 10 @keyframes + global rules (Wave 1, Plan 01.5-02)
    - src/components/Nav.tsx, Footer.tsx (Wave 2, Plan 01.5-03)
    - src/components/WaitlistForm.tsx, WaitlistSuccessCard.tsx (Wave 2, Plan 01.5-04)
    - src/components/Counter.tsx rewritten as 5-digit flip-card (Wave 2, Plan 01.5-05)
    - src/components/HowItWorks.tsx, ResearchSection.tsx (Wave 2, Plan 01.5-06)
    - src/components/Typewriter.tsx trimmed + FirstLetter.tsx rewritten as 3-step state machine (Wave 2, Plan 01.5-07)
    - src/components/FAQ.tsx restyled with 8 verbatim Q&As (Wave 2, Plan 01.5-08)
    - src/App.tsx JSX rewrite + ShareButtons.tsx deletion + build passes (Wave 3, Plan 01.5-09 Tasks 1-3)
    - index.html title updated (Wave 1, Plan 01.5-01 Task 3)
  </what-built>
  <how-to-verify>
    1. Start the dev server in one terminal: `npm run dev` — verify it starts on port 3000 with no startup errors. Visit http://localhost:3000 in a browser.
    2. Open `.planning/reference/Landing.html` in a SECOND browser window (use `file://` URL or drag the file into the browser). Place the two windows side-by-side.
    3. Walk through the UI-SPEC §Acceptance Gates section-by-section parity checklist (UI-SPEC lines 806–817):
       - **Nav:** Fixed top bar with `SEALED` wordmark left and `Join Waitlist ↑` button right. Background blurs as the page scrolls.
       - **Hero:** `SEALED` wordmark in Instrument Serif at fluid 80–132px. Headline `Some letters are / worth waiting for.` with italic on the second line. Two-line tagline. Ink separator PNG below the tagline.
       - **Waitlist form:** Pill-shaped email input + black mono `Join the waitlist →` button. Click submit (the email field requires a value — try `test@example.com`). The form fades out and the success card fades in over ~550ms.
       - **Success card:** White rounded card with dashed-circle decoration in the top-right corner. Black-circle checkmark stamps in (~150ms after the card appears). `Added to waitlist.` title. Hairline divider. `Write your first letter now ↓` button with bobbing arrow.
       - **Counter:** Five white flip-card digits showing the live count (e.g., `00115`). Rotated diamond dot + `SIGN UPS SO FAR` mono label below. When the form submits successfully, the counter increments to `00116` with a staggered flip animation.
       - **Section divider:** Full-width hairline rule below the hero.
       - **How it Works:** Eyebrow `HOW IT WORKS` centered with hairlines. Headline `Three steps. One letter.` (with italic on the second part). 3-column grid of step cards with phone-screenshot PNGs (gradient frame + image). Each card lifts ~6px on hover.
       - **Research:** Off-white background with the manuscript-margin vertical hairlines. Rotated peer-reviewed rubber-stamp SVG top-right. Eyebrow `THE SCIENCE`. Headline `Writing it down actually works.` with subtle highlight under the italic. Deck `Two studies · one conclusion`. Stats poster band with corner ticks (L-brackets at all 4 corners) and `+22%` / `+42%` large figures with `×` serif divider between. Kicker `Your brain treats a written commitment as a promise.` Numbered footnotes for Schippers and Matthews citations.
       - **FirstLetter (write step):** Black background with radial glow. Eyebrow `TRY IT NOW`. Headline `Your first letter starts now.` Subhead with highlighted `January 1, 2027.` Paper pad with typewriter placeholder cycling through 13 phrases. Word count `0 words` initially. `SEAL & SEND` CTA disabled when empty. Type a few words — the CTA enables, the typewriter disappears, the word count updates.
       - **FirstLetter (email step):** Click `SEAL & SEND`. Step transitions to the envelope view: 3 blue postage stamps (wavy + circular + hexagonal), `Where should we send this?` label, `To` row with email input and arrow button. `← Back` returns to the write step.
       - **FirstLetter (success step):** Type a real email in the email step and submit. Step transitions to the success view: envelope-flap SVG covers the pad, wax-seal PNG drops in (~950ms `waxDrop` keyframe with cubic-bezier overshoot), caption `We'll deliver it on January 1, 2027 · in N days` with a live countdown.
       - **Promise strip:** `Secure · Delivered Jan 1, 2027 · Only you can read it` below the pad.
       - **FAQ:** Heading `Frequently Asked Questions (FAQs)` (with parenthetical, no sub-deck). 8 numbered items (01–08). Click any question — the plus icon rotates 45° to an X, the answer expands smoothly (max-height transition 350ms). Verify each of the 8 question texts is exactly the prototype wording.
       - **Footer:** Black background with top-edge gradient hairline. 4-column grid: brand (with `Some letters are worth waiting for.` tag), `Discover`, `Connect` (3 social inline-SVG circles), `Legal`. Bottom row `© 2026 · All rights reserved`.
    4. Open the browser DevTools Network tab — refresh the dev server URL. Confirm the counter fetches from `signup_counter` view (look for a `signup_counter` request to Supabase). If the request 4xx/5xxs, the counter should fall back to `00115` (the seeded floor) — this proves the .catch path works.
    5. Open the browser DevTools Console — perform a hero-form submit and a FirstLetter email submit. The console should remain clean (no NEW errors from the rewrite). The Phase-1 `console.error('Counter fetch failed:', ...)` or `console.error('Subscription failed:', ...)` or `console.error('Waitlist join failed:', ...)` lines may appear if the Supabase env is broken (the Phase 1 issue documented in STATE.md) — that's expected and is NOT a Phase 1.5 regression.
    6. Resize the browser to mobile width (320–540px). The wordmark, headline, counter digits, How cards, Research stats, FirstLetter pad, FAQ, and Footer all reflow correctly per the UI-SPEC mobile overrides.
    7. Type your verdict in chat:
       - `approved` — the side-by-side parity is acceptable
       - `issues: ...` followed by a list of specific section/element discrepancies — Phase 1.5 returns to planning/execute for gap closure
  </how-to-verify>
  <resume-signal>Type `approved` to mark Phase 1.5 complete and proceed to Phase 2 (Signup Flow). Or describe specific issues (`issues: hero headline italic missing on second line`, etc.) to trigger a gap-closure plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 1 data layer → Phase 1.5 surface | The counter fetch, the hero form submit, and the FirstLetter email submit all cross from React component code into the Phase-1 Supabase helpers (`getSignupCount`, `joinWaitlistLocal`). The boundary is preserved unchanged per D-06 — Phase 1.5 does NOT introduce new Supabase calls or new env vars. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.5-09-01 | Tampering | hero-form email + FirstLetter email | mitigate | Input validation surface unchanged from Phase 1: HTML5 `type="email"` + `required` provide first-line defense; `joinWaitlistLocal` is currently a no-op stub. Real validation lands in Phase 2 (`SIGNUP-05` + Turnstile + Edge Function). React 19 escapes the rendered controlled-input value. |
| T-01.5-09-02 | Cross-Site Scripting | rendered DOM | mitigate | Phase 1.5 introduces ONE new `dangerouslySetInnerHTML` site (in FAQ.tsx, with a static developer-authored array — per Plan 01.5-08 threat model). Every other rendered text is either a static literal or a controlled-input value escaped by React 19. No path from user input to `dangerouslySetInnerHTML`. |
| T-01.5-09-03 | Information Disclosure | committed `paper-light.jpg` | accept | Public marketing imagery; replaces an external CDN dependency. No PII. |
| T-01.5-09-04 | Denial of Service | Counter flip timers / FirstLetter step-transition timers | mitigate | Both components install `useEffect` cleanup functions clearing pending `setTimeout` handles. React 19 StrictMode double-invocation in dev is safe per RESEARCH.md Risk #6. |

`[ASVS-L1: visual port preserves Phase 1 data path. Input validation, rate limiting, and CSRF defense remain Phase 2's responsibility per CONTEXT.md scope.]`
</threat_model>

<verification>
After all four tasks:

1. `npm run build` exits 0 (Task 3).
2. The dev server renders the page without runtime errors (Task 4 manual check).
3. Side-by-side parity against `.planning/reference/Landing.html` is approved by the human (Task 4 checkpoint).
4. Phase 1 data layer is verifiably intact: counter fetches, falls back to 115 on failure, increments by +1 on submit. The two console.error placeholder lines are still present in src/App.tsx.
5. `src/components/ShareButtons.tsx` is deleted and has zero remaining callers (Task 2).
6. The build is green; the page is shippable (modulo Phase 2's signup wiring, which is the next phase).
</verification>

<success_criteria>
- All 5 ROADMAP §"Phase 1.5" success criteria pass:
  1. Side-by-side parity against `Landing.html` (Task 4 checkpoint).
  2. Counter fetches from `signup_counter` view, animates from `00000` to the real number, falls back to `00115` on fetch failure, increments by +1 on successful submit (Task 4 DevTools observation + Counter rewrite from Plan 01.5-05 + App.tsx data-layer preservation from Task 1).
  3. `npm run build` exits 0 with no new errors or warnings (Task 3).
  4. The email-form submit still calls `joinWaitlistLocal()` with the await shape preserved so Phase 2 can drop in the real Edge Function call (Task 1 D-06 preservation).
  5. All four required PNG assets are present (Plan 01.5-01).
- D-05 / D-06 / D-07 from CONTEXT.md are all honored (showSticky deleted, data-layer preserved, unused Lucide imports dropped — all verifiable by grep).
</success_criteria>

<output>
After completion, create `.planning/phases/01.5-ui-redesign-inserted/01.5-09-SUMMARY.md` listing: the final src/App.tsx line count, the count of imports kept (Counter + 8 new) vs dropped (lucide, ShareButtons, etc.), the `npm run build` output summary (zero errors), the side-by-side parity verdict from the human, and confirmation that all 10 UI requirements (UI-01..UI-10) are addressed.
</output>
</content>
</invoke>