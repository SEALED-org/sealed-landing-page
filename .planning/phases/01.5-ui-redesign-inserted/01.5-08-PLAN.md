---
phase: 01.5
slug: ui-redesign-inserted
plan: 08
id: 01.5-08
type: execute
wave: 2
depends_on: [01.5-02]
files_modified:
  - src/components/FAQ.tsx
autonomous: true
requirements: [UI-07]
requirements_addressed: [UI-07]
must_haves:
  truths:
    - "FAQ renders the 8 verbatim Q&A blocks from Landing.html lines 1915-1931 (replacing the current placeholder array) with their full HTML answers including <strong>, <br><br>, and curly apostrophes preserved"
    - "FAQ heading is exactly `Frequently Asked Questions (FAQs)` (with the parenthetical) and there is NO sub-deck below it (the current `Everything you need to know` is dropped)"
    - "FAQ plus icon is a single inline SVG with two crossed lines that rotates 45° to an X when the item is open (CSS transform), NOT a Lucide Plus/Minus swap"
    - "FAQ answer expansion uses pure CSS max-height transition (0 ↔ 600px, 350ms ease) via the `.faq-item.open` class — NOT motion/react `height: auto` which mis-measures on first paint (RESEARCH.md `What NOT to use`)"
    - "FAQ uses dangerouslySetInnerHTML for answer rendering because answers contain <strong> and <br><br>; the array is developer-authored so this is safe (UI-SPEC line 582-583)"
  artifacts:
    - path: "src/components/FAQ.tsx"
      provides: "Restyled FAQ accordion with 8 verbatim Q&A blocks and prototype-matching plus-icon rotate animation"
      min_lines: 90
      contains: "export default function FAQ, faqs, dangerouslySetInnerHTML, openIndex, className=\"faq\", className=\"faq-inner\", className=\"faq-head\", className=\"faq-list\", className=\"faq-item\", className=\"faq-q\", className=\"faq-icon\", className=\"faq-a\", className=\"faq-a-inner\", padStart(2, '0'), Frequently Asked Questions (FAQs), What is SEALED, Is SEALED free, Who can I send a letter to, What can I include in a letter, Can I edit a letter after, Is my letter private, Why not just schedule the email from Gmail or Outlook, What if you shut down the app, Sealed means sealed, A letter you can take back, A sealed letter is a promise"
  key_links:
    - from: "src/components/FAQ.tsx state"
      to: "CSS class .faq-item.open in src/index.css"
      via: "conditional className `faq-item ${isOpen ? 'open' : ''}` toggled by onClick"
      pattern: "openIndex|isOpen"
user_setup: []
---

<objective>
Restyle `src/components/FAQ.tsx` to match the prototype's FAQ section visual contract AND replace the current placeholder Q&A array (9 generic items with `\"Answer coming soon.\"`) with the 8 verbatim Q&A blocks from `Landing.html` lines 1915–1931. The new component uses pure CSS for the answer expand/collapse (max-height transition keyed off `.faq-item.open`) and a single rotating plus-icon SVG (NOT a Lucide Plus/Minus swap).

Purpose: The current FAQ uses motion/react's `<AnimatePresence>` with `height: 'auto'` which RESEARCH.md flags as misbehaving on first paint, plus Lucide `Plus` / `Minus` icon swap that doesn't match the prototype's single-rotating-SVG pattern. The 9 placeholder questions are not real content. This plan replaces all three together because the visual restyle and the data swap are inseparable — the new CSS class names (`.faq-item`, `.faq-q`, `.faq-a`) only apply to the new JSX structure, and the new structure requires the 8 real Q&As.

Output: One file fully rewritten (FAQ.tsx). No edits to App.tsx in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.5-ui-redesign-inserted/01.5-CONTEXT.md
@.planning/phases/01.5-ui-redesign-inserted/01.5-RESEARCH.md
@.planning/phases/01.5-ui-redesign-inserted/01.5-UI-SPEC.md
@.planning/phases/01.5-ui-redesign-inserted/01.5-PATTERNS.md
@src/components/FAQ.tsx
@src/index.css

# Prototype source:
# .planning/reference/Landing.html lines 1694-1701 (FAQ section + head markup)
# .planning/reference/Landing.html lines 1915-1931 (8 verbatim Q&A blocks — the source of truth for the array data)
# .planning/reference/Landing.html lines 1939-1946 (per-item markup template — to translate into the JSX map())
# .planning/reference/Landing.html lines 1073-1132 (FAQ CSS — already in src/index.css)

<interfaces>
<!-- FAQ takes no props -->
export default function FAQ() { ... }

<!-- The Q&A array structure -->
const faqs: Array<{ q: string; a: string }> = [...]; // 8 items, full HTML in `a`

<!-- Local state -->
const [openIndex, setOpenIndex] = useState<number | null>(null);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fully rewrite src/components/FAQ.tsx with 8 verbatim Q&A blocks, single-rotating plus-icon SVG, and pure-CSS max-height accordion</name>
  <files>src/components/FAQ.tsx</files>
  <read_first>
    - src/components/FAQ.tsx (current 65-line file using Lucide Plus/Minus + motion/react AnimatePresence + 9 placeholder Q&As — replace entirely)
    - .planning/reference/Landing.html lines 1915-1931 (THE source of truth for the 8 Q&A blocks — copy character-by-character, including special apostrophes U+2019 (`'`), `<strong>` tags, and `<br><br>` line breaks)
    - .planning/reference/Landing.html lines 1939-1946 (per-item markup template from the prototype's JS-generated content; lift the structure into the JSX map() body)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-UI-SPEC.md §FAQ (lines 286–305 — 8-question summary table with key copy phrases) + §FAQ heading (lines 307–310 — `Frequently Asked Questions (FAQs)` with no sub-deck) + §Section-by-Section Visual Contract `7. FAQ` (lines 417–428 — visual rule mapping)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-PATTERNS.md `src/components/FAQ.tsx (RESTYLE + DATA SWAP)` (lines 795–869 — full file template with the 8 verbatim Q&A array, the state pattern, the rotating-plus SVG, and the dangerouslySetInnerHTML guidance)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-RESEARCH.md §Section 9 FAQ (lines 454–502 — markup audit + max-height transition guidance + "DO NOT use Motion's height: auto") + Risk #4 (dangerouslySetInnerHTML safety rationale)
  </read_first>
  <action>
    Replace the entire contents of `src/components/FAQ.tsx` with a new implementation following the structure below.

    **Imports:** Only `useState` from `'react'`. Do NOT import `Plus` or `Minus` from `lucide-react` (drop). Do NOT import `motion` or `AnimatePresence` from `motion/react` (drop). Per PATTERNS.md lines 800–807: the only React import is `useState`. (PATTERNS.md mentions `motion` could be used for the `.faq-head reveal` — for context-budget reasons we skip the reveal here; the static FAQ head appears immediately.)

    **The 8 Q&A array — copy verbatim from Landing.html lines 1915–1931.** Declare at module top (above the default export): `const faqs: Array<{ q: string; a: string }> = [ ... ];`. Each item has `q: string` (the question) and `a: string` (the answer in HTML, containing `<strong>` and `<br><br>`). The 8 items in order:

    1. `q: "What is SEALED?"` `a:` two paragraphs ending `"... It's built on a simple belief: some things are worth writing down before you forget why they mattered."`
    2. `q: "Is SEALED free?"` `a:` two paragraphs; mentions premium for `"longer time horizons, plus the ability to attach photos and voice notes."`
    3. `q: "Who can I send a letter to?"` `a:` two paragraphs; `"Right now, you. Only you."` in the first sentence.
    4. `q: "What can I include in a letter?"` `a:` two paragraphs; closes with `"Some things are better written."`
    5. `q: "Can I edit a letter after it's sealed?"` (note the curly apostrophe in `it's` — U+2019, NOT a straight apostrophe) `a:` two paragraphs; closes with `"A letter you can take back isn't a letter worth opening."` (also curly `isn't`)
    6. `q: "Is my letter private?"` `a:` single paragraph; closes with `"Sealed means sealed."`
    7. `q: "Why not just schedule the email from Gmail or Outlook?"` `a:` long-form with a `<strong>...</strong>` highlight on the sentence beginning `"Simply put: there's no long-term guarantee that a scheduled email will actually be delivered years later."`
    8. `q: "What if you shut down the app? What happens to my letters?"` `a:` two paragraphs; closes with `"A sealed letter is a promise. We keep it, no matter what."`

    The full answer strings (with `<br><br>` between paragraphs and `<strong>` tags preserved) are documented verbatim in PATTERNS.md lines 818–834. Lift them character-by-character from there OR directly from `.planning/reference/Landing.html` lines 1915–1931. The two `&amp;` HTML entities in question 5's `a` string (e.g., if any appear) must be preserved as-is. The em-dash characters (U+2014) in the closing-line "A letter you can take back" / "A sealed letter is a promise" sentences must be preserved.

    **State:** `const [openIndex, setOpenIndex] = useState<number | null>(null);` and `const toggle = (i: number) => setOpenIndex(openIndex === i ? null : i);`. At most one FAQ item is open at a time (per the prototype's single-open behavior).

    **Default export `FAQ`** returning the following JSX tree:

    `<section className=\"faq\">` containing `<div className=\"faq-inner\">` which contains two children:

    (1) **`<div className=\"faq-head\">`** containing `<h2>Frequently Asked Questions (FAQs)</h2>`. The h2 text is exactly `Frequently Asked Questions (FAQs)` (with the parenthetical) per UI-SPEC line 309 — NOT the current `Frequently Asked Questions`. Do NOT add a sub-deck `<p>` element under the h2 — the current `Everything you need to know` is dropped (per UI-SPEC line 310: `sub-deck: none`).

    (2) **`<div className=\"faq-list\">`** containing `faqs.map((faq, i) => ...)` where the per-item JSX is:

    ```
    const num = String(i + 1).padStart(2, '0');
    const isOpen = openIndex === i;
    return (
      <div
        key={i}
        className={`faq-item ${isOpen ? 'open' : ''}`}
        style={{ '--faq-i': i } as React.CSSProperties}
      >
        <button className=\"faq-q\" type=\"button\" onClick={() => toggle(i)} aria-expanded={isOpen}>
          <span className=\"num\">{num}</span>
          <span className=\"label\">{faq.q}</span>
          <svg className=\"faq-icon\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" strokeWidth=\"1.6\" strokeLinecap=\"round\" strokeLinejoin=\"round\">
            <line x1=\"12\" y1=\"5\" x2=\"12\" y2=\"19\" />
            <line x1=\"5\" y1=\"12\" x2=\"19\" y2=\"12\" />
          </svg>
        </button>
        <div className=\"faq-a\">
          {/* eslint-disable-next-line react/no-danger */}
          <div className=\"faq-a-inner\" dangerouslySetInnerHTML={{ __html: faq.a }} />
        </div>
      </div>
    );
    ```

    Per PATTERNS.md lines 841–860. Note the `style={{ '--faq-i': i } as React.CSSProperties}` — the prototype uses a `--faq-i` CSS custom property for cascading delays; setting it as an inline style requires the cast since TypeScript's CSSProperties doesn't allow arbitrary `--*` keys. The plus icon is a **single SVG with two crossed lines** (one vertical `M12,5 → 12,19`, one horizontal `M5,12 → 19,12`); when `.faq-item.open` is set, the global CSS rule `.faq-item.open .faq-icon { transform: rotate(45deg); }` (already in src/index.css per Plan 01.5-02) rotates the whole SVG 45° to become an X. Per UI-SPEC line 580–581: this is the prototype's single-rotating-SVG pattern, NOT a Lucide Plus/Minus swap.

    Per RESEARCH.md §Section 9 FAQ + UI-SPEC line 581: the open/close animation is pure CSS max-height transition (0 ↔ 600px, 350ms ease). The class toggle `.faq-item.open` triggers it. Do NOT use motion/react's `height: 'auto'` (it mis-measures on first paint).

    The `dangerouslySetInnerHTML={{ __html: faq.a }}` pattern is documented as safe in UI-SPEC line 582–583 + RESEARCH.md Risk #4 because the `faqs` array is developer-authored static text (no user input flows into this path). Include the `eslint-disable-next-line react/no-danger` comment as documentation even though no ESLint config exists today; it serves as a marker that the choice is deliberate.

    Add `aria-expanded={isOpen}` to the `<button className=\"faq-q\">` for screen-reader accessibility — this is best practice for disclosure widgets and is not flagged in the prototype but adds zero visual cost. Do NOT add `aria-controls` (would require ID generation per item; not in scope).

    Do NOT add any other accordion behavior (no auto-close-on-scroll, no keyboard navigation beyond the native button), no analytics, no per-item callbacks. The component is pure render + click-toggle.
  </action>
  <verify>
    <automated>
      test -f src/components/FAQ.tsx &amp;&amp; grep -F 'export default function FAQ' src/components/FAQ.tsx &amp;&amp; grep -F 'useState' src/components/FAQ.tsx &amp;&amp; grep -F 'openIndex' src/components/FAQ.tsx &amp;&amp; grep -F 'dangerouslySetInnerHTML' src/components/FAQ.tsx &amp;&amp; grep -F "padStart(2, '0')" src/components/FAQ.tsx &amp;&amp; grep -F 'Frequently Asked Questions (FAQs)' src/components/FAQ.tsx &amp;&amp; grep -F 'What is SEALED?' src/components/FAQ.tsx &amp;&amp; grep -F 'Is SEALED free?' src/components/FAQ.tsx &amp;&amp; grep -F 'Who can I send a letter to?' src/components/FAQ.tsx &amp;&amp; grep -F 'What can I include in a letter?' src/components/FAQ.tsx &amp;&amp; grep -F "Can I edit a letter after" src/components/FAQ.tsx &amp;&amp; grep -F 'Is my letter private?' src/components/FAQ.tsx &amp;&amp; grep -F 'Why not just schedule the email from Gmail or Outlook?' src/components/FAQ.tsx &amp;&amp; grep -F 'What if you shut down the app?' src/components/FAQ.tsx &amp;&amp; grep -F 'Sealed means sealed.' src/components/FAQ.tsx &amp;&amp; grep -F 'A sealed letter is a promise.' src/components/FAQ.tsx &amp;&amp; grep -F 'className="faq"' src/components/FAQ.tsx &amp;&amp; grep -F 'className="faq-inner"' src/components/FAQ.tsx &amp;&amp; grep -F 'className="faq-head"' src/components/FAQ.tsx &amp;&amp; grep -F 'className="faq-list"' src/components/FAQ.tsx &amp;&amp; grep -F 'className="faq-q"' src/components/FAQ.tsx &amp;&amp; grep -F 'className="faq-icon"' src/components/FAQ.tsx &amp;&amp; grep -F 'className="faq-a"' src/components/FAQ.tsx &amp;&amp; grep -F 'className="faq-a-inner"' src/components/FAQ.tsx &amp;&amp; grep -F 'aria-expanded' src/components/FAQ.tsx &amp;&amp; ! grep -E "from 'motion/react'" src/components/FAQ.tsx &amp;&amp; ! grep -F 'AnimatePresence' src/components/FAQ.tsx &amp;&amp; ! grep -E "(Plus|Minus).*from 'lucide-react'" src/components/FAQ.tsx &amp;&amp; ! grep -F 'Everything you need to know' src/components/FAQ.tsx &amp;&amp; ! grep -F 'Answer coming soon.' src/components/FAQ.tsx
    </automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'export default function FAQ' src/components/FAQ.tsx` returns 1
    - Source assertion: `grep -F 'useState' src/components/FAQ.tsx` returns at least 1 (single React import)
    - Source assertion: `grep -F 'openIndex' src/components/FAQ.tsx` returns at least 2 (declaration + toggle reference)
    - Q&A count assertion: count the question keys in the faqs array. Approach: `awk '/const faqs/,/^\\];/' src/components/FAQ.tsx | grep -cE '^\\s+\\{ q:'` should return 8 (each item starts with `{ q:` on its own line) — adapt the pattern if the executor formats the array differently, but the visible question count via `grep -cF '?' src/components/FAQ.tsx` minus question marks inside answers is harder to verify; the per-question assertions below are the authoritative checks.
    - Q&A presence assertion: each of the 8 question texts appears (one match each): `What is SEALED?`, `Is SEALED free?`, `Who can I send a letter to?`, `What can I include in a letter?`, `Can I edit a letter after it's sealed?` (with curly apostrophe), `Is my letter private?`, `Why not just schedule the email from Gmail or Outlook?`, `What if you shut down the app? What happens to my letters?`
    - Answer key-phrase assertion: each of these closing phrases appears in the answers: `Sealed means sealed.`, `A letter you can take back`, `A sealed letter is a promise.`, `Some things are better written.`, `Right now, you. Only you.`, `built on a simple belief`, `longer time horizons`, `Simply put: there's no long-term guarantee`
    - Heading assertion: `grep -F 'Frequently Asked Questions (FAQs)' src/components/FAQ.tsx` returns 1
    - No-sub-deck assertion: `grep -F 'Everything you need to know' src/components/FAQ.tsx` returns 0 (the current sub-deck is dropped per UI-SPEC line 310)
    - No-placeholder assertion: `grep -F 'Answer coming soon.' src/components/FAQ.tsx` returns 0 (the current placeholder array is fully replaced)
    - Class assertion: every required className appears: `faq`, `faq-inner`, `faq-head`, `faq-list`, `faq-item`, `faq-q`, `faq-icon`, `faq-a`, `faq-a-inner`, plus the inner `num` and `label` span classes
    - Padding assertion: `grep -F "padStart(2, '0')" src/components/FAQ.tsx` returns 1 (the per-item number padding)
    - DangerouslySetInnerHTML assertion: `grep -F 'dangerouslySetInnerHTML' src/components/FAQ.tsx` returns 1 (the answer rendering)
    - Eslint-disable comment assertion: `grep -F 'eslint-disable-next-line react/no-danger' src/components/FAQ.tsx` returns 1 (documents the deliberate choice)
    - Aria assertion: `grep -F 'aria-expanded' src/components/FAQ.tsx` returns 1
    - Single-rotating-icon assertion: `grep -c 'className="faq-icon"' src/components/FAQ.tsx` returns 1 (a single SVG, NOT a swap)
    - Icon-shape assertion: `grep -F '&lt;line x1="12" y1="5" x2="12" y2="19" /&gt;' src/components/FAQ.tsx` returns 1 (the vertical line of the plus)
    - Icon-shape assertion: `grep -F '&lt;line x1="5" y1="12" x2="19" y2="12" /&gt;' src/components/FAQ.tsx` returns 1 (the horizontal line of the plus)
    - Inline-style assertion: `grep -F "'--faq-i'" src/components/FAQ.tsx` returns 1 (the CSS custom property for per-item delay) and `grep -F 'as React.CSSProperties' src/components/FAQ.tsx` returns 1 (the cast to allow arbitrary CSS variable keys)
    - Conditional-class assertion: `grep -E 'faq-item \\$\\{(isOpen|openIndex)' src/components/FAQ.tsx` returns at least 1 (the conditional className template literal)
    - No-Motion assertion: `grep -E "from 'motion/react'" src/components/FAQ.tsx` returns 0
    - No-AnimatePresence assertion: `grep -F 'AnimatePresence' src/components/FAQ.tsx` returns 0
    - No-Lucide assertion: `grep -E "(Plus|Minus).*from 'lucide-react'" src/components/FAQ.tsx` returns 0 (Lucide Plus/Minus swap is replaced)
    - No-height-auto assertion: `grep -F "height: 'auto'" src/components/FAQ.tsx` returns 0 (RESEARCH.md `What NOT to use`)
    - File size: `wc -l src/components/FAQ.tsx` returns a value &gt;= 90 and &lt;= 220 (the 8 answers are long strings; the structure is small)
  </acceptance_criteria>
  <done>`FAQ.tsx` is fully rewritten. Renders the 8 verbatim Q&A blocks with `<strong>`, `<br><br>`, and curly apostrophes preserved. Heading is `Frequently Asked Questions (FAQs)` with no sub-deck. Plus icon is a single rotating SVG (45° on `.open`). Accordion is pure-CSS max-height. No motion/react imports, no Lucide imports, no AnimatePresence. Aria-expanded on the toggle button.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `dangerouslySetInnerHTML` consumer | Renders developer-authored static HTML strings (the `faqs[i].a` field). No user input flows into this path; all answers are hard-coded at module top. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.5-08-01 | Cross-Site Scripting | `dangerouslySetInnerHTML` answers | mitigate | The `faqs` array is a hard-coded module-level constant — no user-supplied input enters the path. The `eslint-disable-next-line react/no-danger` comment documents the deliberate choice. UI-SPEC line 582–583 explicitly approves this pattern for developer-authored HTML. If a future plan adds user-editable FAQ content, that plan MUST sanitize before assigning to `__html`. |
| T-01.5-08-02 | Tampering | rendered answer HTML | accept | The HTML strings contain only `<strong>` and `<br>` tags — no `<script>`, no `<iframe>`, no `<a href="javascript:...">`, no event handlers. The closed set of allowed tags is documented in PATTERNS.md lines 819–834. |

`[ASVS-L1: deliberate static dangerouslySetInnerHTML — risk reviewed and accepted per UI-SPEC §FAQ §Component Contracts line 582–583]`
</threat_model>

<verification>
After Task 1:

1. The file exists and passes its grep-based acceptance criteria.
2. `npm run build` is NOT yet expected to pass — App.tsx still imports the old chrome. The build green check is owned by 01.5-09-PLAN.md.
3. The 8 question texts are verifiable by inspection against `.planning/reference/Landing.html` lines 1915–1931 (each question key in the array exactly matches the prototype's question text).
</verification>

<success_criteria>
- `FAQ.tsx` is rewritten. 8 Q&As are present with all special characters (curly apostrophes, em-dashes) preserved.
- Heading matches UI-SPEC verbatim with the parenthetical and no sub-deck.
- Plus icon is a single rotating SVG; accordion is pure CSS; no Lucide / motion-react imports.
- `aria-expanded` is set on the toggle button.
- `dangerouslySetInnerHTML` is used deliberately with an eslint-disable comment as documentation.
</success_criteria>

<output>
After completion, create `.planning/phases/01.5-ui-redesign-inserted/01.5-08-SUMMARY.md` listing the final FAQ file line count, the count of Q&A items (8), confirmation that placeholders were fully replaced, and confirmation that no motion/react or Lucide imports remain.
</output>
</content>
</invoke>