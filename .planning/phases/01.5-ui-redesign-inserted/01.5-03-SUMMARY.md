---
phase: 01.5
slug: ui-redesign-inserted
plan: 03
id: 01.5-03
status: complete
self_check: PASSED
key_files:
  created:
    - src/components/Nav.tsx
    - src/components/Footer.tsx
---

# 01.5-03 — Nav + Footer extraction

## What was built

Two self-contained, prop-less, zero-state React components extracted from the previously inlined chrome in `src/App.tsx`. Both consume only global CSS class names defined by Plan 01.5-02; markup is byte-faithful to `.planning/reference/Landing.html` (Nav lines 1335–1340, Footer lines 1708–1746).

## Tasks completed

### Task 1 — `src/components/Nav.tsx` (19 lines)

Fixed top nav matching the prototype.

- `<nav className="top">` — global `nav.top` rule from 01.5-02 provides the fixed/blurred bar
- `.mark` wordmark "SEALED" with inline-style `fontWeight: 500, fontSize: 20` (per RESEARCH Landmine #10 — these overrides are preserved exactly from the prototype's inline styles)
- `.join` button with inline-style `fontWeight: 500, fontSize: 10`, text "Join Waitlist " + `<span className="arrow">↑</span>` (U+2191)
- onClick handler: `document.getElementById('waitlist')?.scrollIntoView({ behavior: 'smooth' })`

### Task 2 — `src/components/Footer.tsx` (80 lines)

4-column black footer.

- `<footer className="foot">` → `.foot-inner` grid
- **Brand column** (`.foot-brand`): `.mark` "SEALED" + `.tag` "Some letters are worth waiting for."
- **Discover column** (`.foot-col`): links to `#waitlist` ("Join the waitlist") and `#first-letter` ("Write a letter")
- **Connect column** (`.foot-col foot-social`): `.social-list` with three inline SVG icons:
  - Instagram: 22x22 rect+circle+circle (stroke 1.6, currentColor)
  - X (Twitter): 20x20 path (filled currentColor)
  - Email: `mailto:hello@sealed.io`, 22x22 rect+polyline
- **Legal column** (`.foot-col`): `privacy.html`, `terms.html`
- **Bottom row** (`.foot-bottom`): "© 2026 · All rights reserved"

## Must-haves verified

| Must-have | Status |
|----|----|
| Nav renders fixed top bar with SEALED wordmark + Join Waitlist ↑ button, smooth-scroll to `#waitlist` | ✓ |
| Footer matches `Landing.html` lines 1708–1746 (4 cols + 3 inline social SVGs + bottom row) | ✓ |
| Both components default-export PascalCase React 19 functional, no props | ✓ |
| Nav inline-style overrides preserved (Landmine #10): `fontWeight: 500, fontSize: 20` (`.mark`) + `fontWeight: 500, fontSize: 10` (`.join`) | ✓ |

## Key-link contract

| Link | Pattern |
|------|---------|
| Nav button onClick → `#waitlist` | `getElementById('waitlist')?.scrollIntoView({ behavior: 'smooth' })` |
| Footer links → static pages + in-page anchors | `href="privacy.html"`, `href="terms.html"`, `href="#waitlist"`, `href="#first-letter"`, `href="mailto:hello@sealed.io"` |

## Self-Check: PASSED

- [x] Both files default-export PascalCase functions
- [x] Nav `min_lines` ≥ 15 (19), Footer `min_lines` ≥ 50 (80)
- [x] All required `className` strings present
- [x] Smooth-scroll handler uses exact pattern `getElementById('waitlist')?.scrollIntoView({ behavior: 'smooth' })`
- [x] Three inline SVGs match prototype attributes (viewBox 24×24, strokeWidth/Linecap/Linejoin where applicable)
- [x] Inline-style preservation: Nav uses style props (Landmine #10)
- [x] No edits to `App.tsx` or other files

## Notes / Recovery

The original 01.5-03 sub-agent dispatch hit a Bash permission denial before doing any work. The orchestrator executed this plan inline on the main working tree (no worktree) and committed the two files atomically. Behavior matches the worktree contract — neither file modifies shared orchestrator artifacts (STATE.md, ROADMAP.md); those updates come from the post-wave orchestrator step.
