---
phase: 01.5
slug: ui-redesign-inserted
plan: 06
id: 01.5-06
type: execute
wave: 2
depends_on: [01.5-01, 01.5-02]
files_modified:
  - src/components/HowItWorks.tsx
  - src/components/ResearchSection.tsx
autonomous: true
requirements: [UI-04, UI-05]
requirements_addressed: [UI-04, UI-05]
must_haves:
  truths:
    - "HowItWorks renders 3 step cards (01 Write, 02 Seal, 03 Open) with the screenshot PNGs from /assets/step-write.png, /assets/step-seal.png, /assets/step-open.png and the verbatim h3+p copy from UI-SPEC"
    - "HowItWorks step h3s render the number-then-title pattern with inline opacity 0.38 on the number, JSX `{' '}` (en-space U+2002) between number and title text, and italic `<em>` on step 03's `when the time comes.`"
    - "ResearchSection renders the peer-reviewed rubber-stamp SVG (inline, ~25 lines) + eyebrow + headline (with em + ::after-driven highlight) + deck + stats poster (two figures with corner ticks + × divider) + kicker + footnote ol"
    - "All scroll-reveal effects use `motion/react`'s `whileInView` + `viewport={{ once: true, amount: 0.12 }}` per RESEARCH.md Pattern 1; stagger delays 0 / 0.08 / 0.16s for `.reveal` / `.reveal.d1` / `.reveal.d2`"
    - "Both components are no-props default-export functional components matching the codebase convention"
  artifacts:
    - path: "src/components/HowItWorks.tsx"
      provides: "3-column How-it-Works section with step screenshots; scroll-reveal staggered"
      min_lines: 50
      contains: "export default function HowItWorks, className=\"how\", className=\"how-inner\", className=\"how-eyebrow\", className=\"how-h\", className=\"steps-row\", className=\"step-card\", className=\"step-screenshot\", className=\"step-meta\", /assets/step-write.png, /assets/step-seal.png, /assets/step-open.png, opacity: 0.38, motion.div, whileInView, How it works, Three steps., One letter., Write a letter to your future self., Seal it., Open it, when the time comes."
    - path: "src/components/ResearchSection.tsx"
      provides: "Research section with peer-reviewed rubber-stamp SVG + two stats poster + footnotes"
      min_lines: 90
      contains: "export default function ResearchSection, className=\"research\", className=\"research-inner\", className=\"rs-stamp\", className=\"research-eyebrow\", className=\"rs-headline\", className=\"rs-deck\", className=\"rs-stats\", className=\"rs-stat\", className=\"rs-stat-num\", className=\"rs-sign\", className=\"rs-pct\", className=\"rs-divider\", className=\"rs-divider-mark\", className=\"rs-kicker\", className=\"rs-refs\", className=\"rs-tick-bl\", className=\"rs-tick-br\", textPath, vetted, N = 2 STUDIES, PEER REVIEWED, Writing it down, actually works., Study 01, Study 02, +22, +42, Schippers, Matthews, motion.div, whileInView"
  key_links:
    - from: "HowItWorks step screenshots"
      to: "/assets/step-{write,seal,open}.png"
      via: "<img src=\"/assets/step-{name}.png\">"
      pattern: "/assets/step-(write|seal|open)\\.png"
    - from: "Scroll-reveal motion.div in both components"
      to: "motion/react whileInView API"
      via: "viewport={{ once: true, amount: 0.12 }} + transition.delay 0 / 0.08 / 0.16"
      pattern: "whileInView"
user_setup: []
---

<objective>
Create two new static-content components: `src/components/HowItWorks.tsx` (the 3-column step cards section) and `src/components/ResearchSection.tsx` (the peer-reviewed research section with the rubber-stamp SVG, two stats poster, and footnotes). Both are static (zero props, zero state) and rely on the global CSS already declared by Plan 01.5-02 and the asset PNGs already in place from Plan 01.5-01. Scroll-reveal entrance animations use `motion/react`'s `whileInView` API.

Purpose: These are two of the three "static content section" replacements in the redesign. They appear consecutively in the page composition (after the hero section divider, before the FirstLetter section). Bundling them in one plan keeps the plan focused on "static content with scroll reveals + image refs + inline SVG" without dragging in the state-machine complexity of FirstLetter or the FAQ accordion. Both components are independent — no shared props, no shared state, no shared classnames — so the executor can implement them in either order.

Output: Two new files. No edits to App.tsx in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.5-ui-redesign-inserted/01.5-CONTEXT.md
@.planning/phases/01.5-ui-redesign-inserted/01.5-RESEARCH.md
@.planning/phases/01.5-ui-redesign-inserted/01.5-UI-SPEC.md
@.planning/phases/01.5-ui-redesign-inserted/01.5-PATTERNS.md
@src/index.css

# Prototype source for HowItWorks:
# .planning/reference/Landing.html lines 1405-1446 (markup — 3 .step-card divs with .reveal/.reveal.d1/.reveal.d2 classes)
# .planning/reference/Landing.html lines 383-463 (CSS — already in src/index.css)

# Prototype source for ResearchSection:
# .planning/reference/Landing.html lines 1448-1530 (markup — peer-reviewed stamp SVG + eyebrow + headline + deck + stats poster + kicker + footnotes)
# .planning/reference/Landing.html lines 1452-1470 (peer-reviewed rubber-stamp SVG — lift inline verbatim)
# .planning/reference/Landing.html lines 464-713 (CSS — already in src/index.css)

<interfaces>
<!-- Both components take NO props. -->

export default function HowItWorks() { ... }
export default function ResearchSection() { ... }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/components/HowItWorks.tsx — 3-column step-card section with scroll-reveal stagger and PNG screenshots</name>
  <files>src/components/HowItWorks.tsx</files>
  <read_first>
    - .planning/reference/Landing.html lines 1405-1446 (full markup — section.how > .how-inner > .how-eyebrow + .how-h + .steps-row > 3 .step-card divs each with .step-screenshot + .step-meta)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-UI-SPEC.md §How it Works (lines 212–222 — h3 + body copy verbatim for all 3 steps; lines 354–364 — visual contract incl. step-screenshot dimensions and hover lift)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-PATTERNS.md `src/components/HowItWorks.tsx (NEW)` (lines 459–517 — full template with the `&ensp;` JSX-encoding rule from RESEARCH.md Landmine #5 and the reveal-stagger pattern)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-RESEARCH.md Pattern 1 (lines 673–693 — motion/react whileInView API + viewport.amount: 0.12 + once: true) and Landmine #5 (lines 1045–1053 — use JSX `{' '}` for the en-space between the number span and the title text; the value `{' '}` is byte-equivalent to `&ensp;`)
  </read_first>
  <action>
    Create `src/components/HowItWorks.tsx`. Import `motion` from `'motion/react'`. Default-export a function `HowItWorks` taking no props.

    Render a `<section className=\"how\">` containing a `<div className=\"how-inner\">` which contains three children in this order:

    (1) **Eyebrow:** `<motion.div className=\"how-eyebrow\" initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, amount: 0.12 }} transition={{ duration: 0.9, ease: 'easeOut' }}>` containing three children: `<span className=\"rule\" />`, `<span>How it works</span>`, `<span className=\"rule\" />`. The eyebrow uses the global `.how-eyebrow` style which puts hairlines on either side of the text. The copy `How it works` is from UI-SPEC line 214.

    (2) **Headline:** `<motion.h2 className=\"how-h\" initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, amount: 0.12 }} transition={{ duration: 0.9, ease: 'easeOut' }}>` containing the text `Three steps. ` followed by `<em>One letter.</em>`. The copy is from UI-SPEC line 215; the `<em>` italic emphasis is on `One letter.` only.

    (3) **Steps row:** `<div className=\"steps-row\">` containing exactly three `<motion.div className=\"step-card\" key=\"01\" /* etc */ initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, amount: 0.12 }} transition={{ duration: 0.9, ease: 'easeOut', delay: 0 }}>` for the first card, with `delay: 0.08` for the second and `delay: 0.16` for the third. Per RESEARCH.md Pattern 1, these delays match the prototype's `.reveal.d1` / `.reveal.d2` stagger values exactly.

    Each `.step-card` contains two children:

    **`<div className=\"step-screenshot\">`** containing exactly one `<img src=\"/assets/step-{NAME}.png\" alt=\"{descriptive}\" />` where `{NAME}` is `write`, `seal`, or `open` and the alt text is descriptive per UI-SPEC accessibility guidance (e.g., `alt=\"Writing a letter to your future self\"`, `alt=\"Sealing the letter with a chosen open date\"`, `alt=\"Opening the letter on its scheduled day\"`). The `<img>` does NOT need a `width`/`height` attribute — the global `.step-screenshot img` CSS rule (already in src/index.css) handles sizing via `width: 100%; height: 100%; object-fit: cover`.

    **`<div className=\"step-meta\">`** containing an `<h3>` and a `<p>`. The h3 follows the prototype's number-then-title inline-span pattern:
    - Step 01: `<h3><span style={{ opacity: 0.38 }}>01</span>{' '}Write a letter to your future self.</h3>` — the `{' '}` is the en-space U+2002 between the number and the title (per RESEARCH.md Landmine #5; JSX-encoding the en-space character literally in source text would also work but `{' '}` is the documented pattern).
    - Step 02: `<h3><span style={{ opacity: 0.38 }}>02</span>{' '}Seal it.</h3>`
    - Step 03: `<h3><span style={{ opacity: 0.38 }}>03</span>{' '}Open it <em>when the time comes.</em></h3>` — italic em on `when the time comes.` only (UI-SPEC line 220).

    The `<p>` for each step contains the verbatim body copy from UI-SPEC lines 217 / 219 / 221:
    - Step 01: `A hope, a fear, a prediction, a promise. Photos and voice notes welcome.`
    - Step 02: `Pick the date it opens — six months, a year, a decade. Locked until then.` (the dash between `opens` and `six` is the em-dash U+2014).
    - Step 03: `Meet your past self. Reflect on who you were when you wrote it.`

    Do NOT use the `.step-n` CSS class for the number — the prototype uses the inline `<span style=\"opacity:0.38\">NN</span>` pattern (per RESEARCH.md §Section 6 "How it Works" line 247: "the `.step-n` class exists in the CSS but isn't referenced — skip it; the inline-span pattern wins"). The `0.38` opacity is set inline as `style={{ opacity: 0.38 }}`.

    No state, no useEffect, no useRef. Pure rendering.
  </action>
  <verify>
    <automated>
      test -f src/components/HowItWorks.tsx &amp;&amp; grep -F 'export default function HowItWorks' src/components/HowItWorks.tsx &amp;&amp; grep -E "from 'motion/react'" src/components/HowItWorks.tsx &amp;&amp; grep -F 'className="how"' src/components/HowItWorks.tsx &amp;&amp; grep -F 'className="how-inner"' src/components/HowItWorks.tsx &amp;&amp; grep -F 'className="how-eyebrow"' src/components/HowItWorks.tsx &amp;&amp; grep -F 'className="how-h"' src/components/HowItWorks.tsx &amp;&amp; grep -F 'className="steps-row"' src/components/HowItWorks.tsx &amp;&amp; grep -c 'className="step-card"' src/components/HowItWorks.tsx | grep -qE '^3$' &amp;&amp; grep -c 'className="step-screenshot"' src/components/HowItWorks.tsx | grep -qE '^3$' &amp;&amp; grep -F '/assets/step-write.png' src/components/HowItWorks.tsx &amp;&amp; grep -F '/assets/step-seal.png' src/components/HowItWorks.tsx &amp;&amp; grep -F '/assets/step-open.png' src/components/HowItWorks.tsx &amp;&amp; grep -F 'How it works' src/components/HowItWorks.tsx &amp;&amp; grep -F 'Three steps.' src/components/HowItWorks.tsx &amp;&amp; grep -F 'One letter.' src/components/HowItWorks.tsx &amp;&amp; grep -F 'Write a letter to your future self.' src/components/HowItWorks.tsx &amp;&amp; grep -F 'Seal it.' src/components/HowItWorks.tsx &amp;&amp; grep -F 'when the time comes.' src/components/HowItWorks.tsx &amp;&amp; grep -F 'opacity: 0.38' src/components/HowItWorks.tsx &amp;&amp; grep -F "{' '}" src/components/HowItWorks.tsx &amp;&amp; grep -F 'whileInView' src/components/HowItWorks.tsx &amp;&amp; grep -F 'amount: 0.12' src/components/HowItWorks.tsx &amp;&amp; grep -F 'delay: 0.08' src/components/HowItWorks.tsx &amp;&amp; grep -F 'delay: 0.16' src/components/HowItWorks.tsx
    </automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'export default function HowItWorks' src/components/HowItWorks.tsx` returns 1
    - Source assertion: `grep -c 'className="step-card"' src/components/HowItWorks.tsx` returns 3 (one per step)
    - Source assertion: `grep -c 'className="step-screenshot"' src/components/HowItWorks.tsx` returns 3
    - Source assertion: `grep -c 'className="step-meta"' src/components/HowItWorks.tsx` returns 3
    - Image-src assertion: `grep -c '/assets/step-' src/components/HowItWorks.tsx` returns 3 (write/seal/open)
    - Image-src assertion: each of `/assets/step-write.png`, `/assets/step-seal.png`, `/assets/step-open.png` appears exactly once
    - Copy assertion: each of `How it works`, `Three steps.`, `One letter.`, `Write a letter to your future self.`, `Seal it.`, `when the time comes.` appears at least once in the file
    - Copy assertion: `grep -F 'A hope, a fear, a prediction, a promise. Photos and voice notes welcome.' src/components/HowItWorks.tsx` returns 1 (step 01 body)
    - Copy assertion: `grep -F 'Locked until then.' src/components/HowItWorks.tsx` returns 1 (step 02 body suffix)
    - Copy assertion: `grep -F 'Meet your past self.' src/components/HowItWorks.tsx` returns 1 (step 03 body)
    - Number-pattern assertion: each of `&gt;01&lt;`, `&gt;02&lt;`, `&gt;03&lt;` appears exactly once inside an `<span style={{ opacity: 0.38 }}>` element (verify the spans render the strings `01`, `02`, `03`)
    - Inline-style assertion: `grep -c 'opacity: 0.38' src/components/HowItWorks.tsx` returns 3 (one per step number)
    - En-space assertion: `grep -F "{' '}" src/components/HowItWorks.tsx` returns 3 (one per h3 between the number span and the title)
    - Em-tag assertion: `grep -c '&lt;em&gt;' src/components/HowItWorks.tsx` returns 2 (one in the headline `One letter.`, one in step 03's `when the time comes.`)
    - Motion-import assertion: `grep -E "from 'motion/react'" src/components/HowItWorks.tsx` returns 1
    - Motion-use assertion: `grep -c 'whileInView' src/components/HowItWorks.tsx` returns at least 5 (eyebrow + headline + 3 step cards)
    - Stagger assertion: `grep -F 'delay: 0.08' src/components/HowItWorks.tsx` returns 1 (step 02) and `grep -F 'delay: 0.16' src/components/HowItWorks.tsx` returns 1 (step 03)
    - Viewport assertion: `grep -F "{ once: true, amount: 0.12 }" src/components/HowItWorks.tsx` returns at least 1
    - File size: `wc -l src/components/HowItWorks.tsx` returns a value &gt;= 50 and &lt;= 130
  </acceptance_criteria>
  <done>`HowItWorks.tsx` renders the 3-column step-card section with the verbatim copy, the three step-screenshot PNGs from `/assets/`, the inline-style opacity 0.38 number pattern + en-space + italic em on step 03, and motion/react `whileInView` scroll-reveals with the 0 / 0.08 / 0.16 stagger delays.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/components/ResearchSection.tsx — peer-reviewed stamp + headline + two stats poster + footnotes</name>
  <files>src/components/ResearchSection.tsx</files>
  <read_first>
    - .planning/reference/Landing.html lines 1448-1530 (full markup — including the peer-reviewed stamp SVG and the stats poster with corner ticks)
    - .planning/reference/Landing.html lines 1452-1470 (peer-reviewed stamp SVG — lift verbatim with JSX-name conversions)
    - .planning/reference/Landing.html lines 1485-1515 (stats poster markup with the `+22%` / `+42%` figures + corner-tick spans + × divider)
    - .planning/reference/Landing.html lines 1516-1528 (kicker `<p>` + footnote `<ol>`)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-UI-SPEC.md §Research (lines 226–242 — copy contract for headline, deck, stat tags, figcaptions, kicker, footnotes) + §Section-by-Section Visual Contract `5. Research` (lines 366–382 — manuscript margin, corner ticks, sup styling)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-PATTERNS.md `src/components/ResearchSection.tsx (NEW)` (lines 521–586 — full inline-SVG stamp markup + stats poster JSX + kicker + footnotes + scroll-reveal stagger map)
    - .planning/phases/01.5-ui-redesign-inserted/01.5-RESEARCH.md §Section 7 Research (lines 256–283 — markup audit + per-element rationale) and Landmine #8 (the stray `</p>` on Landing.html line 1517 — DROP the stray closing tag, JSX would syntax-error otherwise)
  </read_first>
  <action>
    Create `src/components/ResearchSection.tsx`. Import `motion` from `'motion/react'`. Default-export a function `ResearchSection` taking no props.

    Render `<section className=\"research\">` containing `<div className=\"research-inner\">` which contains seven children in this order:

    (1) **Peer-reviewed rubber stamp** — `<motion.div className=\"rs-stamp\" aria-hidden=\"true\" initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, amount: 0.12 }} transition={{ duration: 0.9, ease: 'easeOut' }}>` containing the inline peer-reviewed-stamp SVG lifted from `.planning/reference/Landing.html` lines 1452–1470. Per PATTERNS.md ResearchSection Task and UI-SPEC line 532, ship inline — do NOT extract to an asset file. The SVG must have `<svg viewBox=\"0 0 100 100\">` and contain: a `<defs>` with `<path id=\"rs-stamp-arc\" d=\"M 50,50 m -38,0 a 38,38 0 1,1 76,0 a 38,38 0 1,1 -76,0\"/>`; two `<circle>` rings (outer r=46 strokeWidth=1, inner r=33 strokeWidth=0.6); a `<text>` with `<textPath href=\"#rs-stamp-arc\" startOffset=\"0\">PEER REVIEWED · METHODOLOGY · </textPath>` (Space Mono 5.4px weight 700 letterSpacing=1.6 fill=currentColor); a centered `<text x=\"50\" y=\"46\" textAnchor=\"middle\">vetted</text>` (Instrument Serif italic 14px fill=currentColor); and a smaller `<text x=\"50\" y=\"60\" textAnchor=\"middle\">N = 2 STUDIES</text>` (Space Mono 4.2px weight 700 letterSpacing=1.4 fill=currentColor). All attributes use JSX-camelCase (`textAnchor`, `fontFamily`, `fontSize`, `fontStyle`, `fontWeight`, `strokeWidth`, `letterSpacing`).

    (2) **Eyebrow** — `<motion.div className=\"research-eyebrow\" initial={{...}} whileInView={{...}} viewport={{ once: true, amount: 0.12 }} transition={{ duration: 0.9, ease: 'easeOut' }}>` containing `<span className=\"rule\" />`, `<span>The science</span>`, `<span className=\"rule\" />`. Copy: `The science` (UI-SPEC line 229).

    (3) **Headline** — `<motion.h2 className=\"rs-headline\" initial={{...}} whileInView={{...}} viewport={{ once: true, amount: 0.12 }} transition={{ duration: 0.9, ease: 'easeOut' }}>` containing `Writing it down ` followed by `<em>actually works.</em>`. The CSS rule `.rs-headline em::after` (already in src/index.css) draws the subtle highlight bar under the italic text — no JSX needed for the highlight.

    (4) **Deck** — `<motion.p className=\"rs-deck\" initial={{...}} whileInView={{...}} viewport={{ once: true, amount: 0.12 }} transition={{ duration: 0.9, ease: 'easeOut' }}>` containing `Two studies ` followed by `<span className=\"mark\">·</span>` followed by ` one conclusion`. Per UI-SPEC line 231 the middle bullet is serif-italic via `.mark` class.

    (5) **Stats poster** — `<motion.div className=\"rs-stats\" initial={{...}} whileInView={{...}} viewport={{ once: true, amount: 0.12 }} transition={{ duration: 0.9, ease: 'easeOut', delay: 0.08 }}>` (the `.d1` stagger = 0.08s) containing:
       - `<span className=\"rs-tick-bl\" aria-hidden=\"true\" />` (empty span — bottom-left L-bracket; CSS draws it via border-color/border-width)
       - `<span className=\"rs-tick-br\" aria-hidden=\"true\" />` (empty span — bottom-right L-bracket)
       - **Study 01 figure:** `<figure className=\"rs-stat\">` containing `<span className=\"rs-stat-tag\"><span className=\"dot\" />Study 01</span>` then `<div className=\"rs-stat-num\"><span className=\"rs-sign\">+</span>22<span className=\"rs-pct\">%</span></div>` then `<figcaption>Performance increase when goals are written.<sup className=\"rs-ref\">1</sup></figcaption>`.
       - **Divider:** `<div className=\"rs-divider\"><span className=\"rs-divider-mark\">×</span></div>` (the `×` is U+00D7 multiplication sign).
       - **Study 02 figure:** identical structure to Study 01 but with `Study 02`, `+42%`, figcaption `More likely to follow through on a written goal.<sup className=\"rs-ref\">2</sup>`.
       Per RESEARCH.md §Research lines 262 + PATTERNS.md lines 542–562, the corner-tick spans must be rendered as empty `<span>` children even though they have no content — the CSS uses them for border-drawn L-brackets at the bottom corners (the top corners are drawn by `::before` and `::after` pseudo-elements on `.rs-stats`).

    (6) **Kicker** — `<motion.p className=\"rs-kicker\" initial={{...}} whileInView={{...}} viewport={{ once: true, amount: 0.12 }} transition={{ duration: 0.9, ease: 'easeOut', delay: 0.16 }}>` containing `Your brain treats a written commitment as a ` followed by `<em>promise.</em>`. Per RESEARCH.md Landmine #8 + PATTERNS.md line 565: the prototype has a stray `</p>` on Landing.html line 1517 — IGNORE it; the JSX is self-closing properly with a single `</motion.p>`.

    (7) **Footnotes** — `<motion.ol className=\"rs-refs\" initial={{...}} whileInView={{...}} viewport={{ once: true, amount: 0.12 }} transition={{ duration: 0.9, ease: 'easeOut', delay: 0.16 }}>` containing two `<li>` items:
       - `<li><span className=\"num\">1</span><span>Schippers, Morisano, Locke &amp; Latham — <em>Contemporary Educational Psychology</em>, n = 2,928, 2020</span></li>`
       - `<li><span className=\"num\">2</span><span>Matthews, PhD — <em>Dominican University of California</em>, n = 267, 2015</span></li>`
       The `&amp;` is the JSX-escaped ampersand. The em-dash between author names and journal title is U+2014.

    Do NOT use `dangerouslySetInnerHTML` anywhere — every content string is a plain JSX child. The only thing that requires care is the corner-tick empty spans (no children needed; CSS does the drawing).
  </action>
  <verify>
    <automated>
      test -f src/components/ResearchSection.tsx &amp;&amp; grep -F 'export default function ResearchSection' src/components/ResearchSection.tsx &amp;&amp; grep -E "from 'motion/react'" src/components/ResearchSection.tsx &amp;&amp; grep -F 'className="research"' src/components/ResearchSection.tsx &amp;&amp; grep -F 'className="research-inner"' src/components/ResearchSection.tsx &amp;&amp; grep -F 'className="rs-stamp"' src/components/ResearchSection.tsx &amp;&amp; grep -F 'className="research-eyebrow"' src/components/ResearchSection.tsx &amp;&amp; grep -F 'className="rs-headline"' src/components/ResearchSection.tsx &amp;&amp; grep -F 'className="rs-deck"' src/components/ResearchSection.tsx &amp;&amp; grep -F 'className="rs-stats"' src/components/ResearchSection.tsx &amp;&amp; grep -c 'className="rs-stat"' src/components/ResearchSection.tsx | grep -qE '^2$' &amp;&amp; grep -c 'className="rs-stat-num"' src/components/ResearchSection.tsx | grep -qE '^2$' &amp;&amp; grep -F 'className="rs-sign"' src/components/ResearchSection.tsx &amp;&amp; grep -F 'className="rs-pct"' src/components/ResearchSection.tsx &amp;&amp; grep -F 'className="rs-divider"' src/components/ResearchSection.tsx &amp;&amp; grep -F 'className="rs-divider-mark"' src/components/ResearchSection.tsx &amp;&amp; grep -F 'className="rs-kicker"' src/components/ResearchSection.tsx &amp;&amp; grep -F 'className="rs-refs"' src/components/ResearchSection.tsx &amp;&amp; grep -F 'className="rs-tick-bl"' src/components/ResearchSection.tsx &amp;&amp; grep -F 'className="rs-tick-br"' src/components/ResearchSection.tsx &amp;&amp; grep -F 'PEER REVIEWED' src/components/ResearchSection.tsx &amp;&amp; grep -F 'N = 2 STUDIES' src/components/ResearchSection.tsx &amp;&amp; grep -F 'vetted' src/components/ResearchSection.tsx &amp;&amp; grep -F 'textPath' src/components/ResearchSection.tsx &amp;&amp; grep -F 'The science' src/components/ResearchSection.tsx &amp;&amp; grep -F 'Writing it down' src/components/ResearchSection.tsx &amp;&amp; grep -F 'actually works.' src/components/ResearchSection.tsx &amp;&amp; grep -F 'one conclusion' src/components/ResearchSection.tsx &amp;&amp; grep -F 'Study 01' src/components/ResearchSection.tsx &amp;&amp; grep -F 'Study 02' src/components/ResearchSection.tsx &amp;&amp; grep -F '+' src/components/ResearchSection.tsx &amp;&amp; grep -F '22' src/components/ResearchSection.tsx &amp;&amp; grep -F '42' src/components/ResearchSection.tsx &amp;&amp; grep -F 'Schippers' src/components/ResearchSection.tsx &amp;&amp; grep -F 'Matthews' src/components/ResearchSection.tsx &amp;&amp; grep -F 'Contemporary Educational Psychology' src/components/ResearchSection.tsx &amp;&amp; grep -F 'Dominican University of California' src/components/ResearchSection.tsx &amp;&amp; grep -F 'promise.' src/components/ResearchSection.tsx &amp;&amp; grep -F 'whileInView' src/components/ResearchSection.tsx &amp;&amp; ! grep -F 'dangerouslySetInnerHTML' src/components/ResearchSection.tsx
    </automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'export default function ResearchSection' src/components/ResearchSection.tsx` returns 1
    - Structure assertion: every required className appears (research, research-inner, rs-stamp, research-eyebrow, rs-headline, rs-deck, rs-stats, rs-stat, rs-stat-num, rs-sign, rs-pct, rs-divider, rs-divider-mark, rs-kicker, rs-refs, rs-tick-bl, rs-tick-br)
    - Stats count: `grep -c 'className="rs-stat"' src/components/ResearchSection.tsx` returns 2 (Study 01 + Study 02)
    - Stamp content: each of `PEER REVIEWED`, `vetted`, `N = 2 STUDIES`, `textPath` appears (the inline SVG stamp is ported)
    - Copy assertion: each of `The science`, `Writing it down`, `actually works.`, `Two studies`, `one conclusion`, `Study 01`, `Study 02`, `Performance increase when goals are written.`, `More likely to follow through on a written goal.`, `Schippers`, `Matthews`, `Contemporary Educational Psychology`, `Dominican University of California`, `2,928`, `267`, `2020`, `2015`, `promise.` appears
    - Figure assertion: each of `&gt;22&lt;`, `&gt;42&lt;` appears (the bare digit children of `.rs-stat-num`, between `.rs-sign` and `.rs-pct`)
    - X-divider assertion: `grep -F '&gt;×&lt;' src/components/ResearchSection.tsx` returns 1 (the multiplication sign U+00D7 in the rs-divider-mark span)
    - Corner-tick assertion: `grep -c 'rs-tick-bl\|rs-tick-br' src/components/ResearchSection.tsx` returns 2 (bottom-left + bottom-right; top corners are CSS pseudo-elements)
    - Footnote assertion: `grep -c '&lt;sup className="rs-ref"&gt;' src/components/ResearchSection.tsx` returns 2 (one per study figcaption)
    - Footnote assertion: `grep -c '&lt;span className="num"&gt;' src/components/ResearchSection.tsx` returns 2 (the numbered badges in `.rs-refs`)
    - Em-tag assertion: `grep -c '&lt;em&gt;' src/components/ResearchSection.tsx` returns at least 4 (`actually works.` in headline + `promise.` in kicker + `Contemporary Educational Psychology` + `Dominican University of California`)
    - Motion-import assertion: `grep -E "from 'motion/react'" src/components/ResearchSection.tsx` returns 1
    - Motion-use assertion: `grep -c 'whileInView' src/components/ResearchSection.tsx` returns at least 7 (stamp + eyebrow + headline + deck + stats + kicker + refs)
    - Stagger assertion: `grep -F 'delay: 0.08' src/components/ResearchSection.tsx` returns at least 1 (stats poster = .d1)
    - Stagger assertion: `grep -F 'delay: 0.16' src/components/ResearchSection.tsx` returns at least 2 (kicker + refs = .d2)
    - No-html-injection assertion: `grep -F 'dangerouslySetInnerHTML' src/components/ResearchSection.tsx` returns 0
    - File size: `wc -l src/components/ResearchSection.tsx` returns a value &gt;= 90 and &lt;= 220
  </acceptance_criteria>
  <done>`ResearchSection.tsx` renders the peer-reviewed rubber-stamp SVG (inline ~25 lines), eyebrow + headline + deck + stats poster (with corner ticks + × divider + two figures) + kicker + footnote ol, all with verbatim copy and the documented motion/react scroll-reveal stagger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None introduced. Both components are static content with no user input, no callbacks, no state.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.5-06-01 | Tampering | inline SVG markup | accept | Both components contain only static JSX literals (peer-reviewed stamp, no user-controlled content). React 19 escapes any text content; no `dangerouslySetInnerHTML` used. |
| T-01.5-06-02 | Information Disclosure | citation copy | accept | Both citations (Schippers + Matthews) are publicly available academic references. No PII, no secrets. |

`[ASVS-L1: N/A — visual port preserves Phase 1 data path; static-content components with no inputs.]`
</threat_model>

<verification>
After both tasks:

1. Both files exist with their respective acceptance criteria satisfied.
2. `npm run build` is NOT yet expected to pass.
3. Visually, the rendered output should reveal as the user scrolls into each section with the documented stagger pattern, but that is verified in Plan 01.5-09.
</verification>

<success_criteria>
- `HowItWorks.tsx` exists with 3 step cards, all step PNG references pointing at `/assets/step-*.png`, the inline opacity-0.38 number pattern, JSX en-space between number and title, em italic on step 03's `when the time comes.` and on the headline's `One letter.`
- `ResearchSection.tsx` exists with the peer-reviewed stamp SVG (inline), eyebrow, headline (with em + CSS-driven highlight), deck (with serif-italic `·` mark), stats poster (2 figures + corner ticks + × divider), kicker (with stray `</p>` from Landing.html dropped), and footnote ol (2 items with em italics on journal names).
- Both components use `motion/react` `whileInView` with `viewport.amount: 0.12` and `viewport.once: true`, matching the prototype's IntersectionObserver threshold.
- Neither component imports from `lucide-react` or `./lib/supabase`. Both take no props.
</success_criteria>

<output>
After completion, create `.planning/phases/01.5-ui-redesign-inserted/01.5-06-SUMMARY.md` listing the two new files, line counts, and the count of `whileInView` instances in each.
</output>
</content>
</invoke>