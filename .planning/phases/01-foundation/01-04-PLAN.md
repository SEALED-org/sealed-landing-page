---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - src/App.tsx
autonomous: false
requirements: [DB-05, COUNTER-01, COUNTER-02, COUNTER-04]
must_haves:
  truths:
    - "`src/App.tsx` no longer imports from `./firebase` — Firebase is gone from the source tree"
    - "`src/App.tsx` imports `joinWaitlistLocal` from `./lib/supabase` and `getSignupCount` from `./lib/supabase`, plus the default `Counter` component"
    - "`npm run build` completes with zero errors and produces a `dist/` folder"
    - "Visiting the dev server (port 3000) renders the hero with no runtime errors"
    - "On a cold page load, the hero counter renders `000` and animates up to the real value returned by `public.signup_counter` (115 on an empty DB, more if rows exist)"
    - "After a successful email submit, the counter locally increments by +1 in the same tab (D-01)"
    - "The pulse indicator (green dot + animate-ping ring + `Live` label) renders identically to its pre-Phase-1 appearance — only the integer rendering changed (COUNTER-04 / D-13)"
    - "If Supabase fetch fails or times out, the counter animates to 115 and a `Counter fetch failed:` error is logged to the browser console (D-11, D-12)"
    - "Both `joinWaitlist(...)` call sites are replaced with `await joinWaitlistLocal(...)` — the `await` shape is preserved so Phase 2 swaps the stub body without restructuring the try/catch/finally skeleton"
    - "No Firebase residue in `src/` after Wave 2 (grep returns zero matches)"
  artifacts:
    - path: "src/App.tsx"
      provides: "Root component now wired to Supabase instead of Firebase, with Counter mounted in the pulse row and joinWaitlistLocal calls preserving the await shape"
      contains: "from './lib/supabase'"
  key_links:
    - from: "src/App.tsx (useEffect on mount)"
      to: "getSignupCount() in src/lib/supabase.ts"
      via: ".then(setWaitlistCount).catch(err => { console.error('Counter fetch failed:', err); setWaitlistCount(115); })"
      pattern: "getSignupCount\\(\\).*then"
    - from: "src/App.tsx (pulse row JSX)"
      to: "<Counter target={waitlistCount ?? 115} />"
      via: "default-imported Counter component from ./components/Counter"
      pattern: "<Counter target="
    - from: "handleSubscribe try block AND FirstLetter.onEmailSubmit try block"
      to: "joinWaitlistLocal(email) in src/lib/supabase.ts"
      via: "await joinWaitlistLocal(email) — preserves the await shape so Phase 2 swaps the stub body without restructuring the wrapper"
      pattern: "await joinWaitlistLocal\\("
    - from: "handleSubscribe try block AND FirstLetter.onEmailSubmit try block"
      to: "setWaitlistCount(c => (c ?? 115) + 1)"
      via: "local +1 increment for immediate feedback (D-01)"
      pattern: "setWaitlistCount\\(.*\\+ 1\\)"
---

<objective>
Apply the surgical edits to `src/App.tsx` that complete Phase 1, split across two focused tasks for reviewability: (Task 1) structural rewire — replace the Firebase import, the hardcoded useState, the Firebase realtime useEffect, and mount the Counter inside the existing pulse row; (Task 2) call-site rewire — replace both `joinWaitlist(...)` call sites with `await joinWaitlistLocal(...)` so Phase 2 can swap the stub body without restructuring the try/catch/finally skeleton. Then (Task 3) run the build and verify no Firebase residue, and (Task 4) human-verify the counter animates to the real number on a fresh page load.

Purpose: This is Wave 2 — the wire-up that requires the database to exist. Plan 03's HALT confirmed the migration is applied; this plan completes the round trip. The split between Task 1 and Task 2 is deliberate: Task 1's four edits all change different lines (import, useState, useEffect, JSX mount) and don't shift each other's line numbers in ways that matter, but Task 2's two edits to the call-site try blocks rewrite shared patterns and benefit from being scoped on their own with a dedicated grep gate. After this plan, the four Phase 1 success criteria from ROADMAP.md are met: (1) `npm run build` is green, (2) the dev server renders the hero with the counter reading from Supabase, (3) the table and view exist (verified in plan 03's HALT), (4) the pulse indicator is unchanged.

Output: One modified file (`src/App.tsx`) with the structural rewire and the call-site rewire applied, a passing build with no Firebase residue, and a verified working counter on the dev server. No other source files are touched. After this plan, Phase 1 is complete and ready for `/gsd-transition` to refresh the planning docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@CLAUDE.md
@.planning/codebase/CONVENTIONS.md
@src/App.tsx
@src/lib/supabase.ts
@src/components/Counter.tsx

<interfaces>
<!-- The exact line-level edits to apply to src/App.tsx. -->
<!-- PATTERNS.md "src/App.tsx" section is the diff-level source of truth. -->
<!-- Task 1 actions reference targets by PATTERN/CONTENT, not line number — -->
<!-- once any one edit applies, downstream line numbers shift, so content-based -->
<!-- targeting keeps the remaining edits robust. -->

Current src/App.tsx (line numbers as of plan creation — informational only; targeting is content-based):
- Line 7: `import { subscribeToWaitlistCount, joinWaitlist } from './firebase';`
- Line 13: `const [waitlistCount, setWaitlistCount] = useState(102);`
- Lines 23-26: Firebase realtime subscription useEffect.
- Line 35: `await joinWaitlist(email);` inside handleSubscribe's try block.
- Lines 113-115: `<span>Join {waitlistCount.toLocaleString()} others on the list</span>`.
- Line 197: `await joinWaitlist(newEmail);` inside FirstLetter onEmailSubmit's try block.

Modules to import (created in plans 01 and 02):
```typescript
import { getSignupCount, joinWaitlistLocal } from './lib/supabase';
import Counter from './components/Counter';
```

Surrounding JSX that MUST stay byte-identical (pulse row, src/App.tsx lines 104-116):
```
<div className="flex items-center justify-center gap-3 text-[10px] sm:text-xs font-mono font-bold uppercase tracking-[0.05em] whitespace-nowrap opacity-60">
  <div className="flex items-center gap-1.5">
    <div className="relative flex h-2 w-2">
      <span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-green-400 opacity-75"></span>
      <span className="relative inline-flex rounded-full h-2 w-2 bg-green-500"></span>
    </div>
    <span>Live</span>
  </div>
  <div className="w-px h-3 bg-black/20" />
  <span>
    Join {waitlistCount.toLocaleString()} others on the list   ← only this inner line changes
  </span>
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Structural rewire of src/App.tsx (import, useState, useEffect, Counter mount)</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (read the WHOLE file so the executor sees current state — edits below target by content/pattern, not by line number)
    - .planning/phases/01-foundation/01-PATTERNS.md (section: "`src/App.tsx` (modified module — 4 surgical edits, everything else untouched)" — every edit including the byte-identical pulse row JSX requirement is documented there with before/after diffs)
    - .planning/phases/01-foundation/01-RESEARCH.md (sections: "Pattern 3: App.tsx rewire (Wave B)", "Common Pitfalls → Pitfall 1: Wiring App.tsx before the migration is applied", "Anti-Patterns → Don't use useState(115)")
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions D-11, D-12, D-13 — fallback to 115, console.error wording, pulse unchanged)
    - src/lib/supabase.ts (created in plan 01 — confirm `getSignupCount` and `joinWaitlistLocal` are both named exports so the import statement is correct)
    - src/components/Counter.tsx (created in plan 02 — confirm it's a default export taking `target: number`)
    - .planning/codebase/CONVENTIONS.md (Imports, Error Handling, Logging sections)
  </read_first>
  <action>Apply these FOUR structural edits to `src/App.tsx`. Each edit targets the OLD CONTENT by pattern — once Edit 1 applies, downstream line numbers shift, but content-based targeting stays correct.

**Edit 1 — Replace the Firebase import.** Find the line containing `from './firebase'` and remove it entirely. In its place, insert two new import lines together:
```
import { getSignupCount, joinWaitlistLocal } from './lib/supabase';
import Counter from './components/Counter';
```
Both imports go on consecutive lines, with the existing surrounding imports (React, lucide icons, FirstLetter, FAQ, ShareButtons) preserved in their current order. Path style is relative + no extension (matches CONVENTIONS.md). Note: `joinWaitlistLocal` is imported alongside `getSignupCount` even though Task 1 doesn't reference `joinWaitlistLocal` directly — Task 2 needs it; importing both here in one statement is cleaner than splitting the import across tasks.

**Edit 2 — Replace the hardcoded counter useState.** Find the line containing `useState(102)` and change it to:
```
const [waitlistCount, setWaitlistCount] = useState<number | null>(null);
```
The generic type annotation matches the existing CONVENTIONS.md pattern for non-inferrable state. `null` is intentional — RESEARCH.md Anti-Pattern explicitly rejects `useState(115)` because the animation would start at 115 and tween to 115 (no movement) before the real fetch resolves.

**Edit 3 — Replace the Firebase realtime useEffect.** Find the useEffect whose body calls `subscribeToWaitlistCount` (a block of ~4 lines including the unsubscribe cleanup return). Remove the entire useEffect block. In its place, insert:
```
useEffect(() => {
  getSignupCount()
    .then(setWaitlistCount)
    .catch((error) => {
      console.error('Counter fetch failed:', error);
      setWaitlistCount(115); // D-11 fallback to seeded floor
    });
}, []);
```
The console.error wording `'Counter fetch failed:'` is locked by D-12 — use it verbatim. The fallback value `115` is the seeded floor per D-11. Empty dependency array `[]` — runs once on mount.

**Edit 4 — Mount Counter inside the pulse row.** Find the JSX `Join {waitlistCount.toLocaleString()} others on the list` (inside the pulse row `<span>`). Replace ONLY the inner text rendering, leaving the wrapping `<span>` and the parent `<div>` with its `flex items-center justify-center gap-3 text-[10px] ... opacity-60` classes (including the `animate-ping` green dot block) BYTE-IDENTICAL. New inner content:
```
Join <Counter target={waitlistCount ?? 115} /> others on the list
```
This is COUNTER-04 / D-13 — the pulse row stays visually unchanged.

Do NOT touch `joinWaitlist(...)` call sites in this task — that is Task 2's scope. Do NOT remove the `showSticky` state or its useEffect — that's a Phase 5 polish concern (deferred per CONTEXT.md). Do NOT touch any other line in App.tsx.</action>
  <verify>
    <automated>set -o pipefail; ! grep -q "from './firebase'" src/App.tsx && ! grep -q "subscribeToWaitlistCount" src/App.tsx && ! grep -q "useState(102)" src/App.tsx && grep -q "from './lib/supabase'" src/App.tsx && grep -q "getSignupCount" src/App.tsx && grep -q "joinWaitlistLocal" src/App.tsx && grep -q "import Counter from './components/Counter'" src/App.tsx && grep -q "useState<number | null>(null)" src/App.tsx && grep -q "getSignupCount()" src/App.tsx && grep -q "Counter fetch failed:" src/App.tsx && grep -q "setWaitlistCount(115)" src/App.tsx && grep -q "<Counter target={waitlistCount ?? 115} />" src/App.tsx && grep -q "animate-ping" src/App.tsx && echo "OK: Task 1 structural rewire complete — Firebase import gone, Supabase + Counter imports added (joinWaitlistLocal imported but not yet called), useState rewritten, fetch useEffect rewritten, Counter mounted in the pulse row with pulse JSX preserved"</automated>
  </verify>
  <done>
    `src/App.tsx`: Firebase import gone. `getSignupCount, joinWaitlistLocal` imported from `./lib/supabase` (joinWaitlistLocal is imported and visible in scope, ready for Task 2 to call). `Counter` default-imported from `./components/Counter`. `useState<number | null>(null)` replaces `useState(102)`. New one-shot fetch useEffect with `.then(setWaitlistCount).catch(...)` replaces the Firebase subscription useEffect, with the `'Counter fetch failed:'` console.error and `setWaitlistCount(115)` fallback both present. `<Counter target={waitlistCount ?? 115} />` mounted inside the pulse row's `<span>`. The pulse row container with `animate-ping` green dot is unchanged. `joinWaitlist(...)` call sites are STILL PRESENT (will be removed in Task 2). `showSticky` state remains in place (Phase 5 will remove it).
  </done>
</task>

<task type="auto">
  <name>Task 2: Call-site rewire of src/App.tsx (replace joinWaitlist with await joinWaitlistLocal at both sites)</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (re-read after Task 1's edits — line numbers have shifted; target by content/pattern)
    - .planning/phases/01-foundation/01-PATTERNS.md (section: "`src/App.tsx` (modified module)" — specifically "Edit 5 — Reconcile the two surviving `joinWaitlist` call sites" with before/after diffs for both call sites)
    - .planning/phases/01-foundation/01-RESEARCH.md (sections: "Pattern 3: App.tsx rewire" — option (a) recommendation to keep the `await` shape; "Code Examples → Example 4: Local +1 stub for Wave B")
    - .planning/phases/01-foundation/01-CONTEXT.md (decision D-01 — local +1 increment)
    - src/lib/supabase.ts (confirm `joinWaitlistLocal(email: string): Promise<void>` is the actual exported signature so the `await` call shape matches)
    - .planning/codebase/CONVENTIONS.md (Error Handling section — the try/catch/finally pattern stays; only the line inside try changes)
  </read_first>
  <action>Apply the call-site rewire at BOTH surviving `joinWaitlist(...)` call sites. Target by content pattern, not by line number — Task 1's edits shifted line numbers, and Task 2 introduces more shifts.

**Call site 1 — `handleSubscribe` try block.** Find the try block inside the `handleSubscribe` arrow function. It currently looks like:
```
try {
  await joinWaitlist(email);
  setIsSubscribed(true);
} catch (error) {
  console.error('Subscription failed:', error);
} finally {
  setIsSubmitting(false);
}
```
Change the body of the `try` ONLY. Replace the two lines `await joinWaitlist(email);` and `setIsSubscribed(true);` with these three lines:
```
await joinWaitlistLocal(email);
setIsSubscribed(true);
setWaitlistCount((c) => (c ?? 115) + 1);
```
Critical: the `await` shape is preserved (Phase 2 will swap the stub body with a real fetch — keeping the `await` means the try/catch/finally wrapper doesn't need restructuring). The `console.error('Subscription failed:', error);` line in the catch and the `setIsSubmitting(false);` in finally stay BYTE-IDENTICAL. Do NOT introduce any new comment inside the try block — the contract is documented in `src/lib/supabase.ts` where the stub lives.

**Call site 2 — `FirstLetter` `onEmailSubmit` callback.** Find the `onEmailSubmit` prop passed to `<FirstLetter ... />` later in the JSX. It currently looks like:
```
onEmailSubmit={async (newEmail) => {
  setEmail(newEmail);
  try {
    await joinWaitlist(newEmail);
    setIsSubscribed(true);
  } catch (error) {
    console.error('Waitlist join failed:', error);
  }
}}
```
Replace the two lines inside the `try` (`await joinWaitlist(newEmail);` and `setIsSubscribed(true);`) with the same three-line pattern (using `newEmail` instead of `email`):
```
await joinWaitlistLocal(newEmail);
setIsSubscribed(true);
setWaitlistCount((c) => (c ?? 115) + 1);
```
The `console.error('Waitlist join failed:', error);` in the catch stays BYTE-IDENTICAL. Outside the try block (`setEmail(newEmail);` above it, and any trailing brackets) is unchanged.

After both edits, the file must contain ZERO references to the bare `joinWaitlist(` (without the `Local` suffix). Both call sites now use `await joinWaitlistLocal(...)`. The local +1 increment fires in both places per D-01.</action>
  <verify>
    <automated>set -o pipefail; COUNT=$(grep -c 'await joinWaitlistLocal(' src/App.tsx) && [ "$COUNT" = "2" ] && ! grep -E 'await joinWaitlist\(' src/App.tsx && grep -q "Subscription failed:" src/App.tsx && grep -q "Waitlist join failed:" src/App.tsx && SETC=$(grep -c "setWaitlistCount((c) => (c ?? 115) + 1)" src/App.tsx) && [ "$SETC" = "2" ] && echo "OK: Task 2 call-site rewire complete — both joinWaitlist sites converted to await joinWaitlistLocal, no bare joinWaitlist references remain, both console.error log lines preserved verbatim, local +1 increment present in both try blocks"</automated>
  </verify>
  <done>
    `src/App.tsx`: exactly 2 occurrences of `await joinWaitlistLocal(` (one in handleSubscribe with `email`, one in FirstLetter.onEmailSubmit with `newEmail`). ZERO occurrences of bare `joinWaitlist(` (without the `Local` suffix). The `console.error('Subscription failed:', error)` and `console.error('Waitlist join failed:', error)` log lines are byte-identical to the pre-edit file. Exactly 2 occurrences of `setWaitlistCount((c) => (c ?? 115) + 1)` (one in each try block). The try/catch/finally skeletons are otherwise unchanged so Phase 2 can swap `joinWaitlistLocal`'s stub body for a real fetch without restructuring.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run npm run build and verify zero errors + zero Firebase residue</name>
  <files></files>
  <read_first>
    - src/App.tsx (the file just edited — for context if the build fails)
    - .planning/phases/01-foundation/01-RESEARCH.md (section: "Code Examples → Example 3: Verification commands for 'no Firebase residue'" — the five verification commands)
    - .planning/phases/01-foundation/01-RESEARCH.md (section: "Common Pitfalls → Pitfall 5: Firebase residue check passes because grep misses CONVENTIONS.md" — IMPORTANT: scope grep to `src/` ONLY, NOT planning docs)
  </read_first>
  <action>Run the production build and the Firebase-residue verification commands, in order, with `set -o pipefail` enabled so any failure terminates the chain.
(1) `npm run build` — this must complete with zero errors and produce a `dist/` folder. If it fails, read the error output, fix the regression in App.tsx, and re-run. Do NOT swallow errors.
(2) `grep -rn "firebase\|Firebase" src/` — must return ZERO matches. The scope is `src/` ONLY — do NOT grep `.planning/`, `node_modules/`, `.git/`, or root `CLAUDE.md`; those contain Firebase references in planning docs that are refreshed at phase transition, not during execution (Pitfall 5 is explicit).
(3) `grep -E '"firebase' package.json` — must return ZERO matches (Firebase was never in package.json, but confirm).
(4) `find src/ -name "firebase*" -type f` — must return ZERO matches (no `firebase.ts`, `firebaseConfig.ts`, etc.).
(5) Confirm the build actually produced `dist/assets/*.js` and that no resulting bundle contains `firebase` symbols.
If any of these fail, fix the offending file/line and re-run from (1). Capture the build output (the final "built in Xs" line) and the result of each verification command for the summary doc.</action>
  <verify>
    <automated>set -o pipefail && npm run build 2>&1 | tee /tmp/build.log | tail -20 && grep -E "(built in|✓ built)" /tmp/build.log && ! grep -rn "firebase\|Firebase" src/ 2>/dev/null && ! grep -E '"firebase' package.json && [ -z "$(find src/ -name 'firebase*' -type f 2>/dev/null)" ] && ls dist/assets/*.js > /dev/null && ! grep -l "firebase" dist/assets/*.js && echo "OK: build green, src/ has zero Firebase references, dist/ exists with bundle, bundle is Firebase-clean"</automated>
  </verify>
  <done>
    `npm run build` exits 0 and prints a "built in Xs" success line (captured in /tmp/build.log). `grep -rn "firebase\|Firebase" src/` returns nothing. `package.json` contains no firebase entry. No file matching `firebase*` exists under `src/`. `dist/assets/*.js` files exist (the glob resolves) and none of them contain the `firebase` substring. With `set -o pipefail` enabled, any single failure in the chain aborts the verify rather than passing silently. (Planning docs in `.planning/`, root `CLAUDE.md`, and `.planning/codebase/CONVENTIONS.md` ARE allowed to still mention Firebase — they get refreshed at the next `/gsd-transition`.)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual verification — counter animates from 000 to the real Supabase number AND fallback path is confirmed</name>
  <files></files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (the full set of decisions D-09 through D-13 — these describe the expected UX, AND D-11 + D-12 lock the fallback behavior as a phase requirement, not an optional)
    - .planning/phases/01-foundation/01-RESEARCH.md (section: "Common Pitfalls → Pitfall 1: Wiring App.tsx before the migration is applied" — describes how to distinguish the success path from the fallback path in DevTools)
    - .planning/ROADMAP.md (Phase 1 Success Criteria 2 — describes the expected dev-mode behavior)
  </read_first>
  <what-built>
    Phase 1 wiring is now complete in source code:
    - `src/App.tsx` is rewired to read from Supabase via `getSignupCount()` and to mount `<Counter target={...} />` in the pulse row.
    - Both `joinWaitlist(...)` call sites are now `await joinWaitlistLocal(...)` — the `await` shape is preserved so Phase 2 can drop in the real Edge Function call without restructuring the try/catch/finally skeleton.
    - `npm run build` is green with no Firebase residue.
    - The shared Supabase project has the table and view live (confirmed in plan 03's HALT).

    What you need to confirm visually is that the round trip works end-to-end on a real page load AND that the fallback path works when Supabase is unreachable. The fallback is not optional — D-11 and D-12 lock it as a phase requirement.
  </what-built>
  <how-to-verify>
    1. Make sure `.env.local` exists at the repo root with both `VITE_SUPABASE_URL=...` and `VITE_SUPABASE_ANON_KEY=...` filled in (same values as the main SEALED-org app's `.env.local`). If you haven't created `.env.local` yet: copy `.env.example` to `.env.local` and paste in the values from the SEALED-org project.
    2. Run `npm run dev`. Vite starts on http://localhost:3000.
    3. Open http://localhost:3000 in a browser. The hero should render with the SEALED logo, headline, email form, and the pulse row underneath.
    4. **Watch the counter (success path)**: when the page first paints, the digits should start at `000`. Over ~1.2 seconds they should animate (rolling up, odometer-feel) to the real number returned from Supabase. On a fresh empty `app_private.waitlist_signups`, that number is `115`. If your colleagues have already added test rows, it'll be `115 + N`.
    5. **Confirm the pulse dot** (the green dot with the pinging ring next to the word "Live") still animates exactly as it did before — same size, same color, same ping ring.
    6. **Open DevTools → Network tab → reload the page**. Find the request to `*supabase.co/rest/v1/signup_counter?select=total`. It should be `200 OK` with a response body like `{"total":115}`. If it's a 404 or 400, the migration isn't actually applied — go back to plan 03's HALT and re-verify.
    7. **Open DevTools → Console**. There should be NO `Counter fetch failed:` error. (If there is, the fetch is failing; investigate.)
    8. **Test the local +1**: type any email into the form and click "Get Early Access". The button briefly shows the loader, then the confirmation chip appears ("You're #N on the list"). The counter on the page should immediately increment by +1 (local-only — the database row insert is Phase 2's job). Reload the page — the counter goes back down to the real DB value (this is correct — no row was actually inserted).
    9. **Test the fallback path (REQUIRED, not optional — D-11 + D-12 lock this as a phase requirement):** stop the dev server. In `.env.local`, change `VITE_SUPABASE_URL` from the real URL to `https://invalid.supabase.co` (keep the anon key — only the URL needs to break). Restart `npm run dev`. Reload the page in the browser. Confirm: (a) the counter animates from `000` to `115` (the fallback floor), (b) DevTools console shows the exact string `Counter fetch failed:` followed by the error object, (c) the page does NOT crash and the rest of the hero renders normally. THEN restore the real `VITE_SUPABASE_URL` in `.env.local`, restart `npm run dev` one more time, and confirm the success path returns (counter goes back to 115 from the real view, no console error).
  </how-to-verify>
  <acceptance_criteria>
    **Success path (all required):**
    - On a fresh page load, the counter renders `000` and animates up to the real number from `public.signup_counter` over ~1.2 seconds.
    - The number shown matches `115 + count(rows in app_private.waitlist_signups)`.
    - DevTools Network tab shows a 200 OK response from `*supabase.co/rest/v1/signup_counter?select=total`.
    - DevTools Console shows NO `Counter fetch failed:` error on the success path.
    - The pulse dot + ping ring + "Live" label render identically to the pre-Phase-1 layout (no visual regression).
    - Submitting an email increments the counter by +1 locally in the same tab (Phase 2 will insert the real row).
    - Reloading after a local-only submit reverts the counter to the real DB value (confirms no false row was inserted).

    **Fallback path (REQUIRED, not optional — D-11 + D-12):**
    - With `VITE_SUPABASE_URL` set to an invalid host, the counter animates from `000` to `115` (the seeded floor fallback).
    - DevTools Console logs `Counter fetch failed:` followed by the error object.
    - The hero continues to render normally (no crash, no blank page).
    - Restoring the real URL and restarting dev returns the success path.
  </acceptance_criteria>
  <resume-signal>Type "phase 1 verified" once BOTH the success path AND the fallback path are confirmed working. If anything is off (counter stays at 000, snaps to 115 with a console error on first load, pulse disappeared, fallback doesn't fire on invalid URL, build broken), describe the symptom and we'll diagnose before closing the phase.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ Supabase REST (PostgREST) | Anon client fetches `public.signup_counter` — single integer, no PII. |
| User input ↔ Local state | Email submit triggers an in-memory `setWaitlistCount(c => (c ?? 115) + 1)` only — no network call in Phase 1, no DB insert. |
| Network failure ↔ UI | A failed fetch falls back to 115 and logs to console — no error UI exposed to users (D-12). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | Tampering | Local +1 increment after email submit | accept | The local +1 in `handleSubscribe` and the `FirstLetter` callback is intentional, purely cosmetic, and resets on page reload. A user inflating their own visible counter has no security impact (no DB write, no broadcast). Phase 2's Edge Function performs the real insert behind Turnstile + IP rate limit — server is the source of truth from page-reload onward. |
| T-01-13 | Information disclosure | DevTools Network tab exposing Supabase REST URL | accept | The anon REST endpoint and the anon key are already in the bundle by design (T-01-01 in plan 01). Showing the request in Network is no incremental disclosure. |
| T-01-14 | Denial of service | Slow Supabase response blocking page render | mitigate | The fetch runs inside `useEffect` (post-render), not inside render. The Counter component renders immediately with `target=115` (because `waitlistCount ?? 115`) so the hero is never blank. The 3s AbortSignal timeout in `getSignupCount` (plan 01) ensures even pathological network conditions fall back to 115 within 3 seconds. |
| T-01-15 | Repudiation | No audit trail of counter reads | accept | Counter reads are unauthenticated and PII-free. PostgREST logs the request server-side; no client-side audit is required. |
| T-01-16 | Information disclosure | console.error revealing internal error details to a casual user opening DevTools | accept | The console.error wording `Counter fetch failed:` plus the error object exposes Supabase error codes and URLs — but anyone opening DevTools can already see the underlying Network tab response. The console.error is intended for Nour's debugging (D-12 is explicit). No PII passes through this code path. |
| T-01-18 | Tampering (silent contract drift at call sites) | Future refactor removing the `await` from `await joinWaitlistLocal(...)` because the stub is sync | mitigate | The `await` is intentional and preserved by Task 2's grep gate (`grep -c 'await joinWaitlistLocal(' src/App.tsx` must equal 2). Phase 2 reintroduces a real network call inside `joinWaitlistLocal` — at that point the `await` becomes load-bearing, but if a Phase 1 reviewer "cleans up" the `await` (because the stub is sync) Phase 2 will silently regress to a non-awaiting call site. The grep gate is the guardrail. |
</threat_model>

<verification>
After all four tasks complete (Task 1 structural, Task 2 call-site, Task 3 build + grep gates, Task 4 Nour's visual verification including the fallback path):

1. `! grep -rn 'firebase\|Firebase' src/` — succeeds.
2. `npm run build` exits 0.
3. `grep -c '<Counter target=' src/App.tsx` prints `1`.
4. `grep -c "useState<number | null>(null)" src/App.tsx` prints `1`.
5. `grep -c "Counter fetch failed:" src/App.tsx` prints `1`.
6. `grep -c "setWaitlistCount((c) => (c ?? 115) + 1)" src/App.tsx` prints `2` (one in handleSubscribe, one in FirstLetter onEmailSubmit).
7. `grep -c 'await joinWaitlistLocal(' src/App.tsx` prints `2`.
8. `! grep -E 'await joinWaitlist\(' src/App.tsx` — succeeds (no bare joinWaitlist without Local suffix).
9. `grep -c "animate-ping" src/App.tsx` prints `1` (pulse intact — COUNTER-04).
10. Nour has typed "phase 1 verified" after confirming BOTH the success path (counter animates to the real number) AND the fallback path (counter animates to 115 with console error when URL is invalid).
</verification>

<success_criteria>
This plan — and Phase 1 as a whole — is complete when all four ROADMAP success criteria are met:

1. `npm run build` completes with zero errors and no Firebase imports remain anywhere in `src/`.
2. Visiting the landing page in dev mode renders the hero without runtime errors, and the counter shows a real number (115 on a fresh DB) sourced from the `public.signup_counter` view via the anon client.
3. `app_private.waitlist_signups` table and `public.signup_counter` view exist in the shared Supabase project, with the view granted to the `anon` role. *(Plan 03 + dashboard verification.)*
4. The "pulse" indicator next to the counter still animates and the page reads identically to its pre-Firebase appearance.

Additionally:
- Both `joinWaitlist(...)` call sites are now `await joinWaitlistLocal(...)` — Phase 2 can swap the stub body without restructuring the try/catch/finally skeleton.
- The local +1 increment fires on email submit in both call sites (D-01).
- The fallback to 115 + console error fires on fetch failure, confirmed at runtime by deliberately breaking `VITE_SUPABASE_URL` (D-11, D-12).
- The pulse JSX (lines around 104-116 of pre-edit App.tsx) is byte-identical except for the inner counter render (D-13 / COUNTER-04).
</success_criteria>

<output>
After completion (post-visual-verification approval), create `.planning/phases/01-foundation/01-04-SUMMARY.md` with: the lines edited in App.tsx (before/after for each of the four structural edits and the two call-site edits), the build output (final "built in Xs" line), the result of each Firebase-residue grep gate, the real counter value observed during Nour's success-path verification (e.g., "counter animated 000 → 115 over ~1.2s, no console error"), the fallback-path confirmation (e.g., "with invalid URL, counter animated 000 → 115 and console showed `Counter fetch failed: ...`"), confirmation that Phase 1's four ROADMAP success criteria are met, the timestamp of Nour's "phase 1 verified" signal, and a one-line pointer noting that `/gsd-transition` should be run next to refresh the planning docs (CONVENTIONS.md, ARCHITECTURE.md, INTEGRATIONS.md still mention Firebase — that's expected and is a phase-transition concern, not a Phase 1 task).
</output>
</content>
</invoke>