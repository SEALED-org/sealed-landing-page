---
phase: 01-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0031_waitlist_signups.sql
  - .planning/phases/01-foundation/HANDOFF-PROMPT.md
autonomous: false
requirements: [DB-01, DB-02, DB-05]
must_haves:
  truths:
    - "D-03: The SQL migration for `app_private.waitlist_signups` and `public.signup_counter` lives in the main SEALED-org repo at `/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/` — the landing page repo never owns schema files; the main app is the single source of truth for the Supabase schema"
    - "D-04: Phase 1's executor writes the SQL migration file and places it in the main repo's migrations folder, then generates a handoff prompt for Nour to paste into the SEALED-org agent — the handoff prompt instructs that agent to run /gsd-quick so the change is documented in the main repo's planning system AND notes the migration exists because of the landing page (audit trail in both repos)"
    - "D-05: Phase 1 does NOT proceed past the wiring step until Nour confirms the SEALED-org agent has applied the migration and the table/view are visible in the Supabase dashboard — this is a HARD handoff blocker enforced by Task 3 (checkpoint:human-verify, autonomous: false)"
    - "The SQL migration file `0031_waitlist_signups.sql` exists in the SIBLING repo at `/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/` (NOT in this repo)"
    - "The migration creates `app_private.waitlist_signups` (PK user_id → auth.users, status check active|unsubscribed, has_letter boolean, created_at) with RLS enabled, anon/authenticated/public revoked, service_role granted full DML"
    - "The migration creates `public.signup_counter` view `with (security_invoker = false)` whose body is `select (115 + count(*))::bigint as total from app_private.waitlist_signups` — no WHERE clause, no status filter (COUNTER-02)"
    - "The view is granted SELECT to anon AND authenticated (COUNTER-03)"
    - "The handoff prompt file exists in this repo and contains the exact instructions for the SEALED-org agent to apply the migration via /gsd-quick"
    - "The handoff prompt is printed to stdout at the end of execution so Nour can copy it directly from the terminal"
  artifacts:
    - path: "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0031_waitlist_signups.sql"
      provides: "SQL DDL for waitlist_signups table + signup_counter view, ready for SEALED-org agent to apply"
      contains: "create table app_private.waitlist_signups"
      min_lines: 30
    - path: ".planning/phases/01-foundation/HANDOFF-PROMPT.md"
      provides: "Copy-pasteable prompt for the SEALED-org sibling agent"
      contains: "0031_waitlist_signups.sql"
      min_lines: 20
  key_links:
    - from: "0031_waitlist_signups.sql → public.signup_counter view"
      to: "app_private.waitlist_signups table"
      via: "select (115 + count(*))::bigint as total from app_private.waitlist_signups"
      pattern: "115 \\+ count\\(\\*\\)"
    - from: "public.signup_counter"
      to: "anon role"
      via: "grant select on public.signup_counter to anon, authenticated"
      pattern: "grant select on public.signup_counter to anon"
    - from: "app_private.waitlist_signups"
      to: "anon role"
      via: "revoke all from anon (intentionally blocking direct access)"
      pattern: "revoke all on app_private.waitlist_signups from anon"
    - from: "HANDOFF-PROMPT.md"
      to: "the SEALED-org sibling agent"
      via: "Nour pastes the prompt verbatim into the sibling repo's Claude Code session"
      pattern: "/gsd-quick"
---

<objective>
**Wave semantics note (READ FIRST):** This plan is in Wave 1 and runs in parallel with plans 01 and 02. Tasks 1 and 2 are autonomous. Task 3 is a BLOCKING human-verify checkpoint — Wave 2 (plan 04) does NOT begin until Nour confirms the migration is applied. The plan's `autonomous: false` flag in the frontmatter signals this to the orchestrator: plan 03 is not "done" when its autonomous tasks finish — it is "done" only when Task 3's resume-signal is received.

Write the SQL migration that creates `app_private.waitlist_signups` and `public.signup_counter` into the SIBLING SEALED-org repo's migrations folder, generate the cross-repo handoff prompt that Nour will paste into the SEALED-org agent, print the prompt to stdout, then HALT execution until Nour confirms the migration has been applied and the table/view are live in the shared Supabase project.

Purpose: Per D-03, this repo never owns Supabase DDL — the main SEALED-org app is the single source of truth for schema. Phase 1 writes the SQL file into the sibling repo at the next sequential 4-digit number (0031, verified — current highest is 0030) following that repo's mature conventions (`app_private` schema, `security_invoker=false` view, revoke-from-anon-then-grant-to-service_role ordering). Per D-04, applying the migration is the sibling agent's job; this executor only writes the file and the handoff prompt. Per D-05, plan 04 (App.tsx surgery + verification) MUST NOT run until the migration is live — otherwise the Counter's success path and its failure-fallback path both render 115 in an empty DB, and there's no way to tell whether the wiring is correct or the database is missing. The HALT task at the end of this plan is the hard gate that prevents that false-pass mode.

Output: A new file in the sibling repo at `/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0031_waitlist_signups.sql`. A new file in this repo at `.planning/phases/01-foundation/HANDOFF-PROMPT.md`. The prompt printed to stdout. A blocking checkpoint that Nour resolves by confirming "migration applied" before plan 04 starts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Sibling-repo migration analogs (READ these via the executor's normal file tools — they are NOT in this repo). -->
<!-- The new migration composes three patterns from these three files. -->

From /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0001_letters_table.sql:
- Pattern: schema usage grant, create table with references auth.users(id) on delete cascade, indexes per filterable column, enable RLS, comment on table.

From /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0014_observability_views.sql:
- Pattern: create or replace view ... with (security_invoker = false) as select count(*)::bigint as <name> from app_private.<table>; comment on view.

From /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0019_harden_private_letter_access.sql:
- Pattern: revoke all on app_private.<table> from anon, authenticated, public; grant select, insert, update, delete on app_private.<table> to service_role.

Verified file naming: 4-digit zero-padded sequential. Highest existing is `0030_seed_test_letter_optional_schedule.sql` (May 24). Next is `0031`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write SQL migration 0031_waitlist_signups.sql into the SIBLING SEALED-org repo</name>
  <files>/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0031_waitlist_signups.sql</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (section: "Pattern 4: SQL Migration (sibling repo)" — the full migration text is drafted there verbatim and verified against sibling-repo conventions)
    - .planning/phases/01-foundation/01-PATTERNS.md (section: "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0031_waitlist_signups.sql" — explains which lines come from which sibling-repo migration and what changes vs each template)
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions D-03, D-04, D-05 — LOCKED ownership rules)
    - /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0001_letters_table.sql (table + schema + RLS + grant + comment template)
    - /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0014_observability_views.sql (security_invoker=false view + count(*)::bigint + view comment template)
    - /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0019_harden_private_letter_access.sql (revoke-from-anon-then-grant-to-service_role ordering)
    - /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/ (run `ls` to CONFIRM the highest existing migration is still `0030_*.sql` before writing `0031_*.sql` — if a 0031 already exists, STOP and report to Nour)
  </read_first>
  <action>Before writing: run `ls /Users/nourismaiel/SEALED\ ORG\ \(FINAL\)/SEALED-org/supabase/migrations/ | tail -3` to confirm the next sequential number is `0031`. If anything `0031_*` already exists, STOP and ask Nour what to do (do NOT overwrite). Otherwise, write a new file at the exact path `/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0031_waitlist_signups.sql` with the migration content drafted in RESEARCH.md Pattern 4 (lines 359-412). The content must include, in order: (1) a multi-line header comment block (4-5 lines) identifying the file, naming the landing-page sibling repo as the reason it exists, and citing the relevant CONTEXT.md decision IDs (D-01 through D-13) and REQUIREMENTS.md IDs (DB-01, DB-02, COUNTER-01, COUNTER-02, COUNTER-03) — match the multi-line rationale-header style of `0014` and `0019`; (2) `create table app_private.waitlist_signups` with `user_id uuid primary key references auth.users(id) on delete cascade`, `status text not null default 'active' check (status in ('active', 'unsubscribed'))`, `has_letter boolean not null default false`, `created_at timestamptz not null default now()`; (3) `create index waitlist_signups_created_at_idx on app_private.waitlist_signups (created_at)` and `create index waitlist_signups_status_idx on app_private.waitlist_signups (status)`; (4) `alter table app_private.waitlist_signups enable row level security`; (5) `revoke all on app_private.waitlist_signups from anon, authenticated, public` followed by `grant select, insert, update, delete on app_private.waitlist_signups to service_role` (do NOT re-issue the schema-usage revoke — that's already in 0019); (6) `comment on table app_private.waitlist_signups is '...'` describing the table's role; (7) `create view public.signup_counter with (security_invoker = false) as select (115 + count(*))::bigint as total from app_private.waitlist_signups` — exact body, no WHERE clause, no status filter (COUNTER-02 is explicit), the `::bigint` cast is mandatory, the `115 +` literal is the seed (COUNTER-01); (8) `grant select on public.signup_counter to anon, authenticated` (COUNTER-03); (9) `comment on view public.signup_counter is '...'` explaining the security_invoker=false isolation and the 115 seed. Match indentation/casing of the sibling repo's `0014` and `0019` exactly (lowercase keywords, 2-space indent inside parens, blank lines between sections). Do NOT add Supabase migration metadata headers (e.g., no `BEGIN;`/`COMMIT;` wrapper — the sibling repo's migrations don't use them; they rely on Supabase's per-file transaction). Do NOT execute the migration. Do NOT touch any other file in the sibling repo.</action>
  <verify>
    <automated>set -o pipefail; MIG="/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0031_waitlist_signups.sql" && test -f "$MIG" && grep -q "create table app_private.waitlist_signups" "$MIG" && grep -q "user_id uuid primary key references auth.users(id) on delete cascade" "$MIG" && grep -q "status text not null default 'active' check (status in ('active', 'unsubscribed'))" "$MIG" && grep -q "has_letter boolean not null default false" "$MIG" && grep -q "created_at timestamptz not null default now()" "$MIG" && grep -q "create index waitlist_signups_created_at_idx" "$MIG" && grep -q "create index waitlist_signups_status_idx" "$MIG" && grep -q "alter table app_private.waitlist_signups enable row level security" "$MIG" && grep -q "revoke all on app_private.waitlist_signups from anon, authenticated, public" "$MIG" && grep -q "grant select, insert, update, delete on app_private.waitlist_signups to service_role" "$MIG" && grep -q "comment on table app_private.waitlist_signups" "$MIG" && grep -q "create view public.signup_counter" "$MIG" && grep -q "with (security_invoker = false)" "$MIG" && grep -q "select (115 + count(\*))::bigint as total" "$MIG" && grep -q "from app_private.waitlist_signups" "$MIG" && if grep -Ev '^[[:space:]]*--' "$MIG" | grep -iEw 'where' > /dev/null; then echo "ERROR: migration contains a WHERE clause on a non-comment line (COUNTER-02 forbids any filter on the view)"; exit 1; fi && grep -q "grant select on public.signup_counter to anon, authenticated" "$MIG" && grep -q "comment on view public.signup_counter" "$MIG" && echo "OK: migration 0031 present in sibling repo with all required clauses and no stray WHERE filter on the view"</automated>
  </verify>
  <done>
    `/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0031_waitlist_signups.sql` exists. Contains: header comment block citing the landing page repo; `create table app_private.waitlist_signups` with all four columns and the user_id FK to auth.users on delete cascade; both indexes; RLS enabled; revoke-from-(anon,authenticated,public) + grant-to-service_role; table comment; `create view public.signup_counter with (security_invoker = false) as select (115 + count(*))::bigint as total from app_private.waitlist_signups` (no WHERE); grant to anon and authenticated; view comment. Naming follows the sibling repo's 4-digit-prefix snake_case convention. No other file in the sibling repo is modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate HANDOFF-PROMPT.md and print it to stdout</name>
  <files>.planning/phases/01-foundation/HANDOFF-PROMPT.md</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (section: "Pattern 5: Cross-repo Handoff Prompt" — the full prompt text is drafted there)
    - .planning/phases/01-foundation/01-PATTERNS.md (section: "HANDOFF-PROMPT.md (new planning artifact)" — explains there is no in-repo analog and recommends a minimal header)
    - .planning/phases/01-foundation/01-CONTEXT.md (section: "specifics" — the three requirements: migration file path, /gsd-quick reference, reason it exists)
  </read_first>
  <action>Create the file `.planning/phases/01-foundation/HANDOFF-PROMPT.md`. Structure: (1) a minimal markdown header in the planning-doc family style — H1 `# Phase 1: Foundation - SEALED-org Handoff Prompt`, three bold metadata lines (`**Audience:**`, `**Triggered after:**`, `**Blocks:**`), then a horizontal rule `---`; (2) a `<prompt>` wrapper containing the verbatim prompt text drafted in RESEARCH.md Pattern 5 lines 422-461 — the prompt MUST include the exact migration file path `supabase/migrations/0031_waitlist_signups.sql`, the explicit instruction to run `/gsd-quick`, the verification steps (table exists, view returns 115, anon read of view succeeds, anon read of table is denied), the request to record in the SEALED-org planning system that the migration exists because of the landing page, and the explicit statement that the landing page repo is BLOCKED on confirmation. Tone: matter-of-fact, second person addressed to the next agent. No marketing language. After writing the file, also `cat` (or equivalent — use `Bash` to print the file contents) the file to stdout so Nour can copy the prompt directly from the terminal output without opening the file. (Stdout printing is the convenience; the file is the audit trail.)</action>
  <verify>
    <automated>test -f .planning/phases/01-foundation/HANDOFF-PROMPT.md && grep -q "0031_waitlist_signups.sql" .planning/phases/01-foundation/HANDOFF-PROMPT.md && grep -q "/gsd-quick" .planning/phases/01-foundation/HANDOFF-PROMPT.md && grep -q "app_private.waitlist_signups" .planning/phases/01-foundation/HANDOFF-PROMPT.md && grep -q "public.signup_counter" .planning/phases/01-foundation/HANDOFF-PROMPT.md && grep -qi "BLOCKED" .planning/phases/01-foundation/HANDOFF-PROMPT.md && grep -qi "landing page" .planning/phases/01-foundation/HANDOFF-PROMPT.md && echo "OK: handoff prompt present with migration path, /gsd-quick reference, table+view names, blocked notice, and landing-page reason"</automated>
  </verify>
  <done>
    `.planning/phases/01-foundation/HANDOFF-PROMPT.md` exists. Contains the minimal markdown header (H1 + three `**Bold:**` metadata lines + horizontal rule) and a `<prompt>` block with the verbatim text. Prompt references: exact migration file path (`supabase/migrations/0031_waitlist_signups.sql`), `/gsd-quick`, the dashboard verification steps including "Run as: anon" for both queries, the request to record the migration's origin in the SEALED-org planning system, and the explicit "landing page repo is BLOCKED" notice. File contents printed to stdout during execution.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HALT — wait for Nour to confirm the SEALED-org agent has applied the migration</name>
  <files></files>
  <read_first>
    - .planning/phases/01-foundation/HANDOFF-PROMPT.md (the prompt Nour just pasted into the sibling agent)
    - .planning/phases/01-foundation/01-CONTEXT.md (decision D-05 — this is a HARD handoff blocker, non-negotiable)
    - .planning/phases/01-foundation/01-RESEARCH.md (section: "Common Pitfalls → Pitfall 1: Wiring App.tsx before the migration is applied" — explains why this halt is critical, because the success path and the failure path both render 115 in an empty DB)
  </read_first>
  <what-built>
    Wave 1 of Phase 1 is now complete in this repo:
    - `@supabase/supabase-js@2.103.2` installed with a committed lockfile.
    - `src/lib/supabase.ts` with the `supabase` client, the `getSignupCount` helper, AND the `joinWaitlistLocal` Phase 1 stub.
    - `src/components/Counter.tsx` (odometer, not yet mounted).
    - `src/vite-env.d.ts` and `.env.example`.
    - The SQL migration `0031_waitlist_signups.sql` has been written into the SIBLING repo at `/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/`.
    - `HANDOFF-PROMPT.md` has been generated and printed to stdout.

    Nothing in this repo is callable end-to-end yet. The Counter cannot animate to a real number because the view it queries does not exist in Supabase yet. Plan 04 (App.tsx surgery + verification) MUST NOT run until the migration is applied. This is the wave boundary: Wave 1 plans 01, 02, and the first two tasks of 03 may complete in parallel, but Wave 2 (plan 04) does not start until THIS task completes via your "migration applied" signal below.
  </what-built>
  <how-to-verify>
    1. Copy the printed HANDOFF-PROMPT.md text from the terminal (or open `.planning/phases/01-foundation/HANDOFF-PROMPT.md`).
    2. Open the SEALED-org repo's Claude Code session (the sibling repo at `/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org`).
    3. Paste the prompt verbatim into that session. The sibling agent will (a) review the file, (b) apply the migration via `supabase db push` or its equivalent, (c) verify the table and view exist in the Supabase dashboard, and (d) record the migration's origin in its own planning system.
    4. Once the sibling agent confirms back to you with "the table and view are live," open the shared Supabase project's dashboard yourself and double-check:
       - Table editor: `app_private.waitlist_signups` exists and shows 0 rows.
       - SQL editor (Run as: anon): `select * from public.signup_counter;` returns one row with `total = 115`.
       - SQL editor (Run as: anon): `select * from app_private.waitlist_signups;` returns a permission-denied error (this is GOOD — it confirms the anon role can only see the view, not the underlying table).
    5. Once both queries behave as expected, return to this repo's session and explicitly approve to advance to Wave 2 (plan 04 / App.tsx surgery).
  </how-to-verify>
  <acceptance_criteria>
    - You have pasted HANDOFF-PROMPT.md into the SEALED-org repo's agent and that agent has applied migration 0031.
    - You confirm via the Supabase dashboard that `app_private.waitlist_signups` table exists and is empty.
    - You confirm via the Supabase dashboard SQL editor that `select * from public.signup_counter;` run AS ANON returns `total = 115`.
    - You confirm via the Supabase dashboard SQL editor that `select * from app_private.waitlist_signups;` run AS ANON returns a permission-denied error.
    - You explicitly type "migration applied" (or paste a screenshot / equivalent confirmation) into this session before the executor advances to plan 04.
  </acceptance_criteria>
  <resume-signal>Type "migration applied" once the table and view are confirmed live in the Supabase dashboard. Do NOT advance otherwise — running plan 04 against a missing migration makes the Counter show 115 (the fallback), which looks identical to the success path and would silently hide a broken wire.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| This repo ↔ Sibling SEALED-org repo (filesystem) | Phase 1 executor writes a file into the sibling repo's `supabase/migrations/` folder. The sibling repo's agent is responsible for applying it. |
| Sibling agent ↔ Supabase production project | The sibling agent runs `supabase db push` against the shared Supabase project on Nour's authority. This executor does NOT have or use Supabase credentials. |
| Anon role ↔ `app_private.waitlist_signups` (after migration applied) | Anon is explicitly REVOKED from the table. The only anon-accessible surface is `public.signup_counter` (a view that exposes one integer). |
| Anon role ↔ `public.signup_counter` (after migration applied) | View runs as definer (`security_invoker = false`), exposes only the aggregate `total`, no row data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Elevation of privilege | Anon client reaching `app_private.waitlist_signups` directly via PostgREST | mitigate | Migration revokes ALL on `app_private.waitlist_signups` from anon, authenticated, and public; grants only to service_role. Verified at acceptance time via `select * from app_private.waitlist_signups` as anon — must return permission denied. The view bypasses this because `security_invoker = false` runs it as the definer (postgres), so it never checks the anon role's grants on the underlying table — but it can only expose the aggregate output, not row data. |
| T-01-07 | Information disclosure | View leaking row-level data | mitigate | View body is `select (115 + count(*))::bigint as total` — pure scalar aggregate. No row columns are SELECTed. No JOINs. There is no way to infer individual rows from `count(*)` without a WHERE clause to test against (and the view has no WHERE clause, intentionally — COUNTER-02). |
| T-01-08 | Tampering (silent wrong number) | View returning incorrect count due to filter | mitigate | View body has NO `where` clause (verified in Task 1's grep gate using word-boundary matching on non-comment lines). The grep gate explicitly fails the task if any non-comment line contains the standalone word `where`. If a future migration adds a filter (e.g., `where status = 'active'`), the count drops and social proof is silently wrong — the grep gate would catch this regression. The comment in the view definition explains why the filter is intentionally absent. |
| T-01-09 | Repudiation / audit trail | Cross-repo handoff of schema change | mitigate | The handoff prompt explicitly asks the sibling agent to run `/gsd-quick` so the migration is recorded in BOTH planning systems (this repo's `.planning/phases/01-foundation/SUMMARY.md` and the sibling repo's planning artifact). The prompt also asks the sibling agent to record WHY the migration exists ("the landing page captures waitlist signups before app launch"), creating a paper trail for future audits. |
| T-01-10 | Credentials leakage in handoff | Prompt accidentally containing service role key or env vars | mitigate | The handoff prompt contains ONLY: file path, instructions to read/apply/verify the SQL, and a request to confirm. No credentials, no env vars, no Supabase project ID, no API keys. Task 2's grep gate could be extended to assert `! grep -q "service_role\|SUPABASE_KEY\|sk_" HANDOFF-PROMPT.md` if extra paranoia is desired — currently the structure of the prompt makes accidental key inclusion impossible. |
| T-01-11 | Premature execution of Wave 2 | Plan 04 running before migration is applied | mitigate | The HALT task (Task 3) is a blocking checkpoint with `autonomous: false` on the plan as a whole. Plan 04's frontmatter sets `depends_on: [01, 02, 03]` so the orchestrator cannot run Wave 2 before THIS plan reports complete — and this plan is not complete until Nour types "migration applied". The wave-semantics note at the top of this plan documents the boundary explicitly so no future reader misreads "wave: 1 with depends_on: []" as "everything in wave 1 can finish in parallel without coordination." Without this halt, the fallback-to-115 path silently hides a missing-migration error. |
</threat_model>

<verification>
After all three tasks complete (including the HALT confirmation):

1. `test -f "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0031_waitlist_signups.sql"` — file exists in sibling repo.
2. `test -f .planning/phases/01-foundation/HANDOFF-PROMPT.md` — file exists in this repo.
3. `ls "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/" | grep -c "^0031"` — prints `1`.
4. Migration body grep gates (from Task 1's automated verify) all pass.
5. Handoff prompt grep gates (from Task 2's automated verify) all pass.
6. Nour has explicitly typed "migration applied" (or equivalent) in the session — the HALT task gates this.
</verification>

<success_criteria>
This plan is complete when:
- `/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0031_waitlist_signups.sql` exists with all required DDL.
- `.planning/phases/01-foundation/HANDOFF-PROMPT.md` exists with the verbatim prompt.
- The handoff prompt has been printed to stdout.
- Nour has confirmed the migration is applied AND the dashboard verification queries behave as expected (anon reads view = 115, anon reads table = permission denied).
- No other file in either repo is modified.
- No Supabase credentials, no `supabase db push` invocation, no schema change applied by this executor.
</success_criteria>

<output>
After completion (post-HALT approval), create `.planning/phases/01-foundation/01-03-SUMMARY.md` with: the migration file path in the sibling repo, the exact view body shipped, the table-grant + view-grant matrix (anon: revoked on table / SELECT on view; service_role: full DML on table), the handoff prompt path, confirmation that Nour approved the gate (include the timestamp), and a one-line pointer to plan 04 as the next executable step.
</output>
</content>
</invoke>