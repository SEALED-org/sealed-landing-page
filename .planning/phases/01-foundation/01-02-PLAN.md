---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Counter.tsx
autonomous: true
requirements: [COUNTER-01, COUNTER-03, COUNTER-04]
must_haves:
  truths:
    - "D-10: On page load the digits render as `000` and animate (rolling odometer style) up to the real value supplied via `target`; animation uses the existing `motion/react` library (no new dependency)"
    - "D-09: Counter displays with leading zeros, growing in width by tier — 3 digits below 1000 (e.g. `115`, `127`, `999`), 4 digits at 1000–9999, 5 digits at 10000+; same width within a tier — no layout shift mid-tier"
    - "If `target` changes after mount (e.g. fetch resolves with a real number), the animation re-runs from the current displayed value to the new target — no snap, no restart from 0"
    - "Component uses only `motion/react` (Motion v12) — no `framer-motion`, no `motion-plus`, no `AnimateNumber`"
    - "No data fetching inside the component — it accepts `target: number` and animates; the fetch is the parent's responsibility (plan 04)"
  artifacts:
    - path: "src/components/Counter.tsx"
      provides: "Default-exported odometer counter component"
      exports: ["default"]
      contains: "useMotionValue"
      min_lines: 30
  key_links:
    - from: "src/components/Counter.tsx"
      to: "motion/react"
      via: "useMotionValue + animate + useTransform + motion.span"
      pattern: "from 'motion/react'"
    - from: "Counter useEffect"
      to: "animate(motionCount, target, { duration, ease })"
      via: "cleanup via controls.stop() in useEffect return"
      pattern: "controls\\.stop"
---

<objective>
Create the odometer Counter component as a standalone, self-contained presentational module that takes a numeric `target` prop and animates a tier-aware leading-zero string from `000` up to that target using `motion/react`'s free imperative API.

Purpose: The hero section's "Join {N} others on the list" line is the single most visible piece of social proof on the landing page. Per Nour's specifics in CONTEXT.md, the digits should roll up from zeros to the real count on every page load (D-10) — a small but deliberate piece of life that signals "this is real, people are actually signing up." Splitting this into its own component (a) keeps `src/App.tsx` surgery in plan 04 tiny and reviewable, (b) makes the animation testable in isolation, (c) means the only thing plan 04 needs to do for Counter wiring is mount `<Counter target={waitlistCount ?? 115} />`.

Output: One new file at `src/components/Counter.tsx`. Self-contained — no edits to App.tsx, no edits to any other component, no fetching, no Supabase imports. Plan 04 wires it in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@CLAUDE.md
@.planning/codebase/CONVENTIONS.md
@src/components/Typewriter.tsx
@src/components/FAQ.tsx
@src/components/ShareButtons.tsx

<interfaces>
<!-- The public surface of the new Counter component. -->
<!-- Plan 04 will import it as `import Counter from './components/Counter';` -->

From src/components/Counter.tsx (to be created):
```typescript
interface CounterProps {
  target: number;
}

export default function Counter(props: CounterProps): JSX.Element;
```

Motion v12 imports needed (all from `motion/react` — never `framer-motion`):
```typescript
import { motion, useMotionValue, useTransform, animate } from 'motion/react';
```

In-repo precedent for the file shape (Typewriter.tsx):
```typescript
interface TypewriterProps { phrases: string[]; className?: string; }
export default function Typewriter({ phrases, className }: TypewriterProps) { ... }
```
Counter mirrors this shape minus the `className` prop (not needed in Phase 1 — the surrounding pulse row already supplies font/opacity utilities).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/components/Counter.tsx (odometer animation)</name>
  <files>src/components/Counter.tsx</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (sections: "Pattern 2: Odometer Counter (Motion v12, free API)" with the verified Motion API citations, "Common Pitfalls → Pitfall 3: useEffect re-runs the animation on every render", "Anti-Patterns to Avoid" — especially "Don't use AnimateNumber" and "Don't import from framer-motion")
    - .planning/phases/01-foundation/01-PATTERNS.md (section: "`src/components/Counter.tsx`" — covers the Typewriter shape analog, the FAQ/FirstLetter motion/react import line, the ShareButtons props convention)
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions D-09, D-10, D-11, D-13 — these LOCK the UX shape)
    - src/components/Typewriter.tsx (the closest in-repo analog: animate-over-time, interface declared inline, default export, useEffect with cleanup)
    - src/components/FAQ.tsx (closest example of correct `motion/react` v12 import line — first 3 lines)
    - src/components/ShareButtons.tsx (props interface convention: `interface ShareButtonsProps { ... }`, destructured signature)
    - .planning/codebase/CONVENTIONS.md (Naming Patterns → React components; Code Style → Imports; Module Design → Exports)
  </read_first>
  <action>Create the file `src/components/Counter.tsx` with one default export. Imports (in order): `useEffect` from `'react'`; then `motion, useMotionValue, useTransform, animate` destructured from `'motion/react'` (single quotes, exact module path — `framer-motion` is forbidden and triggers an instant rejection per Anti-Pattern). Declare an inline `interface CounterProps { target: number; }` directly above the component (no `className`, no other props — Phase 1 doesn't need them; the surrounding pulse row in App.tsx supplies all styling). Define a private helper `function formatCounter(current: number, target: number): string` that returns `Math.floor(current).toString().padStart(width, '0')` where `width = target >= 10000 ? 5 : target >= 1000 ? 4 : 3` — the width is keyed off TARGET (not the current animating value) so that during the 000→target tween the width stays stable in its destination tier (D-09). Export `export default function Counter({ target }: CounterProps)`. Inside: declare `const motionCount = useMotionValue(0)` at the top of the body (do NOT recreate this inside useEffect — Pitfall 3); declare `const display = useTransform(motionCount, (latest) => formatCounter(latest, target))`; then a single `useEffect` with dependency array `[target, motionCount]` whose body calls `const controls = animate(motionCount, target, { duration: 1.2, ease: 'easeOut' })` and whose cleanup return calls `controls.stop()`. The return JSX is `<motion.span>{display}</motion.span>` — nothing else (no className, no wrappers, no pulse — the pulse stays in App.tsx unchanged per COUNTER-04 / D-13). Style: 2-space indent, single quotes for imports, double quotes for JSX attrs, semicolons consistent. Comments: only add one brief inline comment above `formatCounter` explaining why width is keyed off `target` not `current` (non-obvious — it prevents mid-animation layout shift); do not add JSDoc per CLAUDE.md.</action>
  <verify>
    <automated>test -f src/components/Counter.tsx && grep -q "from 'motion/react'" src/components/Counter.tsx && grep -q "useMotionValue" src/components/Counter.tsx && grep -q "useTransform" src/components/Counter.tsx && grep -q "animate" src/components/Counter.tsx && grep -q "controls.stop" src/components/Counter.tsx && grep -q "padStart" src/components/Counter.tsx && grep -q "interface CounterProps" src/components/Counter.tsx && grep -q "export default function Counter" src/components/Counter.tsx && grep -q "motion.span" src/components/Counter.tsx && ! grep -q "framer-motion" src/components/Counter.tsx && ! grep -q "AnimateNumber" src/components/Counter.tsx && ! grep -q "motion-plus" src/components/Counter.tsx && ! grep -q "supabase\|firebase\|fetch\|axios" src/components/Counter.tsx && echo "OK: Counter.tsx structure correct, motion/react only, no fetching, no forbidden imports"</automated>
  </verify>
  <done>
    `src/components/Counter.tsx` exists. Default-exported function component with `interface CounterProps { target: number }`. Uses `useMotionValue(0)`, `useTransform`, `animate(...)`, and renders `<motion.span>{display}</motion.span>`. useEffect has `[target, motionCount]` deps and `controls.stop()` cleanup. All Motion imports come from `'motion/react'` (not `'framer-motion'`, not `'motion-plus/react'`). No `AnimateNumber` usage. No data fetching, no Supabase imports, no Firebase imports. Padding uses `padStart` with tier-aware width based on `target`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new security surface. The Counter component is pure presentational code that receives a number via props and animates it on screen.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | N/A | `src/components/Counter.tsx` | accept | No new security boundary — Counter consumes an in-memory number prop, does no I/O, makes no network calls, reads no env vars, accepts no untrusted input. Security coverage for the data this component eventually displays lives in plan 01 (env contract, AbortSignal timeout) and plan 03 (anon grant on the view, revoke on the underlying table). Per the security_threat_model in CONTEXT.md: "no new security surface — counter is unauthenticated idempotent read." |
</threat_model>

<verification>
After Task 1 completes:

1. `grep -c "from 'motion/react'" src/components/Counter.tsx` — prints `1`.
2. `! grep -q 'framer-motion\|motion-plus\|AnimateNumber' src/components/Counter.tsx` — succeeds.
3. `! grep -q 'firebase\|supabase\|fetch(\|axios\|XMLHttpRequest' src/components/Counter.tsx` — succeeds (component is pure presentational, no I/O).
4. Optional smoke check (no formal test framework in repo yet): the file should be ~30-50 lines, with one default export, one inline interface, one helper function, one component function.
</verification>

<success_criteria>
This plan is complete when:
- `src/components/Counter.tsx` exists with one default export.
- The component uses only `motion/react` (Motion v12) imports: `motion, useMotionValue, useTransform, animate`.
- The component animates `useMotionValue(0)` to `target` via `animate(...)`, formats via `useTransform` with `padStart` keyed off target's tier, and renders `<motion.span>{display}</motion.span>`.
- The `useEffect` dependency array is `[target, motionCount]` and the cleanup returns `controls.stop()`.
- No data fetching, no Supabase imports, no Firebase residue, no `AnimateNumber`, no `framer-motion`.
- No edits to `src/App.tsx`, no edits to any other component, no edits to `package.json`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` with: the file created, the Motion API surface used (useMotionValue + animate + useTransform + motion.span), why per-digit-roll was rejected in favor of single-tween-with-padStart (cites RESEARCH.md decision — tier boundary handling), and a one-line note that the component is not yet mounted anywhere — plan 04 mounts it inside the existing pulse row in App.tsx without changing the surrounding JSX.
</output>
