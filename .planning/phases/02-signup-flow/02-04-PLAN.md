---
phase: 02-signup-flow
plan: 04
type: execute
# Wave/depends_on convention: Plan 04 is the LAST plan in Wave 1 — depends_on enforces
# in-wave ordering against Plans 02 (migration) and 03 (function source) which produce
# the artifacts this plan references. The HALT in Task 2 is the Wave 1 → Wave 2
# boundary; Plan 05 (Wave 2) does NOT start until this plan's checkpoint resolves.
wave: 1
depends_on: [02, 03]
files_modified:
  - .planning/phases/02-signup-flow/HANDOFF-PROMPT.md
autonomous: false
# requirements: [] — Plan 04 is the cross-repo handoff prompt + HALT. SEC-04 traceability
# here is audit-only (the prompt body contains no JWT or service-role values, enforced by
# the grep gate in Task 1's verify). The IMPLEMENTATION of SEC-04 lives in Plans 01, 03,
# and 05 (env validation, function secrets handling, bundle audit). Claiming SEC-04 here
# would overstate this plan's contribution.
requirements: []
must_haves:
  truths:
    - "D-02: A self-contained handoff prompt exists at `.planning/phases/02-signup-flow/HANDOFF-PROMPT.md` and is printed to stdout — Nour copies the prompt and pastes it into the SEALED-org agent so the migration + function get applied/deployed in the main repo"
    - "D-02: Plan 05 (client wiring + verification) MUST NOT start until Nour confirms back with 'phase 2 server live' (or equivalent acknowledgement) — this is the HARD HALT mirroring Phase 1 D-05"
    - "D-03: The prompt asks Nour for Phase-2-scoped deploy work only: (1) `supabase db push` (or equivalent) to apply migration 0032; (2) `supabase functions deploy join-waitlist`; (3) `supabase secrets set TURNSTILE_SECRET_KEY=...`; (4) confirm `SUPABASE_SERVICE_ROLE_KEY` already set; (5) create the Cloudflare Turnstile widget and provide the public site key for `VITE_TURNSTILE_SITE_KEY` (the placeholder Plan 01 wrote into `.env.local` gets overwritten with the real value)"
    - "Audit trail (mirrors Phase 1 D-04): the prompt asks the SEALED-org agent to run `/gsd-quick` so the cross-repo change gets recorded in the main repo's planning system AND notes that the function + migration exist because of the landing page"
    - "Security (mirrors Phase 1 T-01-10): the prompt contains ONLY file paths, deploy commands, verification queries, and the request to confirm. Zero credentials, zero env values, zero anon/service-role JWTs. Nour provides the Turnstile site key back to THIS repo's session verbally; the prompt itself never carries it."
    - "SEC-04 audit-only traceability: Plan 04 enforces SEC-04 by AUDIT — the handoff prompt body MUST contain no JWT-shaped string and no Resend-secret name (Task 1's grep gate refuses to mark the task done if violated). This is a NEGATIVE-SPACE contribution: SEC-04 is implemented by Plans 01 (env validation), 03 (function secrets via Deno.env), and 05 (post-build dist/ grep for `service_role`). Plan 04 only ensures the cross-repo handoff doesn't leak secrets in transit."
    - "Resend secrets are explicitly DEFERRED to Phase 3 — the prompt does NOT mention `RESEND_API_KEY` or `SEALED_FROM_ADDRESS`; those wait for DNS verification (Phase 3 DEPLOY-04)"
    - "The HALT is a `checkpoint:human-verify` task with `gate=blocking`; the plan's `autonomous: false` flag in the frontmatter signals the orchestrator that Wave 2 does not start until this plan reports complete — and this plan is not complete until Nour types the resume signal"
  artifacts:
    - path: ".planning/phases/02-signup-flow/HANDOFF-PROMPT.md"
      provides: "Copy-pasteable prompt for the SEALED-org sibling agent to apply the migration + deploy the function + set the Turnstile secret"
      contains: "0032_signup_attempts.sql"
      min_lines: 50
  key_links:
    - from: "HANDOFF-PROMPT.md"
      to: "the SEALED-org sibling agent"
      via: "Nour pastes the prompt into the sibling repo's Claude Code session"
      pattern: "/gsd-quick"
    - from: "HANDOFF-PROMPT.md deploy commands"
      to: "Supabase production project (shared with main app)"
      via: "supabase db push + supabase functions deploy join-waitlist + supabase secrets set TURNSTILE_SECRET_KEY"
      pattern: "supabase functions deploy join-waitlist"
    - from: "HANDOFF-PROMPT.md Cloudflare step"
      to: "https://dash.cloudflare.com/?to=/:account/turnstile"
      via: "Nour creates the widget, gets the site key (returned to this repo's session) and secret key (set via Supabase secrets in the sibling repo)"
      pattern: "Cloudflare Turnstile dashboard"
    - from: "HALT task (Task 2)"
      to: "Plan 05 (client wiring + verification)"
      via: "Plan 05 depends_on includes plan 04; orchestrator gates Wave 2 on this resume signal"
      pattern: "depends_on: \\[01, 04\\]"
---

<objective>
**Wave semantics note (READ FIRST):** This plan is the LAST plan in Wave 1. Per the planner's `wave = max(deps) + 1` convention, this plan should technically be Wave 2 — but Wave numbers in Phase 2 are semantic (Wave 1 = "code-writing + handoff"; Wave 2 = "client wiring after server is live"), NOT strictly derived from depends_on. Plan 04 keeps `wave: 1` because (a) its first task is a code-writing autonomous step that produces a Wave 1 artifact (the handoff prompt), and (b) Task 2 IS the Wave 1 → Wave 2 boundary — the HALT itself. Plan 05's `depends_on: [01, 04]` then enforces that Wave 2 (client wiring) cannot start until Plan 04's checkpoint resolves. Task 1 is autonomous (write + print the handoff prompt). Task 2 is a BLOCKING human-verify checkpoint — Wave 2 (Plan 05) does NOT begin until Nour confirms the migration is applied, the function is deployed, and `TURNSTILE_SECRET_KEY` is set. The plan's `autonomous: false` flag in the frontmatter signals this to the orchestrator: Plan 04 is not "done" when Task 1 finishes; it is "done" only when Task 2's resume signal is received.

Generate the cross-repo handoff prompt that Nour will paste into the SEALED-org agent, print it to stdout, then HALT execution until Nour confirms the SEALED-org agent has (a) applied migration `0032_signup_attempts.sql`, (b) deployed the `join-waitlist` function, (c) set the `TURNSTILE_SECRET_KEY` Supabase secret, (d) supplied the public Turnstile site key for `VITE_TURNSTILE_SITE_KEY` in this repo's `.env.local`.

Purpose: Per D-02, Phase 2 follows the same HALT-after-handoff pattern Phase 1 used (D-05). Applying the migration and deploying the function are operations that require Supabase credentials this executor does not possess. Per D-03, the prompt asks for Phase-2-scoped deploy work only — Resend secrets and DNS records stay deferred to Phase 3 (no email send happens in Phase 2; the function returns success without dispatching Template 1A). Per D-01/D-04 audit-trail pattern, the prompt asks the sibling agent to run `/gsd-quick` so the cross-repo change is documented on both sides (both repos know the function + migration exist because of the landing page). The HALT itself is the gate that prevents Plan 05 from running against a missing function — without it, Plan 05's verification step would hit a 404 indistinguishable from a misconfigured client (RESEARCH Pitfall 6).

Output: A new file at `.planning/phases/02-signup-flow/HANDOFF-PROMPT.md`. The file's contents printed to stdout so Nour can copy directly from the terminal. A blocking checkpoint Nour resolves by confirming "phase 2 server live" before Plan 05 starts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-signup-flow/02-CONTEXT.md
@.planning/phases/02-signup-flow/02-RESEARCH.md
@.planning/phases/02-signup-flow/02-PATTERNS.md
@.planning/phases/01-foundation/HANDOFF-PROMPT.md
@.planning/phases/01-foundation/01-03-PLAN.md
@CLAUDE.md

<interfaces>
<!-- The Phase 1 handoff prompt is the template — Phase 2's prompt mirrors its structure: -->
<!-- - H1 header `# Phase N: <Name> - SEALED-org Handoff Prompt` -->
<!-- - Three bold metadata lines (`**Audience:**`, `**Triggered after:**`, `**Blocks:**`) -->
<!-- - Horizontal rule -->
<!-- - `<prompt>` wrapper containing the matter-of-fact second-person prompt -->
<!-- - Verification steps (queries, dashboard checks) -->
<!-- - Explicit "landing repo is BLOCKED" notice + the resume-signal Nour will return to this repo -->

<!-- Phase 2 prompt MUST include, verbatim, the following four call-outs: -->

1. Migration apply:
   - File: `supabase/migrations/0032_signup_attempts.sql`
   - Command (sibling agent's choice): `supabase db push` OR `supabase migration up` against the production project.
   - Verification: in Supabase dashboard SQL editor, `select * from app_private.signup_attempts;` returns 0 rows (run as service_role). Run as anon, the same query returns permission-denied (this is GOOD — confirms service-role-only).

2. Function deploy:
   - Directory: `supabase/functions/join-waitlist/`
   - Command: `supabase functions deploy join-waitlist` (against the same production project).
   - Verification: `supabase functions list` shows `join-waitlist` with `ACTIVE` status (or equivalent).

3. Secrets:
   - `supabase secrets set TURNSTILE_SECRET_KEY=<value-from-cloudflare-dashboard>` — Nour creates the Turnstile widget at https://dash.cloudflare.com/?to=/:account/turnstile (Managed challenge mode, hostnames `sealedapp.io` AND `localhost`), copies the SECRET KEY into this command.
   - Verify `SUPABASE_SERVICE_ROLE_KEY` is already set: `supabase secrets list` should show it (it has been set since Phase 1 because `dispatch` and `notify` use it). If absent, set it from the Supabase dashboard's Project Settings → API → service_role key.

4. Cross-repo audit trail:
   - Run `/gsd-quick` in the sibling agent so the migration + function deploy gets logged in the SEALED-org repo's planning system.
   - In the resulting `.planning/quick/` entry (or wherever sibling stores it), include the rationale: "Migration 0032 + function join-waitlist created in response to SEALED-Landing-Page Phase 2's signup flow."
   - Confirm back to this repo's session with the resume signal once all four steps complete.

<!-- The site key (PUBLIC, returned to THIS repo) is verbally communicated by Nour back to this session — the prompt itself does NOT instruct the sibling agent to write it anywhere; the site key belongs in this repo's .env.local, which the sibling agent has no business of writing to. -->

<!-- What the prompt MUST NOT contain (security boundary): -->
- No anon JWT, no service-role JWT.
- No Supabase project ref / URL with the host name.
- No Resend env vars (`RESEND_API_KEY`, `SEALED_FROM_ADDRESS`) — those are Phase 3.
- No DNS instructions for sealedapp.io — those are Phase 3 (DEPLOY-04).
- No email body / Resend template content — Phase 3.
- No instructions to modify any client-side code in this landing repo — only Wave 2 (Plan 05) does that, and only after this HALT resolves.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate HANDOFF-PROMPT.md and print it to stdout</name>
  <files>.planning/phases/02-signup-flow/HANDOFF-PROMPT.md</files>
  <read_first>
    - .planning/phases/01-foundation/HANDOFF-PROMPT.md (the Phase 1 prompt — copy its STRUCTURE verbatim: H1, three `**Bold:**` metadata lines, horizontal rule, `<prompt>` block with verification queries; do NOT copy the schema-specific content)
    - .planning/phases/01-foundation/01-03-PLAN.md (the Phase 1 plan that generated the analog — task 2 there is the template for this task)
    - .planning/phases/02-signup-flow/02-CONTEXT.md (D-01, D-02, D-03 — what Nour does at the gate; specifically D-03 enumerates the exact deploy commands)
    - .planning/phases/02-signup-flow/02-RESEARCH.md (§ "External Service Setup" — the exact Cloudflare URL `https://dash.cloudflare.com/?to=/:account/turnstile` and the widget-config notes: Managed challenge mode, hostnames `sealedapp.io` + `localhost`)
    - .planning/phases/02-signup-flow/02-PATTERNS.md (no analog discovered; the prompt is a fresh artifact — follow the Phase 1 template exactly)
    - /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0032_signup_attempts.sql (confirm the file exists from Plan 02 — Task 1 will reference it by name in the prompt)
    - /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts (confirm the function exists from Plan 03 — Task 1 will reference the directory in the prompt)
  </read_first>
  <action>
    Step 1 — Confirm Plan 02 and Plan 03 outputs are on disk. Run:
      `test -f "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0032_signup_attempts.sql" && test -f "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts" && test -f "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/deno.json"`
    All three must exist. If any is missing, STOP — Plans 02/03 did not complete; do not write the handoff prompt.

    Step 2 — Create the file at `.planning/phases/02-signup-flow/HANDOFF-PROMPT.md`. Structure (mirrors the Phase 1 template byte-for-byte except the section bodies):

    Header block:
    - `# Phase 2: Signup Flow - SEALED-org Handoff Prompt` (H1)
    - `**Audience:** The Claude Code agent in the SEALED-org repo (`/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org`)`
    - `**Triggered after:** Phase 2 Plan 02 (migration 0032) and Plan 03 (join-waitlist function source) written in the sibling repo`
    - `**Blocks:** Plan 05 (landing-repo client wiring + verification)`
    - `---` (horizontal rule)

    Then a `<prompt>` block containing the verbatim second-person prompt. The prompt MUST cover:

    (a) Opening one-paragraph context: the SEALED landing page (sibling repo: `SEALED-Landing-Page`) is Phase 2-deploying its signup flow. Two new files exist in this repo: a migration `supabase/migrations/0032_signup_attempts.sql` and a function `supabase/functions/join-waitlist/{deno.json,index.ts}`. They need to be applied/deployed to the production Supabase project.

    (b) Numbered deploy steps (1-4), one per line group, written so the sibling agent can execute them mechanically:

      1. **Apply migration 0032.** Run `supabase db push` (or `supabase migration up`) against the production project to apply `supabase/migrations/0032_signup_attempts.sql`. After applying, verify in the dashboard SQL editor (run as service_role): `select * from app_private.signup_attempts;` returns 0 rows AND `select column_name, data_type from information_schema.columns where table_schema = 'app_private' and table_name = 'signup_attempts';` returns 4 rows (id, ip, attempted_at, outcome). Run as anon: `select * from app_private.signup_attempts;` returns permission-denied — this is GOOD (table is service_role-only by design).

      2. **Deploy the join-waitlist function.** Run `supabase functions deploy join-waitlist` against the production project. After deploy, verify `supabase functions list` shows `join-waitlist` with ACTIVE status. The function is a PUBLIC endpoint (no service-role guard) called from the landing page browser — do NOT enable any extra Authorization gating.

      3. **Set the Turnstile secret.** Nour: go to https://dash.cloudflare.com/?to=/:account/turnstile and create a Turnstile widget. Configure: Managed challenge mode (Cloudflare picks visible vs invisible per request); hostnames `sealedapp.io` AND `localhost`. Copy the SECRET KEY (private — do NOT paste in any browser or repo file). Run `supabase secrets set TURNSTILE_SECRET_KEY=<paste-secret-here>` against the production project. Verify with `supabase secrets list` that `TURNSTILE_SECRET_KEY` appears (the value is hidden — only the key name shows). Also confirm `SUPABASE_SERVICE_ROLE_KEY` is already set (it has been since Phase 1 because `dispatch` and `notify` use it).

      4. **Return the Turnstile SITE KEY to Nour** (separately — this is the PUBLIC key, safe to share verbally or via the chat session that started this handoff). Nour will paste it into the landing repo's `.env.local` `VITE_TURNSTILE_SITE_KEY=` line, replacing the `REPLACE_ME_TURNSTILE_SITE_KEY_FROM_CLOUDFLARE_DASHBOARD` placeholder Plan 01 wrote.

    (c) Audit trail step (separately numbered as item 5):

      5. **Record the cross-repo change in the sibling planning system.** Run `/gsd-quick` (or this repo's equivalent quick-action workflow) and create an entry that documents: "Migration 0032 (signup_attempts table) + function join-waitlist created in response to the SEALED-Landing-Page repo's Phase 2 (Signup Flow). The function handles Turnstile siteverify → IP rate limit (3/IP/24h) → 4-state re-signup lookup → admin.createUser({email_confirm: false}) → app_private.waitlist_signups insert. Phase 3 of the landing page will extend this with the Resend Template 1A send; for Phase 2 the success path leaves TODO comments at the email-send sites." Reference the landing repo's Phase 2 path `.planning/phases/02-signup-flow/` so the audit trail closes the loop.

    (d) Smoke test (separately, item 6) — optional but recommended:

      6. (Optional smoke test) `supabase functions invoke join-waitlist --no-verify-jwt --body '{"email":"smoketest+1@example.com","turnstileToken":""}'` should return `{ "state": "turnstile_failed" }` 200 (the empty token fails siteverify). This proves the function is deployed, the OPTIONS preflight responds, and `TURNSTILE_SECRET_KEY` is set. Do NOT use a real Cloudflare token here — Nour will exercise the full happy path from the landing page itself during Wave 2 verification (Plan 05).

    (e) Explicit BLOCKED notice + resume signal:

      `The SEALED-Landing-Page repo is BLOCKED waiting on confirmation. Once steps 1-5 (and optionally 6) are complete, return to that repo's Claude Code session and reply with: "phase 2 server live" (or paste a short confirmation that the migration applied + function deployed + Turnstile secret set + site key returned). The landing repo will then run Plan 05 (client wiring + end-to-end verification).`

    Tone: matter-of-fact, second person addressed to the next agent. No marketing language. Numbered list for the deploy steps. After writing the file, also print the file contents to stdout via `Bash(cat .planning/phases/02-signup-flow/HANDOFF-PROMPT.md)` so Nour can copy the prompt directly from the terminal output without opening the file. (Stdout printing is convenience; the file is the audit trail.)

    Do NOT include any of the following in the prompt body: actual env values, JWTs, the Supabase project URL with the real host, anything that touches Resend / RESEND_API_KEY / SEALED_FROM_ADDRESS (those are Phase 3), any DNS instructions for sealedapp.io (Phase 3 DEPLOY-04), any instructions to modify the landing repo's source files.
  </action>
  <verify>
    <automated>set -o pipefail; F=".planning/phases/02-signup-flow/HANDOFF-PROMPT.md" &amp;&amp; test -f "$F" &amp;&amp; grep -q "0032_signup_attempts.sql" "$F" &amp;&amp; grep -q "supabase functions deploy join-waitlist" "$F" &amp;&amp; grep -q "TURNSTILE_SECRET_KEY" "$F" &amp;&amp; grep -q "SUPABASE_SERVICE_ROLE_KEY" "$F" &amp;&amp; grep -q "Turnstile" "$F" &amp;&amp; grep -q "dash.cloudflare.com" "$F" &amp;&amp; grep -q "/gsd-quick" "$F" &amp;&amp; grep -q "app_private.signup_attempts" "$F" &amp;&amp; grep -q "join-waitlist" "$F" &amp;&amp; grep -qi "BLOCKED" "$F" &amp;&amp; grep -qi "phase 2 server live" "$F" &amp;&amp; grep -qi "landing" "$F" &amp;&amp; if grep -qE 'eyJ[A-Za-z0-9_=-]+\.eyJ' "$F"; then echo "FAIL: a JWT-shaped string appears in the prompt — leak risk"; exit 1; fi &amp;&amp; if grep -qiE 'RESEND_API_KEY|SEALED_FROM_ADDRESS' "$F"; then echo "FAIL: Resend secrets mentioned — Phase 2 handoff must NOT cover Phase 3 work"; exit 1; fi &amp;&amp; if grep -qE 'service_role.*=.*eyJ|TURNSTILE_SECRET_KEY=[A-Za-z0-9]' "$F"; then echo "FAIL: an actual secret value appears as part of an assignment in the prompt"; exit 1; fi &amp;&amp; echo "OK: handoff prompt present with migration path, function deploy command, Turnstile secret instruction, /gsd-quick reference, BLOCKED notice, resume signal — no JWTs / no Resend secrets / no inline values"</automated>
  </verify>
  <done>
    `.planning/phases/02-signup-flow/HANDOFF-PROMPT.md` exists. Contains the H1 + three `**Bold:**` metadata lines + horizontal rule, then a `<prompt>` block with the verbatim text. Prompt references: exact migration file path (`supabase/migrations/0032_signup_attempts.sql`), the function directory `supabase/functions/join-waitlist/`, the deploy command (`supabase functions deploy join-waitlist`), the Turnstile secret command (`supabase secrets set TURNSTILE_SECRET_KEY=...`), confirmation of `SUPABASE_SERVICE_ROLE_KEY`, the Cloudflare dashboard URL `https://dash.cloudflare.com/?to=/:account/turnstile`, the request to record the change in the sibling planning system via `/gsd-quick` (referencing the landing repo's Phase 2 path), the optional `functions invoke` smoke test, the explicit "landing repo is BLOCKED" notice, and the resume signal phrase `"phase 2 server live"`. Zero JWTs, zero env values, zero Resend mentions. File contents printed to stdout during execution.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: HALT — wait for Nour to confirm the SEALED-org agent has applied the migration, deployed the function, set the Turnstile secret, and supplied the site key</name>
  <files></files>
  <read_first>
    - .planning/phases/02-signup-flow/HANDOFF-PROMPT.md (the prompt Nour just pasted into the sibling agent)
    - .planning/phases/02-signup-flow/02-CONTEXT.md (D-02 — this is a HARD handoff blocker, non-negotiable; mirrors Phase 1 D-05)
    - .planning/phases/02-signup-flow/02-RESEARCH.md (§ "Common Pitfalls > Pitfall 6: CORS preflight rejected" — explains how a missing/unreachable function looks indistinguishable from misconfigured client code, which is why the HALT is critical)
    - .planning/phases/01-foundation/01-03-PLAN.md (§ Task 3 — the Phase 1 analog of this exact checkpoint pattern; the same logic applies)
  </read_first>
  <what-built>
    Wave 1 of Phase 2 is now complete:

    In THIS repo (landing):
    - `.env.local` corrected: leading space on `VITE_SUPABASE_ANON_KEY` stripped, URL/JWT-ref reconciled against the main app's canonical .env, `VITE_TURNSTILE_SITE_KEY=` line added with the `REPLACE_ME_TURNSTILE_SITE_KEY_FROM_CLOUDFLARE_DASHBOARD` placeholder.
    - `.planning/REQUIREMENTS.md` SEC-02 amended to `3 signup attempts per rolling 24h` with D-05 traceability.
    - `package.json` pins `@marsidev/react-turnstile@1.5.2`; `node_modules` confirms the install.
    - `.planning/phases/02-signup-flow/HANDOFF-PROMPT.md` written and printed to stdout.

    In the SIBLING SEALED-org repo (NOT yet applied to Supabase):
    - `supabase/migrations/0032_signup_attempts.sql` written.
    - `supabase/functions/join-waitlist/deno.json` + `index.ts` written.

    Nothing in this repo is callable end-to-end yet. The Edge Function file sits on disk in the sibling repo but is NOT deployed to the Supabase project — calling it from the landing page would return "function not found." Plan 05 (client wiring + verification) MUST NOT run until: (a) the migration is applied, (b) the function is deployed, (c) `TURNSTILE_SECRET_KEY` is set, and (d) Nour has overwritten the `VITE_TURNSTILE_SITE_KEY` placeholder in `.env.local` with the real public site key from the Cloudflare dashboard. This is the Wave 1 ↔ Wave 2 boundary.
  </what-built>
  <how-to-verify>
    1. Copy the printed HANDOFF-PROMPT.md text from the terminal (or open `.planning/phases/02-signup-flow/HANDOFF-PROMPT.md`).
    2. Open the SEALED-org repo's Claude Code session (the sibling repo at `/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org`).
    3. Paste the prompt verbatim into that session. The sibling agent will (a) apply migration 0032, (b) run `supabase functions deploy join-waitlist`, (c) ask you to create the Cloudflare Turnstile widget and paste the secret key into `supabase secrets set TURNSTILE_SECRET_KEY=...`, (d) verify `SUPABASE_SERVICE_ROLE_KEY` is set, (e) record the change via `/gsd-quick` in the sibling repo's planning system, (f) optionally run the smoke test `supabase functions invoke join-waitlist ...`.
    4. Once the sibling agent confirms back, perform your own dashboard sanity checks:
       - **Supabase SQL editor (Run as: service_role):** `select count(*) from app_private.signup_attempts;` returns 0. `select column_name from information_schema.columns where table_schema='app_private' and table_name='signup_attempts' order by ordinal_position;` returns: `id`, `ip`, `attempted_at`, `outcome`.
       - **Supabase SQL editor (Run as: anon):** `select * from app_private.signup_attempts;` returns permission-denied (GOOD).
       - **Supabase dashboard → Edge Functions:** `join-waitlist` is listed as ACTIVE (or equivalent deployed status).
       - **Supabase dashboard → Project Settings → Functions → Secrets:** `TURNSTILE_SECRET_KEY` is listed (value hidden). `SUPABASE_SERVICE_ROLE_KEY` is listed.
    5. Open the Cloudflare Turnstile dashboard, find the widget you just created, copy the SITE KEY (public — starts with `0x4AAAAA...`), and paste it into THIS repo's `.env.local` file, replacing the `REPLACE_ME_TURNSTILE_SITE_KEY_FROM_CLOUDFLARE_DASHBOARD` placeholder. Save the file.
    6. Once steps 4 + 5 are complete, return to this repo's session and explicitly reply with the resume signal to advance to Plan 05 (Wave 2).
  </how-to-verify>
  <acceptance_criteria>
    - You have pasted HANDOFF-PROMPT.md into the SEALED-org repo's agent and that agent has applied migration 0032.
    - You confirm via the Supabase dashboard that `app_private.signup_attempts` exists, has the 4 expected columns, and is empty.
    - You confirm via the Supabase dashboard that `select * from app_private.signup_attempts;` as anon returns permission-denied.
    - You confirm via the Supabase dashboard that the `join-waitlist` function is deployed and active.
    - You confirm via the Supabase dashboard Secrets list that `TURNSTILE_SECRET_KEY` AND `SUPABASE_SERVICE_ROLE_KEY` are set (values hidden — only key names show).
    - You have overwritten the `REPLACE_ME_TURNSTILE_SITE_KEY_FROM_CLOUDFLARE_DASHBOARD` placeholder in `.env.local` with the real public site key from the Cloudflare Turnstile dashboard.
    - You explicitly type the resume signal (or paste the sibling-agent's confirmation message + your dashboard observations) into this session before the executor advances to Plan 05.
  </acceptance_criteria>
  <resume-signal>Type "phase 2 server live" once the migration is applied, the function is deployed, `TURNSTILE_SECRET_KEY` is set, and `VITE_TURNSTILE_SITE_KEY` in `.env.local` holds the real site key (not the placeholder). Do NOT advance otherwise — running Plan 05 against a missing function returns `FunctionsRelayError` indistinguishable from a misconfigured client; running it against an unset Turnstile secret returns silent server_error 500s indistinguishable from a code bug.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| This repo ↔ Sibling SEALED-org repo (via Nour's copy-paste) | The handoff prompt is plaintext markdown that travels through Nour's terminal / chat session. It must not contain credentials. |
| Nour ↔ Cloudflare Turnstile dashboard | Nour creates the widget on his Cloudflare account; the secret key is generated by Cloudflare and pasted by Nour into the sibling agent's `supabase secrets set` command. This executor never sees the secret. |
| Nour ↔ `.env.local` in this repo | Nour pastes the public site key into the file manually after the sibling agent's deploy completes. The placeholder Plan 01 wrote is the audit trail of "secret slot expected here." |
| Sibling agent ↔ Supabase production project | The sibling agent runs `supabase db push`, `supabase functions deploy`, and `supabase secrets set` on Nour's authority. This executor does not have or use Supabase credentials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-21 | Information disclosure | Handoff prompt accidentally containing a JWT, env value, or secret | mitigate | Task 1 grep gate: `if grep -qE 'eyJ[A-Za-z0-9_=-]+\.eyJ' "$F"; then ... exit 1; fi` — refuses to mark the task done if any JWT-shaped string appears. The prompt is structured around file paths + command names + dashboard URLs; there is no slot in the prompt where a literal env value would naturally go. |
| T-02-22 | Information disclosure (scope creep) | Phase 3's Resend secrets mentioned in Phase 2's prompt — would prematurely send the wrong message about what's deployed | mitigate | Task 1 grep gate refuses to mark done if `RESEND_API_KEY` or `SEALED_FROM_ADDRESS` appear. The deferred-to-Phase-3 boundary is explicit in D-03. |
| T-02-23 | Repudiation | Migration / function deployed without traceability | mitigate | Step 5 of the prompt asks the SEALED-org agent to run `/gsd-quick` and record the change with a reference to this repo's `.planning/phases/02-signup-flow/` path. Audit trail closed on both sides. |
| T-02-24 | Tampering | Wrong version of the function / migration deployed | mitigate | Task 1 step 1 verifies that the migration and function files exist on disk BEFORE writing the handoff — if Plans 02/03 didn't complete, the prompt is not generated. The sibling agent's deploy command (`supabase functions deploy join-waitlist`) reads from the directory; there is no version-selection ambiguity. |
| T-02-25 | Premature execution of Wave 2 | Plan 05 running before the function is deployed | mitigate | The HALT task (Task 2) is a blocking checkpoint with `autonomous: false` on the plan. Plan 05's frontmatter sets `depends_on: [01, 04]` so the orchestrator cannot run Wave 2 before THIS plan reports complete — and this plan is not complete until Nour types "phase 2 server live". Without this halt, Plan 05's verification step would hit a 404 / `FunctionsRelayError` indistinguishable from misconfigured client code. |
| T-02-26 | Information disclosure | Turnstile SITE KEY ending up in a non-VITE_ slot or committed to git | accept (low risk, public key) | The site key is public by design (it's embedded in the browser bundle so Cloudflare can validate the page origin). The placeholder approach Plan 01 used means the slot is reserved in `.env.local` (gitignored) and the file never reaches GitHub. Phase 5's Vercel env setup will inject the same value at build time. |
| T-02-27 | Tampering | Turnstile widget configured for the wrong hostnames (e.g. only `sealedapp.io` but not `localhost`) | mitigate | The prompt explicitly tells Nour to include BOTH `sealedapp.io` AND `localhost` in the widget's hostname allowlist — otherwise local dev verification (Plan 05) would silently fail. |
</threat_model>

<verification>
After both tasks complete (including the HALT confirmation):

1. `test -f .planning/phases/02-signup-flow/HANDOFF-PROMPT.md` — file exists in this repo.
2. All grep gates in Task 1's automated verify pass (migration path, deploy command, secret command, Cloudflare URL, `/gsd-quick`, BLOCKED notice, resume signal; no JWTs, no Resend secrets, no inline values).
3. Nour has explicitly typed "phase 2 server live" (or equivalent) in the session — the HALT task gates this.
4. The Supabase dashboard verification queries (in `<how-to-verify>` step 4) all behave as expected.
5. `.env.local`'s `VITE_TURNSTILE_SITE_KEY=` now holds a real key (not the placeholder).
</verification>

<success_criteria>
This plan is complete when:
- `.planning/phases/02-signup-flow/HANDOFF-PROMPT.md` exists with the verbatim prompt.
- The handoff prompt has been printed to stdout.
- Nour has confirmed (a) migration 0032 applied, (b) join-waitlist function deployed, (c) `TURNSTILE_SECRET_KEY` set, (d) `SUPABASE_SERVICE_ROLE_KEY` confirmed already set, (e) `VITE_TURNSTILE_SITE_KEY` in `.env.local` updated with the real site key, (f) sibling repo's planning system documents the change via `/gsd-quick`.
- No source code in this repo is modified by this plan (the only file touched in this repo is the new HANDOFF-PROMPT.md).
- No `supabase db push`, no `supabase functions deploy`, no `supabase secrets set` was invoked by THIS executor — all of those happened in the sibling agent at Nour's direction.
</success_criteria>

<output>
After completion (post-HALT approval), create `.planning/phases/02-signup-flow/02-04-SUMMARY.md` with: the handoff prompt path, a structured list of what the sibling agent did (migration apply, function deploy, secrets set, audit trail recorded), confirmation that Nour approved the gate (include the timestamp + resume signal text), the four dashboard verification observations Nour reported (table columns, anon denial, function active, secrets list), the new `.env.local` state (placeholder replaced with real site key), and a one-line pointer to Plan 05 (client wiring + end-to-end verification) as the next executable step.
</output>
