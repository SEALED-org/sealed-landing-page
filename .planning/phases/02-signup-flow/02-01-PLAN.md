---
phase: 02-signup-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.local
  - .planning/REQUIREMENTS.md
  - package.json
autonomous: true
requirements: [SEC-04]
must_haves:
  truths:
    - "D-03: `.env.local` has the two pre-existing bugs fixed — leading space on `VITE_SUPABASE_ANON_KEY=` stripped, and the URL/JWT-ref typo reconciled against the main app's canonical .env so the bearer header is well-formed and the URL host matches the JWT `ref` claim"
    - "D-03: A new client-exposed env var `VITE_TURNSTILE_SITE_KEY=...` is present in `.env.local` so the new module-load validation in Plan 05 has a value to consume"
    - "D-05: `.planning/REQUIREMENTS.md` SEC-02 wording reads `Server-side IP rate limit of 3 signup attempts per rolling 24h per IP enforced in the Edge Function.` (not `1 attempt per IP per day`) and carries a sibling note citing this phase's CONTEXT.md D-05 rationale (NAT / shared-IP false positives)"
    - "D-07: `@marsidev/react-turnstile@1.5.2` is added to `package.json` `dependencies` so Plan 05 can `import { Turnstile, type TurnstileInstance } from '@marsidev/react-turnstile'` without an additional install step"
    - "SEC-04 traceability: the new `VITE_TURNSTILE_SITE_KEY` is the ONLY new `VITE_*` env var; nothing service-role-shaped (`service_role`, `SUPABASE_SERVICE_ROLE_KEY`, `TURNSTILE_SECRET_KEY`) has been added to any `VITE_*` slot or to `.env.local`"
  artifacts:
    - path: ".env.local"
      provides: "Three working VITE_ env vars: SUPABASE_URL (canonical host), SUPABASE_ANON_KEY (no leading whitespace), TURNSTILE_SITE_KEY"
      contains: "VITE_TURNSTILE_SITE_KEY="
      min_lines: 4
    - path: ".planning/REQUIREMENTS.md"
      provides: "SEC-02 wording amended to 3 attempts / IP / rolling 24h per D-05"
      contains: "3 signup attempts per rolling 24h"
      min_lines: 100
    - path: "package.json"
      provides: "@marsidev/react-turnstile@1.5.2 dependency available to the Vite bundle"
      contains: "@marsidev/react-turnstile"
      min_lines: 26
  key_links:
    - from: ".env.local VITE_TURNSTILE_SITE_KEY"
      to: "src/lib/supabase.ts module-load validation (added in Plan 05)"
      via: "import.meta.env.VITE_TURNSTILE_SITE_KEY read at module init"
      pattern: "VITE_TURNSTILE_SITE_KEY"
    - from: "package.json @marsidev/react-turnstile"
      to: "src/components/WaitlistForm.tsx Turnstile mount (added in Plan 05)"
      via: "import { Turnstile, type TurnstileInstance } from '@marsidev/react-turnstile'"
      pattern: "@marsidev/react-turnstile"
    - from: ".planning/REQUIREMENTS.md SEC-02"
      to: "Edge Function rate-limit threshold (Plan 03)"
      via: "RATE_LIMIT_THRESHOLD = 3 in the function — wording amendment and code threshold must agree"
      pattern: "3 signup attempts per rolling 24h"
---

<objective>
Fix the three pre-existing Phase 2 preconditions in the landing repo before any Edge Function / migration work begins: (1) repair the two `.env.local` bugs called out in STATE.md Blockers (leading space on `VITE_SUPABASE_ANON_KEY`, URL/JWT-ref mismatch) and add the new `VITE_TURNSTILE_SITE_KEY` slot; (2) amend `.planning/REQUIREMENTS.md` SEC-02 to the revised 3-attempts-per-IP-per-24h wording per D-05; (3) install `@marsidev/react-turnstile@1.5.2` so Plan 05 can mount the widget without an extra install hop.

Purpose: Per D-03, Phase 2 is the moment the landing repo finally talks to a real Supabase endpoint — the Phase 1 stub masked the env-var bugs because no network call was made. The moment Plan 05 wires `supabase.functions.invoke('join-waitlist', ...)`, a leading whitespace in the anon key produces a malformed bearer header (silent 401 with a confusing CORS-shaped error) and the URL/ref mismatch produces a DNS-shaped failure indistinguishable from "function not deployed." Both must be fixed BEFORE the verification step. Per D-05, the REQUIREMENTS.md amendment is part of Phase 2's work (not a precondition Nour does manually) — the executor edits it atomically with the rationale linked to 02-CONTEXT.md D-05. Per D-07, `@marsidev/react-turnstile@1.5.2` is the chosen wrapper (verified npm version, handles React 19 StrictMode double-mount); installing it now means Plan 05 is a single-pass edit with no intermediate `npm install` step.

Output: A corrected `.env.local`, an amended `.planning/REQUIREMENTS.md`, and an updated `package.json` (+ lockfile if npm produces one). No source code changes — these are config / docs / dep work only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-signup-flow/02-CONTEXT.md
@.planning/phases/02-signup-flow/02-RESEARCH.md
@.planning/phases/02-signup-flow/02-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- The current contents of .env.local (this repo) — both bugs are visible here. -->
<!-- VITE_SUPABASE_ANON_KEY has a leading space after the `=` sign on its own line. -->
<!-- VITE_SUPABASE_URL host is `tiaeiioiyelhekgrllnjn.supabase.co` (11 chars in subdomain). -->
<!-- JWT `ref` claim inside VITE_SUPABASE_ANON_KEY decodes to `tiaeioiylephekgrllnj` (different ordering). -->
<!-- Nour must cross-check against the canonical main-repo .env at /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/.env or .env.local to pick the right host. -->

From .planning/REQUIREMENTS.md current SEC-02 wording (line 17):
  `**SEC-02**: Server-side IP rate limit of 1 signup attempt per day per IP enforced in the Edge Function`
Phase 2 D-05 changes this to:
  `**SEC-02**: Server-side IP rate limit of 3 signup attempts per rolling 24h per IP enforced in the Edge Function`
plus a sibling note: `(Amended Phase 2 per 02-CONTEXT.md D-05 — rationale: NAT/shared-IP false positives.)`

From package.json current dependencies (lines 11-19):
  `@supabase/supabase-js`: `2.103.2`
  Add: `@marsidev/react-turnstile`: `1.5.2` (RESEARCH §"Standard Stack > Core" — verified via `npm view @marsidev/react-turnstile version` → '1.5.2', published 2026-05-05)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix .env.local — strip leading space, reconcile URL/JWT ref, add VITE_TURNSTILE_SITE_KEY</name>
  <files>.env.local</files>
  <read_first>
    - .env.local (the current file — note: both bugs are on the value side of the `=` sign of `VITE_SUPABASE_ANON_KEY` and on the host of `VITE_SUPABASE_URL`)
    - .planning/STATE.md (§ Blockers/Concerns — the two issues are catalogued there with both subdomain strings spelled out for cross-reference)
    - .planning/phases/02-signup-flow/02-CONTEXT.md (D-03 — the three things Nour must do, of which two are in this repo and live in this task)
    - .planning/phases/02-signup-flow/02-RESEARCH.md (§ "Pitfall 1: Service role key inlined into client bundle" — confirms that ONLY VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, VITE_TURNSTILE_SITE_KEY belong with the VITE_ prefix; nothing service-role-shaped)
    - /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/.env (or .env.local — whichever exists) — the CANONICAL source for `VITE_SUPABASE_URL` because the main app talks to the same Supabase project; copy the URL host from there verbatim
  </read_first>
  <action>
    Step 1 — Reconcile the URL host. Run `cat "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/.env" 2>/dev/null || cat "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/.env.local" 2>/dev/null` to find the canonical `SUPABASE_URL` (or `VITE_SUPABASE_URL`) in the main repo. The host segment of that URL (e.g. `xxxxxxxxxxxxx.supabase.co`) is canonical. Compare with this repo's current `VITE_SUPABASE_URL=https://tiaeiioiyelhekgrllnjn.supabase.co` AND with the `ref` claim embedded in this repo's current `VITE_SUPABASE_ANON_KEY` JWT (decode the JWT payload: base64-decode the segment between the first and second `.`; look for `"ref":"..."`). Whichever subdomain the main repo uses is correct; the other side of the discrepancy is the typo. If the main repo's .env is unreadable, STOP and ask Nour to confirm — do NOT guess; a wrong host produces a silent DNS failure indistinguishable from a deploy error.

    Step 2 — Strip the leading whitespace on `VITE_SUPABASE_ANON_KEY=`. The current file has a space immediately after the `=` on that line (between `=` and `eyJ...`). Remove it so the line reads `VITE_SUPABASE_ANON_KEY=eyJ...` with no whitespace between `=` and the first character of the JWT. Vite parses dotenv files literally; the leading space becomes part of the value and corrupts the bearer header.

    Step 3 — Append a new line: `VITE_TURNSTILE_SITE_KEY=` followed by the placeholder string `REPLACE_ME_TURNSTILE_SITE_KEY_FROM_CLOUDFLARE_DASHBOARD`. The handoff prompt (Plan 04) instructs Nour to overwrite this with the real public site key from the Cloudflare Turnstile dashboard before Wave 2 verification. Using a recognizable placeholder (vs leaving the line blank) means the module-load validation in `src/lib/supabase.ts` (added in Plan 05) gets a truthy value, the dev server boots, but the Turnstile widget itself will fail to render with a clear Cloudflare-side error — that is the desired "loud failure" mode for an unconfigured env.

    Step 4 — Verify the final file has EXACTLY three `VITE_*` lines (URL, ANON_KEY, TURNSTILE_SITE_KEY) plus the header comment block that is already in the file (preserve verbatim — do NOT delete or rewrite the comment lines).

    Step 5 — Audit: confirm no value in the file matches the JWT shape of a service-role key — service-role JWTs have `"role":"service_role"` in the payload, anon JWTs have `"role":"anon"`. The existing anon key is correct shape; verify after editing that you did NOT swap in something with `service_role` in the payload. This is the SEC-04 guardrail (RESEARCH Pitfall 1).

    Do NOT touch `.env.example` (Phase 1 already wrote that template; it is the public-friendly version with all real values redacted, separately maintained).
  </action>
  <verify>
    <automated>set -o pipefail; F=".env.local" &amp;&amp; test -f "$F" &amp;&amp; if grep -qE '^VITE_SUPABASE_ANON_KEY=[[:space:]]' "$F"; then echo "FAIL: VITE_SUPABASE_ANON_KEY still has leading whitespace after the equals sign"; exit 1; fi &amp;&amp; grep -qE '^VITE_SUPABASE_URL=https://[a-z0-9]+\.supabase\.co$' "$F" &amp;&amp; grep -qE '^VITE_SUPABASE_ANON_KEY=eyJ' "$F" &amp;&amp; grep -qE '^VITE_TURNSTILE_SITE_KEY=' "$F" &amp;&amp; [ "$(grep -c '^VITE_' "$F")" = "3" ] &amp;&amp; if grep -q 'service_role' "$F"; then echo "FAIL: .env.local contains the string service_role — never allow service-role-shaped values in this file"; exit 1; fi &amp;&amp; echo "OK: .env.local has exactly 3 VITE_ lines, no leading whitespace on the anon key, no service_role string"</automated>
  </verify>
  <done>
    `.env.local` has three `VITE_*` lines in this order: `VITE_SUPABASE_URL=https://<canonical-host>.supabase.co`, `VITE_SUPABASE_ANON_KEY=eyJ...` (no whitespace between `=` and `eyJ`), `VITE_TURNSTILE_SITE_KEY=REPLACE_ME_TURNSTILE_SITE_KEY_FROM_CLOUDFLARE_DASHBOARD`. The host and the JWT `ref` claim agree (both reference the same Supabase project as the main SEALED-org repo). The header comment block from the original file is preserved. The file contains no string with `service_role` substring.
  </done>
</task>

<task type="auto">
  <name>Task 2: Amend REQUIREMENTS.md SEC-02 to the 3-attempt threshold</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (find the line that begins `- [ ] **SEC-02**:` under the `### Security & Spam Defense` heading — the current wording is on line 17 of the file)
    - .planning/phases/02-signup-flow/02-CONTEXT.md (D-05 — the full rationale paragraph for why 1 → 3: modern NAT, shared IPs, dorm/household density, Turnstile as primary defense)
    - .planning/phases/02-signup-flow/02-PATTERNS.md (§ "`.planning/REQUIREMENTS.md` (docs, n/a — text amendment)" — the exact amendment format including the sibling-note style)
  </read_first>
  <action>
    Locate the SEC-02 bullet under `### Security & Spam Defense`. Replace the body wording from `Server-side IP rate limit of 1 signup attempt per day per IP enforced in the Edge Function` to `Server-side IP rate limit of 3 signup attempts per rolling 24h per IP enforced in the Edge Function`. Append, on the same bullet, an italicised sibling note (matching the style of any existing in-line clarifications in REQUIREMENTS.md) reading `(Amended Phase 2 per 02-CONTEXT.md D-05 — rationale: NAT/shared-IP false positives; Turnstile remains the primary bot defense.)`.

    Do NOT change the requirement ID (`SEC-02` stays). Do NOT change the checkbox state (`- [ ]` stays). Do NOT touch any other requirement bullet. Do NOT modify the heading structure or the traceability table at the bottom of the file (the SEC-02 row in the traceability table can keep its existing "Pending" status — the amendment is in the wording only). Do NOT renumber or re-order. The diff must be exactly two edits: the threshold phrase and the appended sibling note.
  </action>
  <verify>
    <automated>set -o pipefail; F=".planning/REQUIREMENTS.md" &amp;&amp; test -f "$F" &amp;&amp; grep -q "SEC-02" "$F" &amp;&amp; grep -q "3 signup attempts per rolling 24h" "$F" &amp;&amp; grep -q "02-CONTEXT.md D-05" "$F" &amp;&amp; if grep -qE 'SEC-02.*1 signup attempt per day' "$F"; then echo "FAIL: old SEC-02 wording (1 attempt per day) still present"; exit 1; fi &amp;&amp; grep -qE 'SEC-02.*3 signup attempts per rolling 24h' "$F" &amp;&amp; echo "OK: SEC-02 amended to 3/24h with D-05 reference and old wording removed"</automated>
  </verify>
  <done>
    `.planning/REQUIREMENTS.md` SEC-02 reads `Server-side IP rate limit of 3 signup attempts per rolling 24h per IP enforced in the Edge Function` followed by `(Amended Phase 2 per 02-CONTEXT.md D-05 — rationale: NAT/shared-IP false positives; Turnstile remains the primary bot defense.)` on the same bullet. The old `1 signup attempt per day` wording is removed. The SEC-02 ID, checkbox state, and traceability table row are unchanged. No other requirement is touched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Install @marsidev/react-turnstile@1.5.2</name>
  <files>package.json</files>
  <read_first>
    - package.json (current `dependencies` block — verify `@marsidev/react-turnstile` is NOT already listed before adding it)
    - .planning/phases/02-signup-flow/02-RESEARCH.md (§ "Standard Stack > Core (NEW in Phase 2)" — confirms `1.5.2` is the exact version verified via `npm view @marsidev/react-turnstile version` on 2026-05-28; do not pick a different version range)
    - .planning/phases/02-signup-flow/02-CONTEXT.md (D-07 — lazy execute-on-submit mode is the chosen lifecycle; this library is selected specifically because it exposes the ref-based `.execute()` API)
  </read_first>
  <action>
    Run `npm install --save-exact @marsidev/react-turnstile@1.5.2` from the repo root. The `--save-exact` flag pins the version literally (no caret, no tilde) — Phase 2 RESEARCH §Standard Stack pinned this against the npm registry on 2026-05-28; allowing a range risks future minor versions breaking the ref API. After install: confirm `package.json` `dependencies` now contains `"@marsidev/react-turnstile": "1.5.2"` (literal, no `^` or `~`). If npm produces a `package-lock.json` (it should — Phase 1's plan 01 already committed one), confirm it lists `@marsidev/react-turnstile@1.5.2` with its transitive deps locked. Do NOT modify any other dependency. Do NOT bump any existing version. If the install fails (e.g., network issue), STOP and report the exact npm error to the user — do NOT proceed with a manual edit of `package.json` as a workaround (the lockfile and node_modules would drift).
  </action>
  <verify>
    <automated>set -o pipefail; F="package.json" &amp;&amp; test -f "$F" &amp;&amp; grep -q '"@marsidev/react-turnstile": "1.5.2"' "$F" &amp;&amp; test -d "node_modules/@marsidev/react-turnstile" &amp;&amp; node -e "const p=require('./node_modules/@marsidev/react-turnstile/package.json'); if(p.version!=='1.5.2'){console.error('FAIL: installed version', p.version); process.exit(1);} console.log('OK: @marsidev/react-turnstile@'+p.version+' installed');"</automated>
  </verify>
  <done>
    `package.json` `dependencies` lists `"@marsidev/react-turnstile": "1.5.2"` exactly (no `^`, no `~`). The package is unpacked at `node_modules/@marsidev/react-turnstile/` and its `package.json` reports `"version": "1.5.2"`. No other dependency version changed. The lockfile (if present) has the new entry.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time `.env.local` ↔ client bundle | Vite inlines every `VITE_*` variable as a string literal into the production JS bundle. Anything in `.env.local` with `VITE_` prefix is effectively public. |
| `.env.local` ↔ git | This file is in `.gitignore` (per `.env.example` precedent from Phase 1); it never reaches GitHub. But it lives on Nour's disk and on any future CI runner. |
| `package.json` / lockfile ↔ npm registry | `npm install` resolves names against the public registry; tampering with the registry could swap a malicious package under the same name. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information disclosure | Service-role key accidentally added with `VITE_` prefix during the `.env.local` edit | mitigate | Task 1 grep gate `if grep -q 'service_role' "$F"; then ... exit 1; fi` runs after every edit; refuses to mark the task done if the substring appears. Reinforces RESEARCH Pitfall 1. |
| T-02-02 | Tampering | Wrong Supabase host (the typo'd one) staying in place after the edit | mitigate | Task 1 step 1 forces a cross-check against the main repo's canonical `.env`; the only way the host gets written is by reading the canonical source. Mismatch between URL host and JWT `ref` would produce a DNS-shaped failure in Plan 05 verification — Plan 05's verification step explicitly checks that the function invoke returns a response rather than a name-resolution error. |
| T-02-03 | Tampering / supply-chain | Malicious package masquerading as `@marsidev/react-turnstile` | accept | npm registry namespace squatting is the residual risk. The `@marsidev` scope is a real namespace owned by the project maintainer (verified via the npm package page and GitHub repo cross-link). Pinning `--save-exact` to `1.5.2` (a version published 2026-05-05) avoids future-version drift. A lockfile commit (Phase 1's plan 01 established this) gives downstream agents a reproducible install. |
| T-02-04 | Repudiation | Requirements amendment without traceability to the rationale | mitigate | Task 2's appended sibling note `(Amended Phase 2 per 02-CONTEXT.md D-05 — rationale: NAT/shared-IP false positives; Turnstile remains the primary bot defense.)` creates an audit trail back to the decision document. Anyone reading SEC-02 in 6 months can find why it was relaxed from 1 to 3. |
| T-02-05 | Denial of service (self-inflicted) | Mismatch between REQUIREMENTS.md threshold and Edge Function code | mitigate | Plan 03 (Edge Function) reads the same `02-CONTEXT.md` D-05 and codes `RATE_LIMIT_THRESHOLD = 3`. Plan 03's verify step grep-checks the Edge Function source for `= 3` against the migration's check constraint outcomes; if Plan 03 codes `1` while Plan 01 amends to `3`, Plan 03's plan-checker review will surface the mismatch. The two plans share the same source-of-truth (D-05); no derived numbers. |
</threat_model>

<verification>
After all three tasks complete:

1. `.env.local` exists, has exactly 3 `VITE_*` lines, no leading whitespace on `VITE_SUPABASE_ANON_KEY=`, the URL host matches the JWT `ref` claim, contains `VITE_TURNSTILE_SITE_KEY=` (with either the real key or the placeholder).
2. `.planning/REQUIREMENTS.md` SEC-02 reads the 3/24h wording with the D-05 reference; the 1/day wording is gone.
3. `package.json` dependencies contains `"@marsidev/react-turnstile": "1.5.2"` and `node_modules/@marsidev/react-turnstile/package.json` reports the same.
4. No source file under `src/` is modified by this plan.
5. The grep gate `grep -q 'service_role' .env.local` returns nothing (SEC-04 guardrail).
</verification>

<success_criteria>
This plan is complete when:
- `.env.local` has exactly the three `VITE_*` lines (URL canonical, ANON_KEY whitespace-stripped, TURNSTILE_SITE_KEY present).
- `.planning/REQUIREMENTS.md` SEC-02 reads `3 signup attempts per rolling 24h` with the `02-CONTEXT.md D-05` reference; the old `1 signup attempt per day` text is gone.
- `package.json` pins `@marsidev/react-turnstile` at `1.5.2` (literal, no range operator), and `node_modules` confirms the install.
- No `src/` file is touched.
- The SEC-04 audit (no `service_role` string anywhere in `.env.local`) passes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-signup-flow/02-01-SUMMARY.md` with: the three before/after diffs (`.env.local` lines added/changed with the host placeholder noted, REQUIREMENTS.md SEC-02 wording change quoted, the new `package.json` line), the SEC-04 audit result ("no service_role string in .env.local"), and a one-line pointer that Plans 02, 03, 04 can now run in parallel (all Wave 1) and Plan 05 will consume these env vars + dependency after Nour confirms the cross-repo deploy.
</output>
