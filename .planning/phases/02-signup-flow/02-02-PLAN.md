---
phase: 02-signup-flow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0032_signup_attempts.sql
autonomous: true
requirements: [SEC-02, DB-03]
must_haves:
  truths:
    - "D-01: The SQL migration for `app_private.signup_attempts` lives in the main SEALED-org repo at `/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/`, NOT in this landing repo — the main app is the single source of truth for Supabase server-side code (mirrors Phase 1 D-03/D-04)"
    - "D-04: The migration creates `app_private.signup_attempts` with schema `(id bigserial pk, ip text not null, attempted_at timestamptz not null default now(), outcome text not null)` and a `check` constraint on `outcome` covering the five legal values from D-04 (`success`, `duplicate_unverified`, `duplicate_verified_no_letter`, `duplicate_verified_with_letter`, `db_error`)"
    - "D-04: A composite index `(ip, attempted_at desc)` exists so the rate-limit count query in Plan 03 (`select id where ip = ? and attempted_at > now() - interval '24h'`) is index-supported and stays sub-millisecond"
    - "D-04: The table is service-role-only — `revoke all from anon, authenticated, public` plus `grant select, insert, update, delete on table` and `grant usage, select on sequence app_private.signup_attempts_id_seq` to `service_role`; the anon role NEVER touches this table directly"
    - "D-04: The migration carries a header comment block citing 02-CONTEXT.md D-04, D-05, D-06 and notes the deferred pruning policy (~500k rows threshold) so the next maintainer knows why no prune cron ships with this migration"
    - "SEC-02 (amended) traceability: the migration's `check (outcome in (...))` and the index on `(ip, attempted_at desc)` together implement the storage side of the 3-attempts-per-IP-per-24h rule that Plan 03 enforces in code; the threshold value itself is not encoded in the migration (no `check (count <= 3)` constraint — that would require a trigger and is overkill per D-04 race-condition acceptance)"
  artifacts:
    - path: "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0032_signup_attempts.sql"
      provides: "DDL for the IP rate-limit ledger table, ready for Nour's SEALED-org agent to apply"
      contains: "create table app_private.signup_attempts"
      min_lines: 30
  key_links:
    - from: "app_private.signup_attempts"
      to: "service_role only"
      via: "revoke all from anon/authenticated/public + grant DML to service_role"
      pattern: "grant select, insert, update, delete on app_private.signup_attempts to service_role"
    - from: "signup_attempts_ip_attempted_at_idx"
      to: "Plan 03 rate-limit count query"
      via: "composite (ip, attempted_at desc) index supports `where ip = ? and attempted_at > ?`"
      pattern: "signup_attempts_ip_attempted_at_idx"
    - from: "outcome check constraint"
      to: "Plan 03 recordAttempt() call sites"
      via: "every value passed to .insert({ outcome }) must match one of the five legal values"
      pattern: "check \\(outcome in"
---

<objective>
Write the SQL migration `0032_signup_attempts.sql` into the sibling SEALED-org repo's `supabase/migrations/` folder, creating the `app_private.signup_attempts` table that Plan 03's Edge Function will read for rate-limit counts and insert into on every Turnstile-passing submission. The migration follows the sibling repo's mature conventions (4-digit sequential prefix, `app_private` schema, RLS enabled, revoke-from-anon-then-grant-to-service_role ordering, header comment block citing the landing-page CONTEXT decisions, `comment on table` description). This plan does NOT apply the migration — Nour's handoff to the SEALED-org agent (Plan 04) does that.

Purpose: Per D-04, IP rate-limit attempt records live in a dedicated ledger table rather than as a column on `waitlist_signups`, because (a) attempts include duplicates that don't create waitlist rows, (b) attempts include error outcomes for debugging, (c) the table is service-role-only — the anon client never sees it. Per the Phase 1 D-03/D-04 carry-forward, all schema lives in the main SEALED-org repo; this executor writes the file there but does not apply it. The next sequential number is `0032` — Phase 1 shipped `0031_waitlist_signups.sql`; verifying this before writing avoids overwriting the wrong file. Per RESEARCH §"Migration file naming + structure" the SQL body is already drafted verbatim and verified against `0031`'s conventions; this task transcribes that drafted SQL into the actual file path.

Output: A new file at `/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0032_signup_attempts.sql`. No file in this repo is modified by this plan. No `supabase db push` is invoked — the migration sits on disk in the sibling repo waiting for Nour's handoff in Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-signup-flow/02-CONTEXT.md
@.planning/phases/02-signup-flow/02-RESEARCH.md
@.planning/phases/02-signup-flow/02-PATTERNS.md
@.planning/phases/01-foundation/01-03-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Sibling-repo migration analogs. These files live in the OTHER repo — read them via normal Read tool against their absolute paths. -->

From /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0031_waitlist_signups.sql (Phase 1's sibling — same author, same subsystem):
- Header comment block style: multi-line, names the landing page sibling repo, cites the Phase's CONTEXT decision IDs and REQUIREMENTS IDs.
- `create table app_private.<name>` with explicit column types.
- One or more `create index` lines named `<table>_<col>[_<col>]_idx`.
- `alter table app_private.<name> enable row level security`.
- `revoke all on app_private.<name> from anon, authenticated, public`.
- `grant select, insert, update, delete on app_private.<name> to service_role`.
- `comment on table app_private.<name> is '...'` describing role and accessor matrix.
- NO `BEGIN;` / `COMMIT;` wrapper — Supabase wraps each migration in its own transaction.
- For `uuid` primary keys, no sequence grant is needed. For `bigserial` (Phase 2's case), an additional `grant usage, select on sequence ... to service_role` line IS required.

From the migrations directory listing (last verified 2026-05-28):
- Highest existing migration: `0031_waitlist_signups.sql`.
- Next sequential number: `0032`.
- If anything `0032_*` already exists when this plan runs, STOP and ask Nour (do NOT overwrite).

Outcome value vocabulary (locked by 02-CONTEXT.md D-04 line 61):
- 'success'
- 'duplicate_unverified'
- 'duplicate_verified_no_letter'
- 'duplicate_verified_with_letter'
- 'db_error'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify next migration number is 0032 then write 0032_signup_attempts.sql in the sibling repo</name>
  <files>/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0032_signup_attempts.sql</files>
  <read_first>
    - /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0031_waitlist_signups.sql (the immediate sibling — copy the comment-block prose style, the revoke→grant ordering, the comment-on-table style; this is the closest analog because same author + same subsystem)
    - .planning/phases/02-signup-flow/02-RESEARCH.md (§ "Migration file naming + structure" lines 1004-1045 — the migration text is drafted there verbatim and verified against 0031; the bigserial sequence grant, the index direction `(ip, attempted_at desc)`, and the check-constraint outcome vocabulary are all locked there)
    - .planning/phases/02-signup-flow/02-PATTERNS.md (§ "`supabase/migrations/0032_signup_attempts.sql` (migration, CRUD ledger)" — explains which lines come from 0031 and what the bigserial-specific additions are)
    - .planning/phases/02-signup-flow/02-CONTEXT.md (D-04, D-05, D-06 — outcome vocabulary, rate-limit threshold rationale, attempt-counting policy)
    - /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/ (run `ls` to CONFIRM the highest existing migration is still `0031_*.sql` before writing `0032_*.sql` — if a `0032_*` already exists, STOP and report to Nour; do NOT overwrite)
  </read_first>
  <action>
    Step 1 — Verify the sequential number. Run `ls "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/" | tail -3` and confirm the highest existing file is `0031_waitlist_signups.sql`. If any `0032_*.sql` already exists, STOP — do NOT overwrite — print the conflict and ask Nour for instructions.

    Step 2 — Write a new file at the exact path `/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0032_signup_attempts.sql`. The content must include, in this order:

    (a) A multi-line header comment block (5-7 `--` lines) matching the prose style of `0031`. It must: identify the file by name; name the landing-page sibling repo as the reason it exists; cite `02-CONTEXT.md D-04, D-05, D-06`; cite `REQUIREMENTS.md SEC-02 (amended Phase 2 per D-05 — 3 attempts per IP per rolling 24h)`; mention that pruning is deferred to ~500k rows (no prune cron in Phase 2). Match `0031`'s lowercase-keyword + sentence-style prose; do not use marketing language.

    (b) `create table app_private.signup_attempts (` followed by four column declarations:
        - `id bigserial primary key,`
        - `ip text not null,`
        - `attempted_at timestamptz not null default now(),`
        - `outcome text not null check (outcome in ('success', 'duplicate_unverified', 'duplicate_verified_no_letter', 'duplicate_verified_with_letter', 'db_error'))`
      Close with `);`. Match `0031`'s 2-space indent inside the parens, lowercase keywords, trailing comma absent on the last column.

    (c) `create index signup_attempts_ip_attempted_at_idx on app_private.signup_attempts (ip, attempted_at desc);` — the `desc` is intentional so the rate-limit query (`order by attempted_at desc limit ...` semantics) is index-direction-aligned. Index naming matches `0031`'s `<table>_<col>[_<col>]_idx` style.

    (d) `alter table app_private.signup_attempts enable row level security;`

    (e) A short `--` comment line stating that Plan 03's `join-waitlist` Edge Function is the only consumer (service-role only), followed by:
        - `revoke all on app_private.signup_attempts from anon, authenticated, public;`
        - `grant select, insert, update, delete on app_private.signup_attempts to service_role;`
        - `grant usage, select on sequence app_private.signup_attempts_id_seq to service_role;`
      The sequence grant is mandatory because `bigserial` creates an implicit sequence that requires its own grant for `service_role` to insert (this is the bigserial-specific addition vs `0031`'s uuid PK, called out in PATTERNS § "Phase 2 extras").

    (f) `comment on table app_private.signup_attempts is` followed by a single-quoted prose description (one statement, broken across two string literals concatenated with whitespace in the SQL idiom — match `0031` line 33-34's style). The text must mention: (1) "Phase 2 IP rate limit ledger", (2) "3 attempts per IP per rolling 24h enforced by supabase/functions/join-waitlist", (3) "service_role: full DML; anon/authenticated/public: revoked", (4) "Pruning deferred until ~500k rows".

    (g) A trailing multi-line `--` comment block documenting the race-condition acceptance (RESEARCH Pattern 2 note): two concurrent submissions from the same IP within milliseconds may both pass the count check; Turnstile is the primary defense; strict serialization is overkill per D-05 rationale.

    Match `0031` indentation exactly (lowercase, 2-space indent inside parens, blank lines between major sections). Do NOT wrap in `BEGIN;` / `COMMIT;` (Supabase migrations don't use them). Do NOT add Supabase-CLI metadata headers. Do NOT execute the migration (no `supabase db push`, no SQL connection). Do NOT touch any other file in either repo.
  </action>
  <verify>
    <automated>set -o pipefail; MIG="/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0032_signup_attempts.sql" &amp;&amp; test -f "$MIG" &amp;&amp; grep -q "create table app_private.signup_attempts" "$MIG" &amp;&amp; grep -q "id bigserial primary key" "$MIG" &amp;&amp; grep -q "ip text not null" "$MIG" &amp;&amp; grep -q "attempted_at timestamptz not null default now()" "$MIG" &amp;&amp; grep -q "outcome text not null check (outcome in" "$MIG" &amp;&amp; grep -q "'success'" "$MIG" &amp;&amp; grep -q "'duplicate_unverified'" "$MIG" &amp;&amp; grep -q "'duplicate_verified_no_letter'" "$MIG" &amp;&amp; grep -q "'duplicate_verified_with_letter'" "$MIG" &amp;&amp; grep -q "'db_error'" "$MIG" &amp;&amp; grep -q "create index signup_attempts_ip_attempted_at_idx on app_private.signup_attempts (ip, attempted_at desc)" "$MIG" &amp;&amp; grep -q "alter table app_private.signup_attempts enable row level security" "$MIG" &amp;&amp; grep -q "revoke all on app_private.signup_attempts from anon, authenticated, public" "$MIG" &amp;&amp; grep -q "grant select, insert, update, delete on app_private.signup_attempts to service_role" "$MIG" &amp;&amp; grep -q "grant usage, select on sequence app_private.signup_attempts_id_seq to service_role" "$MIG" &amp;&amp; grep -q "comment on table app_private.signup_attempts" "$MIG" &amp;&amp; grep -q "02-CONTEXT.md" "$MIG" &amp;&amp; grep -qi "D-04" "$MIG" &amp;&amp; grep -qi "D-05" "$MIG" &amp;&amp; grep -qi "D-06" "$MIG" &amp;&amp; grep -qiE "prune|pruning" "$MIG" &amp;&amp; if grep -qiE 'begin[[:space:]]*;|commit[[:space:]]*;' "$MIG"; then echo "FAIL: migration wraps in BEGIN/COMMIT — sibling repo convention forbids this"; exit 1; fi &amp;&amp; if grep -iqE 'grant[^;]*to[[:space:]]+(anon|authenticated|public)' "$MIG"; then echo "FAIL: a public/anon/authenticated grant snuck in — table must be service_role only"; exit 1; fi &amp;&amp; echo "OK: 0032 migration present in sibling repo with all required clauses; service_role-only; no BEGIN/COMMIT; no public grants"</automated>
  </verify>
  <done>
    `/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0032_signup_attempts.sql` exists. Contains: a header comment block citing 02-CONTEXT.md D-04/D-05/D-06 and the SEC-02 amendment + pruning policy; `create table app_private.signup_attempts (id bigserial primary key, ip text not null, attempted_at timestamptz not null default now(), outcome text not null check (outcome in (...)))` with all five outcome values; the `(ip, attempted_at desc)` composite index; RLS enabled; revoke-from-(anon,authenticated,public) + grant-DML-to-service_role + grant-sequence-to-service_role; `comment on table` description; race-condition acceptance comment. Naming follows the sibling repo's 4-digit-prefix snake_case convention. No `BEGIN;`/`COMMIT;` wrapper. No grants to non-service-role roles. No other file in either repo is modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| This repo ↔ Sibling SEALED-org repo (filesystem) | This executor writes a single file into the sibling repo's `supabase/migrations/` folder. The sibling repo's agent (invoked via Nour's handoff in Plan 04) is responsible for applying the migration. |
| `app_private.signup_attempts` ↔ anon role (after migration applied) | Anon is explicitly REVOKED from the table. There is no view layer exposing this table (unlike `waitlist_signups` which has `public.signup_counter`). This table is server-internal. |
| `app_private.signup_attempts` ↔ service-role (after migration applied) | Full DML + sequence usage. Plan 03's Edge Function uses `adminClient()` (service-role) to read counts and insert rows. |
| Migration text ↔ Audit trail | The header comment block embeds the CONTEXT decision IDs, making the migration's reason traceable from the file alone. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Elevation of privilege | Anon client reading or writing `app_private.signup_attempts` directly | mitigate | Migration revokes ALL from anon/authenticated/public and grants only to service_role. The table has no public view (unlike `waitlist_signups`'s `signup_counter`). The grep gate in Task 1 fails if any `grant ... to (anon|authenticated|public)` line slips in. Acceptance verification (Plan 04 handoff agent) confirms `select * from app_private.signup_attempts` as anon returns permission-denied. |
| T-02-07 | Information disclosure | Outcome enum leaking signup state information to a privileged-but-curious operator | accept | The five outcome values describe the function's branch taken. Anyone with service-role access already has direct read on `waitlist_signups`, `auth.users`, and `letters`; the outcome enum doesn't expose anything they don't already know. Pruning at ~500k rows (deferred) bounds long-term exposure. |
| T-02-08 | Tampering | Wrong outcome value inserted (typo) bypassing the check constraint | mitigate | The `check (outcome in (...))` constraint on the column is the source of truth for the legal values. Plan 03's `recordAttempt()` function uses a TypeScript union of the same five strings; a typo in the function would be caught at compile time AND rejected at DB insert time. Two-layer defense. |
| T-02-09 | Denial of service | Single IP spamming attempts to bloat the table indefinitely | mitigate (partially) | Plan 03's threshold of 3 attempts per IP per 24h is the primary throttle. The race-condition acceptance comment in the migration documents the bounded-overflow tradeoff. Pruning (deferred at ~500k rows) is the long-term mitigation. Per D-05, this is acceptable because Turnstile is the primary bot defense — sustained attack would also need to defeat Turnstile. |
| T-02-10 | Repudiation | Migration applied without traceability | mitigate | Header comment block cites 02-CONTEXT.md D-04/D-05/D-06 and the SEC-02 amendment. The handoff prompt (Plan 04) asks the SEALED-org agent to record in its own planning system that the migration originated from this landing repo. Audit trail preserved in both repos. |
</threat_model>

<verification>
After this plan's single task completes:

1. `test -f "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0032_signup_attempts.sql"` — file exists in sibling repo.
2. `ls "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/" | grep -c "^0032"` — prints `1`.
3. All grep gates in Task 1's automated verify pass (table, columns, check constraint, index, RLS, revoke, grants, sequence grant, comment, no BEGIN/COMMIT, no public grants).
4. No file in THIS repo is modified by this plan.
5. No `supabase db push` was invoked; no Supabase connection was opened; the migration sits on disk in the sibling repo only.
</verification>

<success_criteria>
This plan is complete when:
- `/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/migrations/0032_signup_attempts.sql` exists with all required DDL (table with check constraint, index, RLS, revoke, three grants including the sequence grant, comment on table, race-condition acceptance comment).
- The header comment block traces back to 02-CONTEXT.md D-04/D-05/D-06 and the SEC-02 amendment + pruning deferral.
- Naming and indentation match `0031_waitlist_signups.sql` conventions.
- No other file in either repo is modified.
- No schema change was applied by this executor — the migration is staged for Plan 04's handoff.
</success_criteria>

<output>
After completion, create `.planning/phases/02-signup-flow/02-02-SUMMARY.md` with: the migration file path in the sibling repo, the table's column list with types, the index name and direction, the grant matrix (anon: revoked / service_role: full DML + sequence usage), the five outcome values from the check constraint, confirmation that no other file was modified, and a one-line note that Plan 03 (Edge Function) consumes this table via service-role client and Plan 04 (handoff) will deliver the migration to Nour for apply.
</output>
