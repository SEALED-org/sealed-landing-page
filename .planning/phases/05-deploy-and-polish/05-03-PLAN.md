---
phase: 05-deploy-and-polish
plan: 03
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - src/components/WaitlistForm.tsx
  - src/components/FirstLetter.tsx
  - src/index.css
autonomous: true
requirements: [CONTENT-02, CONTENT-03, CONTENT-04, CONTENT-05, CONTENT-06]
must_haves:
  truths:
    - "Both email inputs have a programmatic accessible name via an associated <label> or aria-label (CONTENT-04)"
    - "Keyboard tabbing through links, buttons, inputs, and the textarea shows a visible focus ring on each (CONTENT-05)"
    - "Pointer (mouse) clicks do not show the keyboard focus ring, preserving the locked visual design"
    - "Form error states already display inline visible messages via aria-live (CONTENT-06 confirmed) and the paper texture is self-hosted (CONTENT-03 confirmed) — no external CDN image refs"
    - "The Dr. Gail Matthews citation in the Research section reads as the verified figure (+42%, Dominican University of California, 2015) — confirmed accurate by Nour (CONTENT-02, D-12), no edit needed"
  artifacts:
    - path: "src/index.css"
      provides: ":focus-visible ring + .sr-only visually-hidden utility"
      contains: "focus-visible"
    - path: "src/components/WaitlistForm.tsx"
      provides: "Waitlist email input with an associated label"
      contains: "label"
  key_links:
    - from: "WaitlistForm email input"
      to: "its <label>"
      via: "htmlFor/id pairing or aria-label"
      pattern: "htmlFor=|aria-label="
    - from: "src/index.css :focus-visible rule"
      to: "interactive elements"
      via: "outline ring shown for keyboard focus only"
      pattern: ":focus-visible"
---

<objective>
Close the accessibility gaps: give both email inputs a real programmatic label (CONTENT-04), add a keyboard-only `:focus-visible` ring across interactive elements without altering the pointer-click look (CONTENT-05), and confirm the already-met CONTENT-06 (inline error messages), CONTENT-03 (self-hosted paper texture), and CONTENT-02 (Matthews citation) hold.

Purpose: Today zero `<label>` elements exist (inputs rely on `placeholder` only) and `outline: none` is set globally on inputs with only one `:focus` replacement — keyboard users get no visible focus on the textarea, the FirstLetter email field, buttons, or links (RESEARCH §Accessibility). These are real, grep-verified gaps. Fixes stay within the locked visual design (visually-hidden labels + `:focus-visible`), which is why `--skip-ui` is acceptable here. CONTENT-02/03/06 were met in prior phases and are confirmed (not re-edited) here per D-09/D-12.
Output: labels added in `WaitlistForm.tsx` and `FirstLetter.tsx`; `.sr-only` utility + `:focus-visible` ring + restored input focus rings in `src/index.css`; confirm-only assertions for CONTENT-02/03/06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-deploy-and-polish/05-CONTEXT.md
@.planning/phases/05-deploy-and-polish/05-RESEARCH.md

<interfaces>
<!-- Verified accessibility state (RESEARCH §Accessibility audit). -->
Email inputs needing a label:
  WaitlistForm.tsx ~line 75: <input type="email" placeholder="Your email address" ...> — NO id, NO label
  FirstLetter.tsx ~line 502: <input type="email" id="fl-email-input" placeholder="your@email.com" ...> — has id, NO label;
    a visible "To" label exists as <span className="fl-address-to-label"> (line ~501)
  FirstLetter.tsx ~line 230: <textarea className="fl-textarea" id="fl-textarea" ...> — NO accessible name

outline:none declarations to pair with :focus-visible (verified line numbers):
  index.css:287  form.waitlist input[type=email]  { outline: none; }   (has a :focus replacement at 289-292)
  index.css:852  textarea.fl-textarea             { outline: none; }   (NO focus replacement)
  index.css:963  .fl-email-field                  { outline: none; }   (NO focus replacement)

CONTENT-06 (met, confirm-only): aria-live="polite" error slots in WaitlistForm (waitlist-error-slot),
  FirstLetter (fl-error-slot), WaitlistSuccessCard, Counter.
CONTENT-03 (met, confirm-only): paper texture at public/assets/paper-light.jpg; zero i.postimg.cc refs.
CONTENT-02 (met, confirm-only): Research-section citation = "+42%, n=267, Dominican University of California, 2015"
  confirmed accurate by Nour (D-12). The Research-section copy lives in src/App.tsx or a ResearchSection component.

<!-- NOTE: this plan also touches FirstLetter.tsx and index.css, which Plan 05-02 edits.
     Sequenced into Wave 2 (depends_on 05-02) so there is no same-wave file conflict. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add accessible labels to both email inputs and the letter textarea (CONTENT-04)</name>
  <files>src/components/WaitlistForm.tsx, src/components/FirstLetter.tsx</files>
  <read_first>
    - src/components/WaitlistForm.tsx lines 68-114 (the form + email input at line 75)
    - src/components/FirstLetter.tsx lines 230-258 (textarea) and 499-525 (email input + "To" span)
    - .planning/phases/05-deploy-and-polish/05-RESEARCH.md §"Accessibility (CONTENT-04 / CONTENT-05) — concrete audit"
    - src/index.css (to confirm the .sr-only utility added in Task 2 exists for visually-hidden labels)
  </read_first>
  <action>Give each control a programmatic accessible name without changing the visible design. For the WaitlistForm email input (line ~75): add `id="waitlist-email"` and a visually-hidden `<label htmlFor="waitlist-email" className="sr-only">Email address</label>` immediately before the input inside the form (the label is hidden via the `.sr-only` utility added in Task 2). For the FirstLetter email input (`id="fl-email-input"`, line ~502): the visible `.fl-address-to-label` "To" span is not a sufficient name; add `aria-label="Email address"` to the input (or wire `aria-labelledby` to the existing label spans) — `aria-label` is the lowest-risk choice that does not alter layout. For the FirstLetter textarea (`id="fl-textarea"`, line ~230): add `aria-label="Write your letter"`. Do not change placeholder text, layout, or any handler.</action>
  <verify>
    <automated>cd "$REPO" && grep -q 'htmlFor="waitlist-email"\|aria-label="Email address"' src/components/WaitlistForm.tsx && grep -q 'aria-label="Email address"\|aria-labelledby' src/components/FirstLetter.tsx && grep -q 'aria-label="Write your letter"' src/components/FirstLetter.tsx && npm run build >/dev/null 2>&1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - WaitlistForm email input has an associated `<label htmlFor="waitlist-email">` (with matching `id="waitlist-email"`) OR an `aria-label`
    - FirstLetter `#fl-email-input` has `aria-label="Email address"` (or `aria-labelledby` pointing at a real element)
    - FirstLetter `#fl-textarea` has `aria-label="Write your letter"`
    - No visible placeholder text or layout changed
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Both email inputs and the letter textarea expose a programmatic accessible name; nothing visible changed; build is green.</done>
</task>

<task type="auto">
  <name>Task 2: Add .sr-only utility and keyboard-only :focus-visible rings (CONTENT-05)</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css lines 283-292 (waitlist input outline:none + its :focus replacement)
    - src/index.css lines 850-857 (textarea.fl-textarea outline:none)
    - src/index.css lines 962-969 (.fl-email-field outline:none)
    - src/index.css top @layer utilities / @theme region (where .text-balance lives) to co-locate .sr-only
    - .planning/phases/05-deploy-and-polish/05-RESEARCH.md §"Pattern 4: Visible focus ring"
  </read_first>
  <action>Add two things to `src/index.css`. (1) A `.sr-only` visually-hidden utility (the standard clip-rect pattern: position absolute, 1px width/height, overflow hidden, clip, white-space nowrap) co-located with the existing `.text-balance` utility — used by the WaitlistForm label from Task 1. (2) A keyboard-only focus ring using `:focus-visible` applied to interactive elements: `:where(a, button, input, textarea, [tabindex]):focus-visible { outline: 2px solid #000; outline-offset: 2px; }`. Because three inputs set `outline: none` unconditionally (index.css:287, 852, 963), ensure the `:focus-visible` rule still wins for keyboard focus — for the two inputs that currently have NO focus replacement (`textarea.fl-textarea` at 852, `.fl-email-field` at 963), the `:focus-visible` ring now provides their keyboard indicator. Keep the existing waitlist `:focus` border/shadow (289-292) as the pointer affordance; the `:focus-visible` ring adds the keyboard ring on top without removing the locked pointer styling. On the dark FirstLetter background, a black ring may be low-contrast — add a scoped override so `.fl-textarea:focus-visible, .fl-email-field:focus-visible` use a light ring (e.g. `outline-color: #fff` or a white-alpha) for visibility against dark. Do not change any pointer-state (`:focus` without `-visible`) appearance.</action>
  <verify>
    <automated>cd "$REPO" && grep -q "focus-visible" src/index.css && grep -q "sr-only" src/index.css && npm run build >/dev/null 2>&1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/index.css` contains a `.sr-only` rule (clip-based visually-hidden)
    - `src/index.css` contains a `:focus-visible` ring rule covering a, button, input, textarea, and `[tabindex]`
    - A scoped `:focus-visible` override gives `.fl-textarea` / `.fl-email-field` a light-colored ring for the dark background
    - The existing `form.waitlist input[type=email]:focus` pointer styling (lines 289-292) is unchanged
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Keyboard tabbing shows a visible ring on links, buttons, both inputs, and the textarea (light ring on dark surfaces); mouse clicks keep the locked look; `.sr-only` supports the hidden label.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm CONTENT-06 (inline errors), CONTENT-03 (self-hosted texture), CONTENT-02 (Matthews citation)</name>
  <files>src/components/WaitlistForm.tsx, src/index.css</files>
  <read_first>
    - src/components/WaitlistForm.tsx lines 115-130 (waitlist-error-slot with aria-live="polite")
    - src/components/FirstLetter.tsx lines 526-553 (fl-error-slot with aria-live="polite")
    - src/App.tsx (Research section copy — locate the Matthews citation; or the ResearchSection component if separate)
    - .planning/phases/05-deploy-and-polish/05-CONTEXT.md §D-08, §D-09, §D-11, §D-12, §CONTENT-03
  </read_first>
  <action>This is a confirm-only / no-regression task — CONTENT-06, CONTENT-03, and CONTENT-02 were met in prior phases (per D-09, D-12, and the CONTEXT confirmation). Do NOT add new UI and do NOT edit the locked copy (D-11/D-12). Verify by assertion: (a) the inline error slots remain present with `aria-live="polite"` (WaitlistForm `waitlist-error-slot`, FirstLetter `fl-error-slot`); (b) no external image CDN reference (`i.postimg.cc` or any non-local image host) exists anywhere in `src/`, `index.html`, `verify.html`, `public/privacy.html`, or `public/terms.html`; (c) the Research-section Dr. Gail Matthews citation reads as the verified figure — locate the citation in `src/App.tsx` (or the Research component) and confirm it states the +42% / Dominican University of California / 2015 figure that Nour verified (D-12). If the error slots were accidentally removed by Task 1/2, restore them; if an external image CDN reference is found, replace it with the self-hosted `public/assets/` equivalent. If the citation text differs from the verified figure, surface the discrepancy in the SUMMARY rather than silently editing locked copy. Record all three confirmations (CONTENT-06, CONTENT-03, CONTENT-02) in the SUMMARY.</action>
  <verify>
    <automated>cd "$REPO" && grep -q 'aria-live="polite"' src/components/WaitlistForm.tsx && grep -q 'aria-live="polite"' src/components/FirstLetter.tsx && test "$(grep -rn 'i.postimg.cc' src/ index.html verify.html public/ 2>/dev/null | wc -l | tr -d ' ')" = "0" && grep -rqi 'Matthews' src/ && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `aria-live="polite"` error slots still present in WaitlistForm.tsx and FirstLetter.tsx
    - `grep -rn "i.postimg.cc" src/ index.html verify.html public/` returns no matches
    - No external (non-local) image host referenced for the paper texture; it loads from `public/assets/paper-light.jpg`
    - The Matthews citation is located in source and confirmed to match the verified figure (D-12); any discrepancy is surfaced in the SUMMARY (not silently edited)
  </acceptance_criteria>
  <done>Inline aria-live error display confirmed intact (CONTENT-06); zero external CDN image refs confirmed (CONTENT-03); Matthews citation confirmed accurate (CONTENT-02, D-12); all three documented in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard user → DOM focus | Focus indicators must be perceivable for non-pointer users (accessibility/legal surface) |
| build → CDN (dist/) | Image assets must be self-hosted, not fetched from an untrusted third-party CDN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-A11Y | Repudiation / compliance | form inputs + interactive elements | mitigate | Real `<label>`/`aria-label` + `:focus-visible` rings; lack of these is an accessibility-compliance exposure, not a code exploit. Verified by source assertions. |
| T-05-CDN | Tampering / Information Disclosure | external image CDN | accept (verified absent) | CONTENT-03 already met — zero `i.postimg.cc` refs; texture is self-hosted in `public/assets/`. Task 3 asserts this still holds. An external CDN image could be swapped/tracked by a third party; self-hosting removes that boundary. |
| T-05-SC | Tampering | npm/pip/cargo installs | n/a | No package installs. CONTENT-02/03/04/05/06 are HTML/CSS-only. The dist/ secret-leak gate is built in Plan 04. |
</threat_model>

<verification>
- `npm run build` exits 0.
- Both inputs + textarea carry a programmatic accessible name; `.sr-only` + `:focus-visible` present in index.css.
- aria-live error slots intact; zero external CDN image refs; Matthews citation confirmed.
- Keyboard-tab spot check (Plan 05 human-verify) shows visible focus rings; mouse click does not.
</verification>

<success_criteria>
- CONTENT-04 satisfied: every form input has an associated label / accessible name.
- CONTENT-05 satisfied: visible keyboard focus rings on all interactive elements, locked pointer look preserved.
- CONTENT-06 + CONTENT-03 + CONTENT-02 confirmed still met (no regression, no locked-copy edit).
</success_criteria>

<output>
Create `.planning/phases/05-deploy-and-polish/05-03-SUMMARY.md` when done.
</output>
