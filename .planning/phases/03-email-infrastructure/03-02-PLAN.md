---
phase: 03-email-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/notify/emails/WaitlistConfirmationEmail.tsx
  - /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/notify/emails/SealLetterEmail.tsx
  - /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/deno.json
autonomous: true
requirements:
  - EMAIL-A2
  - EMAIL-B1
  - EMAIL-03

must_haves:
  truths:
    - "WaitlistConfirmationEmail renders with cream background (#faf6ef), Georgia serif, S E A L E D wordmark header, and D-04 body copy — no images, no web fonts"
    - "SealLetterEmail renders with the same aesthetic plus a dark-ink CTA Link to verifyUrl and '7 days' expiry note"
    - "Neither template uses dangerouslySetInnerHTML"
    - "join-waitlist/deno.json includes react@18.3.1, @react-email/components@0.0.22 (NOT 0.0.23+), and compilerOptions for JSX"
    - "Templates export named components (not default) matching the DeliveryLetterEmail.tsx pattern"
  artifacts:
    - path: "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/notify/emails/WaitlistConfirmationEmail.tsx"
      provides: "Template 1A — Path A waitlist confirmation"
      contains: "backgroundColor: '#faf6ef'"
    - path: "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/notify/emails/SealLetterEmail.tsx"
      provides: "Template 1B — Path B verify-to-seal"
      contains: "verifyUrl"
    - path: "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/deno.json"
      provides: "Import map extension for React Email rendering in join-waitlist"
      contains: "@react-email/components@0.0.22"
  key_links:
    - from: "join-waitlist/index.ts (Plan 03)"
      to: "WaitlistConfirmationEmail.tsx"
      via: "import { WaitlistConfirmationEmail } from '../notify/emails/WaitlistConfirmationEmail.tsx'"
      pattern: "WaitlistConfirmationEmail"
    - from: "join-waitlist/index.ts (Plan 03)"
      to: "SealLetterEmail.tsx"
      via: "import { SealLetterEmail } from '../notify/emails/SealLetterEmail.tsx'"
      pattern: "SealLetterEmail"
---

<objective>
Create the two React Email templates (1A and 1B) in notify/emails/ and extend join-waitlist/deno.json
so the function can import and render them.

Purpose: Plan 03 wires the sends; it needs the template components to exist and the import map
to include react + @react-email/components before it can compile. Both templates must pass
mail-tester >= 9/10 — they achieve this through pure auth headers (DKIM/SPF/DMARC), websafe
Georgia serif, no images, no web fonts, and Resend's automatic plain-text generation.

Output: Three files — WaitlistConfirmationEmail.tsx, SealLetterEmail.tsx, updated deno.json.
</objective>

<execution_context>
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/03-email-infrastructure/03-CONTEXT.md
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/03-email-infrastructure/03-RESEARCH.md
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/03-email-infrastructure/03-PATTERNS.md
</execution_context>

<context>
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/ROADMAP.md

<interfaces>
<!-- From DeliveryLetterEmail.tsx — the exact template structure to clone -->

Import block (lines 1-4):
  import { Body, Container, Head, Html, Preview, Section, Text, Link } from '@react-email/components';
  import * as React from 'react';
  (1A omits Link; 1B includes Link)

Props type pattern (lines 6-10):
  export type DeliveryLetterEmailProps = { bodyText: string; composedOn: string; appLetterUrl: string; };
  1A: no props — use Record<string, never> or export type WaitlistConfirmationEmailProps = Record<string, never>
  1B: export type SealLetterEmailProps = { verifyUrl: string; }

Component: named export (NOT default export)
  export const WaitlistConfirmationEmail = () => (...)
  export const SealLetterEmail = ({ verifyUrl }: SealLetterEmailProps) => (...)

Body + Container anchor (lines 16-18):
  <Body style={{ backgroundColor: '#faf6ef', fontFamily: 'Georgia, "Times New Roman", serif' }}>
    <Container style={{ padding: '48px 32px', maxWidth: 560 }}>

Muted text style: color: '#7a716a', fontSize: 13
Body text style: color: '#3a342e', fontSize: 16, lineHeight: 1.7
Wordmark style: color: '#3a342e', fontSize: 13, letterSpacing: '0.15em', opacity: 0.45

Security comment from analog (line 19):
  {/* T-02-07 mitigation: <Text> auto-escapes; never use dangerouslySetInnerHTML for letter content. */}
  Apply same comment in 1B at the verifyUrl Link site.

From notify/deno.json (current full file):
  { "imports": { "react": "npm:react@18.3.1", "@react-email/components": "npm:@react-email/components@0.0.22",
    "@supabase/supabase-js": "npm:@supabase/supabase-js@2.103.2" },
    "compilerOptions": { "jsx": "react-jsx", "jsxImportSource": "react" } }

From join-waitlist/deno.json (current full file — only has supabase-js):
  { "imports": { "@supabase/supabase-js": "npm:@supabase/supabase-js@2.103.2" } }
  Add react + @react-email/components + compilerOptions to match notify/deno.json shape exactly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WaitlistConfirmationEmail.tsx (Template 1A)</name>
  <files>/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/notify/emails/WaitlistConfirmationEmail.tsx</files>

  <read_first>
    1. /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/notify/emails/DeliveryLetterEmail.tsx
       — Full file. This is the exact template to clone structurally. Read the import block,
         props type, Body/Container inline styles, Section structure, Text styles. Use these
         values verbatim — do not invent new style values.
    2. /Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/03-email-infrastructure/03-CONTEXT.md
       — D-01 (cream aesthetic), D-02 (wordmark, no images), D-03 (minimal voice), D-04 (exact approved copy for 1A).
    3. /Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/03-email-infrastructure/03-PATTERNS.md
       — Section "WaitlistConfirmationEmail.tsx" — what 1A replaces/adds vs the analog.
  </read_first>

  <action>
    Create /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/notify/emails/WaitlistConfirmationEmail.tsx.

    File structure (clone DeliveryLetterEmail.tsx, adapt as specified):

    Imports: Body, Container, Head, Html, Preview, Section, Text from '@react-email/components' (omit Link — 1A has no link).
    Also: import * as React from 'react';

    Props type: export type WaitlistConfirmationEmailProps = Record<string, never>;

    Component: export const WaitlistConfirmationEmail = () => (...)
    No default export — named export only, matching the analog.

    JSX structure:
    - Html > Head / (Preview: "You're on the SEALED waitlist.") / Body
    - Body style: backgroundColor '#faf6ef', fontFamily 'Georgia, "Times New Roman", serif'
    - Container style: padding '48px 32px', maxWidth 560
    - Section 1 — wordmark header (D-02):
        Text style: color '#3a342e', fontSize 13, letterSpacing '0.15em', opacity 0.45
        Content: S E A L E D
    - Section 2 style marginTop 32 — body copy (D-04 exact):
        Text 1: color '#3a342e', fontSize 16, lineHeight 1.7 — "You're on the waitlist."
        Text 2: color '#3a342e', fontSize 16, lineHeight 1.7 — "Nothing to do now. We'll email you when SEALED opens in 2027."
    - Section 3 style marginTop 48 — sign-off:
        Text: color '#7a716a', fontSize 13 — "— SEALED"

    Voice rules (D-03): no em-dashes, no fluff. Copy is already locked in D-04 — use it verbatim.
    No images anywhere. No web fonts (Georgia is websafe). No dangerouslySetInnerHTML.
  </action>

  <verify>
    <automated>
      grep -c "backgroundColor: '#faf6ef'" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/notify/emails/WaitlistConfirmationEmail.tsx"
      # Must output 1

      grep -c "S E A L E D" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/notify/emails/WaitlistConfirmationEmail.tsx"
      # Must output 1

      grep -c "You're on the waitlist" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/notify/emails/WaitlistConfirmationEmail.tsx"
      # Must output 1

      grep -c "dangerouslySetInnerHTML" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/notify/emails/WaitlistConfirmationEmail.tsx"
      # Must output 0

      grep -c "export default" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/notify/emails/WaitlistConfirmationEmail.tsx"
      # Must output 0 (named export only)

      grep -c "export const WaitlistConfirmationEmail" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/notify/emails/WaitlistConfirmationEmail.tsx"
      # Must output 1
    </automated>
  </verify>

  <acceptance_criteria>
    Source assertions:
    - File exists at notify/emails/WaitlistConfirmationEmail.tsx
    - Contains "backgroundColor: '#faf6ef'" (cream paper per D-01)
    - Contains "Georgia" in fontFamily string
    - Contains "S E A L E D" text (wordmark per D-02)
    - Contains "You're on the waitlist." (D-04 copy)
    - Contains "Nothing to do now." (D-04 copy)
    - Contains "— SEALED" sign-off
    - Contains "export const WaitlistConfirmationEmail" (named export)
    - Does NOT contain "export default" (analog uses named exports only)
    - Does NOT contain "dangerouslySetInnerHTML"
    - Does NOT contain any image src or url() reference
    - Does NOT import from "framer-motion" or "motion"

    Behavior assertion (post-handoff, after DNS verified + secrets set):
    - renderAsync(React.createElement(WaitlistConfirmationEmail, {})) returns valid HTML string
    - Rendered HTML contains visible "S E A L E D" and "You're on the waitlist."
    - No verification link appears anywhere in the HTML body
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create SealLetterEmail.tsx (Template 1B) and extend join-waitlist/deno.json</name>
  <files>
    /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/notify/emails/SealLetterEmail.tsx
    /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/deno.json
  </files>

  <read_first>
    1. /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/notify/emails/DeliveryLetterEmail.tsx
       — Full file. Same clone base as 1A; pay special attention to the Link import and usage at lines 26-28
         for the CTA pattern (1B uses a dark-ink button style, not the muted link style in the analog).
    2. /Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/03-email-infrastructure/03-CONTEXT.md
       — D-04 (exact approved copy for 1B including subject, body, CTA text, "7 days" note, sign-off).
    3. /Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/03-email-infrastructure/03-PATTERNS.md
       — Section "SealLetterEmail.tsx" — exact Link CTA style block, D-04 copy structure for 1B.
    4. /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/deno.json
       — Current state (only has @supabase/supabase-js). Read before editing.
    5. /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/notify/deno.json
       — Target shape to match exactly for the react + @react-email/components imports and compilerOptions.
  </read_first>

  <action>
    PART A — Create SealLetterEmail.tsx:

    Create /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/notify/emails/SealLetterEmail.tsx.

    Imports: Body, Container, Head, Html, Link, Preview, Section, Text from '@react-email/components'.
    Also: import * as React from 'react';

    Props type: export type SealLetterEmailProps = { verifyUrl: string; }
    Component: export const SealLetterEmail = ({ verifyUrl }: SealLetterEmailProps) => (...)
    No default export — named export only.

    JSX structure:
    - Html > Head / (Preview: "Your letter is ready. Verify your email to seal it.") / Body
    - Body style: backgroundColor '#faf6ef', fontFamily 'Georgia, "Times New Roman", serif'
    - Container style: padding '48px 32px', maxWidth 560
    - Section 1 — wordmark header same as 1A:
        Text: color '#3a342e', fontSize 13, letterSpacing '0.15em', opacity 0.45 — "S E A L E D"
    - Section 2 style marginTop 32 — body text (D-04):
        Text: color '#3a342e', fontSize 16, lineHeight 1.7 — "Your letter is ready. Verify your email to seal it."
    - Section 3 style marginTop 32 — CTA button:
        Security comment: {/* T-03 mitigation: <Link href={verifyUrl}> is safe; verifyUrl is a server-generated UUID URL. Never interpolate into raw HTML strings. */}
        Link: href={verifyUrl}, style: display 'inline-block', backgroundColor '#3a342e', color '#faf6ef',
          padding '12px 24px', fontSize 14, textDecoration 'none'
        Content: "Verify and seal"
    - Section 4 style marginTop 32:
        Text: color '#7a716a', fontSize 13 — "This link works for 7 days."
    - Section 5 style marginTop 32:
        Text: color '#7a716a', fontSize 13 — "— SEALED"

    No shortened URLs — the verifyUrl prop will be the full "https://sealedapp.io/verify?token=<uuid>" string.

    PART B — Extend join-waitlist/deno.json:

    Edit /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/deno.json.
    Add to imports: "react": "npm:react@18.3.1" and "@react-email/components": "npm:@react-email/components@0.0.22".
    Add entire compilerOptions block: { "jsx": "react-jsx", "jsxImportSource": "react" }.
    Preserve "@supabase/supabase-js": "npm:@supabase/supabase-js@2.103.2" at its current version exactly.
    CRITICAL: pin @react-email/components to 0.0.22 — 0.0.23+ removes renderAsync and breaks the function.
    Result must match notify/deno.json structure exactly (same key order within imports is fine to differ,
    content must be identical in terms of packages + versions + compilerOptions).
  </action>

  <verify>
    <automated>
      grep -c "verifyUrl" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/notify/emails/SealLetterEmail.tsx"
      # Must output >= 2 (props type + usage)

      grep -c "backgroundColor: '#3a342e'" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/notify/emails/SealLetterEmail.tsx"
      # Must output 1 (CTA button)

      grep -c "7 days" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/notify/emails/SealLetterEmail.tsx"
      # Must output 1

      grep -c "dangerouslySetInnerHTML" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/notify/emails/SealLetterEmail.tsx"
      # Must output 0

      grep -c "0.0.22" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/deno.json"
      # Must output 1 (react-email pinned to 0.0.22)

      grep -c "react-jsx" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/deno.json"
      # Must output 1 (compilerOptions added)

      grep -c "2.103.2" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/deno.json"
      # Must output 1 (supabase-js version preserved)
    </automated>
  </verify>

  <acceptance_criteria>
    Source assertions (SealLetterEmail.tsx):
    - File exists at notify/emails/SealLetterEmail.tsx
    - Contains "backgroundColor: '#faf6ef'" (body)
    - Contains "S E A L E D" wordmark
    - Contains "Your letter is ready." (D-04 copy)
    - Contains "Verify and seal" (CTA text)
    - Contains "7 days" (expiry note per D-04)
    - Contains "— SEALED" sign-off
    - Contains "href={verifyUrl}" on a Link component
    - Link CTA style contains "backgroundColor: '#3a342e'" (dark ink)
    - Does NOT contain "dangerouslySetInnerHTML"
    - Does NOT contain any shortened URL or redirect domain in verifyUrl default
    - Named export "export const SealLetterEmail" only — no default export

    Source assertions (join-waitlist/deno.json):
    - Contains "react": "npm:react@18.3.1"
    - Contains "@react-email/components": "npm:@react-email/components@0.0.22" (pinned — NOT 0.0.23+)
    - Contains "@supabase/supabase-js": "npm:@supabase/supabase-js@2.103.2" (unchanged)
    - Contains "compilerOptions" block with "jsx": "react-jsx" and "jsxImportSource": "react"

    Behavior assertion (post-handoff):
    - renderAsync(React.createElement(SealLetterEmail, { verifyUrl: 'https://sealedapp.io/verify?token=test' }))
      returns valid HTML string containing the verify link href
    - Rendered HTML contains "Verify and seal" and "7 days"
    - No other links appear in the HTML (no unsubscribe, no image URLs)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template props → rendered HTML | verifyUrl is the only variable input; it is a server-generated UUID URL from the token table |
| Email client → rendered HTML | All inline styles; no external resources fetched by email clients |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | SealLetterEmail verifyUrl rendering | mitigate | Link href={verifyUrl} — React Email Link component handles href safely; comment added at render site per PATTERNS.md. Never interpolate into raw HTML strings. |
| T-03-08 | Information Disclosure | Email templates expose user data | accept | Neither 1A nor 1B renders recipient email, letter body, or any PII. Only the verifyUrl (a UUID token) appears. |
| T-03-09 | Denial of Service | Web font loading blocked in inboxes | mitigate | Georgia (websafe serif) only — per D-01. No @font-face, no Google Fonts link in templates. Eliminates web-font blocking risk. |
| T-03-10 | Information Disclosure | Image tracking pixels | accept | No images in either template per D-02. Zero pixel-tracking surface. |
</threat_model>

<verification>
After this plan completes:
- WaitlistConfirmationEmail.tsx exists with cream aesthetic, S E A L E D wordmark, D-04 copy, no link
- SealLetterEmail.tsx exists with cream aesthetic, S E A L E D wordmark, D-04 copy, dark-ink CTA button, 7-day note
- join-waitlist/deno.json has react@18.3.1 + @react-email/components@0.0.22 + compilerOptions
- Plan 03 can import from these templates without a JSX compiler error
</verification>

<success_criteria>
Both templates exist with correct aesthetics (D-01), copy (D-04), no images (D-02), no dangerouslySetInnerHTML.
join-waitlist/deno.json is extended with react + @react-email/components@0.0.22 + compilerOptions.
All grep-based acceptance criteria pass.
</success_criteria>

<output>
After completion, create /Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/03-email-infrastructure/03-02-SUMMARY.md
</output>
