---
phase: 03-email-infrastructure
plan: 05
type: execute
wave: 4
depends_on:
  - 03-04
files_modified: []
autonomous: false
requirements:
  - EMAIL-03
  - EMAIL-A2
  - EMAIL-B1
  - EMAIL-04
  - DEPLOY-04

must_haves:
  truths:
    - "dig confirms DKIM CNAME, SPF TXT (on send.), and DMARC TXT (_dmarc.) all resolve correctly"
    - "Resend dashboard shows sealedapp.io as Verified"
    - "A Path A signup receives Template 1A in a real inbox within 60 seconds with passing SPF/DKIM/DMARC headers"
    - "Template 1A has no verification link in the body"
    - "A 1B test trigger send delivers Template 1B with a working verification link whose token exists in app_private.verification_tokens with expires_at 7 days from now"
    - "mail-tester.com scores >= 9/10 on Template 1A"
    - "mail-tester.com scores >= 9/10 on Template 1B"
    - "RESEND_API_KEY and SEALED_FROM_ADDRESS are not present in any committed file or VITE_* variable (supabase secrets list confirms)"
  artifacts:
    - path: "Resend dashboard — sealedapp.io domain"
      provides: "Verified domain status confirming DKIM signing is active"
    - path: "Real inbox (Nour's email)"
      provides: "Template 1A received with passing auth headers — visual and header check"
    - path: "mail-tester.com report — 1A"
      provides: "Score >= 9/10 confirming SPF/DKIM/DMARC pass + no blacklist"
    - path: "mail-tester.com report — 1B"
      provides: "Score >= 9/10 confirming SPF/DKIM/DMARC pass + working link"
  key_links:
    - from: "dig TXT _dmarc.sealedapp.io"
      to: "DMARC policy enforcement"
      via: "DNS TXT record"
      pattern: "p=reject.*sp=reject"
    - from: "Template 1A inbox"
      to: "Resend DKIM signing"
      via: "email headers Authentication-Results"
      pattern: "dkim=pass"
---

<objective>
Verify that Phase 3 email infrastructure is fully live and deliverable. All five phase success
criteria are confirmed here: DNS records resolve, both templates arrive in real inboxes with
passing auth headers, mail-tester scores >= 9/10 on both, and no secrets are in committed code.

Purpose: This is the acceptance proof. Code passing grep checks (Plans 01-03) is necessary but
not sufficient — the only proof that emails arrive, are authenticated, and are not spam-flagged
is an actual mail-tester run and real inbox receipt. This plan cannot run until Plan 04 is
complete (infrastructure must be live and DNS must have propagated).

Output: Confirmed Phase 3 success criteria 1-5. Phase 4 is unblocked.
</objective>

<execution_context>
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/03-email-infrastructure/03-CONTEXT.md
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/03-email-infrastructure/03-RESEARCH.md
</execution_context>

<context>
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/ROADMAP.md
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify DNS records resolve (Phase 3 success criterion 1)</name>

  <read_first>
    1. /Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/03-email-infrastructure/03-RESEARCH.md
       — Section "Resend DNS Records Shape" — the four records and their expected shapes.
       — Section "dig verification commands" — the exact commands to run.
  </read_first>

  <what-built>
    Plan 04 added four DNS records at the registrar:
    - DKIM CNAME on resend._domainkey.sealedapp.io
    - SPF TXT on send.sealedapp.io
    - MX on send.sealedapp.io
    - DMARC TXT on _dmarc.sealedapp.io

    Resend's dashboard was told about sealedapp.io and is polling for the records.
  </what-built>

  <how-to-verify>
    Run all four dig commands and confirm each resolves:

      dig CNAME resend._domainkey.sealedapp.io +short
      # Must return a non-empty value (the Resend DKIM target CNAME)

      dig TXT send.sealedapp.io +short
      # Must return a string containing "include:_spf.resend.com"

      dig TXT _dmarc.sealedapp.io +short
      # Must return a string containing "p=reject" and "sp=reject"

      dig MX send.sealedapp.io +short
      # Must return at least one MX record (Resend's bounce endpoint)

    Also verify in the Resend dashboard (resend.com/domains):
    - sealedapp.io shows status "Verified" (green)
    - If still "Pending", wait and retry — propagation can take up to 4 hours

    If any dig command returns empty, DNS has not propagated yet. Wait and retry.
    Do NOT proceed to Tasks 2-4 until all four records resolve AND Resend shows Verified.
  </how-to-verify>

  <resume-signal>
    Type "dns verified" after all four dig commands return non-empty values AND Resend dashboard
    shows sealedapp.io as Verified.
  </resume-signal>

  <acceptance_criteria>
    - dig CNAME resend._domainkey.sealedapp.io +short returns non-empty
    - dig TXT send.sealedapp.io +short contains "include:_spf.resend.com"
    - dig TXT _dmarc.sealedapp.io +short contains "p=reject" and "sp=reject"
    - dig MX send.sealedapp.io +short returns at least one MX record
    - Resend dashboard shows sealedapp.io status as "Verified"
    (Phase 3 success criterion 1 confirmed)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Template 1A — real inbox + mail-tester (success criteria 2 and 4)</name>

  <read_first>
    1. /Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/03-email-infrastructure/03-RESEARCH.md
       — Section "mail-tester.com >= 9/10 Requirements" — the check table and strategy.
       — Section "Common Pitfalls" — Pitfall 1 (DNS must be verified before this step).
    2. /Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/03-email-infrastructure/03-CONTEXT.md
       — D-04 (Template 1A approved copy — confirms what the received email should look like).
  </read_first>

  <what-built>
    Plans 01-03 wired Template 1A into join-waitlist at the new-user success path.
    Plan 04 deployed the function and set secrets.
    DNS is now verified (Task 1 confirmed).
    A real signup from the landing page will now trigger a 1A send.
  </what-built>

  <how-to-verify>
    Step 1 — mail-tester run for Template 1A:
      a) Go to mail-tester.com and get a fresh test email address (e.g. test-abc123@mail-tester.com)
      b) Submit a new signup on the landing page using that mail-tester address as the email
         (use a fresh address not previously signed up; the function sends 1A on the 'new' path)
      c) Wait up to 60 seconds
      d) Click "Check your score" on mail-tester.com
      e) Record the score and screenshot any warnings

    Required: Score >= 9/10.
    If < 9/10, identify the deduction reason:
    - Authentication failure (DKIM/SPF/DMARC fail) → DNS not fully propagated; wait and retry
    - List-Unsubscribe missing → acceptable for transactional email; note it but do not block
    - Blacklist hit → contact Resend support; check Resend dashboard IP reputation

    Step 2 — Real inbox check:
      a) Submit another new signup using your own email address (nourismaieel@gmail.com or similar)
      b) Wait up to 60 seconds
      c) Open the email and verify:
         - Subject: "You're on the list."
         - Body shows "S E A L E D" wordmark and "You're on the waitlist."
         - No verification link appears anywhere in the body
         - Email renders with cream background in Gmail / Apple Mail
      d) Check email headers (Show original → Authentication-Results):
         - dkim=pass
         - spf=pass (or dkim alignment pass under DMARC)
         - dmarc=pass
  </how-to-verify>

  <resume-signal>
    Type "1A verified" after:
    - mail-tester score is recorded (must be >= 9/10)
    - Real inbox received the email within 60 seconds
    - Authentication-Results headers show passing DKIM/DMARC
  </resume-signal>

  <acceptance_criteria>
    - Template 1A received in real inbox within 60 seconds of signup submission
    - Email subject is "You're on the list."
    - Email body contains "You're on the waitlist." and "S E A L E D" wordmark
    - Email body does NOT contain any verification link or URL
    - Authentication-Results header contains "dkim=pass" and "dmarc=pass"
    - mail-tester.com score is >= 9/10
    - No blacklist flags on the mail-tester report
    (Phase 3 success criteria 2 and 4 partially confirmed — both templates needed for full criterion 4)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Template 1B — 1B test trigger + mail-tester (success criteria 3 and 4)</name>

  <read_first>
    1. /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts
       — Review the 1B test trigger block added by Plan 03 Task 2. Understand the endpoint shape:
         POST /join-waitlist?test_1b=1 with Authorization: Bearer {TEST_TRIGGER_KEY} and body { email }.
    2. /Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/03-email-infrastructure/03-CONTEXT.md
       — D-04 (Template 1B approved copy — subject, body, CTA, "7 days" note).
  </read_first>

  <what-built>
    Plan 03 added a guarded 1B test trigger to join-waitlist. It:
    - Requires Authorization: Bearer {TEST_TRIGGER_KEY}
    - Looks up the user by email, creates a verification token (7-day expiry), builds the verify URL,
      renders SealLetterEmail, sends via Resend, returns { ok: true, token_id }
    Plan 04 set TEST_TRIGGER_KEY as a secret and deployed the function.
  </what-built>

  <how-to-verify>
    Step 1 — Retrieve TEST_TRIGGER_KEY:
      supabase secrets list  (shows key is present but value is masked)
      Access the actual value from supabase secrets get TEST_TRIGGER_KEY or from your password manager
      where you stored it during the Plan 04 handoff.

    Step 2 — mail-tester run for Template 1B:
      a) Go to mail-tester.com and get a fresh test email address
      b) Ensure a user with that email exists in the database (sign up on the landing page first if needed)
      c) Send 1B to the mail-tester address via the test trigger:
           curl -X POST "https://<project-ref>.supabase.co/functions/v1/join-waitlist?test_1b=1" \
             -H "Authorization: Bearer <TEST_TRIGGER_KEY>" \
             -H "Content-Type: application/json" \
             -d '{"email":"<mail-tester-address>"}'
         Response should be: { "ok": true, "token_id": "<uuid>" }
      d) Wait up to 60 seconds, then click "Check your score" on mail-tester.com
      e) Record the score >= 9/10

    Step 3 — Real inbox check for 1B:
      a) Send 1B to your own email using the test trigger (use your real email, not a test one — you
         need to be able to click the link)
           curl -X POST "https://<project-ref>.supabase.co/functions/v1/join-waitlist?test_1b=1" \
             -H "Authorization: Bearer <TEST_TRIGGER_KEY>" \
             -H "Content-Type: application/json" \
             -d '{"email":"<your-email>"}'
      b) Wait up to 60 seconds
      c) Open the email and verify:
         - Subject: "Seal your letter."
         - Body shows "S E A L E D" wordmark
         - Body shows "Your letter is ready. Verify your email to seal it."
         - CTA button "Verify and seal" is present and links to https://sealedapp.io/verify?token=<uuid>
         - "This link works for 7 days." text is present
      d) Check authentication headers: dkim=pass, dmarc=pass

    Step 4 — Verify token TTL:
      a) Copy the token from the verify URL in the email (or from the curl response token_id)
      b) In Supabase dashboard → Table Editor → app_private.verification_tokens
         (or: supabase db remote execute "SELECT expires_at, used_at FROM app_private.verification_tokens WHERE id = '<token_id>'::uuid;")
      c) Confirm expires_at is approximately 7 days from now (not 1 hour, not 24 hours)
      d) Confirm used_at IS NULL (token has not been consumed)
  </how-to-verify>

  <resume-signal>
    Type "1B verified" after:
    - mail-tester score is >= 9/10 for Template 1B
    - Real inbox received 1B within 60 seconds
    - Verification link is present and points to sealedapp.io/verify?token=<uuid>
    - Token row in app_private.verification_tokens has expires_at ~7 days from now and used_at IS NULL
  </resume-signal>

  <acceptance_criteria>
    - Template 1B received in real inbox within 60 seconds of test trigger call
    - Email subject is "Seal your letter."
    - Email body contains "S E A L E D" wordmark, "Your letter is ready.", "Verify and seal" CTA
    - Email body contains "This link works for 7 days."
    - CTA href is https://sealedapp.io/verify?token=<uuid> (full URL, not shortened, not a Supabase auth link)
    - Authentication-Results header contains "dkim=pass" and "dmarc=pass"
    - mail-tester.com score is >= 9/10
    - Token in app_private.verification_tokens has expires_at between 6.9 and 7.1 days from now
    - Token has used_at IS NULL (single-use, not pre-consumed)
    (Phase 3 success criteria 3 and 4 confirmed)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Confirm secrets not in committed files (success criterion 5)</name>

  <read_first>
    1. /Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/STATE.md
       — Blockers/Concerns: "Service role key bundle leak risk. Add a CI grep on dist/ for the service
         role key prefix before any Vercel deploy."
  </read_first>

  <what-built>
    Plan 04 set RESEND_API_KEY, SEALED_FROM_ADDRESS, and TEST_TRIGGER_KEY via supabase secrets set.
    Plan 03 reads these from Deno.env — they are never written to any file.
  </what-built>

  <how-to-verify>
    Run from the main SEALED-org repo:

      grep -r "RESEND_API_KEY=re_" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/" --include="*.ts" --include="*.json" --include="*.env" --include="*.toml"
      # Must return 0 results

      grep -rn "re_[A-Za-z0-9]" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/" --include="*.ts" --include="*.json"
      # Inspect results — Resend API keys start with "re_"; must not appear in source files

    Run from the landing page repo:

      grep -rn "RESEND_API_KEY\|SEALED_FROM_ADDRESS" "/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/src/"
      # Must return 0 results (these are server-side secrets; never in the React bundle)

      grep -rn "VITE_RESEND\|VITE_SEALED_FROM" "/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/"
      # Must return 0 results (no VITE_ exposure of secrets)

    Also verify the Supabase dashboard:
      supabase secrets list (from main repo)
      # Shows RESEND_API_KEY, SEALED_FROM_ADDRESS, TEST_TRIGGER_KEY present and masked
  </how-to-verify>

  <resume-signal>
    Type "secrets clean" after all grep commands return 0 results and supabase secrets list shows
    the three secrets present.
  </resume-signal>

  <acceptance_criteria>
    - grep for "re_[A-Za-z0-9]" in .ts and .json files returns 0 matches (no Resend API key in source)
    - grep for VITE_RESEND* or VITE_SEALED_FROM* returns 0 matches
    - supabase secrets list shows RESEND_API_KEY, SEALED_FROM_ADDRESS, TEST_TRIGGER_KEY (values masked)
    - No .env or .env.local file in main repo contains RESEND_API_KEY
    (Phase 3 success criterion 5 confirmed)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification run → real inbox | SPF/DKIM/DMARC authentication headers in the received email are the proof of domain verification |
| mail-tester.com → send pipeline | Third-party scoring service checks SpamAssassin + auth headers + blacklists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-19 | Information Disclosure | mail-tester.com receives a real test email | accept | mail-tester addresses are ephemeral; no PII in the email body (only "You're on the waitlist." / "Verify and seal"); acceptable for a deliverability test |
| T-03-20 | Repudiation | 1B test trigger creates real tokens in the database | accept | Tokens expire in 7 days and are only dangerous if Phase 4 verify-email is deployed; since Phase 4 is not yet deployed, these test tokens cannot seal any letter; tokens have no letter_id yet |
</threat_model>

<verification>
Phase 3 is complete when all five success criteria from ROADMAP.md are confirmed:

1. dig confirms DKIM, SPF, DMARC records resolve; Resend dashboard shows Verified — Task 1
2. Path A signup receives Template 1A within 60s with passing SPF/DKIM/DMARC, no verify link — Task 2
3. 1B test trigger delivers Template 1B with 7-day verification link — Task 3
4. mail-tester >= 9/10 on both templates — Tasks 2 + 3
5. RESEND_API_KEY and SEALED_FROM_ADDRESS only in supabase secrets, not in committed files or VITE_* — Task 4
</verification>

<success_criteria>
All five ROADMAP Phase 3 success criteria confirmed:
1. dig + Resend dashboard: DNS verified
2. Real inbox: 1A arrives within 60s, no verify link, DKIM/DMARC pass
3. Real inbox: 1B arrives with 7-day token link, DKIM/DMARC pass
4. mail-tester >= 9/10 on both 1A and 1B
5. Secrets in supabase secrets only, zero committed or VITE_* exposure

Phase 4 (Letter + Verify Flow) is unblocked.
</success_criteria>

<output>
After completion, create /Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/03-email-infrastructure/03-05-SUMMARY.md
</output>
