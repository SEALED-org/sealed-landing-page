---
phase: 03-email-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts
autonomous: true
requirements:
  - EMAIL-A2
  - EMAIL-B1
  - EMAIL-03
  - EMAIL-04
  - DEPLOY-04

must_haves:
  truths:
    - "New-user success path (~line 199) sends Template 1A with idempotencyKey '1a-{user_id}' — stable per user"
    - "Unverified resend path (~line 162) sends Template 1A with idempotencyKey '1a-resend-{user_id}-{timestamp}' — not deduplicated within Resend 24h window"
    - "Email send failures on either 1A path do NOT change the structured { state } response — caught, logged, continue"
    - "1B test trigger endpoint sends Template 1B with a real verification token to a provided email — guarded by TEST_TRIGGER_KEY header check (D-09 build 1B fully + test-deliver via one-off trigger)"
    - "RESEND_API_KEY and SEALED_FROM_ADDRESS read from Deno.env at function startup and injected into sendResendEmail — never read inside templates or the shared wrapper (D-05 from-address SEALED <letters@sealedapp.io>)"
    - "Existing { state } response contract is unchanged — no new response shapes, no new status codes on normal paths"
    - "join-waitlist/index.ts imports renderAsync from @react-email/components (0.0.22 — not renamed to render)"
  artifacts:
    - path: "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts"
      provides: "1A wiring at both TODO stubs + 1B test trigger + env DI block"
      contains: "WaitlistConfirmationEmail"
  key_links:
    - from: "join-waitlist/index.ts new-user success path"
      to: "WaitlistConfirmationEmail + sendResendEmail"
      via: "renderAsync + sendResendEmail call with idempotencyKey '1a-{user_id}'"
      pattern: "1a-.*created\\.user\\.id"
    - from: "join-waitlist/index.ts unverified path"
      to: "WaitlistConfirmationEmail + sendResendEmail"
      via: "renderAsync + sendResendEmail call with timestamp-scoped idempotencyKey"
      pattern: "1a-resend-.*Date\\.now"
    - from: "join-waitlist/index.ts 1B test trigger"
      to: "SealLetterEmail + create_verification_token RPC"
      via: "supabase.rpc('create_verification_token') + renderAsync + sendResendEmail"
      pattern: "create_verification_token"
---

<objective>
Wire Template 1A into the two existing TODO stubs in join-waitlist/index.ts and add the guarded
1B test-trigger endpoint.

Purpose: This is the final code-write task before the HALT. After this plan, the function sends
real confirmation emails on both the new-user success path and the unverified-resend path. The 1B
test trigger lets Nour prove deliverability and run mail-tester against Template 1B without
pulling Phase 4's letter-body wiring forward. The trigger is removed in Phase 4 when production
wiring lands.

Output: Modified join-waitlist/index.ts with 1A sends, env DI, and guarded 1B test trigger.
No changes to any other file.
</objective>

<execution_context>
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/03-email-infrastructure/03-CONTEXT.md
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/03-email-infrastructure/03-RESEARCH.md
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/03-email-infrastructure/03-PATTERNS.md
</execution_context>

<context>
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/ROADMAP.md
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/STATE.md

<interfaces>
<!-- From join-waitlist/index.ts — current file, exact insertion sites -->

Existing imports block (lines 1-12):
  // supabase/functions/join-waitlist/index.ts
  import { adminClient } from '../_shared/admin-client.ts';

New imports to ADD (after existing import):
  import { renderAsync } from '@react-email/components';
  import * as React from 'react';
  import { WaitlistConfirmationEmail } from '../notify/emails/WaitlistConfirmationEmail.tsx';
  import { SealLetterEmail } from '../notify/emails/SealLetterEmail.tsx';
  import { sendResendEmail } from '../_shared/resend.ts';

Env DI block to ADD (after the corsHeaders/JSON_HEADERS constants block, before Deno.serve):
  const env = {
    RESEND_API_KEY: Deno.env.get('RESEND_API_KEY') ?? '',
    SEALED_FROM_ADDRESS: Deno.env.get('SEALED_FROM_ADDRESS') ?? 'SEALED <letters@sealedapp.io>',
  };
  // Note: intentionally NOT a fail-fast guard here — a missing RESEND_API_KEY must not
  // abort the signup. The 1A send is wrapped in try/catch; failure logs but does not
  // change the { state: 'success' } or { state: 'unverified' } responses.

TODO site 1 — unverified resend path (line 162 of current file):
  case 'unverified':
    await recordAttempt(supabase, ip, 'duplicate_unverified');
    // TODO(Phase 3): resend Template 1A via generateLink + sendResendEmail.
    return jsonResponse({ state: 'unverified' }, 200);

  Needs: user_id for idempotency key — call supabase.rpc('lookup_user_id_by_email', { p_email: body.email })
  before the try/catch send block. (lookup_user_id_by_email returns uuid | null per Plan 01 migration.)

TODO site 2 — new-user success path (lines 199-206 of current file):
  // TODO(Phase 3 — EMAIL-A2 / EMAIL-03 / EMAIL-04): send Template 1A.
  //   ...
  return jsonResponse({ state: 'success' }, 200);

  user_id is already available as created.user.id at this point.

sendResendEmail type (from _shared/resend.ts):
  sendResendEmail({ from: string, to: string[], subject: string, html: string,
    idempotencyKey: string, apiKey: string, fetchImpl?, outboxId?, letterId? })
  Returns Promise<{ id: string }>. Throws on non-2xx.

Idempotency keys:
  - 1A new-user: `1a-${created.user.id}`
  - 1A unverified resend: `1a-resend-${userId}-${Date.now()}`
  - 1B test trigger: `1b-test-${tokenRow.id}`

Privacy logging rule (CODEX MEDIUM from _shared/resend.ts lines 31-33):
  // CODEX MEDIUM email-body privacy: log ONLY status + user_id.
  // Never log body.email, html, env.RESEND_API_KEY, or Resend response body.
  console.log('1a-sent', { user_id: created.user.id });

create_verification_token RPC (from Plan 01 migration):
  supabase.rpc('create_verification_token', { p_user_id: userId })
  Returns: Array<{ id: uuid, token: string }> — read data[0].token and data[0].id

1B verify URL shape: https://sealedapp.io/verify?token=${tokenRow.token}
1B subject: 'Seal your letter.' (D-04)
1A subject: "You're on the list." (D-04)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire 1A sends and add env DI block to join-waitlist/index.ts</name>
  <files>/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts</files>

  <read_first>
    1. /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts
       — Full file. Understand the existing structure before any edit. Note lines 160-163 (unverified case),
         lines 199-206 (new-user success path), and the existing import block at line 12.
    2. /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/notify/index.ts
       — Env DI pattern (lines 13-20) and render+send idiom (lines 26-28). These are the patterns to follow.
    3. /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/_shared/resend.ts
       — Full signature of sendResendEmail. Confirm the `to` field is string[] and apiKey is a parameter.
         Read the CODEX MEDIUM logging comment (lines 31-33) for the privacy constraint.
    4. /Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/03-email-infrastructure/03-PATTERNS.md
       — Sections "join-waitlist/index.ts" (both TODO sites) and "Idempotency Key Reference" table.
    5. /Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/03-email-infrastructure/03-RESEARCH.md
       — Pitfall 4 (stable idempotency key on unverified path) and Pitfall 7 (missing secrets must not abort signup).
  </read_first>

  <action>
    Edit /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts.

    CHANGE 1 — Add imports (after the existing import line 12 for adminClient):
      import { renderAsync } from '@react-email/components';
      import * as React from 'react';
      import { WaitlistConfirmationEmail } from '../notify/emails/WaitlistConfirmationEmail.tsx';
      import { sendResendEmail } from '../_shared/resend.ts';

    CHANGE 2 — Add env DI block (after the JSON_HEADERS constant definition, before the verifyTurnstile function):
      const env = {
        RESEND_API_KEY: Deno.env.get('RESEND_API_KEY') ?? '',
        SEALED_FROM_ADDRESS: Deno.env.get('SEALED_FROM_ADDRESS') ?? 'SEALED <letters@sealedapp.io>',
      };
      Comment: explain that missing RESEND_API_KEY is NOT a startup abort — email failure must not fail the signup.

    CHANGE 3 — Replace TODO at line 162 (unverified resend path):
      Remove the comment "TODO(Phase 3): resend Template 1A via generateLink + sendResendEmail."
      Before the return statement, add:
        a) RPC call: const { data: uidData } = await supabase.rpc('lookup_user_id_by_email', { p_email: body.email });
           const userId: string | null = uidData ?? null;
        b) try/catch block wrapping the 1A send:
             if (userId && env.RESEND_API_KEY) {
               const html = await renderAsync(React.createElement(WaitlistConfirmationEmail, {}));
               await sendResendEmail({
                 from: env.SEALED_FROM_ADDRESS,
                 to: [body.email],
                 subject: "You're on the list.",
                 html,
                 idempotencyKey: `1a-resend-${userId}-${Date.now()}`,
                 apiKey: env.RESEND_API_KEY,
               });
               // CODEX MEDIUM email-body privacy: log only status + user_id. Never log recipient or html.
               console.log('1a-resend-sent', { user_id: userId });
             }
        c) The catch must log and continue — MUST NOT throw or change the return value.
        d) The return jsonResponse({ state: 'unverified' }, 200) line is UNCHANGED.

    CHANGE 4 — Replace TODO at lines 199-204 (new-user success path):
      Remove the multi-line TODO comment block (lines 199-204).
      Before the return jsonResponse({ state: 'success' }, 200) line, add:
        try/catch block:
          if (env.RESEND_API_KEY) {
            const html = await renderAsync(React.createElement(WaitlistConfirmationEmail, {}));
            await sendResendEmail({
              from: env.SEALED_FROM_ADDRESS,
              to: [body.email],
              subject: "You're on the list.",
              html,
              idempotencyKey: `1a-${created.user.id}`,
              apiKey: env.RESEND_API_KEY,
            });
            // CODEX MEDIUM email-body privacy: log only status + user_id. Never log recipient or html.
            console.log('1a-sent', { user_id: created.user.id });
          }
        Catch must log (console.error('1a-send-error', err.message)) and continue.
        The return jsonResponse({ state: 'success' }, 200) line is UNCHANGED.

    INVARIANTS — do not violate these:
    - All existing structured { state } responses are preserved unchanged.
    - The existing try/catch at the outer Deno.serve level is untouched.
    - No new response shapes or status codes are introduced for normal paths.
    - renderAsync is the function imported (NOT render — 0.0.22 uses renderAsync).
    - React.createElement is used at call sites — NOT JSX syntax in .ts files.
  </action>

  <verify>
    <automated>
      grep -c "renderAsync" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts"
      # Must output >= 2 (import + at least 2 call sites)

      grep -c "1a-resend-.*Date.now" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts"
      # Must output 1 (timestamp-scoped key on unverified path)

      grep -c "rpc('lookup_user_id_by_email'" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts"
      # Must output >= 1 (RPC call on unverified path and/or test trigger)

      grep -c "TODO(Phase 3)" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts"
      # Must output 0 (both TODOs replaced)

      grep -c "RESEND_API_KEY" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts"
      # Must output >= 3 (env DI + 2 guard checks)

      grep -c "state: 'success'" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts"
      # Must output 1 (unchanged return)

      grep -c "state: 'unverified'" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts"
      # Must output 1 (unchanged return)

      grep -c 'catch' "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts"
      # Must output >= 2 (one catch per 1A send site — Pitfall 7: email failure must not abort signup)

      grep -c 'console.error' "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts"
      # Must output >= 2 (privacy-safe error logging at both 1A catch sites)
    </automated>
  </verify>

  <acceptance_criteria>
    Source assertions:
    - File contains "import { renderAsync } from '@react-email/components'"
    - File contains "import { WaitlistConfirmationEmail }" (not default import)
    - File contains "import { sendResendEmail } from '../_shared/resend.ts'"
    - File contains "const env = {" with RESEND_API_KEY and SEALED_FROM_ADDRESS reads from Deno.env
    - File contains "1a-resend-" + "Date.now()" on the unverified path (timestamp-scoped)
    - File contains "1a-${created.user.id}" on the new-user success path (stable)
    - File contains "lookup_user_id_by_email" RPC call on the unverified path
    - File does NOT contain "TODO(Phase 3)" (both stubs replaced)
    - File does NOT contain "generateLink" (RESEARCH Pitfall 3 — generateLink not used for 1A)
    - Both send sites are wrapped in try/catch that does not re-throw
    - { state: 'success' } and { state: 'unverified' } return statements are unchanged

    Behavior assertion (post-handoff, after secrets set + DNS verified):
    - A new-user POST to join-waitlist returns { state: 'success' } and triggers a 1A send to the email
    - A second POST with the same email returns { state: 'unverified' } and triggers a 1A resend
    - If RESEND_API_KEY is missing, both paths still return their expected states (no 500)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add guarded 1B test-trigger to join-waitlist/index.ts</name>
  <files>/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts</files>

  <read_first>
    1. /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts
       — Read the file as it exists AFTER Task 1. Understand the Deno.serve handler structure and
         where the OPTIONS / POST method checks are (lines 128-129). The 1B test trigger goes BEFORE
         the existing POST pipeline (after the OPTIONS check, before the Turnstile block).
    2. /Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/03-email-infrastructure/03-RESEARCH.md
       — Open Question 2 (test trigger design — guarded endpoint with TEST_TRIGGER_KEY check).
       — Security Domain section (T-03 threat: 1B trigger must not be an open endpoint).
    3. /Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/03-email-infrastructure/03-CONTEXT.md
       — Claude's Discretion: "test-trigger form for 1B — a temporary script, a guarded debug endpoint,
         or a manual generateLink + sendResendEmail invocation; planner picks the least-intrusive way."
  </read_first>

  <action>
    Edit /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts.

    ADD the SealLetterEmail import (alongside the other email imports from Task 1 — edit the import block):
      import { SealLetterEmail } from '../notify/emails/SealLetterEmail.tsx';

    ADD a 1B test-trigger block inside Deno.serve, AFTER the OPTIONS check and BEFORE the POST gate.
    The trigger activates when req.method === 'POST' AND the URL has ?test_1b=1 query parameter
    AND the Authorization header equals 'Bearer ' + (Deno.env.get('TEST_TRIGGER_KEY') ?? '').

    Guard logic:
      const url = new URL(req.url);
      if (url.searchParams.get('test_1b') === '1') {
        // Test trigger — guards against open endpoint abuse (T-03 threat: letter bombing)
        const triggerKey = Deno.env.get('TEST_TRIGGER_KEY') ?? '';
        const authHeader = req.headers.get('Authorization') ?? '';
        if (!triggerKey || authHeader !== `Bearer ${triggerKey}`) {
          return new Response(JSON.stringify({ error: 'unauthorized' }), { status: 401, headers: JSON_HEADERS });
        }
        // Parse { email } from request body — only field needed
        let triggerBody: { email: string };
        try { triggerBody = await req.json(); } catch { return new Response(JSON.stringify({ error: 'invalid body' }), { status: 400, headers: JSON_HEADERS }); }
        if (!triggerBody.email) { return new Response(JSON.stringify({ error: 'email required' }), { status: 400, headers: JSON_HEADERS }); }

        // Lookup user_id by email (user must already exist from a prior signup)
        const supabase = adminClient();
        const { data: userId, error: lookupErr } = await supabase.rpc('lookup_user_id_by_email', { p_email: triggerBody.email });
        if (lookupErr || !userId) {
          return new Response(JSON.stringify({ error: 'user not found' }), { status: 404, headers: JSON_HEADERS });
        }

        // Create 7-day verification token
        const { data: tokenRows, error: tokenErr } = await supabase.rpc('create_verification_token', { p_user_id: userId });
        if (tokenErr || !tokenRows?.[0]) {
          return new Response(JSON.stringify({ error: 'token creation failed' }), { status: 500, headers: JSON_HEADERS });
        }
        const tokenRow = tokenRows[0];
        const verifyUrl = `https://sealedapp.io/verify?token=${tokenRow.token}`;

        // Render and send 1B
        const html = await renderAsync(React.createElement(SealLetterEmail, { verifyUrl }));
        await sendResendEmail({
          from: env.SEALED_FROM_ADDRESS,
          to: [triggerBody.email],
          subject: 'Seal your letter.',
          html,
          idempotencyKey: `1b-test-${tokenRow.id}`,
          apiKey: env.RESEND_API_KEY,
        });
        // CODEX MEDIUM email-body privacy: log only status + token_id. Never log recipient or html.
        console.log('1b-test-sent', { token_id: tokenRow.id, user_id: userId });
        return new Response(JSON.stringify({ ok: true, token_id: tokenRow.id }), { status: 200, headers: JSON_HEADERS });
      }

    The remainder of the Deno.serve handler (Turnstile check, rate limit, createUser, etc.) is UNCHANGED.
    This entire block is removed in Phase 4 when the production "letter written → 1B" wiring lands.
    Add a comment above the block: // TODO(Phase 4 — remove this test trigger when production 1B wiring lands)

    The TEST_TRIGGER_KEY secret is set by Nour during the Plan 04 handoff alongside RESEND_API_KEY.
    NEVER log triggerBody.email, the verifyUrl value, the apiKey, or the raw Authorization header.
  </action>

  <verify>
    <automated>
      grep -c "test_1b" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts"
      # Must output >= 2 (searchParams check + comment)

      grep -c "TEST_TRIGGER_KEY" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts"
      # Must output >= 1 (env read)

      grep -c "create_verification_token" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts"
      # Must output >= 1

      grep -c "SealLetterEmail" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts"
      # Must output >= 2 (import + createElement call)

      grep -c "1b-test-" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts"
      # Must output 1 (idempotency key)

      grep -c "TODO(Phase 4" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts"
      # Must output 1 (removal reminder)
    </automated>
  </verify>

  <acceptance_criteria>
    Source assertions:
    - File contains "import { SealLetterEmail }" (in import block)
    - File contains "test_1b" searchParams check
    - File contains "TEST_TRIGGER_KEY" env read inside the guard
    - File contains "Authorization" header check against "Bearer " + triggerKey
    - File contains "create_verification_token" RPC call inside the trigger block
    - File contains "sealedapp.io/verify?token=" verify URL construction
    - File contains "1b-test-${tokenRow.id}" idempotency key
    - File contains "TODO(Phase 4 — remove" comment on the trigger block
    - Trigger block returns 401 when TEST_TRIGGER_KEY is missing or auth header doesn't match
    - CODEX MEDIUM privacy comment present in the trigger block log statement

    Security assertions:
    - File does NOT log triggerBody.email anywhere in the trigger block
    - File does NOT return the verifyUrl in the response body (returns token_id only)
    - File does NOT skip the triggerKey guard if Deno.env.get('TEST_TRIGGER_KEY') is empty string

    Behavior assertion (post-handoff, after secrets set):
    - POST /join-waitlist?test_1b=1 with Authorization: Bearer <wrong-key> returns 401
    - POST /join-waitlist?test_1b=1 with correct key + { email: <existing-user> } sends 1B and returns { ok: true, token_id: "..." }
    - POST /join-waitlist (no ?test_1b param) continues through normal Turnstile pipeline unchanged
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → join-waitlist test trigger | TEST_TRIGGER_KEY is a shared secret; Authorization header guards the endpoint |
| Edge Function → Resend API | RESEND_API_KEY injected as arg to sendResendEmail — never logged, never in client bundle |
| Edge Function → Postgres (via PostgREST) | create_verification_token and lookup_user_id_by_email are SECURITY DEFINER wrappers (Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Tampering | 1B test trigger open access | mitigate | TEST_TRIGGER_KEY guard + Authorization header check; returns 401 if key missing or wrong; key set via supabase secrets set at handoff |
| T-03-12 | Information Disclosure | RESEND_API_KEY in function log | mitigate | env.RESEND_API_KEY is passed to sendResendEmail arg only; CODEX MEDIUM logging comment at every call site; never console.log'd |
| T-03-13 | Denial of Service | Recipient address in test trigger abused to spam | mitigate | Trigger requires user to already exist (lookup_user_id_by_email returns user_id or 404); cannot mint tokens for arbitrary non-registered addresses |
| T-03-14 | Repudiation | 1A resend deduplication masking repeated sends | mitigate | Timestamp-scoped idempotency key `1a-resend-${userId}-${Date.now()}` bypasses Resend's 24h dedup window; each resend request is treated as distinct |
| T-03-15 | Information Disclosure | SEALED_FROM_ADDRESS and recipient email in logs | mitigate | Log only user_id and token_id; never log body.email, triggerBody.email, or env values (CODEX MEDIUM at every call site) |
</threat_model>

<verification>
After this plan completes:
- join-waitlist/index.ts has 1A send at new-user success path (idempotency: 1a-{user_id})
- join-waitlist/index.ts has 1A send at unverified resend path (idempotency: 1a-resend-{user_id}-{timestamp})
- join-waitlist/index.ts has guarded 1B test trigger behind TEST_TRIGGER_KEY check
- All existing { state } response shapes are preserved unchanged
- Both TODO(Phase 3) comments are gone; one TODO(Phase 4 — remove) comment marks the test trigger
- grep assertions all pass
- The function is ready for `supabase functions deploy join-waitlist` at the Plan 04 handoff
</verification>

<success_criteria>
join-waitlist/index.ts wires Template 1A at both TODO stubs with correct idempotency keys, and
includes the guarded 1B test trigger. All { state } response contracts unchanged. All grep-based
acceptance criteria pass. The file is ready for deployment in the Plan 04 handoff.
</success_criteria>

<output>
After completion, create /Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/03-email-infrastructure/03-03-SUMMARY.md
</output>
