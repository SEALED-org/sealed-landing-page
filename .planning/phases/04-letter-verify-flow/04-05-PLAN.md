---
phase: 04-letter-verify-flow
plan: 05
type: execute
wave: 3
depends_on:
  - "04-03"
  - "04-04"
files_modified:
  - src/lib/supabase.ts
  - src/App.tsx
  - src/components/FirstLetter.tsx
autonomous: true
requirements:
  - LETTER-01
  - LETTER-02
  - LETTER-03
  - LETTER-04
  - LETTER-05
  - LETTER-06
  - EMAIL-01

must_haves:
  truths:
    - "Writing a letter and submitting via FirstLetter creates a draft row in app_private.letters (verified via DB query after a test submit)"
    - "The letter body is passed to joinWaitlist and included in the Edge Function invoke body"
    - "A Path A submit (hero form, no letter) sends no letter body — joinWaitlist called without letter param"
    - "Submitting with a 2,001+ character letter is blocked client-side before the form submits (live count caps input)"
    - "The live word count updates as the user types"
    - "The typewriter placeholder cycles when the textarea is empty"
    - "The 2.5s sealing animation plays before the success step renders"
    - "npm run build exits 0 with no TypeScript errors"
  artifacts:
    - path: "src/lib/supabase.ts"
      provides: "joinWaitlist(email, letter, token) — letter? param added to invoke body"
      contains: "letter"
    - path: "src/App.tsx"
      provides: "onEmailSubmit threads letter from FirstLetter into joinWaitlist; handleSubscribe unchanged (no letter)"
      contains: "onEmailSubmit"
    - path: "src/components/FirstLetter.tsx"
      provides: "letter state passed to onEmailSubmit; 2000-char cap enforced on textarea; onEmailSubmit signature gains letter param"
      contains: "letter"
  key_links:
    - from: "FirstLetter.tsx handleEmailSubmit"
      to: "onEmailSubmit(email, token, letter)"
      via: "prop call with letter state from component"
      pattern: "onEmailSubmit.*letter"
    - from: "App.tsx onEmailSubmit lambda"
      to: "joinWaitlist(newEmail, letter, turnstileToken)"
      via: "passing letter through the closure"
      pattern: "joinWaitlist.*letter"
    - from: "supabase.ts joinWaitlist"
      to: "supabase.functions.invoke('join-waitlist', { body: { email, letter, turnstileToken } })"
      via: "functions.invoke body"
      pattern: "letter"
---

<objective>
Thread the `letter` state from `FirstLetter.tsx` all the way to the `join-waitlist` Edge Function by updating three files: `supabase.ts` (add `letter?` param to `joinWaitlist`), `App.tsx` (pass `letter` in the `onEmailSubmit` closure), and `FirstLetter.tsx` (update `onEmailSubmit` prop signature and call; add 2000-char textarea cap).

Purpose: Without this plan the letter body is discarded client-side — the draft is never created. This is the last code change before the HALT deploy checkpoint.

Output: Three edited files that complete the client-side Path B wiring.
</objective>

<execution_context>
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/04-letter-verify-flow/../../../get-shit-done/workflows/execute-plan.md
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/04-letter-verify-flow/../../../get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/PROJECT.md
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/ROADMAP.md

<!-- D-02: 2000-char cap enforced client-side (textarea maxLength + count display) -->
<!-- D-03: writing IS signing up — FirstLetter calls the same joinWaitlist function -->
<!-- Hero form (handleSubscribe) does NOT send a letter — Path A only -->

<interfaces>
<!-- Current supabase.ts joinWaitlist signature -->
export async function joinWaitlist(
  email: string,
  turnstileToken: string,
): Promise<WaitlistState>

<!-- New signature after this plan -->
export async function joinWaitlist(
  email: string,
  turnstileToken: string,
  letter?: string,    -- optional; omitted for Path A; passed for Path B
): Promise<WaitlistState>

<!-- Current FirstLetterProps interface -->
interface FirstLetterProps {
  initialEmail?: string;
  onEmailSubmit: (email: string, turnstileToken: string) => Promise<WaitlistState>;
  waitlistCount: number;
}

<!-- New FirstLetterProps interface after this plan -->
interface FirstLetterProps {
  initialEmail?: string;
  onEmailSubmit: (email: string, turnstileToken: string, letter: string) => Promise<WaitlistState>;
  waitlistCount: number;
}

<!-- Current App.tsx FirstLetter usage (lines 110-127) -->
<FirstLetter
  initialEmail={isSubscribed ? email : undefined}
  onEmailSubmit={async (newEmail, turnstileToken) => {
    try {
      const state = await joinWaitlist(newEmail, turnstileToken);
      ...
    }
  }}
  waitlistCount={waitlistCount ?? 115}
/>

<!-- New App.tsx FirstLetter usage -->
<FirstLetter
  initialEmail={isSubscribed ? email : undefined}
  onEmailSubmit={async (newEmail, turnstileToken, letter) => {
    try {
      const state = await joinWaitlist(newEmail, turnstileToken, letter);
      ...
    }
  }}
  waitlistCount={waitlistCount ?? 115}
/>

<!-- handleSubscribe in App.tsx (hero form — Path A, no letter) -->
-- stays exactly the same; joinWaitlist called without letter arg (letter param is optional)

<!-- Current FirstLetter.tsx handleEmailSubmit call (line 142) -->
const state = await onEmailSubmit(email, token);

<!-- New call -->
const state = await onEmailSubmit(email, token, letter);

<!-- letter state in FirstLetter.tsx -->
const [letter, setLetter] = useState('');  -- already exists
-- wordCount already computed from letter
-- handleLetterChange already sets letter state
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add letter? param to joinWaitlist in supabase.ts</name>
  <files>src/lib/supabase.ts</files>
  <read_first>
    - `src/lib/supabase.ts` — read the full file; note the exact joinWaitlist signature and the invoke call body on line 81
  </read_first>
  <behavior>
    - Behavior 1: `joinWaitlist(email, token)` — letter omitted → invoke body has no `letter` field (Path A; existing tests pass)
    - Behavior 2: `joinWaitlist(email, token, '')` — empty string letter → letter omitted from invoke body (trim check: if letter is empty, omit it)
    - Behavior 3: `joinWaitlist(email, token, 'Dear future me...')` — non-empty letter → invoke body includes `letter: 'Dear future me...'`
    - Behavior 4: Return type and WaitlistState remain unchanged
  </behavior>
  <action>
    In `src/lib/supabase.ts`:

    1. Add `letter?: string` as the third parameter to `joinWaitlist`. Place it after `turnstileToken` and before the `: Promise<WaitlistState>` return.

    2. Update the `supabase.functions.invoke` body to conditionally include `letter`:
       Change:
         `{ body: { email, turnstileToken } }`
       To:
         `{ body: { email, turnstileToken, ...(letter && letter.trim() ? { letter: letter.trim() } : {}) } }`
       This ensures an empty or whitespace-only letter is not sent (Path A behavior preserved).

    3. Update the JSDoc comment to document the new parameter:
       Add: `@param letter Optional letter body for Path B signups. Omit or pass empty string for Path A (no letter).`

    Do NOT change WaitlistState, JoinWaitlistResponse, getSignupCount, or any other export.
  </action>
  <verify>
    <automated>
      grep -c "letter\?: string\|letter?: string" src/lib/supabase.ts
      grep -c "letter.*trim\|trim.*letter" src/lib/supabase.ts
      cd "/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design" && npx tsc --noEmit 2>&1 | grep "supabase.ts" | head -5
    </automated>
  </verify>
  <done>
    joinWaitlist has `letter?: string` third parameter. Invoke body conditionally includes letter when non-empty after trim. No TypeScript errors in supabase.ts. Existing callers without the letter arg (handleSubscribe in App.tsx) still type-check because the param is optional.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread letter through App.tsx onEmailSubmit + update FirstLetter.tsx signature</name>
  <files>src/App.tsx, src/components/FirstLetter.tsx</files>
  <read_first>
    - `src/App.tsx` — read lines 110-135 (FirstLetter usage and handleSubscribe); confirm exact lambda signature
    - `src/components/FirstLetter.tsx` — read lines 1-20 (FirstLetterProps interface), line 142 (the onEmailSubmit call inside handleEmailSubmit), and lines 103-114 (handleLetterChange and handleSealAndSend for understanding the letter state flow)
    - `src/lib/messages.ts` — confirm MESSAGES keys are unchanged (no new message keys needed for this plan)
  </read_first>
  <behavior>
    - Behavior 1: User writes "Dear future me..." in FirstLetter textarea and submits → letter state passed to onEmailSubmit → joinWaitlist receives letter → Edge Function creates draft row
    - Behavior 2: User submits empty textarea (letter.trim().length === 0) — this is blocked by `handleSealAndSend` (already returns early if letter is empty); the 'email' step is only reached when letter has content. onEmailSubmit always receives non-empty letter from FirstLetter.
    - Behavior 3: Hero form handleSubscribe calls joinWaitlist without letter arg — no TypeScript error (letter is optional in joinWaitlist)
    - Behavior 4: Textarea enforces maxLength=2000 to cap input at the client layer (D-02)
    - Behavior 5: Live character count replaces or supplements the word count — show "N / 2000 characters" below the textarea when letter has content (the word count can remain; add the char count as a second line or replace per D-02 visibility requirement)
    - Behavior 6: npm run build exits 0
  </behavior>
  <action>
    In `src/App.tsx`:
    1. Update the `onEmailSubmit` inline lambda inside `<FirstLetter ...>` (lines ~112-125):
       - Change: `async (newEmail, turnstileToken) => {`
       - To: `async (newEmail, turnstileToken, letter) => {`
       - Change: `const state = await joinWaitlist(newEmail, turnstileToken);`
       - To: `const state = await joinWaitlist(newEmail, turnstileToken, letter);`
    2. `handleSubscribe` and the hero form are NOT changed — they use no letter.

    In `src/components/FirstLetter.tsx`:
    1. Update `FirstLetterProps` interface:
       - Change: `onEmailSubmit: (email: string, turnstileToken: string) => Promise<WaitlistState>;`
       - To: `onEmailSubmit: (email: string, turnstileToken: string, letter: string) => Promise<WaitlistState>;`

    2. In `handleEmailSubmit` (around line 142):
       - Change: `const state = await onEmailSubmit(email, token);`
       - To: `const state = await onEmailSubmit(email, token, letter);`
       The `letter` state variable already exists in the component scope.

    3. Add `maxLength={2000}` to the `<textarea>` element (D-02 client cap):
       Find the `<textarea ... className="fl-textarea" ...>` and add `maxLength={2000}`.

    4. Add a character count display below the word count (D-02 visibility):
       The existing word count span `<span id="fl-words">{wordCount} words</span>` is in the fl-pad-foot div. Add alongside it (not replacing it):
       `<span id="fl-chars" style={{ marginLeft: 12, opacity: 0.55 }}>{letter.length} / 2000</span>`
       This renders as e.g. "42 / 2000" to the right of the word count.

    Do NOT change: step machine logic, animation timing, Turnstile handling, LEAVE_MS/ENTER_MS, or the success step rendering.
  </action>
  <verify>
    <automated>
      grep -c "letter: string\)" src/components/FirstLetter.tsx
      grep -c "onEmailSubmit(email, token, letter)" src/components/FirstLetter.tsx
      grep -c "maxLength.*2000\|maxLength={2000}" src/components/FirstLetter.tsx
      grep -c "joinWaitlist(newEmail, turnstileToken, letter)" src/App.tsx
      cd "/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design" && npm run build 2>&1 | tail -8
    </automated>
  </verify>
  <done>
    FirstLetterProps.onEmailSubmit signature includes `letter: string`. handleEmailSubmit calls `onEmailSubmit(email, token, letter)`. textarea has maxLength=2000. App.tsx onEmailSubmit lambda passes letter to joinWaitlist. `npm run build` exits 0. handleSubscribe (hero form) still type-checks without letter argument.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser UI → joinWaitlist | letter body (user input) crosses from component state to the Edge Function invoke call; must be trimmed and bounded |
| Client bundle | supabase.ts is client-side code — must never import admin-client or service-role key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-24 | Tampering | letter body bypass of 2000 cap | mitigate | maxLength=2000 on textarea (client UX guard); Edge Fn trims and rejects >2000 (server layer); DB CHECK is the final guard (defense-in-depth, D-02) |
| T-04-25 | Information Disclosure | letter body in client bundle logs | mitigate | supabase.ts does not log the letter body — only the returned WaitlistState; App.tsx console.error only logs 'Waitlist join failed' with the error object (no body echoed) |
| T-04-26 | Tampering | hero form accidentally sending letter | mitigate | handleSubscribe calls joinWaitlist with no letter argument; the optional param defaults to undefined → omitted from invoke body |
| T-04-27 | Elevation of Privilege | client code reaching app_private | mitigate | supabase.ts uses the anon key (createClient with VITE_SUPABASE_ANON_KEY); all app_private access goes through SECURITY DEFINER wrappers in the Edge Fn; no .schema('app_private') call exists in any client file |
</threat_model>

<verification>
1. `grep -c "letter: string\)" src/components/FirstLetter.tsx` >= 1 (prop signature updated)
2. `grep -c "onEmailSubmit(email, token, letter)" src/components/FirstLetter.tsx` >= 1
3. `grep -c "maxLength.*2000" src/components/FirstLetter.tsx` >= 1
4. `grep -c "joinWaitlist(newEmail, turnstileToken, letter)" src/App.tsx` >= 1
5. `grep -c "letter\?: string" src/lib/supabase.ts` >= 1
6. `npm run build` exits 0 (TypeScript compilation passes)
7. `grep -c "joinWaitlist.*letter\|handleSubscribe" src/App.tsx` confirms handleSubscribe does NOT pass a letter arg
</verification>

<success_criteria>
letter state flows from FirstLetter textarea → onEmailSubmit prop → App.tsx lambda → joinWaitlist(email, token, letter) → Edge Function invoke body. Hero form (handleSubscribe) is unchanged and type-checks without a letter arg. Textarea has maxLength=2000. Character count visible while typing. npm run build exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/04-letter-verify-flow/04-05-SUMMARY.md` recording:
- Exact line numbers changed in all three files
- Confirmation that `npm run build` exits 0
- Whether the character count was added as a separate span or merged with word count
- TypeScript errors found (if any) and how resolved
</output>
