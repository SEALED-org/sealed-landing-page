---
phase: 04-letter-verify-flow
plan: 06
type: execute
wave: 4
depends_on:
  - "04-03"
  - "04-04"
  - "04-05"
files_modified: []
autonomous: false

requirements:
  - LETTER-01
  - LETTER-02
  - LETTER-03
  - LETTER-04
  - LETTER-05
  - LETTER-06
  - LETTER-07
  - SEC-05
  - EMAIL-B2
  - EMAIL-B3
  - DB-04
  - EMAIL-01

user_setup:
  - service: supabase-cli
    why: "Deploy DB migration (0035) and Edge Functions (verify-email, join-waitlist redeploy) to hosted Supabase project"
    env_vars: []
    dashboard_config:
      - task: "Run supabase db push in sibling repo to apply migration 0035"
        location: "Terminal in /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org"
      - task: "Run supabase functions deploy verify-email in sibling repo"
        location: "Terminal in /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org"
      - task: "Run supabase functions deploy join-waitlist in sibling repo"
        location: "Terminal in /Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org"

must_haves:
  truths:
    - "app_private.letters table has the letters_body_length CHECK (char_length(body) <= 2000) applied in the hosted DB"
    - "public.upsert_pending_letter and public.seal_letter_with_token exist in the hosted DB and are executable by service_role"
    - "The verify-email Edge Function is deployed and returns { state } on POST with a token"
    - "The join-waitlist Edge Function is redeployed with the Path B letter wiring and test trigger removed"
    - "A real end-to-end Path B test: write a letter → submit email → receive Template 1B → click link → letter sealed in DB"
    - "A Path A test: submit email only → receive Template 1A → no letters row created in DB"
  artifacts:
    - path: "(sibling) supabase — hosted DB"
      provides: "0035 migration applied (upsert_pending_letter + seal_letter_with_token + body CHECK)"
      contains: "seal_letter_with_token"
    - path: "(sibling) supabase — hosted Edge Functions"
      provides: "verify-email deployed; join-waitlist redeployed"
      contains: "verify-email"
  key_links:
    - from: "verify.html ?token=<hex>"
      to: "deployed verify-email Edge Function"
      via: "POST /functions/v1/verify-email"
      pattern: "verify-email"
    - from: "deployed verify-email"
      to: "app_private.letters sealed_at + app_private.schedules"
      via: "seal_letter_with_token RPC"
      pattern: "seal_letter_with_token"
---

<objective>
[BLOCKING] Human-action HALT: deploy migration 0035, redeploy join-waitlist, deploy verify-email, then run end-to-end Path B and Path A smoke tests to confirm the full letter + verify flow is live.

Purpose: All code is written. The DB and Edge Functions must be deployed before any live verification is possible. This is the Phase 3–style HALT: Claude cannot run `supabase db push` or `supabase functions deploy` (no CLI auth in this context).

Output: Confirmed live deployment with successful smoke tests for both Path A and Path B.
</objective>

<execution_context>
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/04-letter-verify-flow/../../../get-shit-done/workflows/execute-plan.md
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/04-letter-verify-flow/../../../get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/PROJECT.md
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/ROADMAP.md
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/04-letter-verify-flow/04-01-SUMMARY.md
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/04-letter-verify-flow/04-03-SUMMARY.md
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/04-letter-verify-flow/04-04-SUMMARY.md
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/04-letter-verify-flow/04-05-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>[BLOCKING] Deploy: supabase db push + functions deploy</name>

  <what-built>
    Plans 04-01 through 04-05 have written and committed:
    - `(sibling) supabase/migrations/0035_letter_seal_rpcs.sql` — body CHECK + upsert_pending_letter + seal_letter_with_token
    - `(sibling) supabase/functions/verify-email/index.ts` — new Edge Function
    - `(sibling) supabase/config.toml` — [functions.verify-email] verify_jwt=false
    - `(sibling) supabase/functions/join-waitlist/index.ts` — letter param + Path B + test trigger removed
    - `src/lib/supabase.ts`, `src/App.tsx`, `src/components/FirstLetter.tsx` — client wiring + 2000-char cap
  </what-built>

  <how-to-verify>
    Run these commands IN ORDER in the sibling repo (`/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org`):

    **Step 1 — Apply DB migration:**
    ```
    cd "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org"
    supabase db push
    ```
    Expected: "Applied 1 migration" (or similar). No error. If prompted to confirm, enter y.

    **Step 2 — Verify RPCs exist in hosted DB (Supabase SQL Editor or psql):**
    ```sql
    SELECT routine_name FROM information_schema.routines
    WHERE routine_schema = 'public'
      AND routine_name IN ('upsert_pending_letter', 'seal_letter_with_token');
    ```
    Expected: 2 rows returned.

    **Step 3 — Deploy join-waitlist (letter param + Path B + test trigger removed):**
    ```
    supabase functions deploy join-waitlist
    ```
    Expected: deployment success, no errors.

    **Step 4 — Deploy verify-email (new function):**
    ```
    supabase functions deploy verify-email
    ```
    Expected: deployment success, no errors.

    **Step 5 — Smoke test Path B (letter flow) using the landing dev server:**
    ```
    cd "/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design"
    npm run dev
    ```
    a. Open http://localhost:3000
    b. Scroll to the FirstLetter section
    c. Write a short letter (e.g. "Dear future me, I hope 2027 is wonderful.")
    d. Click "Seal & send" — transitions to the email step
    e. Enter a real email address you can receive on
    f. Submit — wait for the success animation (sealed envelope)
    g. Check your email — Template 1B should arrive within 60 seconds with a verify link
    h. Click the verify link (currently http://localhost:3000/verify?token=... won't work from email since the local server isn't public — instead, COPY the token from the URL in the email and manually run:)
       ```
       curl -X POST https://<SUPABASE_PROJECT_REF>.supabase.co/functions/v1/verify-email \
         -H "Content-Type: application/json" \
         -H "apikey: <SUPABASE_ANON_KEY>" \
         -H "Authorization: Bearer <SUPABASE_ANON_KEY>" \
         -d '{"token":"<TOKEN_FROM_EMAIL>"}'
       ```
       Expected response: `{"state":"sealed"}`
    i. Verify in Supabase SQL Editor:
       ```sql
       SELECT id, user_id, sealed_at, deliver_at, char_length(body) as body_len
       FROM app_private.letters
       ORDER BY created_at DESC LIMIT 1;
       ```
       Expected: sealed_at is NOT NULL, deliver_at = '2027-01-01 13:00:00+00'
       ```sql
       SELECT * FROM app_private.schedules ORDER BY created_at DESC LIMIT 1;
       ```
       Expected: status='pending', deliver_at='2027-01-01 13:00:00+00'

    **Step 6 — Smoke test Path A (no letter):**
    a. Use the hero form on http://localhost:3000 (or a different email)
    b. Submit email only — no letter written
    c. Verify Template 1A arrives (no verification link in the email body)
    d. Verify in Supabase SQL Editor:
       ```sql
       SELECT COUNT(*) FROM app_private.letters WHERE user_id = (
         SELECT id FROM auth.users WHERE email = '<YOUR_TEST_EMAIL>' LIMIT 1
       );
       ```
       Expected: 0 (no letters row for Path A signup)

    **Step 7 — Verify double-seal idempotency (SEC-05):**
    Re-run the curl from Step 5h with the SAME token.
    Expected response: `{"state":"already_sealed"}` (not an error)
  </how-to-verify>

  <resume-signal>
    Type "deployed" once Steps 1-7 are all confirmed, along with any issues encountered (e.g. migration error, email not received, unexpected DB state).
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Post-deploy verification: Path A + Path B + SEC-05 smoke tests</name>

  <what-built>
    The full letter + verify flow is deployed. This checkpoint captures the final acceptance results from Step 5-7 above.
  </what-built>

  <how-to-verify>
    Confirm all of the following are true (from the hands-on tests in the previous checkpoint):

    1. **Path B letter sealed:** `app_private.letters` has a row with `sealed_at IS NOT NULL` and `deliver_at = '2027-01-01 13:00:00+00'`
    2. **Schedule created:** `app_private.schedules` has a row for that letter with `status='pending'` and `deliver_at='2027-01-01 13:00:00+00'`
    3. **No outbox pre-insert:** `SELECT COUNT(*) FROM app_private.notification_outbox WHERE letter_id = '<LETTER_ID>'` returns 0 (the cron will create them on Jan 1 — not now)
    4. **Template 1B received:** Email arrived in inbox with a verify link (format: `https://sealedapp.io/verify?token=<hex>`)
    5. **Path A no letter:** The Path A test email's user has 0 rows in app_private.letters; Template 1A was received (no verify link)
    6. **Double-seal returns 'already_sealed':** Second curl returns `{"state":"already_sealed"}` (not an error, not 'invalid')
    7. **test_1b trigger gone:** `curl -X GET https://<PROJECT_REF>.supabase.co/functions/v1/join-waitlist?test_1b=1` returns 405 (not the old test-trigger response)
    8. **npm run build exits 0** in the landing repo (no TypeScript errors introduced)
  </how-to-verify>

  <resume-signal>
    Type "verified" when all 8 checks pass, or describe which checks failed and what was observed.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human operator → hosted Supabase | CLI auth (supabase login) gates db push and functions deploy |
| Smoke test → live Edge Functions | Real tokens are minted during smoke test; test letters are real rows in app_private.letters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-28 | Tampering | Smoke test letter rows in production DB | accept | Smoke test letters will have is_canary=false and sealed_at set — they will be delivered on Jan 1, 2027 via the real cron. Use a test email address you control and are OK receiving the letter. Alternatively, delete the test row via the Supabase SQL Editor after verification. |
| T-04-29 | Repudiation | test_1b trigger removal confirmed | mitigate | Step 7 confirms the old trigger is gone (returns 405, not the old test response); TEST_TRIGGER_KEY secret can be unset in Phase 5 cleanup |
| T-04-30 | Information Disclosure | smoke test token in curl command | accept | Token is single-use and 7-day TTL; used during smoke test, so already marked used_at; no further risk |
| T-04-31 | Denial of Service | migration failure leaves DB in partial state | mitigate | supabase db push is transactional; if 0035 fails, the DB remains at the 0034 state; no partial migration persists |
</threat_model>

<verification>
The following must ALL be true before typing "verified":
1. `SELECT routine_name FROM information_schema.routines WHERE routine_schema='public' AND routine_name IN ('upsert_pending_letter','seal_letter_with_token')` → 2 rows
2. POST to verify-email with valid token → `{"state":"sealed"}`
3. POST to verify-email with same token → `{"state":"already_sealed"}`
4. `app_private.letters` row has sealed_at NOT NULL, deliver_at='2027-01-01 13:00:00+00'
5. `app_private.schedules` row for that letter has status='pending'
6. `app_private.notification_outbox` has 0 rows for that letter_id
7. Path A test email: 0 rows in app_private.letters for that user
8. `npm run build` exits 0
</verification>

<success_criteria>
All eight post-deploy checks pass. The full Path B loop (write → submit → 1B email → verify link → sealed in DB → schedules row pending → cron ready for Jan 1 2027) is confirmed end-to-end on the live hosted Supabase project. Path A (no letter) confirmed independent. Double-seal idempotency confirmed. test_1b trigger confirmed absent.
</success_criteria>

<output>
After verification is complete, create `.planning/phases/04-letter-verify-flow/04-06-SUMMARY.md` recording:
- Migration 0035 applied successfully (timestamp)
- Edge Functions deployed: verify-email (new), join-waitlist (redeployed)
- Smoke test results for all 8 checks (pass/fail)
- Any issues encountered and how resolved
- The letter_id and user_id of the smoke test letter (for audit trail)
- Whether smoke test rows were cleaned up from the DB
</output>
