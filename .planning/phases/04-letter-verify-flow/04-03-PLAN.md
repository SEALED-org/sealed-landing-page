---
phase: 04-letter-verify-flow
plan: 03
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - "(sibling) supabase/functions/join-waitlist/index.ts"
autonomous: true
requirements:
  - LETTER-02
  - LETTER-05
  - LETTER-06
  - EMAIL-B2
  - EMAIL-B3

must_haves:
  truths:
    - "Submitting via the FirstLetter section with a letter body creates a draft row in app_private.letters AND sends Template 1B (not 1A)"
    - "Submitting without a letter body (Path A, hero form) sends Template 1A and does NOT create a letters row"
    - "A user who re-submits with a new letter body while in 'unverified' state gets the draft updated and a fresh 1B re-sent (D-04 newest-wins)"
    - "A user who re-submits WITHOUT a new letter body while in 'unverified' state gets a FRESH 1B re-sent for their existing pending letter (D-04: re-send existing letter's verify link) — NOT 1A"
    - "The ?test_1b=1 trigger block is removed from the function"
    - "Email send failures (Resend outage) do not change the { state } response — draft + token persist; user can re-trigger via D-04 re-signup"
  artifacts:
    - path: "(sibling) supabase/functions/join-waitlist/index.ts"
      provides: "Path B branch: upsert_pending_letter + create_verification_token + 1B send; ?test_1b=1 removed"
      contains: "upsert_pending_letter"
  key_links:
    - from: "join-waitlist/index.ts Path B branch"
      to: "public.upsert_pending_letter"
      via: "supabase.rpc('upsert_pending_letter', { p_user_id, p_body })"
      pattern: "upsert_pending_letter"
    - from: "join-waitlist/index.ts Path B branch"
      to: "public.create_verification_token"
      via: "supabase.rpc('create_verification_token', { p_user_id })"
      pattern: "create_verification_token"
    - from: "join-waitlist/index.ts"
      to: "sendResendEmail (SealLetterEmail)"
      via: "sendResendEmail({ ..., html, text, idempotencyKey: '1b-' + tokenRow.id })"
      pattern: "SealLetterEmail"
---

<objective>
Edit `join-waitlist/index.ts` to: (a) accept a `letter` field in the request body, (b) implement the full Path B branch for new signups (upsert draft → mint token → send 1B), (c) handle D-04 re-signup with a pending letter (update body if new letter, then refresh token → re-send 1B; OR just refresh token → re-send 1B if no new letter — D-04 ALWAYS sends 1B for unverified state), and (d) remove the `?test_1b=1` test trigger block.

Purpose: Until this plan runs, Path B is identical to Path A — letters are never stored and 1B is never sent in production. This plan wires the real production Path B.

Output: Updated `(sibling) supabase/functions/join-waitlist/index.ts` with letter support, Path B branching, and the test trigger deleted.
</objective>

<execution_context>
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/04-letter-verify-flow/../../../get-shit-done/workflows/execute-plan.md
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/04-letter-verify-flow/../../../get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/PROJECT.md
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/ROADMAP.md
@/Users/nourismaiel/Downloads/SEALED Landing Page Claude Design/.planning/phases/04-letter-verify-flow/04-01-SUMMARY.md

<!-- D-02: reject letter body > 2,000 chars in the Edge Fn (before calling upsert_pending_letter) -->
<!-- D-03: writing a letter IS signing up — same function, no separate endpoint -->
<!-- D-04: re-signup with pending letter → ALWAYS re-send 1B (with body update if new letter, without if not) -->
<!-- Pitfall 4: fresh token per re-send avoids Resend 24h dedupe window -->
<!-- Pitfall 7: email send failure must not change the { state } response -->

<interfaces>
<!-- Current join-waitlist/index.ts — key structures to preserve and modify -->

RequestBody type (current):
  { email: string; turnstileToken: string }
  → CHANGE TO: { email: string; turnstileToken: string; letter?: string }

LookupState type (unchanged):
  'new' | 'unverified' | 'verified_no_letter' | 'verified_with_letter'

State type (unchanged):
  'success' | 'unverified' | 'verified_no_letter' | 'verified_with_letter' |
  'rate_limited' | 'turnstile_failed' | 'server_error'

Existing RPCs already deployed (reuse verbatim):
  supabase.rpc('create_verification_token', { p_user_id: uid }) → { data: [{id, token}] }
  supabase.rpc('lookup_user_id_by_email', { p_email }) → { data: uuid }

New RPC from Plan 04-01 (0035 migration — will be deployed before this function runs):
  supabase.rpc('upsert_pending_letter', { p_user_id: uid, p_body: letter })

sendResendEmail call shape (from _shared/resend.ts, already used for 1A):
  sendResendEmail({ from, to, subject, html, text, headers, idempotencyKey, apiKey })
  idempotencyKey for 1B: '1b-' + tokenRow.id   (fresh token_id per send → no Resend dedupe, Pitfall 4)
  idempotencyKey for 1B re-send (D-04): '1b-resend-' + tokenRow.id   (fresh token minted, so id is new)

renderEmail helper (already in scope):
  async function renderEmail(element): Promise<{ html: string; text?: string }>

SealLetterEmail (already imported):
  React.createElement(SealLetterEmail, { verifyUrl })
  verifyUrl = `https://sealedapp.io/verify?token=${tokenRow.token}`

Test trigger block to DELETE (lines 167-216 in current file):
  The entire `if (url.searchParams.get('test_1b') === '1') { ... }` block including
  the URL construction at line 171. After removal, the `url` variable is also unused
  if it was only used for this — remove the `const url = new URL(req.url)` line too
  if no other usage exists.

create_waitlist_signup call (line 305 current):
  supabase.rpc('create_waitlist_signup', { p_user_id: created.user.id, p_has_letter: false })
  → CHANGE p_has_letter to: p_has_letter: letter.trim().length > 0
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove ?test_1b=1 trigger + add letter param to RequestBody</name>
  <files>(sibling) supabase/functions/join-waitlist/index.ts</files>
  <read_first>
    - `(sibling) supabase/functions/join-waitlist/index.ts` — read the FULL file before editing; identify the exact line range of the `?test_1b=1` block (currently lines 167-216), the RequestBody type (line 79), and the `const url = new URL(req.url)` line (line 171)
    - `(sibling) supabase/functions/_shared/resend.ts` — confirm sendResendEmail parameter shape
    - `(sibling) supabase/functions/notify/emails/SealLetterEmail.tsx` — confirm the props interface ({ verifyUrl: string })
  </read_first>
  <behavior>
    - Behavior 1: POST with letter body > 2,000 chars → function returns { state: 'server_error' } with status 400 (before any DB writes)
    - Behavior 2: POST with letter='' or letter absent → treated as Path A (no letter); create_waitlist_signup called with p_has_letter=false
    - Behavior 3: POST with letter=non-empty → treated as Path B; create_waitlist_signup called with p_has_letter=true
    - Behavior 4: GET /?test_1b=1 → returns 405 (method not allowed) or falls through to the POST gate — the test trigger block is entirely absent
  </behavior>
  <action>
    1. Update `RequestBody` type: add `letter?: string` field.

    2. Delete the entire `?test_1b=1` block: from the `const url = new URL(req.url)` line (if only used there) through the closing `}` of that if-block (currently ending around line 216). If `url` is referenced anywhere else, keep the declaration but remove the if-block. Confirm by reading the file first.

    3. After the body parse (currently line ~225), extract and validate the `letter` field:
       - `const letter = (body.letter ?? '').trim()` — normalize to empty string if absent
       - Add: `if (letter.length > 2000) { return jsonResponse({ state: 'server_error' }, 400); }` — D-02 server-side cap (Edge Fn layer; DB CHECK is the second guard)

    4. Update `create_waitlist_signup` call: change `p_has_letter: false` to `p_has_letter: letter.length > 0`.

    Do NOT implement the Path B RPC calls yet — that is Task 2. This task only cleans the trigger and wires the `letter` variable.
  </action>
  <verify>
    <automated>
      grep -c "test_1b" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts"
      grep -c "letter.*string\|letter\?: string" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts"
      grep -c "p_has_letter.*letter" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts"
    </automated>
    <!-- test_1b grep must return 0 -->
  </verify>
  <done>
    test_1b absent from file (grep = 0). RequestBody includes `letter?: string`. letter extracted and trimmed after body parse. 2000-char guard present. p_has_letter uses `letter.length > 0`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Path B branch (new signup + D-04 re-signup with or without new letter)</name>
  <files>(sibling) supabase/functions/join-waitlist/index.ts</files>
  <read_first>
    - `(sibling) supabase/functions/join-waitlist/index.ts` — current state after Task 1 (read again to see exact positions of the `case 'new'` fallthrough, the `case 'unverified'` block, and where the 1A send currently lives)
    - `04-01-SUMMARY.md` — confirm the exact RPC signature of upsert_pending_letter as written
    - `04-RESEARCH.md` Pitfall 4 — fresh token per re-send avoids Resend 24h dedupe window
    - `04-RESEARCH.md` Pitfall 7 — email send failure must not change the { state } response
  </read_first>
  <behavior>
    - Behavior 1: case 'new' + letter.length > 0 → createUser → create_waitlist_signup(p_has_letter=true) → upsert_pending_letter(uid, letter) → create_verification_token(uid) → send 1B (try/catch best-effort) → return { state: 'success' }
    - Behavior 2: case 'new' + letter.length === 0 → createUser → create_waitlist_signup(p_has_letter=false) → send 1A (unchanged path) → return { state: 'success' }
    - Behavior 3: case 'unverified' + letter.length > 0 → upsert_pending_letter(uid, letter) [newest wins, per D-04] → create_verification_token(uid) [FRESH token, not reuse] → send 1B (try/catch) → return { state: 'unverified' }
    - Behavior 4: case 'unverified' + letter.length === 0 → lookup_user_id_by_email → create_verification_token(uid) [FRESH token] → send 1B for the EXISTING pending letter body (NOT 1A — D-04: "just re-send the existing letter's verify link") → return { state: 'unverified' }
    - Behavior 5: case 'verified_with_letter' → return existing { state: 'verified_with_letter' } — letter already sealed, no action
    - Behavior 6: 1B send failure (Resend outage) → catch block logs error, does NOT throw; { state: 'unverified' } or { state: 'success' } returned normally; draft + token already persisted (Pitfall 7)
  </behavior>
  <action>
    Modify `join-waitlist/index.ts` in two places:

    Place 1 — `case 'new':` fall-through section (currently at the bottom of the switch, before the rate-limit check):
    After the user is created and create_waitlist_signup is called, add a Path B branch that sends 1B when a letter is present, otherwise falls through to the existing 1A send:

    if (letter.length > 0) {
      // Path B: draft letter + token + 1B
      const { error: letterErr } = await supabase.rpc('upsert_pending_letter', {
        p_user_id: created.user.id,
        p_body: letter,
      });
      if (letterErr) throw letterErr;
      const { data: tokenRows, error: tokenErr } = await supabase.rpc(
        'create_verification_token', { p_user_id: created.user.id }
      );
      if (tokenErr || !tokenRows?.[0]) throw tokenErr ?? new Error('token creation failed');
      const tokenRow = tokenRows[0];
      const verifyUrl = `https://sealedapp.io/verify?token=${tokenRow.token}`;
      try {
        if (env.RESEND_API_KEY) {
          const { html, text } = await renderEmail(
            React.createElement(SealLetterEmail, { verifyUrl })
          );
          await sendResendEmail({
            from: env.SEALED_FROM_ADDRESS,
            to: [body.email],
            subject: 'Seal your letter.',
            html,
            text,
            headers: LIST_HEADERS,
            idempotencyKey: `1b-${tokenRow.id}`,
            apiKey: env.RESEND_API_KEY,
          });
          console.log('1b-sent', { token_id: tokenRow.id, user_id: created.user.id });
        }
      } catch (err) {
        console.error('1b-send-error', (err as Error).message);
      }
      // 1A send is skipped for Path B — only 1B is sent
      await recordAttempt(supabase, ip, 'success');
      return jsonResponse({ state: 'success' }, 200);
    }
    // letter.length === 0: fall through to existing 1A send below

    Place 2 — `case 'unverified':` block (D-04 re-signup handling):
    Replace the existing fall-through (which currently re-sends 1A) with a block that ALWAYS sends 1B for the unverified state, regardless of whether a new letter was submitted. The user has a pending letter — they always receive 1B (D-04: "just re-send the existing letter's verify link" when no new letter; "update body then re-send 1B" when new letter). Remove the fall-through to the 1A re-send block entirely for this case.

    After `await recordAttempt(supabase, ip, 'duplicate_unverified')`, replace everything that follows in the `case 'unverified'` handler up to the `return` with:

    try {
      const { data: uidData } = await supabase.rpc('lookup_user_id_by_email', { p_email: body.email });
      const userId: string | null = uidData ?? null;
      if (userId && env.RESEND_API_KEY) {
        if (letter.length > 0) {
          // D-04: new body on re-submit — update the existing draft (newest-wins)
          const { error: letterErr } = await supabase.rpc('upsert_pending_letter', {
            p_user_id: userId, p_body: letter,
          });
          if (letterErr) console.error('upsert-letter-error', letterErr.message);
        }
        // Always mint a fresh token and re-send 1B (D-04: re-send existing letter's verify link)
        const { data: tokenRows, error: tokenErr } = await supabase.rpc(
          'create_verification_token', { p_user_id: userId }
        );
        if (!tokenErr && tokenRows?.[0]) {
          const tokenRow = tokenRows[0];
          const verifyUrl = `https://sealedapp.io/verify?token=${tokenRow.token}`;
          const { html, text } = await renderEmail(
            React.createElement(SealLetterEmail, { verifyUrl })
          );
          await sendResendEmail({
            from: env.SEALED_FROM_ADDRESS,
            to: [body.email],
            subject: 'Seal your letter.',
            html,
            text,
            headers: LIST_HEADERS,
            idempotencyKey: `1b-resend-${tokenRow.id}`,   // fresh id = no Resend dedupe (Pitfall 4)
            apiKey: env.RESEND_API_KEY,
          });
          console.log('1b-resend-sent', { token_id: tokenRow.id, user_id: userId });
        }
      }
    } catch (err) {
      console.error('1b-resend-error', (err as Error).message);
    }
    return jsonResponse({ state: 'unverified' }, 200);
    // NOTE: The old 1A re-send fall-through for 'unverified' is now deleted.
    // 'unverified' always sends 1B — the user has a pending letter regardless of
    // whether they submitted a new body (D-04).

    Never log the verifyUrl, html body, or raw token value — only token_id and user_id (CLAUDE.md privacy-safe logging convention).
  </action>
  <verify>
    <automated>
      grep -c "upsert_pending_letter" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts"
      grep -c "create_verification_token" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts"
      grep -c "SealLetterEmail" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts"
      grep -c "1b-resend-" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts"
      grep -c "verifyUrl\|verify_url\|verify_Url" "/Users/nourismaiel/SEALED ORG (FINAL)/SEALED-org/supabase/functions/join-waitlist/index.ts"
    </automated>
    <!-- verifyUrl grep must NOT appear in console.log/error — only in the sendResendEmail call and createElement -->
    <!-- upsert_pending_letter: at minimum 1 (new Path B); may be 2 if the 'unverified' conditional also calls it -->
    <!-- create_verification_token: must appear >= 2 (one in new Path B, one in unverified handler) -->
    <!-- SealLetterEmail: must appear >= 2 (createElement in both 1B send sites) -->
    <!-- 1b-resend- idempotency key: must appear >= 1 (unverified handler) -->
  </verify>
  <done>
    upsert_pending_letter called in 'new' Path B branch AND conditionally in 'unverified' D-04 branch (when letter.length > 0). create_verification_token minted freshly in BOTH 'new' Path B and 'unverified' handlers. SealLetterEmail rendered and sent via sendResendEmail with idempotencyKey '1b-{tokenRow.id}' (new) and '1b-resend-{tokenRow.id}' (re-send). The 'unverified' case NEVER sends 1A — it always sends 1B (D-04). 1B send wrapped in try/catch (best-effort). verifyUrl never logged (privacy-safe). test_1b still absent.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → join-waitlist | letter body (untrusted user input) + email + turnstile token cross from client; service-role key never leaves the Edge Fn environment |
| join-waitlist → app_private | upsert_pending_letter RPC is the only write path for letter content; app_private is sealed from the Data API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Tampering | letter body injection | mitigate | body is passed as parameterized p_body to the RPC (no dynamic SQL in join-waitlist); DB CHECK and RPC both reject >2000 chars |
| T-04-14 | Repudiation | ?test_1b=1 removal | mitigate | The test trigger (which required TEST_TRIGGER_KEY but was still an attack surface) is deleted; production 1B now requires a real signup + upsert_pending_letter call |
| T-04-15 | Denial of Service | 1B email flooding via D-04 | mitigate | Rate limit (check_signup_rate_limit, 0033) still applies before the 'new' path; 'unverified' re-sends use the existing duplicate_unverified path that's already rate-counted |
| T-04-16 | Information Disclosure | verifyUrl in logs | mitigate | verifyUrl and raw token are never logged — only token_id and user_id (CLAUDE.md privacy-safe convention) |
| T-04-17 | Spoofing | letter minted for wrong user | mitigate | upsert_pending_letter(p_user_id) is called with the uid from createUser or lookup_user_id_by_email — the user's own id; no IDOR possible |
</threat_model>

<verification>
1. `grep -c "test_1b"` returns 0
2. `grep -c "upsert_pending_letter"` returns >= 1 (new-user Path B branch; unverified branch calls it conditionally)
3. `grep -c "create_verification_token"` returns >= 2 (new Path B + unverified handler)
4. `grep -c "1b-resend-"` returns >= 1 (D-04 fresh idempotency key in unverified handler)
5. `grep -c "SealLetterEmail"` returns >= 2 (createElement calls for both 1B send sites)
6. `grep "console.log.*verifyUrl\|console.error.*token[^_]"` returns 0 (privacy)
7. `grep -c "WaitlistConfirmationEmail\|1a-"` in the 'unverified' case block returns 0 — the old 1A re-send fall-through is removed
8. Function file has no syntax errors (check by reading through; Deno type errors will surface at deploy time)
</verification>

<success_criteria>
join-waitlist/index.ts: ?test_1b=1 block absent, letter param accepted and 2000-char capped, Path B creates draft + token + sends 1B for new signups, D-04 re-signup with new letter updates draft + mints fresh token + re-sends 1B, D-04 re-signup WITHOUT new letter mints fresh token + re-sends 1B for existing pending letter (never 1A for unverified state), all email sends wrapped in try/catch (best-effort, Pitfall 7), no privacy violations in logs.
</success_criteria>

<output>
After completion, create `.planning/phases/04-letter-verify-flow/04-03-SUMMARY.md` recording:
- Exact line ranges of the two new Path B code blocks (for future reference)
- Confirmation that test_1b block was successfully removed
- The idempotency key patterns used for 1B (new: '1b-{tokenRow.id}', re-send: '1b-resend-{tokenRow.id}')
- Confirmation that the 'unverified' case now always sends 1B and never falls through to 1A
- Whether any type errors were found (will surface at supabase deploy time)
</output>
