---
phase: quick-260724-tb6
plan: "01"
type: execute
mode: quick-full
quick_id: 260724-tb6
slug: tighten-landing-page-spacing-by-removing
title: Tighten landing-page spacing by removing hidden success-card layout space
date: 2026-07-24
wave: 1
depends_on: []
files_modified:
  - src/components/WaitlistSuccessCard.tsx
  - src/index.css
autonomous: true
status: planned
must_haves:
  truths:
    - "Before signup succeeds, WaitlistSuccessCard contributes no DOM node, height, focus target, live region, or other accessibility footprint."
    - "After signup succeeds, the success card mounts once and retains its 0.55-second entrance, stamp animation, links, and write-letter interaction."
    - "The hero, divider, How It Works, and Research section transitions use the exact approved desktop and smaller-screen padding values."
    - "At the approved viewports, the hidden-card path and surrounding section transitions match the target visual spacing while the divider remains centered."
    - "Copy, typography, card-internal spacing, Research bottom padding, First Letter, FAQ, Footer, component props, data behavior, and interactions remain unchanged."
  artifacts:
    - path: src/components/WaitlistSuccessCard.tsx
      provides: "Zero-footprint hidden state and unchanged public success-card contract"
      contains: "if (!isVisible)"
    - path: src/index.css
      provides: "Mount-triggered success animation and exact section-transition spacing"
      contains: "@keyframes wls-enter"
  key_links:
    - from: src/App.tsx
      to: src/components/WaitlistSuccessCard.tsx
      via: "isSubscribed continues to drive the existing isVisible prop"
      pattern: "isVisible={isSubscribed}"
    - from: src/components/WaitlistSuccessCard.tsx
      to: src/index.css
      via: "The mounted waitlist-success class triggers the entrance keyframe"
      pattern: "className=\"waitlist-success\""
    - from: src/App.tsx
      to: src/index.css
      via: "Existing hero, divider, How It Works, and Research class names consume the revised padding rules"
      pattern: "section-divider"
---

# Quick Task 260724-tb6: Tighten landing-page transition spacing

<objective>
Remove the invisible success card from pre-signup layout and tighten only the approved section transitions.

Purpose: Eliminate the unexplained hero gap and reduce excess whitespace between the hero, How It Works, and Research sections without changing content, component contracts, data behavior, or interactions.

Output: A conditionally mounted success card with a CSS mount animation, plus the exact approved responsive padding values in `src/index.css`.
</objective>

<execution_context>
@/Users/nourismaiel/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nourismaiel/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/components/WaitlistSuccessCard.tsx
@src/index.css
@src/App.tsx

<interfaces>
From `src/components/WaitlistSuccessCard.tsx`:

```typescript
interface WaitlistSuccessCardProps {
  onWriteLetter: () => void;
  isVisible: boolean;
}

export default function WaitlistSuccessCard({
  onWriteLetter,
  isVisible,
}: WaitlistSuccessCardProps)
```

Preserve this public prop contract unchanged. `src/App.tsx` already passes
`isVisible={isSubscribed}` and an `onWriteLetter` callback that smoothly scrolls
to `#first-letter`; neither call site nor state flow needs modification.
</interfaces>

<locked_decisions>
- **D-01 — Hidden state:** When `isVisible` is false, return `null`; the card must leave no visual or accessibility footprint.
- **D-02 — Entrance:** Preserve the 0.55-second success entrance by triggering a CSS keyframe when the component mounts.
- **D-03 — Exact spacing:** Hero bottom `48px -> 32px`; divider outer top `8px -> 0`; divider inner vertical `48px -> 24px`; How desktop `96px 24px 128px -> 64px 24px 80px`; How smaller-screen bottom `96px -> 72px`; Research desktop top `88px -> 64px`; Research mobile top `56px -> 48px`.
- **D-04 — Scope boundary:** Do not change copy, typography, card-internal spacing, Research bottom padding, First Letter, FAQ, Footer, public props, data behavior, or interactions.
- **D-05 — Responsive verification:** Check `390x844`, `768x1024`, `1440x1000`, and approximately `2048px` desktop.
- **D-06 — Acceptance:** Hidden card height is zero; form/error-to-counter is approximately `48px`; desktop counter-to-How eyebrow is approximately `145px`; desktop last-step-to-Research eyebrow is approximately `144px`; mobile equivalents are approximately `113px` and `120px`; divider is centered; reveal and success states remain intact.
- **D-07 — Delivery:** Stay on the current branch. Do not push or deploy.
</locked_decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the success card zero-footprint until it mounts</name>
  <files>src/components/WaitlistSuccessCard.tsx, src/index.css</files>
  <action>
Implement D-01 and D-02 without changing the `WaitlistSuccessCardProps` interface or its rendered card content.

In `WaitlistSuccessCard.tsx`, return `null` immediately when `isVisible` is false. When true, render the existing success-card tree with the stable `waitlist-success` class and existing `aria-live="polite"` behavior. Remove the permanent `is-mounted`/conditional `is-visible` class construction because visibility is now represented by component presence, not a hidden DOM node.

In `src/index.css`, add a `wls-enter` keyframe from `opacity: 0; transform: translateY(10px)` to `opacity: 1; transform: translateY(0)`, and apply it to `.waitlist-success` for exactly `0.55s ease both`. Remove the old hidden/display/transition/pointer-event state rules and obsolete `.is-mounted`/`.is-visible` selectors. Retarget the existing stamp animation from `.waitlist-success.is-visible .wls-mark` to the mounted `.waitlist-success .wls-mark` so its timing and behavior remain unchanged.

Do not add timers, effects, local state, wrapper placeholders, visually hidden elements, or changes in `App.tsx`; mounting from the existing `isSubscribed` state is the animation trigger.
  </action>
  <verify>
    <automated>npm run build:check</automated>
    <inspection>Before success, browser inspection finds no `.waitlist-success` element and no success-card focus targets or live region. After a successful signup, exactly one `.waitlist-success` element mounts, runs the 0.55-second entrance, shows the existing stamp animation, and its write-letter button still scrolls smoothly to `#first-letter`.</inspection>
  </verify>
  <done>The false state returns `null` with zero layout/accessibility footprint, while the true state mounts the unchanged card and visibly preserves the approved entrance and interaction.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the exact responsive section-transition spacing</name>
  <files>src/index.css</files>
  <action>
Implement D-03 by changing only these declarations:

- `header.hero`: `padding: 132px 24px 32px`.
- `.section-divider`: outer top padding from `8px` to `0`, preserving `24px` horizontal and `0` bottom padding.
- `.section-divider .inner`: `padding: 24px 0`.
- Base `section.how`: keep `32px` top and `24px` horizontal, change bottom to `72px`.
- `@media (min-width: 768px) section.how`: `padding: 64px 24px 80px`.
- Desktop `section.research`: keep `24px` horizontal and `96px` bottom, change top to `64px`.
- `@media (max-width: 760px) section.research`: keep `20px` horizontal and `64px` bottom, change top to `48px`.

Honor D-04: make no other CSS or component changes. In particular, do not adjust typography, card/step spacing, Research bottom padding, First Letter, FAQ, Footer, reveal selectors, or any JS behavior.

After the edits, run the production build/security check. Start the local app and inspect D-05 viewports: `390x844`, `768x1024`, `1440x1000`, and a desktop viewport approximately `2048px` wide. Use `getBoundingClientRect()` in the browser to measure the D-06 transitions and confirm values are approximately the approved targets (allow only normal subpixel/font-rendering variance). Check both the initial form/error path and the mounted success-card path. Confirm the divider rule remains horizontally centered and that existing scroll reveals still occur without clipping or overlap.
  </action>
  <verify>
    <automated>npm run build:check</automated>
    <responsive>
At `390x844`, verify hidden card height `0`, form/error-to-counter approximately `48px`, counter-to-How eyebrow approximately `113px`, and last-step-to-Research eyebrow approximately `120px`.

At `768x1024`, `1440x1000`, and approximately `2048px` desktop, verify hidden card height `0`, form/error-to-counter approximately `48px`, counter-to-How eyebrow approximately `145px`, and last-step-to-Research eyebrow approximately `144px`.

At all four viewports, verify the divider is centered, the success state remains usable, and existing reveals, First Letter, FAQ, Footer, copy, typography, and interactions show no regression.
    </responsive>
  </verify>
  <done>All seven approved padding changes are exact, `npm run build:check` passes, and the four responsive inspections satisfy D-05/D-06 without modifying anything named in D-04.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

This change introduces no new trust boundary, external input, persistence, authentication, network request, or secret handling. It only changes conditional presentation of existing trusted client state and static CSS layout.

## STRIDE disposition

| Threat ID | Category | Component | Disposition | Rationale |
|---|---|---|---|---|
| T-Q260724-TB6-01 | Tampering | Client-side `isVisible` presentation | accept | Browser-side visual state is already user-observable and cannot grant privileges or alter persisted data; the existing subscription state and handlers are unchanged. |
| T-Q260724-TB6-02 | Information disclosure | Success-card live region | mitigate | Returning `null` before success removes the card and live region entirely, so hidden content is not exposed to accessibility APIs. |
</threat_model>

<verification>
1. Run `npm run build:check`; both the Vite production build and bundle secret scan must pass.
2. Verify initial, error, and successful signup states at `390x844`, `768x1024`, `1440x1000`, and approximately `2048px` desktop.
3. Measure the D-06 distances with browser geometry rather than visual estimation alone, and confirm the divider is centered.
4. Confirm the success card enters over 0.55 seconds on mount, its stamp animation and write-letter action work, and existing section reveals are preserved.
5. Review the diff: only `src/components/WaitlistSuccessCard.tsx` and `src/index.css` may change; `src/App.tsx`, copy, public props, data code, First Letter, FAQ, Footer, and unrelated CSS must remain untouched.
6. Remain on the current branch and do not push or deploy (D-07).
</verification>

<source_coverage_audit>
| Source | Item | Coverage |
|---|---|---|
| GOAL | Remove hidden success-card space and tighten approved section transitions | Tasks 1-2 |
| REQ | No ROADMAP requirement IDs apply to this quick task | Excluded: quick task outside phase requirements |
| RESEARCH | No research phase requested or needed; existing React/CSS patterns are sufficient | Excluded per approved quick-task scope |
| CONTEXT | D-01 zero-footprint hidden state | Task 1 |
| CONTEXT | D-02 0.55-second mount entrance | Task 1 |
| CONTEXT | D-03 exact spacing values | Task 2 |
| CONTEXT | D-04 protected scope | Tasks 1-2 and diff review |
| CONTEXT | D-05 viewport matrix | Task 2 and overall verification |
| CONTEXT | D-06 measured acceptance and preserved behavior | Tasks 1-2 and overall verification |
| CONTEXT | D-07 current branch, no push/deploy | Overall verification |
</source_coverage_audit>

<success_criteria>
- `WaitlistSuccessCard` renders nothing when `isVisible=false`, and its public props and success-state content remain unchanged.
- The success card's mounted entrance lasts 0.55 seconds and existing stamp/write-letter behavior remains intact.
- Every D-03 spacing declaration exactly matches the approved value, with no out-of-scope changes.
- `npm run build:check` passes.
- All D-05 viewports meet the D-06 spacing, centering, reveal, and regression checks.
- The implementation stays on the current branch with no push or deployment.
</success_criteria>

<output>
After completion, create `.planning/quick/260724-tb6-tighten-landing-page-spacing-by-removing/260724-tb6-SUMMARY.md`.
</output>
